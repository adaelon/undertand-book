# SESSION_CHECKPOINT - 2026-07-11 17:39

## 新鲜度自检
- 写入时功能 commit:`77b782e feat: add desktop library and provider settings`。
- 读入时对比 `git log -3`;checkpoint 自身会有后续 docs commit,以 Git 为准。
- worktree 仍有大量既有用户资料、日志和临时产物;不得清理或回退。

## 当前在做什么
WD2 桌面设置已提交并产正式 Setup,等待用户手工验收书库选择、Provider 热加载和 Tauri ACL。

## 下一步（可直接接手）
1. 关闭旧 `E:\allwork\Understand Book\UnderstandBook.exe`,运行 `dist\UnderstandBookSetup.exe` 覆盖安装。
2. 在设置选择普通父目录和已有 `.understand-book`,确认即时切换、重启保持且旧书不迁移。
3. 在设置保存 Native/ReAct、Base URL、Model、API Key,无需重启直接发起一次真实聊天。
4. 打开设置确认 Codex plugin 状态不再出现 `Command codex_plugin_status not allowed by ACL`。
5. 在 Codex App 新任务调用 `$understand-book-build <Markdown/EPUB 或可信 paper workspace>` 验收安装版 sidecar。

## 未提交 / 未完成
- 本轮源码与文档已提交;仅剩用户手工安装和真实 provider/plugin 流程验收。
- `dist/UnderstandBookSetup.exe`:33,607,278 bytes,SHA-256 `F7E666CE0B3209CA4BE7A29E417933F775F6341A60DCD150FF05B760E1BF88DB`。

## 冷启动读序
1. `docs/adr/0069-user-selected-desktop-library-root.md` 与 `docs/adr/0070-desktop-provider-settings-and-hot-reload.md`。
2. `apps/desktop/README.md`、`apps/desktop/src-tauri/src/library_settings.rs`、`apps/desktop/src-tauri/src/main.rs`。
3. `apps/desktop/src-tauri/build.rs`、`capabilities/default.json` 与 `permissions/autogenerated/`。
4. `crates/server/src/host.rs`、`crates/runtime/src/lib.rs` 的 Provider 路径。
5. `packages/web/src/App.vue`、`desktop-library.ts`、`desktop-provider.ts`。

## 本会话决策摘要
- ADR-0069:用户书库根目录存入 LocalAppData 设置,验证后即时切换,不迁移旧书库。
- ADR-0070:Provider 设置确定性校验后热替换 adapter;API Key 暂按用户确认明文保存且不回传 UI。

## 验证证据
- `cargo test -p understand-book-desktop --offline`:11 passed。
- `cargo test -p runtime provider_config --offline`:2 passed。
- `cargo test -p server desktop_ --offline`:4 passed;provider fallback 定向测试通过。
- `pnpm -C packages/web test`:10 files / 53 passed;typecheck 与 production build 通过。
- `pnpm -C apps/desktop package:windows`:release NSIS 成功,导出 hash 见上。
