[package]
name = "read-tools"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
base-schema = { path = "../base-schema" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
