[package]
name = "reader"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
read-tools = { path = "../read-tools" }
memory = { path = "../memory" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
base-schema = { path = "../base-schema" }
