[package]
name = "runtime"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
artifact-tools = { path = "../artifact-tools" }
base-schema = { path = "../base-schema" }
book-tool-contracts = { path = "../book-tool-contracts" }
read-tools = { path = "../read-tools" }
memory = { path = "../memory" }
reader = { path = "../reader" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
ureq = { version = "2", features = ["json"] }
url = "2"
dotenvy = "0.15"
ts-rs = "10"
