# 代码链路(改动账本)

> 每个切片完成时当场追加一条,精确到 文件:符号。供日后反查「当时为什么改了这个文件」。

## 2026-07-11 · Codex plugin 一命令自动预构建 [Plugin/Core]

**触达**:
- `.codex-plugin/plugin.json` - Codex 插件根清单；与 `.claude-plugin` 共用同一 `skills/`、`agents/` 和确定性脚本源树。
- `skills/build/SKILL.md:$understand-book-build` - 强制执行 `next -> 专用 subagent -> write/gate -> close -> next`，仅 `done`、`needs_user` 或明确失败可停止。
- `skills/build/automatic-build.ts:automaticBuildNext/recordAutomaticBuildAttempt` - 把 core next-action 展开为可执行命令；失败次数原子写入 workspace，三次失败后输出 `needs_user`，成功或用户确认 reset 才清除。
- `packages/core/src/build-orchestrator.ts:resolveAutomaticBuildTarget` - 按显式 workspace/book id 或输入 sha256 唯一解析可信 paper workspace；未匹配 PDF、未可信基座和多匹配均 fail-fast，非 paper 文件进入 technical_learning workspace。
- `packages/core/src/build-orchestrator.ts:buildAutomaticBuildSnapshot/nextAutomaticBuildAction` - 依 artifact/hash/schema 状态逐段推进 Pass1、paper metadata/lexicon、profile sidecar、Pass2、BookStructure 和 PaperReadingGuide。
- `skills/build/pass1-batch.ts:--preserve-foundation` - paper Pass1 收口不得覆盖 Workbench 的 reconciled `source.txt/source_manifest/PDF maps`。
- `packages/core/src/paper-reading-guide-verification.ts:verifyPaperReadingGuideProjection` - 以 TS/Zod 重放 Rust 读时 loader 的 paper header、LID、metadata evidence、lexicon 和 BookStructure 引用门禁，预构建运行期不调用 Cargo。
- `skills/build/verify-paper-reading-guide.ts` - 调 TS projection gate，并写带输入 hash、计数和 verifier version 的 verification artifact。
- `packages/core/test/build-orchestrator.test.ts` / `automatic-build-cli.test.ts` - 覆盖 paper target、foundation 保留、阶段推进、批次、跨进程重试耗尽与 reset。

**入口**:`$understand-book-build <md|epub|paper book_id|paper workspace>`。
**测试**:新增 TS projection gate 有效/悬空证据测试；Core orchestrator/plan/Workbench 相关测试通过，真实 `.understand-book/ph8-smoke` 无 Cargo 验证通过。Codex plugin validator 与两个 skill quick validation均通过；personal marketplace 安装为 `understand-book@personal`。全新 `codex exec` 对单窗口 Markdown 真模型运行完成 Pass1 -> profile sidecar -> Pass2 -> BookStructure，独立 `next` 复算为 `done`。已知验收问题：本次 `codex exec` 在产物完成后未自行退出，需清理残留进程；不影响 workspace 完成状态，但后续需继续调查 Codex multi-agent 生命周期。

## 2026-07-11 · PDF 原生文本选择几何对齐 [Web]

**触达**:
- `packages/web/src/components/PdfReaderPane.vue:renderTextLayer/textLayerTasks/.pdf-text-layer` - 删除按字符数与固定 `0.55` 系数估算宽度的手写 text span，改由 PDF.js `TextLayer` 根据字体 ascent、旋转、canvas 字宽与逐项 `scale-x` 生成透明选择层；切书或重载时取消未完成的 TextLayer task。
- `packages/web/src/components/PdfReaderPane.test.ts` - 以两页文档断言每页构造一个官方 TextLayer，并锁定 viewport、文本内容、动态 span 与 `scaleX` 样式契约。

**入口**:paper reader PDF 正文原生拖选 -> 浏览器 Range -> `/pdf_selection.resolve`，既有 source map / LID / quote 契约不变。

**测试**:`pnpm -C packages/web test`(8 files / 49 tests)、`typecheck`、production `build` 全通过；真实 Chrome 124 `1440x900` 验证首两页 text layer 与 canvas 同尺寸，长标题左侧内缘命中 offset `0`、右侧内缘命中 offset `47/47`，Range 与 span 左右边界完全相同，选区请求正常提交 page 0 bbox 且无 PDF 错误。

## 2026-07-11 · PDF reader 页面流与兼容渲染修复 [Web]

**触达**:
- `packages/web/src/components/PdfReaderPane.vue:pdfjs imports/.pdf-page-list/.pdf-text-layer` - 改用 legacy PDF.js worker，grid 隐式行按页框最大内容定高，运行时 text span 通过 deep selector 获得绝对定位与透明选择层样式。
- `packages/web/src/components/PdfReaderPane.test.ts` - 覆盖 legacy worker 入口、动态文字层生成及页面流样式契约。

**入口**:paper reader 的 PDF 中央阅读表面。
**测试**:`pnpm -C packages/web test`(8 files / 49 tests)；`typecheck`；production build；真实 Chrome 124 在 `1440x1000` / `390x844` 验证相邻页零重叠、canvas 非空、文字层不遮挡及零横向溢出。

## 2026-07-11 · Reader trust 与界面选择解耦 [Web]

**触达**:
- `packages/web/src/surface-selection.ts:chooseAppSurface/readSurfacePreference/writeSurfacePreference` - 将 artifact readiness 与用户当前界面拆开，按 `book_id` 在浏览器会话内保存 Workbench/reader 选择。
- `packages/web/src/App.vue:init/applyWorkbenchAction/enterReader/openBuildWorkbench` - 构建完成后保持 Workbench，只有显式命令才双向切换；未可信状态仍强制工作台。
- `packages/web/src/components/BuildWorkbenchPane.vue:canEnterReader` / `TopBar.vue` - 可信且无活动 gate 时显示“进入阅读”，reader 为 paper workspace 提供“构建工作台”返回入口。
- `packages/web/src/surface-selection.test.ts` / `components/BuildWorkbenchPane.test.ts` - 覆盖可信后留驻、冷启动默认、per-book session 选择和显式进入命令。

**入口**:Build Workbench “进入阅读”；reader 顶栏“构建工作台”。
**测试**:`pnpm -C packages/web test -- surface-selection BuildWorkbenchPane`(8/8)；`pnpm -C packages/web typecheck`。

## 2026-07-10 · 单次来源复核重跑后的人工终裁 [Core/Server/Web]

**触达**:
- `packages/core/src/source-reconciliation.ts:SourceReconciliationAcceptance/sourceReconciliationTrusted/sourceReconciliationAccepted/acceptSourceReconciliationManualOverride` - 将 deterministic trusted 固定为 `unresolved=0`，另以完整 `manual_override` provenance 表达 accepted；人工终裁保留 residual diagnostics，并把规范化 reviewed draft 作为 stage source。
- `packages/core/src/zod.ts:SourceReconciliationAcceptanceZ/SourceReconciliationReportZ` - 固化 `manual_override + single_review_then_override_v1 + accepted_at + residual_unresolved_count + decision_count` 报告契约。
- `packages/core/src/workbench-stage-runner.ts:runSourceReconciliation` - 完整有效 decisions 只触发一次 reviewed rerun；若仍有 residual，写 acceptance/source.txt、回答旧复核请求并以 `ready + stage_completed` 结束；同 fingerprint 再次调用直接复用首次终裁。
- `packages/core/src/build-workbench.ts:detectBuildReadiness` / `packages/core/src/paper-projection-chain.ts:assertTrustedPaperProjectionSource` - source stage 与 projection source gate 改认 explicit accepted；source hash、manifest、schema 与下游 artifact 门禁不变。
- `crates/server/src/lib.rs:source_reconciliation_manual_override_accepted/source_review_ready_for_rerun/build_source_review_snapshot` - 完整校验五字段、残余计数与正整数 decision count；valid override 后不再报告 ready_for_rerun，invalid/mode-only acceptance 继续复核，stale fingerprint 仍优先，同时保留 residual report 与 decisions 审计快照。
- `packages/web/src/source-review-batch.ts:getSourceReviewManualOverride/getSourceReviewAutoRerunRequest` - 以与 Core/Server 相同的完整字段约束识别终裁；无效 acceptance 不得关闭复核或停止必要的 rerun。
- `packages/web/src/components/BuildWorkbenchPane.vue:currentGateJobs/pendingDecisionItems/pendingPermissionItems/foundationAutoSelectedForBook watcher` - 历史 `stale_input` job 的 pending 不参与当前用户门禁；source done 且 foundation 首次 missing 时自动选中 `hybrid_foundation`，当前 ready job 可直接启动。
- `packages/web/src/components/BuildWorkbenchPane.vue:sourceReviewManualOverride/sourceReviewVisible` - valid 终裁后关闭逐项复核区，并显示残余数量与终裁时间。
- `docs/预购建流程.md` / `docs/切片方案-paper-pdf-first-hybrid.md:PH16-PH18` - 将 Workbench 输入、durable job、单次来源重跑、manual override、hybrid foundation、reader handoff、projection 与恢复路径整理为统一冷启动流程，并移除“终裁后仍回到同轮阻塞”的旧口径。
- `packages/core/test/{source-reconciliation,build-workbench,workbench-stage-runner,paper-projection-chain}.test.ts` / `crates/server/src/lib.rs:source_review_manual_override_*` / `packages/web/src/{source-review-batch,components/BuildWorkbenchPane}.test.ts` - 覆盖 trusted/accepted 分离、严格跨层 acceptance、残余诊断、单次重跑、stale 不绕过、历史 job 门禁隔离、下一阶段选择及 readiness/projection 放行。

**入口**:全部当前 source review decisions 可用 -> builtin `source_reconciliation` 单次重跑 -> deterministic pass 或 `report.acceptance.mode=manual_override` -> source stage done -> hybrid/projection 下游；终裁报告再次进入 snapshot 时 `ready_for_rerun=false`。
**信任边界**:`manual_override` 只能消费已持久化的完整复核决定，不能跳过首轮复核；它是 accepted provenance，不把 residual report 伪装成 deterministic trusted，也不绕过 hash/schema/LID/下游 artifact gate。
**测试**:`pnpm -C packages/core test`(33 files / 187 tests) + typecheck；`cargo test -p server`(77 lib + 4 static)；`pnpm -C packages/web test`(6 files / 44 tests) + typecheck + build，全部通过。真实 book `1`:source done、foundation missing、11 residual、ready job；生产 DOM 显示人工终裁 banner，自动选中混合阅读基座且“启动所选阶段”可用。

## 2026-07-10 · 来源复核 LLM 批量自动采用与状态校正 [Web]

**触达**:
- `packages/web/src/source-review-batch.ts:getSourceReviewLlmBatchTargets/evaluateSourceReviewLlmBatchEligibility/runSourceReviewLlmBatch` - 逐项串行调用既有 analyze；点击即授权，只有契约有效、非 `uncertain`、`confidence >= 0.8` 且修订非空的结果才立即以 `manual_edit` 持久化。分析/契约/低置信度/不确定/写入失败保留人工复核，已成功项不因后续失败回滚。
- `packages/web/src/App.vue:applyAllSourceReviewWithLlm` - 接入批量编排、逐项 snapshot 回写、进度/错误状态与书本或证据切换取消保护。
- `packages/web/src/components/SourceReviewWorkspace.vue:llmBatchTargetCount/resolvedCount/requestAllLlmAnalysis` - 增加一键批处理、进度和失败列表；按服务端证据门禁统计“已解决/待人工”，`keep_blocked` 或缺必要证据的旧决策仍留在待处理队列。
- `packages/web/src/components/BuildWorkbenchPane.vue:reasonText` - `ready_for_rerun=true` 时将旧 report 的 unresolved reason 解释为“复核已完成，等待重新运行来源对齐”，避免与“待人工 0”矛盾。
- `packages/web/src/source-review-batch.test.ts`、`packages/web/src/components/SourceReviewWorkspace.test.ts`、`packages/web/src/components/BuildWorkbenchPane.test.ts` - 覆盖阈值边界、契约失败、逐项即时持久化、部分成功不回滚、失败留人工、状态计数和待重跑文案。

**入口**:`SourceReviewWorkspace` 点击“LLM 处理全部” -> `App.applyAllSourceReviewWithLlm` -> 对每个待处理 block 串行 `/build_workbench/source_review.analyze` -> 合格项立即 `/build_workbench/source_review.resolve(decision=manual_edit)`；失败项回到同一工作台逐项复核。
**信任边界**:批量按钮授权的是逐项写入复核决策，不直接写 stage `source.txt`；所有成功决策仍需执行一次 source reconciliation，随后按 deterministic trusted 或 ADR-0065 explicit accepted 判定进入下游。
**测试**:定向 Vitest 3 文件 `18/18`；`pnpm -C packages/web test` 全量 `6 files / 25 tests`、`typecheck`、production `build` 通过；`cargo test -p runtime -p server` 通过 runtime 59 + server 73 + static 4，共 136 tests（其余 0-test targets/doc-tests 成功）。production dist 含 `pdf.worker-CPbhI6B3.mjs` 与 `pdf.worker-BASQQs-o.mjs`。

## 2026-07-10 · 来源复核 LLM 差异分析与可编辑修订 [Runtime/Server/Web]

**触达**:
- `crates/runtime/src/lib.rs:ModelAdapter::complete_structured/NativeAdapter::complete_structured` - 在既有 `.env` provider (`OPENCODE_API_KEY/OPENCODE_BASE_URL/FLUID_LLM_MODEL`) 上增加通用 JSON completion；native/ReAct 共享结构化调用，legacy/fake adapter 可由 `complete().answer` 兼容解析。
- `crates/server/src/lib.rs:route_workbench_source_review_analyze/validate_source_review_llm_output` - 新增 `POST /build_workbench/source_review.analyze`；只读取当前 report block 的 Markdown/PDF 提取文本，防 prompt injection，校验差异分类、recommendation、置信度、长度和完整 replacement 后返回 `source_review_llm_suggestion.v1`。
- `packages/web/src/api.ts:SourceReviewLlmSuggestion/workbenchSourceReviewAnalyze` - 固化逐项差异、建议、完整修订稿与 warning 的类型化契约。
- `packages/web/src/App.vue:analyzeSourceReview` / `BuildWorkbenchPane.vue` - 保存每个 block 的临时分析/loading/error，证据变化时清空；不把 suggestion 混入 Workbench snapshot 或 artifact。
- `packages/web/src/components/SourceReviewWorkspace.vue:activeLlmSuggestion/applyLlmSuggestion` - 三联证据下增加 LLM 分析入口、逐项 Markdown/PDF 差异、置信度、warning、渲染预览与可编辑 Markdown；“采用 LLM 修订”复用既有 `manual_edit` 决策。
- `crates/runtime/src/lib.rs:tests::native_adapter_requests_and_parses_structured_json`、`crates/server/src/lib.rs:tests::source_review_llm_analysis_*`、`packages/web/src/components/SourceReviewWorkspace.test.ts` - 覆盖 provider JSON、服务端契约/零落盘、前端差异展示与编辑后采用。

**入口**:`SourceReviewWorkspace` 当前 block -> `POST /build_workbench/source_review.analyze` -> `.env` LLM -> 临时 suggestion -> 用户点击采用 -> `/build_workbench/source_review.resolve(decision=manual_edit)` -> 原有 content/realignment gate。
**信任边界**:LLM 看不到原始 PDF 图像，只比较 Markdown 与 PDF 提取正文；分析调用不写 `review-decisions.json/source.txt`，也不替代 content equivalence、deterministic aligner 或阈值。
**测试**:`cargo test -p runtime -p server`(59 + 73 lib + 4 main)；`pnpm -C packages/web test`(11)、`typecheck`、`build`；真实 `.env` smoke 对 `block-57` 返回 `keep_markdown/confidence=1/replacement=METHODS`，且零决策落盘；8787 worker 仍为 `200 text/javascript`。

## 2026-07-10 · 生产 PDF worker 模块 MIME [Server]

**触达**:
- `crates/server/src/main.rs:mime_for` - 将 Vite/PDF.js 产出的 `.mjs` 与 `.js` 一并作为 `text/javascript; charset=utf-8` 服务，避免浏览器拒绝动态 worker 模块并降级 fake worker 失败。
- `crates/server/src/main.rs:tests::mime_types_cover_vite_outputs` - 新增 `.mjs` MIME 回归断言，测试先复现 `application/octet-stream` 再验证修复。

**入口**:8787 生产静态服务 -> `/assets/pdf.worker-*.mjs` -> PDF.js Worker 动态 import。
**测试**:`cargo test -p server mime_types_cover_vite_outputs`;`cargo test -p server`；真实 8787 GET 返回 `200 text/javascript`，Chrome 生产页面渲染 `407x544` 非空 PDF 画布且零 console error/request failure。

## 2026-07-10 · 来源复核单问题三联工作台 [Server/Web]

**触达**:
- `packages/web/src/components/SourceReviewWorkspace.vue:activeBlock/decision watcher/review-triptych` - 复核从 unresolved 长列表改成单问题状态机；桌面同步显示原版 PDF、渲染后 Markdown、PDF 提取正文，窄屏切为三页签，服务端决策写回后自动前进到下一未处理项。
- `packages/web/src/components/SourceReviewPdfPage.vue:loadPdfDocument/renderCurrentPage` - 从同源端点读取 PDF 字节，以 PDF.js legacy worker 渲染当前物理页，支持适栏、缩放和新窗口打开；页索引变化时只重绘当前页。
- `packages/web/src/components/BuildWorkbenchPane.vue` / `packages/web/src/App.vue` - 将三联复核工作台提升到 Build Workbench 首屏并透传 original PDF URL，既有决策 API payload 不变。
- `crates/server/src/lib.rs:original_pdf_path/workbench_input_pdf_path` - `/book/pdf/original` 在可信 source manifest 缺失时回退到当前书 workbench input manifest 的相对 `paper.pdf`；拒绝绝对路径与目录逃逸，不改变 reader 已有 source manifest 优先级。
- `packages/web/src/components/{SourceReviewWorkspace,SourceReviewPdfPage,BuildWorkbenchPane}.test.ts` / `crates/server/src/lib.rs:tests::workbench_original_pdf_*` - 覆盖单项导航、决策落盘后前进、手工修正透传、物理页重绘和 draft PDF 只读回退。

**入口**:Build Workbench `source_review.unresolved` -> `SourceReviewWorkspace` -> `/book/pdf/original` + 三源同步 -> `/build_workbench/source_review.resolve`。
**测试**:`pnpm -C packages/web test`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;`cargo test -p server`；真实 `.understand-book/1` 在 1440x1000 显示三栏、390x844 显示单页签，PDF 画布 `407x544` 且像素非空，问题 1→3 同步切到文件第 3 页，缩放后画布宽 `407→508`，零横向溢出/console error。

## 2026-07-10 · 来源对齐复核可判读闭环 [Core/Server/Web]

**触达**:
- `packages/core/src/source-reconciliation.ts:reconciliationUnits/comparisonText/reconcilePaperSource/globalEvidenceCandidate` - 合并段内公式上下文，以 LaTeX/Unicode/HTML/排版等价规则比较来源；仅保留真实残差，并附 Markdown/PDF 上下文、PDF 页行、首差异和问题描述。单调顺序失配时只让达到可信门槛的全局锚点替换证据页，低分全局候选不得冲掉正确的邻近上下文。
- `packages/core/src/source-reconciliation.ts:buildReviewedDraftFromDecisions/reviewCandidateAndReconcile` - 支持保留 Markdown、采用 PDF、系统候选和手工修正；有效人工决策成为确定性 gate 输入，不再重跑后无限回到同一问题。
- `packages/core/src/workbench-stage-runner.ts:decisionsForCurrentReport/sourceReviewReady` - 部分决策继续持久化并保持 `needs_user`，旧 block/旧指纹决策自动过滤，只有当前问题全部有可用答案才生成 reviewed draft。
- `crates/server/src/lib.rs:workbench_config_hash/input_fingerprint_from_manifest/current_workbench_job_input` - source reconciliation 配置升至 v4；旧 report 通过动态指纹标记 stale，创建 job 时刷新 manifest 配置。
- `crates/server/src/lib.rs:source_review_ready_for_rerun/route_workbench_source_review_resolve` - 校验每种决策的真实证据，新增 `manual_edit + replacement_text` 持久化。
- `packages/web/src/components/BuildWorkbenchPane.vue` / `packages/web/src/api.ts` - 复核面改为明确问题、Markdown/PDF 双栏上下文、首差异、PDF 采用结果和按证据显示的操作；新增手工修正闭环。
- `packages/core/test/{source-reconciliation,workbench-stage-runner}.test.ts` / `packages/web/src/components/BuildWorkbenchPane.test.ts` / `crates/server/src/lib.rs:tests::source_review_*` - 覆盖格式等价、双侧证据、部分决策、人工 override、手工修正 API 与 UI payload。

**入口**:`adapter_mode=builtin` 运行 `source_reconciliation` -> `/book/build_workbench` 的 `source_review` -> `/build_workbench/source_review.resolve` -> 重跑同一 stage。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;`cargo test -p server`;`pnpm -C packages/web test`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`；真实 `.understand-book/1` 回放从 249 个 unresolved 收敛为 30 个，`$ \\mu $` 自动通过，8 个原错页残留重新定位到正确 PDF 页，剩余项双侧证据完整；Playwright 以 1440x900/390x844 验证双栏/单栏、零横向溢出和零 console error。

## 2026-07-10 · 论文书创建入口 [Server/Web]

**触达**:
- `crates/server/src/lib.rs:route_create_book/is_valid_book_id` - 新增受当前 `.understand-book` 根目录约束的论文书创建命令，拒绝非法 ID 与既有目录，再复用 Workbench input import。
- `crates/server/src/lib.rs:list_book_library` - 书库同时枚举可信 `base.json` reader workspace 与有效 input manifest draft，并返回目标 route。
- `packages/web/src/api.ts:createBook` - 类型化 `POST /book/create` 上传契约和书库 route。
- `packages/web/src/App.vue:submitCreateBook` - 书库弹窗增加“打开/新建论文书”页签，读取 Markdown/PDF，创建后直接切换 Build Workbench。
- `crates/server/src/lib.rs:tests::book_create_writes_draft_under_library_root_and_switches_workbench` - 覆盖目录、manifest、snapshot、书库回显；拒绝测试覆盖非法 ID、重复目录和缺失输入不留空目录。

**入口**:顶部“打开书” -> “新建论文书” -> `POST /book/create` -> Build Workbench。
**测试**:`cargo test -p server book_create`;`cargo test -p server book_library_lists_reader_and_workbench_dirs_from_current_book_parent`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-07-10 · PH17/PH18 completion audit fixes [Server/Web]

**触达**:
- `crates/server/src/lib.rs:AppState.workbench_loaded_revision/route_build_workbench_state` - 用 latest durable job event revision 驱动一次性 Book/Reader 重载，覆盖 base 不变但 paper projection sidecar 更新的情况。
- `packages/web/src/App.vue:workbenchControlPending` - artifact gate 已可信但 job 仍 running/needs_user/interrupted 时保留 Workbench，不提前隐藏 projection 控制与恢复状态。
- `crates/server/src/lib.rs:append_permission_audit` - audit ID 从现存最大序号递增，retention 截断后不重复。
- `crates/server/src/lib.rs:tests::builtin_stage_runner_reaches_reader_and_reloads_app_book` - 扩展为同 base 新增 paper BookStructure 后按 job revision 重载 profile 的回归测试。

**入口**:`GET /book/build_workbench` snapshot revision；前端 init/action/refresh surface selection。
**测试**:`cargo test -p server builtin_stage_runner_reaches_reader_and_reloads_app_book`;`cargo test -p server workbench_resolves_build_decisions_and_executor_permissions`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;`cargo fmt -p server -- --check`;`git diff --check`。

## 2026-07-10 · PH18b operational warnings, audit, and retention [Core/Server/Web]

**触达**:
- `crates/server/src/lib.rs:write_build_job_atomic/enforce_build_job_retention` - 每 job 最多 200 events、每 workspace 最多 20 jobs，优先保留 running/newer job 并删除旧文件。
- `crates/server/src/lib.rs:append_permission_audit` - 权限决策独立原子写 `.build/audit/permissions.json`，最多保留 200 条，不随 job 清理丢失。
- `crates/server/src/lib.rs:operational_warnings` - snapshot 汇总 stale input、interrupted run 与 failed job 的结构化警告。
- `packages/core/src/workbench-stage-runner.ts:appendEvent/runWorkbenchStage` - 子进程写 job 时同样执行 event 上限，失败摘要包含 stage/run/log path/recoverable。
- `packages/web/src/api.ts:BuildWorkbenchSnapshot.operations` / `BuildWorkbenchPane.vue` - UI 显示运行警告、结构化 failure log 路径和最近权限审计。
- `crates/server/src/lib.rs:tests::workbench_retention_bounds_jobs_and_events` - 覆盖 job/event 文件级有界清理；既有 permission/stale 测试扩展 audit/warning 断言。

**入口**:`GET /book/build_workbench` 的 `operations`；`POST /build_workbench/permission.resolve` audit 写入。
**测试**:`cargo test -p server workbench`;`cargo test -p server fake_executor_failure_exposes_actionable_summary`;`cargo test -p server interrupted_builtin_run_is_detected_and_resumed_from_durable_job`;`cargo test -p server workbench_retention_bounds_jobs_and_events`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-07-10 · PH18a interrupted run recovery and resumable polling [Core/Server/Web]

**触达**:
- `crates/server/src/lib.rs:recover_orphaned_active_runs` - snapshot 依据 durable heartbeat 将超时 builtin run 标记为 `interrupted`，保留 stage/run 以供恢复并写结构化 failure summary。
- `crates/server/src/lib.rs:route_workbench_job_resume` - interrupted builtin job 使用新 run_id/PID/token 重启固定 runner，记录 `job_recovered`。
- `packages/core/src/workbench-stage-runner.ts:runWorkbenchStage` - 长阶段每 2 秒原子刷新 heartbeat，结束后清理 timer。
- `packages/web/src/components/BuildWorkbenchPane.vue` - 刷新后若 durable job 仍 running 自动恢复轮询，显示 failure summary 与 recoverable 提示。
- `packages/web/src/App.vue:refreshBuildWorkbench` - 轮询 snapshot 变为 reader 时重新执行 reader 初始化。
- `crates/server/src/lib.rs:tests::interrupted_builtin_run_is_detected_and_resumed_from_durable_job` - 覆盖 stale heartbeat→interrupted→resume→真实 source stage 完成。

**入口**:`GET /book/build_workbench` orphan detection；`POST /build_workbench/job.resume` durable recovery。
**测试**:`cargo test -p server interrupted_builtin_run_is_detected_and_resumed_from_durable_job`;`pnpm -C packages/core test -- workbench-stage-runner build-workbench`;`pnpm -C packages/core typecheck`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-07-10 · PH17d paper projections and sidecar planning [Core/Server/Web]

**触达**:
- `packages/core/src/workbench-stage-runner.ts:workbenchStageCommand/runPaperProjection` - 从 PH8 plan 生成固定 projection 命令，外部执行前后字节级保护 `source.txt/source_manifest.json`，写 stage completion 并重算 readiness。
- `crates/server/src/lib.rs:spawn_builtin_stage_runner` - builtin runner 扩展到 paper metadata/lexicon/profile sidecar/Pass2/BookStructure/PaperReadingGuide；Pass1 保持 executor artifact 路径。
- `crates/server/src/lib.rs:route_sidecar_plan_draft` - 新增固定 PH9 planner 端点，仅接受 request、受限 target view 与 LID/section scope。
- `skills/build/sidecar-plan.ts:readBookId` - pre-base draft 从 input manifest 取得 canonical book_id。
- `packages/web/src/components/BuildWorkbenchPane.vue:draftSidecarPlan` / `packages/web/src/App.vue:draftSidecarPlan` - Workbench 自然语言生成 plan draft 后沿用确认表单。
- `packages/core/test/workbench-stage-runner.test.ts` / `crates/server/src/lib.rs:tests::workbench_sidecar_plan_draft_runs_fixed_planner_for_manifest_book` - 覆盖 projection command contract/completion marker 与真实 sidecar planner。

**入口**:`adapter_mode=builtin` 的 PH8 stages；`POST /build_workbench/sidecar_plan.draft` -> confirm。
**测试**:`pnpm -C packages/core test -- workbench-stage-runner source-reconciliation paper-projection-chain build-workbench`;`cargo test -p server workbench`;`cargo test -p server builtin_stage_runner_reaches_reader_and_reloads_app_book`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-07-10 · PH17c async stage launch and reader handoff [Server/Web]

**触达**:
- `skills/build/workbench-stage-runner.ts` - 固定 CLI 入口以 run token 等待 server PID/job 落盘后调用 core stage runtime。
- `crates/server/src/lib.rs:spawn_builtin_stage_runner` - `shell=false` 启动固定 Node/tsx runner，stdout/stderr、PID、contract 和 command summary 落 `.build/executor-runs/<run_id>`。
- `crates/server/src/lib.rs:route_build_workbench_state` - snapshot 重算为 reader 后从可信 artifacts 重载 `Book/Reader`，避免前端 init 读到导入前旧书。
- `crates/server/src/lib.rs:is_current_existing_technical_book` - 有 paper input manifest 的 workspace 不再误走 technical fast-path。
- `packages/web/src/api.ts:WorkbenchAdapterMode` / `BuildWorkbenchPane.vue:selectedAdapterMode` - 暴露并默认选择“运行内置阶段”，显示 stage lifecycle/failure 类型。
- `crates/server/src/lib.rs:tests::builtin_stage_runner_reaches_reader_and_reloads_app_book` - 真实 Rust→tsx→core→artifact→snapshot→Book reload 集成测试。

**入口**:`POST /build_workbench/job.start` with `adapter_mode=builtin`，轮询 `GET /book/build_workbench` 完成 reader handoff。
**测试**:`cargo test -p server builtin_stage_runner_reaches_reader_and_reloads_app_book`;`cargo test -p server workbench`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;`pnpm -C packages/core typecheck`。

## 2026-07-10 · PH17b deterministic foundation stage runtime [Core]

**触达**:
- `packages/core/src/workbench-stage-runner.ts:runWorkbenchStage` - 从固定 input manifest/job 读取输入，受控运行 source reconciliation 或 hybrid foundation，原子更新 job 与 stage/readiness 事件。
- `packages/core/src/workbench-stage-runner.ts:runSourceReconciliation` - 首次 unresolved 进入 `needs_user`，复核决策齐备后生成 reviewed draft 并重跑同一 gate。
- `packages/core/src/workbench-stage-runner.ts:runHybridFoundation` - 从可信 stage source 构建 base/source manifest/PDF maps，随后重算 readiness 决定 job `done`。
- `packages/core/src/build-workbench.ts:BuildJobState` - 增加 stage lifecycle 事件、`interrupted` 状态和结构化 failure summary。
- `packages/core/test/workbench-stage-runner.test.ts` - 用真实最小 PDF 覆盖 foundation reader gate 与复核后重跑。

**入口**:`runWorkbenchStage({book_dir,job_id,stage})`，后续由 server 受控子进程启动。
**测试**:`pnpm -C packages/core test -- workbench-stage-runner source-reconciliation build-workbench`;`pnpm -C packages/core typecheck`。

## 2026-07-10 · PH17a source review decision gate [Core]

**触达**:
- `packages/core/src/source-reconciliation.ts:buildReviewedDraftFromDecisions` - 将 PH16 的逐 block 复核决策确定性应用到 Markdown span，缺决策、保持阻塞或缺证据时拒绝继续。
- `packages/core/src/source-reconciliation.ts:reconcilePaperSource` - unresolved block 记录 source span、Markdown/PDF evidence 与候选文本，并统一 `source_review_decisions.v1` 契约。
- `packages/core/src/zod.ts:SourceReconciliationReportZ` - 校验复核证据和 source span 字段。
- `packages/core/test/source-reconciliation.test.ts` - 覆盖 PDF 证据采纳、决策不完整、显式保持阻塞和带标题 fuzzy cursor 的回归。

**入口**: PH16 `review-decisions.json` -> `buildReviewedDraftFromDecisions` -> `reviewCandidateAndReconcile` -> trusted source gate。
**测试**:`pnpm -C packages/core test -- source-reconciliation`;`pnpm -C packages/core typecheck`。

## 2026-07-09 · PH16 source reconciliation review surface [Server/Web]

**触达**:
- `crates/server/src/lib.rs:build_source_review_snapshot` - `/book/build_workbench` 追加 `source_review` snapshot,包含 report、unresolved blocks、review draft、review decisions 和 rerun readiness。
- `crates/server/src/lib.rs:route_workbench_source_review_resolve` - 新增 `/build_workbench/source_review.resolve`,写 `.build/source-reconciliation/review-decisions.json`,记录 job event,并在复核决策齐备时回答来源对齐构建决策。
- `packages/web/src/api.ts:SourceReviewSnapshot` - 前端类型化 source review snapshot、决策 artifact 和 resolve payload。
- `packages/web/src/components/BuildWorkbenchPane.vue:resolveSourceReview` - Workbench 显示 Markdown/PDF/candidate 证据,提供采用 Markdown/PDF/候选/保持阻塞四个复核决策按钮。
- `packages/web/src/App.vue:resolveSourceReview` - 接入 source review resolve API 并复用 Workbench action refresh/handoff 流。

**入口**:`/book/build_workbench`;`/build_workbench/source_review.resolve`;`BuildWorkbenchPane` 的“来源对齐复核”面板。
**测试**:`cargo test -p server source_review`;`cargo test -p server build_workbench_snapshot`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-07-09 · PH15 interactive Workbench action UI [Web]

**触达**:
- `packages/web/src/components/BuildWorkbenchPane.vue:defineEmits` - 新增 create/start/resume/decision/permission action 输出,把 BuildDecisionRequest 与 ExecutorPermissionRequest 分开处理。
- `packages/web/src/components/BuildWorkbenchPane.vue:startSelectedJob` - Workbench UI 支持阶段、执行器、执行模式、Run ID、自动轮询、active run telemetry 和失败恢复提示。
- `packages/web/src/App.vue:applyWorkbenchAction` - 统一调用 build controller API,刷新 snapshot,并在 readiness handoff 到 reader 时重新初始化阅读表面。
- `packages/web/src/api.ts:WorkbenchAdapterMode` - 抽出 Workbench executor adapter mode 类型,供 UI 与 API payload 共用。

**入口**:`BuildWorkbenchPane` 的创建/启动/恢复/决策/权限按钮;`/build_workbench/job.create`;`/build_workbench/job.start`;`/build_workbench/job.resume`;`/build_workbench/decision.resolve`;`/build_workbench/permission.resolve`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-07-09 · PH14 executor adapter skeleton [Server/Web]

**触达**:
- `crates/server/src/lib.rs:write_executor_contract/apply_executor_adapter_skeleton` - 写 `.build/executor-runs/<run_id>/contract.json` 与 `prompt.md`,并用 fake adapter 记录完成、失败或权限请求事件。
- `crates/server/src/lib.rs:route_workbench_job_start` - `job.start` 通过 adapter skeleton 启动 run,不执行 shell、不自动批准权限、不写 trusted stage artifact。
- `packages/web/src/api.ts:workbenchJobStart` - 前端类型暴露 `adapter_mode` 和 executor contract/completion/failure 事件。
- `packages/web/src/components/BuildWorkbenchPane.vue:eventTypeLabels` - Workbench 事件日志显示 PH14 executor 事件标签。

**入口**:`/build_workbench/job.start` with `adapter_mode=contract_only|fake_success|fake_failure|fake_permission`。
**测试**:`cargo test -p server codex_executor_skeleton`;`cargo test -p server fake_executor`;`cargo test -p server workbench_job`;`pnpm -C packages/web typecheck`。

## 2026-07-09 · PH13 durable Build controller lifecycle [Server/Web]

**触达**:
- `crates/server/src/lib.rs:route_workbench_job_create` - 新增 create/reuse job API,按 current input fingerprint 复用未完成 job 并标记旧输入 job stale。
- `crates/server/src/lib.rs:route_workbench_job_start/route_workbench_job_resume/route_workbench_job_event_append` - 新增 durable run lifecycle 与 event append,只写 `.build/jobs/*.json`。
- `crates/server/src/lib.rs:route_workbench_decision_resolve/route_workbench_permission_resolve` - 新增构建决策与 executor 权限 resolve API,保持两类请求分离。
- `packages/web/src/api.ts:workbenchJobCreate/workbenchJobStart/workbenchDecisionResolve/workbenchPermissionResolve` - 前端暴露 PH13 controller API 类型。

**入口**:`/build_workbench/job.create`;`/build_workbench/job.start`;`/build_workbench/job.resume`;`/build_workbench/job.event.append`;`/build_workbench/decision.resolve`;`/build_workbench/permission.resolve`。
**测试**:`cargo test -p server workbench_job`;`cargo test -p server workbench_resolves`;`pnpm -C packages/web typecheck`。

## 2026-07-09 · PH12 Workbench import and draft workspace [Server/Web]

**触达**:
- `crates/server/src/lib.rs:route_workbench_input_import` - 新增 `/build_workbench/input.import`,把上传/选择的 `paper.md + paper.pdf` 写入 draft workspace,生成 `.build/input/manifest.json` 和 current input fingerprint。
- `crates/server/src/lib.rs:route_build_workbench` - snapshot 返回 `input.manifest/fingerprint/ready`,并用 current fingerprint 标记 stale source reconciliation。
- `packages/web/src/api.ts:api.workbenchInputImport` - 前端新增 PH12 input manifest/fingerprint 类型与导入 API。
- `packages/web/src/components/BuildWorkbenchPane.vue:importWorkbenchInput` - Workbench 支持上传文件或服务器路径导入输入,显示 manifest hash 摘要。
- `packages/web/src/App.vue:importWorkbenchInput` - 接入 Workbench input import 后刷新 snapshot。

**入口**:`/build_workbench/input.import`;`/book/build_workbench`;`BuildWorkbenchPane` 的“导入输入”。
**测试**:`cargo test -p server workbench_input`;`cargo test -p server build_workbench`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-07-09 · 完整构建工作台切片重定向 [Docs]

**触达**:
- `CONTEXT.md:Build Workbench` - 将 Workbench 术语从静态 build-mode 展示面修正为承载上传、job 创建/续跑、executor 启动、用户决策、权限审批和 reader handoff 的 build-mode 控制台。
- `CONTEXT.md:Build controller` - 新增服务端构建控制层术语,明确它驱动 executor/job/event,但不能绕过 `.build/<stage>` artifact gate。
- `docs/切片方案-paper-pdf-first-hybrid.md:PH10` - 将已实现的 PH10 明确标注为 snapshot shell,不是完整构建工作台。
- `docs/切片方案-paper-pdf-first-hybrid.md:PH12-PH18` - 新增完整构建工作台路线:上传/导入、job lifecycle、Codex executor adapter、交互 UI、source review、stage runner、恢复与观测。
- `SESSION_CHECKPOINT.md` - 整页刷新冷启动入口,下一步指向 PH12-PH18 完整 Build Workbench。

**入口**: 后续接手从 `docs/切片方案-paper-pdf-first-hybrid.md` 的 `3.1 Complete Build Workbench Completion Plan` 开始,优先 PH12。
**测试**:`rg -n "PH12|PH13|PH14|PH15|PH16|PH17|PH18|Build controller|snapshot shell" CONTEXT.md docs/切片方案-paper-pdf-first-hybrid.md SESSION_CHECKPOINT.md`;`git diff --check`。

## 2026-07-09 · Workbench gate 仅作用于 paper profile [Server]

**触达**:
- `crates/server/src/lib.rs:route_build_workbench` - 读取当前 `Book::content_profile_id`,已有 `base.json/source.txt` 的 `technical_learning` 书直接返回 reader/trusted_book,不读取 paper source alignment/PDF foundation artifacts。
- `crates/server/src/lib.rs:is_current_existing_technical_book` - 仅在目录 `base.json` 书号与内存 Book 一致且 `source.txt` 存在时启用 tech fast-path,保留无 base 的预构建目录进入 Workbench。
- `crates/server/src/lib.rs:tests::build_workbench_routes_existing_technical_book_to_reader_without_paper_gate` - 覆盖 tech 书即使有损坏 `source_manifest.json` 也不被 paper gate 阻断。
- `crates/server/src/lib.rs:tests::write_workbench_review_artifacts` - paper Workbench fixture 显式挂载最小 paper profile,不再依赖默认 tech 书误走 paper 检查。

**入口**:`/book/build_workbench`;`/build_workbench/sidecar_plan.confirm`;`/book/open` 打开的 pre-base 目录。
**测试**:`cargo test -p server build_workbench`;`cargo test -p server book_open_allows_prebase_directory_to_enter_workbench`;`cargo test -p server sidecar_plan_confirm_writes_confirmed_plan_and_build_spec`;`cargo test -p server`。

## 2026-07-09 · 前端中文化与项目文案记忆 [Web/Docs]

**触达**:
- `packages/web/src/components/BuildWorkbenchPane.vue` - Workbench 阶段、状态、决策、权限、辅助产物计划和事件文案改为中文显示映射,保留底层枚举/API 值。
- `packages/web/src/components/PdfReaderPane.vue` - PDF 阅读面页数、映射能力、加载态、空状态和来源按钮中文化。
- `packages/web/src/App.vue` / `packages/web/src/components/{TopBar,LeftRail,ReaderPane,RightRail}.vue` - 主阅读流程的按钮、弹窗、空状态、右栏问答和笔记/历史文案中文化。
- `docs/项目记忆.md` - 落项目记忆:主要面向中文用户,新增前端按钮/状态/流程描述默认中文。

**入口**: 前端所有主入口:TopBar、书库弹窗、Workbench、PDF Reader、左右栏、正文浮层、来源预览、笔记编辑器、右栏问答。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web);`git diff --check`。

## 2026-07-09 · PH10/PH11 Workbench UI and PDF.js reader [Server/Web]

**触达**:
- `crates/server/src/lib.rs:route_build_workbench` - 新增 `/book/build_workbench` 只读 snapshot,暴露 readiness/stage DAG/jobs/sidecar plan,不把 job state 当 artifact truth。
- `crates/server/src/lib.rs:route_sidecar_plan_confirm` - 新增 `/build_workbench/sidecar_plan.confirm`,持久化用户编辑后的 `sidecar_plan.json`、`form_draft.json` 和 `sidecar_build_spec.json`。
- `crates/server/src/lib.rs:route_open_book` - 支持手动打开尚无 `base.json` 的目录并进入 Workbench,避免 pre-reader build state 被 Book::load 阻断。
- `packages/web/src/api.ts:BuildWorkbenchSnapshot/api.buildWorkbench/api.sidecarPlanConfirm` - 前端新增 PH10 Workbench 类型与 client。
- `packages/web/src/components/BuildWorkbenchPane.vue` - 新增 Workbench 页面,分开展示 build decisions、executor permissions、stage DAG、job events 与 sidecar plan 表单。
- `packages/web/src/App.vue:init` - reader 初始化前先读取 Workbench readiness;非 trusted book 停在 Workbench, trusted book 才进入 reader。
- `packages/web/src/components/PdfReaderPane.vue` - 替换 iframe PDF surface 为官方 `pdfjs-dist` canvas/text-layer 懒渲染、LID overlay、PDF selection resolve 与 range projection。
- `packages/web/package.json` / `pnpm-lock.yaml` - web package 声明 `pdfjs-dist` 依赖以打包 pdf.js worker。

**入口**:`/book/build_workbench`;`/build_workbench/sidecar_plan.confirm`;reader init -> Workbench gate -> `BuildWorkbenchPane` 或 `PdfReaderPane`。
**测试**:`cargo test -p server`;`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-09 · PH10/PH11 frontend follow-up slice plan [Docs]

**触达**:
- `docs/切片方案-paper-pdf-first-hybrid.md:PH10` - 落档 Build Workbench 前端预构建页切片,明确 readiness/job/decision/permission/sidecar plan UI 边界。
- `docs/切片方案-paper-pdf-first-hybrid.md:PH11` - 落档 PDF.js 正文阅读面切片,把当前最小 PDF surface 后续收敛为 canvas/text-layer/lazy page surface。
- `docs/切片方案-paper-pdf-first-hybrid.md:Grill Traceability Patch Chapter` - 将 G11/G17b/G35/G36/G49/G50 指向 PH10/PH11,避免把 PH6/PH7 最小实现误读为完整前端闭环。
- `SESSION_CHECKPOINT.md` - 冷启动指向 PH10/PH11 两块待实现前端切片。

**入口**: 后续实现从 `docs/切片方案-paper-pdf-first-hybrid.md` 的 PH10 或 PH11 开切;PH10 先扫 Workbench/readiness 状态,PH11 先扫 `PdfReaderPane` 与 PDF runtime endpoints。
**测试**:`rg -n "PH10|PH11|G49|G50|G11|G36" docs/切片方案-paper-pdf-first-hybrid.md SESSION_CHECKPOINT.md`;`git diff --check`。

## 2026-06-22 · S0 monorepo 骨架 + 插件外壳 + TS↔Rust 基座 schema 链路

**触达**:
- `Cargo.toml` / `pnpm-workspace.yaml` / `package.json` / `tsconfig.json` — monorepo 三面骨架(cargo workspace + pnpm workspace 并置)`[ADR-0021]`
- `.claude-plugin/plugin.json` — 本地 Claude Code 插件清单(name understand-book)`[ADR-0022]`
- `skills/build/SKILL.md` — `/understand-book:build` 入口(插件 skill 命名空间 `/插件名:skill文件夹名`)+ 7 段管线编排骨架(占位,实编排留 S1–S3)`[ADR-0022]`
- `agents/pass1-local-extractor.md` / `agents/pass2-longrange-linker.md` — subagent 占位(I/O 契约已声明,实现留 S3)`[ADR-0010]`
- `crates/base-schema/src/lib.rs` — Rust 权威基座 schema:`Span/NodeKind/LidNode/GraphNodeType/GraphNode/EdgeScope/GraphEdge/ReadOnlyBase`(serde+ts-rs+schemars)+ `sample_base()` `[ADR-0021]`
- `crates/base-schema/tests/roundtrip.rs` — `rust_sample_roundtrips`(Rust 自洽)+ `ts_fixture_deserializes_zero_mismatch`(跨语言闸:读 TS fixture 零失配 + 逐字段比对)
- `packages/core/src/generated/*.ts` — ts-rs 生成的 8 个 TS 类型(权威产物,`export_to` 相对 src 目录三级)
- `packages/core/src/zod.ts` — 镜像 Rust 定义的 zod 自检 schema(产出前运行时闸)
- `packages/core/src/sample.ts` — `buildSampleBase()` 按生成类型构造样例基座
- `packages/core/test/roundtrip.test.ts` — vitest:zod 自检 + 产 `crates/base-schema/fixtures/sample-base.json`

**入口**:`/understand-book:build <epub|md>`(预构建,插件 slash 命令;`claude --plugin-dir <repo>` 加载);基座 schema 重生成 = `cargo test -p base-schema`(触发 ts-rs export)。
**测试**(三道确定性闸,全绿 = S0 判据① 闭环):
- `cargo test -p base-schema` — 8 export_bindings + rust_sample_roundtrips + ts_fixture_deserializes_zero_mismatch
- `pnpm -C packages/core test` — zod 自检 + 产 fixture
- `pnpm -C packages/core typecheck` — tsc --noEmit,验 sample 符合 ts-rs 生成类型

**未闭**:判据②(`/understand-book:build` 被 Claude Code 识别为命令)需在 harness 内加载插件验证。

## 2026-06-22 · S1(核心)段级 LID 切分器 + 分区不变式闸 + md 适配器

**触达**:
- `packages/core/src/segment.ts:segment` — Model A 切分(章/节=纯结构容器 span=子并集;标题=容器首个叶子段)→ `LidNode[]` `[ADR-0008]`
- `packages/core/src/partition.ts:checkPartitionInvariant` — 分区不变式闸:LID 唯一 / 同级数值递增 / 父⊇子 / 叶子全覆盖·无重叠 / 非内容字节显式归类 + 切段覆盖率
- `packages/core/src/md-adapter.ts:markdownToBlocks` — md → 忠实块映射 `SourceBlock[]`(`#` 行=heading,span 含 marker;空行分段)
- `packages/core/test/fixtures/sample.md` — 测试样书
- `packages/core/test/segment.test.ts` — vitest:切分→不变式→Model A 结构→span 回切→`LidNodeZ` 校形状(5 例)
- `packages/core/src/epub-adapter.ts:xhtmlToBlocks/epubToSource` — epub 解 zip(fflate)+ spine 顺序解析 xhtml(node-html-parser),忠实块映射(h1-6/p/li/blockquote/pre;嵌套块不重复下钻)→ `{source, blocks}`
- `packages/core/test/epub-adapter.test.ts` — vitest:块抽取 + 合成 epub 全链路分区不变式 100%(2 例)
- `skills/build/split-lid.ts` — S1 CLI(经 tsx 运行):epub/md→segment→分区不变式闸,打印报告、违例非零退出
- `.npmrc` — `node-linker=hoisted`(Windows 无符号链接权限,避 pnpm EPERM symlink)

**入口**:`pnpm exec tsx skills/build/split-lid.ts <epub|md>`(SKILL.md 编排第 1 段调它)。
**测试**:`pnpm -C packages/core test`(9 例:S0 2 + segment 5 + epub 2,覆盖率 100%)+ `pnpm -C packages/core typecheck`(tsc 0)+ `cargo test -p base-schema`。
**真书实测**:《游戏编程模式》epub → 3047 块 → 3399 节点(3047 叶 + 352 容器),source 25 万字,**分区不变式 ok、切段覆盖率 100.0000%** ⇒ S1 判据达成。
**未闭**:产物写盘 `.understand-book/`(留 S3/S7 整合);字节精确 span(中文 UTF-16↔字节,影响 S4 `book.text` 取真原文)留精化。

## 2026-06-22 · S2 窗口切分(LID 子树窗口 + 双约束预算 + 确定性 token 近似)

**触达**:
- `packages/core/src/window.ts:splitWindows` — 按 LID 章/节子树切窗口:atomic 子树整窗、超限子树内逐叶贪心细分(吸附 LID 边界不腰斩)、过小由贪心累加自然合并、顶层间封口(不跨卷);单叶超硬闸自成一窗标 `overBudget`(诚实不腰斩)`[ADR-0009]`
- `packages/core/src/window.ts:estimateTokens` — 确定性近似 token(CJK `/[一-鿿]/` 按 1、其余 0.25,ceil),零依赖
- `packages/core/src/window.ts` — `WindowBudget`(maxInputTokens 硬闸 + maxLeavesSoft 软闸)/ `DEFAULT_BUDGET`(12000/80 占位,留实测)/ `Window`(leafLids/tokens/spans/overBudget)
- `packages/core/test/window.test.ts` — vitest 8 例:estimateTokens + 划分(无遗漏无重叠)+ token 一致 + 细分 + 过小合并 + 软闸 + 不跨卷 + 单叶超限不腰斩
- `skills/build/window-cli.ts` — S2 CLI(tsx):适配器→segment→splitWindows→窗口数/token 分布 + 窗口层划分校验,违例非零退出

**入口**:`pnpm exec tsx skills/build/window-cli.ts <epub|md> [maxInputTokens] [maxLeavesSoft]`(SKILL.md 编排第 2 段;消费 S1 `LidNode[]` + source)。
**测试**:`pnpm -C packages/core test`(17 例:S0 2 + segment 5 + epub 2 + window 8,全绿)+ `typecheck`(tsc 0)。
**真书实测**:《游戏编程模式》3047 叶/正文≈174k tok。默认 12000/80→**64 窗口**(利用率 22.7%,median 3233),小预算 2000/40→152 窗口(57.3%);两者 **partition.ok、单叶超限=0、每窗 ≤ 硬闸** ⇒ S2 判据达成。完整预算扫描(软闸/硬闸敏感性 + 三层约束根因)见 `docs/实测-S2窗口利用率.md`;结论回填 ADR-0009「何时回头」。
**未闭**:软闸/硬闸最终数字待 S3 接 Pass1 抽取质量实测后定(当前为占位);融合批次打包进单次 subagent 调用 + 并发数留 S3。

## 2026-06-22 · S3 语义边两遍抽取确定性骨架(Pass1 输入 + merge+闸 + 目录 + schema 补 direction)

**触达**:
- `crates/base-schema/src/lib.rs:Direction` — 新增边方向枚举(directed/undirected),merge 去重键成员 `[ADR-0011/0023]`;`GraphEdge` 加 `direction` 字段;**不补 description**(收窄 ADR-0010,见 ADR-0023)。重生成 `generated/{Direction,GraphEdge}.ts`,同步 `zod.ts`/`sample.ts`/`sample_base()`/roundtrip fixture。
- `packages/core/src/pass1-input.ts:buildPass1Input/buildPass1Inputs` — Window → 每段前缀 `[LID]` 标注的抽取输入正文(回填红线物理前提 `[ADR-0004]`),纯确定性 `[ADR-0010]`
- `packages/core/src/merge.ts:mergeAndGate` — 节点按类型合并 occurrences(实体/概念并集 name keep-last、断言 keep-last)+ 确定性图谱闸(悬空丢不重建 + 最小连坐:边端缺失只丢边/断言锚悬空丢断言连坐边/实体部分 occ 悬空剔锚、全悬空丢节点)+ 边去重 (source,target,type,direction) keep higher weight(undirected 规范化端点)+ 可观测报告(锚定率/每条丢弃可见)`[ADR-0010/0011]`
- `packages/core/src/catalog.ts:projectCatalog` — merge 后 nodes 确定性投影全局目录(零幽灵,切片0 不带 summary)`[ADR-0010]`
- `agents/pass1-local-extractor.md` — Pass1 subagent 真实 prompt(抽实体/概念/断言+局部边,严格 JSON,回填红线/单一职责/不产长程边)
- `skills/build/SKILL.md` — 编排骨架第 2–5 段指向真实模块(window/pass1-input/merge/catalog)
- `skills/build/pass1-smoke.ts` — Pass1 小样真实抽取 smoke(list/emit/verify)
- `packages/core/test/{pass1-input,merge,catalog}.test.ts` — vitest 新增 17 例(pass1-input 4 + merge 9 + catalog 4)

**入口**:`buildPass1Inputs(windows,nodes,source)` → subagent `pass1-local-extractor`(harness 供 LLM,5 并发)→ `mergeAndGate(outputs, lidNodes)` → `projectCatalog(nodes)`。smoke:`pnpm exec tsx skills/build/pass1-smoke.ts <书> [<idx> emit|verify <json>]`。
**测试**:`pnpm -C packages/core test`(34 例全绿)+ `typecheck`(tsc 0)+ `cargo test -p base-schema`(11,含 ts_fixture 零失配,direction 贯通 Rust→TS)。
**小样真实抽取 smoke**:窗口 #12「2.4 类风格化还是函数风格化」(18 段)真实抽取 13 节点/8 边 → **回填红线窗口外 LID=0**、闸后全留、目录 13 条零幽灵 ⇒ pass1 prompt + 回填红线 + 确定性闸 链路打通。
**未闭**:全书 64 窗口真实 LLM Pass1 抽取 + 图谱锚定率≥90% 实测 + 固化 `.understand-book/` 留下一刀(S3 收尾);Pass2 长程边切片0 砍、留切片1+。

## 2026-06-22 · S3.5 小规模 Pass1 实测(4 窗口手抽 + 产小基座)

**触达**:
- `skills/build/pass1-batch.ts` — 消费多窗口抽取结果 → `mergeAndGate` → 锚定率(全书分母 + 已抽窗口分母)+ 固化 `.understand-book/<book_id>/base.json`(`ReadOnlyBaseZ` 产出前校验)
- `skills/build/fixtures/pass1-sample-4windows.json` — 窗口 #9/#12/#14/#17 的手工真实抽取结果(可复现基座 + S4 夹具来源)

**入口**:`pnpm exec tsx skills/build/pass1-batch.ts <书> skills/build/fixtures/pass1-sample-4windows.json "9,12,14,17"`(PowerShell 注意给索引列表加引号,逗号否则被当数组操作符)。
**实测**:41 抽取节点→merge 跨窗口合并 1(`entity:flyweight_pattern` 跨 #12/#14 occ 并集)→闸后 40 节点/26 边/目录 40 零幽灵/零丢弃;基座 zod 校验通过。**已抽窗口锚定率 55.36%(31/56)**,根因(非内容叶+召回)+ 全书口径建议见 `docs/实测-S3小规模锚定率.md`,回填 ADR-0011。
**未闭**:`.understand-book/` 基座 gitignore(生成物,由 fixture + pass1-batch 重建);全书锚定率定量仍留下一刀。

## 2026-06-23 · S4 读时确定性叶子工具(Rust:manifest/text/context-near/concept)

**触达**:
- `skills/build/pass1-batch.ts` — 固化 base.json 时同写 `.understand-book/<id>/source.txt`(原文旁路,book.text 取真原文用)`[ADR-0024]`
- `crates/base-schema/src/lib.rs:Span` — 注释口径修正「字节区间」→「UTF-16 code unit 区间」(消 schema↔segment.ts 实现矛盾);重生成 `generated/Span.ts` `[ADR-0024]`
- `crates/read-tools/src/lib.rs:Book` — 新 crate(纯函数库)。`load/new`(serde 读 base.json + source.txt,原文存 `Vec<u16>`)/ `text`(按 UTF-16 span 切真原文,单 LID 或 LID 区间)/ `manifest`(tree + stats_by_lid:child/leaf/anchored)/ `context_near`(树邻接 parent·child·prev·next + scope=local 边,自指边跳过、weight 降序、同 LID 去重、top-K)/ `concept`(全量 occurrences + related_entities);helpers `nodes_anchored_at`/`lids_of_node`/`parent_lid`;`ToolError` 信封(LID_NOT_FOUND/CONCEPT_NOT_FOUND,禁宽松降级)`[ADR-0014/0015/0024]`
- `crates/read-tools/src/main.rs` — CLI:`manifest [lid]` / `text <lid> [end_lid]` / `context <lid> [k]` / `concept <name>`
- `crates/read-tools/Cargo.toml` — 依赖 base-schema(path)+ serde/serde_json

**入口**:`cargo run -p read-tools -- <book_dir> <cmd>`(headless 库+CLI;localhost/HTTP 暴露留 S7)。`<book_dir>` = `.understand-book/<id>`。S5+ 直接调 `Book::{context_near,text,...}`。
**测试**:`cargo test -p read-tools`(6:text 2 / manifest 1 / context 2 / concept 1);全 workspace `cargo test` 全绿(17 = base-schema 11 + read-tools 6)。
**真基座实测**(《游戏编程模式》小基座 3399 LID / 40 图谱节点):`text 11.18.4` 逐字 == TS 预览(**UTF-16 跨语言一致**,证 ADR-0024 切法);`concept 享元模式` occ 跨 11.19/12.10 多锚;`context 12.10.2` near = 树邻接3(parent/prev/next)+ local边2(11.19.5 exemplifies 0.8 / 12.10.5 about 0.7),自指边(享元↔共享实例同锚 12.10.2)正确跳过。
**未闭**:context mid/far(概念二跳 / long_range)+ via `concept` kind 留切片1+;`book.query`(S5);`range` 仅支 LID 区间(段内字符 range 留后);near 默认 K=10 占位待 S8 回填;manifest leaf_count O(n²) 暂可接受(慢再优化)。

## 2026-06-23 · S5a book.query 内层 mini-loop 确定性骨架 + ModelAdapter trait + FakeAdapter(Rust)

**触达**:
- `crates/runtime/Cargo.toml` — 新 crate(模块 E 自建运行时),依赖 `read-tools`(检索骨架)+ `base-schema` `[ADR-0025]`
- `crates/runtime/src/lib.rs:Scope` — scope 两档枚举(Local/Chapter,顶档 chapter,无 global)`[ADR-0016/0025]`
- `...:ModelAdapter`(trait)— `complete(CompletionRequest{system,user}) → ParsedResponse{sufficient,answer?,citations,model_supplement}`,loop 与后端之间 provider 无关薄层 `[ADR-0016]`
- `...:retrieve` — 确定性档位检索(local=anchor∪`context_near`;chapter=local∪章内 anchored 叶);`chapter_root` 用 `NodeKind∈{Section,Chapter}` 上溯定位章(非物化路径段数)、`anchored_leaves_under` 收章内有锚定的叶 LID `[ADR-0025]`
- `...:query` — 内层 loop:合一轮 + **确定性交叉验停**(`citations⊆证据集` 悬空滤净 = 结构红线)+ 零有效/不足外扩(local→chapter)+ 触顶 `incomplete`+`CONTEXT_BUDGET_EXCEEDED` `[ADR-0004/0016]`
- `...:QueryResponse/Citation/SupplementOut` — 对外响应(符 V3 §4.1 核心子集;citations 全真 LID、`model_supplement` 无 LID 物理隔离)
- `...:#[cfg(test)] FakeAdapter` — 脚本化确定性测试替身(loop 每轮调一次 complete,按序吐)

**入口**:`runtime::query(book, q, anchor, adapter)`(S6 外层 E loop 调它;S5b NativeAdapter 真实对接)。
**测试**:`cargo test -p runtime`(4:留证 local 收口 / 零有效强制外扩+悬空滤净 / 触顶 incomplete / retrieve 含 anchor 真原文);全 workspace `cargo test` 全绿(21 = base-schema 11 + read-tools 6 + runtime 4)。
**未闭**:**S5b** = NativeAdapter(读 `.env` OpenAI-兼容端点 + CLI `query` 子命令 + 真实 glm-5.1 端到端裸调人工验);scope 档检索体积上限待实测;chapter 档真基座行为待 S5b 真跑观察。

## 2026-06-23 · S5b NativeAdapter(读 .env OpenAI 兼容)+ CLI + 真实 glm-5.1 端到端验

**触达**:
- `crates/runtime/Cargo.toml` — 加 `ureq`(features json,同步 HTTP)+ `dotenvy` 依赖(切片0 不上 tokio/async,承 `[ADR-0024]` 精神)
- `crates/runtime/src/lib.rs:NativeAdapter` — `from_env`(读 `OPENCODE_API_KEY/OPENCODE_BASE_URL/FLUID_LLM_MODEL`)+ `complete`(POST `{base}/chat/completions`,`response_format=json_object` + system 拼 `OUTPUT_CONTRACT`,`strip_fence` 剥 markdown 容错,`LlmOut` 中间解析 → `ParsedResponse`)`[ADR-0003/0025]`
- `...:OUTPUT_CONTRACT`(const)— JSON 输出契约;**S5b 真跑调优**:澄清 `sufficient` 语义 + `answer`/`model_supplement` 分工(否则 answer 错塞 supplement + 误判 incomplete,见 ADR-0025 S5b 回填)
- `crates/runtime/src/main.rs` — CLI:`runtime <book_dir> query <anchor_lid> <question...>`(读 .env,真实后端裸调 query 输出 JSON)

**入口**:`cargo run -p runtime -- <book_dir> query <anchor_lid> <question...>`(S6 外层 E loop 改调 `runtime::query`)。
**测试**:全 workspace `cargo test` 全绿(21)+ `clippy --workspace` 零警告;NativeAdapter 真实调用走人工验(真实 LLM 不可确定性复现,不入自动测,守 A2/B2)。
**真跑实测**(glm-5.1,anchor `11.18.4`):契约调优后 `answer` 完整正确(命令模式↔闭包)+ `citations` 3 条全真 LID(`11.18.2/4/5`,逐字原文)+ `model_supplement` 物理隔离(仅 GoF 定义)+ `scope_used=local` 收口不过度外扩 + `incomplete=false`。**结构红线 100%**。
**未闭**:scope 档检索体积上限 / chapter 真基座行为待更多查询观察;`book.query` 经 provider 抽象多后端留切片1+;S6 外层 E loop。

## 2026-06-23 · S6a 最小 memory 模块(Record + save/recall + 独立 JSON 落盘)

**触达**:
- `crates/runtime/src/memory.rs` — 新模块(读时运行时状态,非冻结基座)。`Record/Anchor/MemCitation/Usage`(符 V3 §4.3/`[ADR-0015]`,`type` serde rename)+ `SaveInput`(mem_id/citations 可缺)+ `RecallQuery`(切片0 维度);`MemoryStore::{open,default_path,save,recall}` —— `save`=内容寻址 mem_id(`fnv1a` 稳定哈希,`mem_{book|type|anchor|content}`)upsert + note/highlight citation 自动派生(锚回 anchor.lid)+ count 累加 + 整文件重写;`recall`=book_id/lid/type/layer 精确 + text 子串合取过滤、按 mem_id 排序(确定性);时间戳/路径调用方注入(可测)`[ADR-0026/0015/0006]`
- `...default_path` — `UNDERSTAND_BOOK_MEMORY_DIR` env 覆盖,否则 `<home>/.understand-book/memory/memory.json`;**绝不落进 `.understand-book/<book_id>/`(只读基座),守物理隔离 `[ADR-0006]`**
- `crates/runtime/src/lib.rs` — 加 `pub mod memory;`;错误信封复用 `read_tools::ToolError`(IO/解析失败 → `INTERNAL_ERROR`,守统一信封 `[ADR-0015]`)

**入口**:`MemoryStore::open(path)` → `save(SaveInput, now)` / `recall(&RecallQuery)`(S6b 外层 loop 的 `memory.save/recall` 工具调它;落盘默认走 `default_path()`)。
**测试**:`cargo test -p runtime`(10:4 query + 6 memory)全绿;全 workspace `cargo test`(27 = base-schema 11 + read-tools 6 + runtime 10)+ `clippy --workspace` 零警告。memory 6 测:save→recall 往返 / citation 自动派生 / position 不派生 / 内容寻址 upsert 幂等(count 累加不重复)/ recall 维度过滤合取 / 落盘隔离持久化重载。
**未闭**:concept recall 维度 + 跨书 + Phase1/2 consolidation + 四层产物 + 遗忘留议题7/切片1+;memory.* 接进外层 loop tool 集留 **S6b**;真实 home 目录落盘走 S6c 真跑(单测用 temp 隔离)。

## 2026-06-23 · S6b 外层 E 编排 loop + ModelAdapter.chat + 双重停机(FakeAdapter 确定性测)

**触达**:
- `crates/runtime/src/lib.rs` — `ModelAdapter` trait 加 `chat(&[Message],&[ToolSpec])→AssistantTurn`(`complete` 内层不动);新协议类型 `Role/Message/ToolCall/ToolSpec/AssistantTurn`;`message_to_json`(Message→OpenAI 请求体形:assistant tool_calls 嵌套 `{id,type,function}`、tool 带 tool_call_id);`NativeAdapter::chat`(POST `/chat/completions` 带 `tools` schema,解析 `choices[0].message.{content,tool_calls}` + `usage.total_tokens`→AssistantTurn)`[ADR-0026]`;内层 query 测的 FakeAdapter 补 `chat` 桩(unimplemented,该测不涉外层)
- `crates/runtime/src/orchestrator.rs` — 新模块。`run(book,store,adapter,question,now,cfg)→OuterOutcome`:messages 会话态 loop——`chat`→无 tool_calls 即终答(incomplete=false)/有则追加 assistant 回合 + 逐个 `dispatch` 回填 `role:tool` 结果 → 双重停机(`turns≥max_turns ∨ spent>token_budget`,`usage.total_tokens` 累加、无 usage 退 `estimate_tokens` 兜底)触顶标 `incomplete`+`CONTEXT_BUDGET_EXCEEDED`;`tool_specs()` 7 工具 JSON-Schema(book.query/text/context/concept/manifest + memory.save/recall);`dispatch` 派发到 `crate::query`/`Book::*`/`MemoryStore::*`,**错误原样回喂 ToolError 信封(不降级)**`[ADR-0026/0015/0016]`;`OuterConfig`(max_turns=12/token_budget=120k 占位)
- `crates/runtime/src/lib.rs` — 加 `pub mod orchestrator;`

**入口**:`runtime::orchestrator::run(...)`(S6c CLI `chat` 子命令 + 真实后端驱动它)。
**测试**:`cargo test -p runtime`(14:4 query + 6 memory + 4 orchestrator)全绿;全 workspace `cargo test`(31)+ `clippy --workspace --all-targets` 零警告。orchestrator 4 测(FakeAdapter 双队列脚本化 chat+complete):多跳收敛(query→memory.save→终答,内层 complete 联动、citation 自动锚回)/ max_turns 触顶标 incomplete / 工具错误回喂 LID_NOT_FOUND 信封非静默 / 报错后 loop 继续并收敛。
**未闭**:`NativeAdapter::chat` 真实 glm-5.1 验证(原生 tools 是否稳)+ CLI `chat` 子命令留 **S6c**(真实 LLM 不入自动测,守 A2/B2);max_turns/token_budget 实测回填 ADR-0016。

## 2026-06-23 · S6c CLI chat 子命令 + 真实 glm-5.1 外层 loop 端到端人工验

**触达**:
- `crates/runtime/src/main.rs` — CLI 重构为子命令分发:`query`(S5b 内层,不变)+ 新 `chat <question...>`(读 `.env` NativeAdapter + `MemoryStore::open(default_path())`,驱动 `orchestrator::run`,输出 `OuterOutcome` JSON)`[ADR-0026]`;`now_ts()`(epoch 秒,memory 时间戳)`[ADR-0025/0026]`
- (`NativeAdapter::chat` 实现已随 S6b 落地;S6c 只加 CLI 入口 + 真跑验)

**入口**:`cargo run -p runtime -- <book_dir> chat <question...>`(读 `.env`,真实后端驱动外层 E loop)。
**测试**:全 workspace `cargo test`(31)+ `clippy` 零警告不变;`chat` 走真实 LLM 人工验(不入自动测,守 A2/B2,镜像 S5b)。
**真跑实测**(glm-5.1,书 `game-programming-patterns`,问「查证命令模式核心思想并记笔记」):外层 loop **10 轮自主多跳**(concept/query/text + memory.save)收敛、`incomplete=false`、tokens 31608(<120k budget)。确定性核查 memory 库:落 1 条 note,`anchor.lid="11"` + `citations=[{lid:"11",book_id}]`,LID 11 = 真 LID(`text 11`→"第2章 命令模式",可跳原文)。**多跳收敛 + 触顶诚实 + recall 带可验证 LID citation 三判据兑现**;回填 ADR-0026 何时回头(glm-5.1 tools 稳 / usage 口径 / max_turns 偏近上限待 S8)。
**未闭**:max_turns/token_budget 实测数(单次 10/12 偏近,待 S8 金标准集观察)回填 ADR-0016;reader.* 四动作 + 闭环留 S7;consolidation/跨书/concept recall 维度留议题7/切片1+。

## 2026-06-23 · S7 命令优先阅读器 + 闭环四动作(memory 抽 crate 拆环 + reader crate + 接入外层 loop)

**触达**:
- `crates/memory/{Cargo.toml,src/lib.rs}` — **S7a 从 runtime 抽出的独立 crate**(deps: read-tools/serde/serde_json):内容原样搬 `runtime/src/memory.rs`(`Record/Anchor/MemCitation/Usage/SaveInput/RecallQuery/MemoryStore` + 6 测),零行为变更。拆 `runtime↔reader` 循环依赖(crate 强制 DAG)+ 兑现 memory 独立模块地位 `[ADR-0027/0006]`
- `crates/runtime/src/memory.rs` — **删除**(搬至 memory crate);`runtime/src/lib.rs` 删 `pub mod memory;`;`runtime/Cargo.toml` 加 `memory`+`reader` 依赖;`orchestrator.rs`/`main.rs` 的 `crate::memory::`/`runtime::memory::` → `memory::` `[ADR-0027]`
- `crates/reader/{Cargo.toml,src/lib.rs}` — **S7b 新 crate**(deps: read-tools/memory/serde;dev: base-schema)。`Reader`(会话态:leaf_lids 叶序 + anchor_idx + radius + selection)/ `Viewport{anchor_lid,visible_lids}` + `ViewportEffect/HighlightEffect/NoteEffect/ReaderState`(返 effect 非裸 ack)。`new`(叶序=lid_nodes 过滤 children 空)/ `viewport`(**叶序滑动窗口** anchor±radius saturating)/ `goto_lid`(叶→锚该叶、容器→子树首叶、不存在→LID_NOT_FOUND 不降级)/ `scroll`(delta clamp)/ `highlight`(委托 memory.save,content=book.text 原文片段)/ `note`(委托 memory.save,content=text)/ `state` / `render`(读 memory.recall 画标注,**标注单源=记忆层**)`[ADR-0007/0015/0027]`
- `crates/runtime/src/orchestrator.rs` — **S7c reader 接入外层 loop**:`tool_specs()` 加 reader.gotoLid/scroll/highlight/note/state 五工具、**移出 book.manifest**(真跑实测全树 token 炸弹);`dispatch` 加 reader.* 分支 + `reader:&mut Reader` 参数;`run` 建会话态 `Reader::new(book,DEFAULT_RADIUS)` 贯穿 loop;SYSTEM_PROMPT 强化「要求翻页/高亮/记笔记必须调对应 reader 工具」;新增 `UB_TRACE` env-gated loop trace(诊断设施)`[ADR-0026/0027]`

**入口**:`reader::Reader::{new,goto_lid,scroll,highlight,note,state,render}`(headless 库);经外层 `runtime::orchestrator::run` 的 reader.* 工具由 agent 驱动;CLI `runtime <book_dir> chat <q>`(`UB_TRACE=1` 开 trace)。
**测试**:`cargo test --workspace`(**42 = base-schema 11 + memory 6 + read-tools 6 + reader 10 + runtime 9**)+ `clippy --workspace --all-targets` 零警告。reader 10 测:叶序窗口/scroll clamp/goto 叶·容器·不存在/note·highlight 委托 memory 单源/highlight 取原文/render 读 recall 画标注/标注跨 reader 实例可见(证单源)。orchestrator +1:`closed_loop_query_goto_highlight_note`(FakeAdapter 脚本化「问→跳转→高亮→记笔记」,标注真落 memory、citation 锚回真 LID)。
**真跑实测**(glm-5.1,书 game-programming-patterns,临时 memory 隔离):给定 LID「把 11.18.2 高亮并记笔记」→ 外层 loop **turn1 并行调 reader.highlight+reader.note → turn2 终答**,incomplete=false、turns=2、tokens 2644。memory 库落 highlight(content=11.18.2 真原文片段)+ note(content=笔记),**两条 citation 均锚回真 LID 11.18.2**。**闭环判据兑现**(切片0 总判据第 3 条)。诊断发现回填 ADR-0027:① book.manifest 全树 token 炸弹(一次 38k→205k 触顶)→ 移出外层工具集;② glm 不给 LID 时倾向 book.* 反复探索不调 reader → prompt 强化。
**未闭**:DEFAULT_RADIUS(=3)叶序窗口半径 / max_turns / token_budget 实测回填 ADR-0016/0027(S8);不给明确 LID 时的定位语义(book.query/concept 探索收敛)属切片0 已有部分,与 reader 闭环正交;reader 全集(面板系统)+ 段内字符 range + 真 GUI 渲染层留切片1+;memory 默认落 `~/.understand-book/memory/`(真跑用 UNDERSTAND_BOOK_MEMORY_DIR 隔离)。

## 2026-06-23 · S8 金标准集 + 验收闸(内层 book.query 主度量;真跑结构红线 100%)

**选址决策**:金标准集针对**内层 `book.query`**(每条 `{q, anchor_lid, expect_cite[]}`),非外层 E loop——结构红线度量最干净、可复现、省 token,anchor 预供;直接消费 `runtime::query`。外层 loop 闭环已于 S7 兑现(切片0 总判据第 3 条),不在 S8 重复度量。

**触达**:
- `crates/runtime/src/goldset.rs` — 新模块 `[ADR-0004]`。`GoldItem{id,q,anchor_lid,expect_cite}` / `ItemResult` / `GoldReport`;**纯函数闸**:`structural_check`(返回 citation 全是真 LID 无悬空=结构红线,列悬空 LID)+ `semantic_metrics`(recall=|返回∩期望|/|期望|、precision=|返回∩期望|/|返回|,**人工评的客观信号非自动判定**)+ `build_report`(聚合结构红线率/均值/incomplete 数);runner `evaluate_item`/`run_goldset`(逐条 `crate::query`,返回去重 citation LID)。自动验收只到结构红线;语义达阈+人工试读由人裁(守 B2)`[ADR-0004]`
- `crates/runtime/src/lib.rs` — 加 `pub mod goldset;`
- `crates/runtime/src/main.rs` — CLI 加 `goldset <file.json>` 子命令:读金标准集 JSON → `run_goldset`(NativeAdapter 真跑)→ 输出 `GoldReport` JSON + stderr 一行汇总;**结构红线 <100% 非零退出**(确定性闸,违 ADR-0004 即 fail);errored 显式告警
- `crates/runtime/goldset/game-programming-patterns.json` — **12 条金标准集**(从真基座 40 节点确定性推导),覆盖 4 章区:命令模式/闭包/撤销/头等函数(11.18–11.19)、享元/工厂方法/对象池/状态(11.19/12.10)、简单性/抽象解耦(9.x)、观察者/数据绑定(13.12);每条 expect_cite = 锚定 claim 的真 LID
- `goldset.rs` runner **逐条容错**:provider 偶发错(空响应)重试一次,仍失败记 `ItemResult.error`+`GoldReport.errored` 单列(不算结构红线分母,显式记录非静默降级 `[ADR-0015]`);真跑首跑遇 1 条 glm 空响应即此设计的由来

**入口**:`cargo run -p runtime -- <book_dir> goldset crates/runtime/goldset/game-programming-patterns.json`(读 `.env`,真实后端)。
**测试**:`cargo test --workspace`(**47 = base-schema 11 + memory 6 + read-tools 6 + reader 10 + runtime 14**;runtime +5 goldset:结构红线判悬空 / 语义 recall·precision / 聚合 / errored 排除 / run_goldset 经 FakeAdapter 确定性端到端)+ `clippy -p runtime` 零警告。真 LLM 不入自动测(守 A2/B2)。
**真跑实测**(glm-5.1,真基座 game-programming-patterns):**结构红线 100.0%(12/12,0 悬空)** + mean_recall 1.00 + mean_precision 0.75 + incomplete 0 + errored 0;12 条全 local 档收口;answer 抽查贴原文正确。**切片0 总判据①(结构红线 100%)兑现** + 语义信号达基线(recall≥0.9 建议阈)。回填 ADR-0004(结构红线兑现 + 语义阈值建议 + 金标准集规模 12)、ADR-0016(内层 scope 全 local 收口)。
**未闭(切片0 收尾,需用户裁)**:① **人工试读认可**(语义质量最终拍板,人类动作);② **命令面契约冻结为基线**(V3 §6,切片0 完成充要)→ 冻结后切片0 完成,可起切片1+。

## 2026-06-23 · S9 ModelAdapter 确定性产物 JSON 规范性硬化(鲁棒抽取 + 抽不到诚实报错)

> 切片0 收尾后第一刀(不在切片0 判据内);**内部硬化,不动命令面契约**。

**触达**:
- `crates/runtime/src/lib.rs:extract_json_object` — 新增鲁棒 JSON 对象抽取:`strip_fence` 剥围栏 → 扫描第一个**平衡 `{}` 子串**(跳过字符串字面量内的括号 + `\` 转义,引号内 `{`/`}` 不计深度)→ 抽不到返 `None`。只负责「形状」抽取,值正确性仍由内层 `query` 的 `citations⊆证据集` 确定性交叉验停再校验 `[ADR-0016 决策5/ADR-0004]`;仅扫 ASCII 字节,切片落 char 边界(注释证安全)
- `crates/runtime/src/lib.rs:NativeAdapter::complete` — `serde_json::from_str(strip_fence(content))` → 先 `extract_json_object(content)`,**抽不到合法 JSON / 空响应 → 显式 `AdapterError`**(经 `query` 映射 `PROVIDER_ERROR`,不静默成功,守禁宽松降级 `[ADR-0015]`);外层 `chat`(原生 tool_calls)不动
- **不改** `OUTPUT_CONTRACT`/prompt(强后端走 `json_object` 模式,鲁棒抽取纯防御偶发;放宽 prompt 自由推理属 ReActAdapter/切片1+);**不动** `ParsedResponse`/命令面契约/错误信封/值校验

**入口**:`NativeAdapter::complete`(内层 `query` 唯一确定性产物路径调它)。
**测试**:`cargo test --workspace`(**50 = base-schema 11 + memory 6 + read-tools 6 + reader 10 + runtime 17**;runtime +3 S9:三形态抽对 / 字符串内括号·转义跳过 / 空·纯散文·不平衡→None)+ `clippy -p runtime --all-targets` 零警告。真 LLM 不入自动测(守 A2/B2)。
**判据**:① 三形态单测全过;② 抽不到/空内容 → `PROVIDER_ERROR` 非静默(`None`→`AdapterError`→`query` 映射);③ 真跑回归结构红线 100% **待人工验**(走 `.env`/glm-5.1,B2)。
**未闭**:判据③ 真跑回归(glm-5.1 偶发空/啰嗦响应消化验证)由用户拍板跑;内层 `query` 是否需逐条重试(goldset runner 已有重试,内层裸调暂无)留实测 `[ADR-0016]`;ReActAdapter 完整解析回填(注模板/多字段宽松容错)留切片1+。

## 2026-06-24 · S10a 新 server crate:book.* 只读 GET REST 投影 + API DTO ts-rs 导出 packages/web

> 切片1「前端阅读器」第一刀(A4 子切片);`[ADR-0028]` 命令面 REST 投影落地。本刀只投影 `book.*`(reader/memory→S10b、query→S10c、agent→S10f、静态资源→S10e)。

**触达**:
- `crates/server/Cargo.toml` — 新 crate(`tiny_http=0.12`,依赖 `read-tools`;`base-schema` 仅 dev 供 `sample_base`)。坐 DAG 顶端(S10a 只需 read-tools)`[ADR-0028 决策2]`
- `crates/server/src/lib.rs:route` — **纯函数路由** `route(&AppState, method, url) -> Reply`(脱 socket 可单测,守 A2):`/book/manifest`(GET)+ `/book/text?lid=&end=` + `/book/context?lid=&k=` + `/book/concept?name=` 1:1 投影 `Book` 四叶子 `[ADR-0028 决策3]`;`AppState{book}`(S10b 扩 reader/store)
- `crates/server/src/lib.rs:err_reply/status_for` — `ToolError` §4.4 信封**原样透传** + category→HTTP status(validation 400/not_found 404/provider 502/budget 429/internal 500);非 GET→405 `METHOD_NOT_ALLOWED`、未知路由→404 `ROUTE_NOT_FOUND`(传输层码,非冻结 command 枚举)
- `crates/server/src/lib.rs:parse_query/percent_decode` — 最小 query 解析 + `%XX`/`+` 解码(支持 CJK 概念名);`INVALID_RANGE`(缺必填参)/`INVALID_K`(k 非整数)
- `crates/server/src/main.rs:main` — tiny_http 同步、4 worker 线程共享 `Arc<Mutex<AppState>>`(单会话单书,不上 tokio `[ADR-0024/0028]`);`Book::load(<dir>)`,`UNDERSTAND_BOOK_DIR`/`UNDERSTAND_BOOK_ADDR`(默认 127.0.0.1:8787)
- `crates/read-tools/src/lib.rs` — `Manifest/ManifestNode/LidStats/Context/ContextItem/Via/Concept/ToolError` 补 `#[derive(TS)]` + `#[ts(export_to="../../../packages/web/src/generated/")]`(+ Cargo `ts-rs=10`)`[ADR-0028 决策6]`
- `crates/runtime/src/lib.rs` — `QueryResponse/Citation/SupplementOut` 补 `#[derive(TS)]` 同导出目标(+ Cargo `ts-rs=10`);S10a 先导出类型,query 端点 S10c 接
- `packages/web/src/generated/*.ts` — ts-rs 新生成 11 个 API DTO TS 类型(`ManifestNode.span` 跨指 base-schema → `import { Span } from "../../../core/src/generated/Span"`,跨 crate 相对路径实测正确)

**入口**:`cargo run -p server -- <book_dir>`(localhost REST);前端经 `GET /book/*` 消费(S10d)。API TS 类型重生成 = `cargo test -p read-tools`/`-p runtime`(触发 ts-rs export)。
**测试**:`cargo test --workspace`(**71 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 20 + server 10**;server 10:四端点正确/错误透传 status 映射/未知路由 404/非 GET 405/percent 解码 CJK·空格/query 解析)+ `clippy -p server -p read-tools -p runtime` 净。
**实测落点回填**:① ts-rs 跨 crate 导出目标 = `packages/web/src/generated/`,跨指 base 类型由 ts-rs 自动算相对 import,可行(`[ADR-0028/0021]`);② tiny_http 路由 = `url().split('?')` 手解 query + 自写 `percent_decode`(无额外依赖)。
**未闭(B2,需真书)**:手动 curl 各 GET 返正确 JSON / 错误返信封——需一本预构建真书目录,留有真书时验(路由逻辑已由纯函数单测确定性覆盖)。`ToolError` 暂无 `recovery` 字段(各 command 真产 recovery 时再补,不擅扩冻结契约)。

## 2026-06-24 · S10b reader.*/memory.* POST 端点 + Reader/MemoryStore 会话状态

> 切片1 第二刀;`[ADR-0028 决策3]` 可变命令 → POST 投影。承 S10a 的 server crate。

**触达**:
- `crates/server/Cargo.toml` — 加 `reader`/`memory` 依赖(route 需 Reader/MemoryStore/SaveInput/RecallQuery/Anchor)
- `crates/server/src/lib.rs:AppState` — 扩 `{book, reader: Reader, store: MemoryStore}`(单会话:只读书 + 会话态阅读器 + 用户私有记忆库,物理隔离 `[ADR-0006]`)
- `crates/server/src/lib.rs:Req` — 新增请求输入结构 `{method, url, body, now}`;`route` 签名改 `route(&mut AppState, Req) -> Reply`(POST 带 JSON body + 注入时间戳,守 A2 确定性)
- `crates/server/src/lib.rs:route/route_book/route_mut` — 按命名空间前缀分派:`/book/*`→GET(route_book,S10a 逻辑抽出)、`/reader/*`+`/memory/*`→POST(route_mut),错方法→405、未知路径→404。字段级不相交借用(reader mut + book shared + store mut 同时)
- `crates/server/src/lib.rs:route_mut` — POST 七端点:`/reader/{goto,scroll,highlight,note,state}`(reader.* 返 effect;highlight/note 委托 `memory.save`,标注单源 `[ADR-0015]`)+ `/memory/{save,recall}`(直读写记忆层;save layer 缺省按 type 默认 position→session 否则 long_term `[ADR-0006]`;recall 各维度 Some 过滤、book_id 缺即跨书)
- `crates/server/src/lib.rs:body_value` — JSON body 解析(空 body→空对象供 reader.state 无字段端点;非法→400 INVALID_RANGE)
- `crates/server/src/main.rs:main` — 构建 `Reader::new(&book, DEFAULT_RADIUS)` + `MemoryStore::open(default_path())`(物理隔离库)入 AppState;worker 读 POST body(`rq.as_reader().read_to_string`)+ 生成 `now_ts`(epoch 毫秒)+ 传 `Req`
- `crates/server/src/main.rs:now_ts` — memory 时间戳(epoch 毫秒串;ISO 化留后)

**入口**:`cargo run -p server -- <book_dir>`;前端经 `POST /reader/*`、`POST /memory/*` 驱动(S10d)。标注回显 = 客户端调 `POST /memory/recall`(标注单源 memory,无独立 render 端点)。
**测试**:`cargo test --workspace`(**73 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 20 + server 12**;server +2:reader goto/scroll/highlight/note/state effect + 非法 LID 404 不降级 + memory save/recall 往返 + 缺字段 400 + 坏 body 400 + 错方法 405)+ `clippy -p server` 净。
**未闭(B2,需真书)**:手动 POST 驱动真 reader/标注落盘/recall 回显——留有真书时验(纯函数 route 已确定性覆盖全部分支)。query 端点(`/book/query`)= S10c。

## 2026-06-24 · S10c book.query POST 端点(接 runtime::query + adapter 注入)

> 切片1 第三刀;`[ADR-0014/0016/0028]` LLM 命令 REST 投影。承 S10b 的 server crate。

**触达**:
- `crates/server/Cargo.toml` — 加 `runtime` 依赖(route_query 需 `runtime::query` + `ModelAdapter`/`NativeAdapter`)
- `crates/server/src/lib.rs:AppState` — 扩 `adapter: Box<dyn ModelAdapter + Send>`(`+ Send` 供 `Arc<Mutex<_>>` 跨 worker 线程;LLM 后端经它触模型)`[ADR-0028]`
- `crates/server/src/lib.rs:route` — 单列 `POST /book/query` 分支于 GET-only `route_book` 之前(`book.*` 命名空间但 LLM 命令、秒级非确定性叶子,方法分派对它例外)`[ADR-0014]`;GET→`query_method_not_allowed` 405(文案单列免误导)
- `crates/server/src/lib.rs:route_query` — body `{q, anchor_lid?}` → 直调 `runtime::query`(确定性档位检索 + LLM 合一轮 + 确定性交叉验停 = 结构红线 `[ADR-0004/0016]`)→ 返 `QueryResponse`;缺 q→400 `INVALID_RANGE`;**anchor 缺省取 `reader.state().viewport.anchor_lid`**(读到哪问到哪);`scope` 入参暂不接(内层切片0 固定 local→chapter auto,无 scope 旋钮,留切片1+);provider 错经 query 映射 `PROVIDER_ERROR`(category=provider→502)透传不降级 `[ADR-0015]`
- `crates/server/src/lib.rs:UnconfiguredAdapter` — `.env` 缺失兜底 adapter(complete/chat 均返 `AdapterError`);book/reader/memory 浏览不被 LLM 配置阻塞,仅 query 触模型时诚实报错 `[ADR-0015]`
- `crates/server/src/main.rs:main` — `NativeAdapter::from_env()` 装配 adapter,失败则 eprintln 警告 + 兜底 `UnconfiguredAdapter` 入 `AppState`

**入口**:`cargo run -p server -- <book_dir>`(读 `.env`);前端经 `POST /book/query`(body `{q, anchor_lid?}`)消费(S10d 问答框)。
**测试**:`cargo test --workspace`(**78 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 20 + server 17**;server +5:StubAdapter 确定性返 QueryResponse(scope local/全真 LID citation)+ 显式 anchor + 缺 q 400 + GET 405 + provider 错 502)+ `clippy -p server` 净。**StubAdapter 实现 `ModelAdapter`** 把 query 路由层脱真 LLM 做成确定性可测(守 A2,超出原 checkpoint 的"仅 B2")。
**未闭(B2,需真书 + `.env`)**:真跑 `POST /book/query` 返结构红线 100% 的 `QueryResponse`(走 glm-5.1,真 LLM 不入自动测,守 A2/B2,镜像 S5b/S8)由用户拍板跑。`scope` 入参待 `runtime::query` 长出 scope 旋钮(切片1+)。

## 2026-06-24 · S10d Vue/Vite 前端 app(连续正文 + 四动作 + 标注 overlay + 读位感 + 问答框)

> 切片1 第四刀;`[ADR-0028]` 前端栈落地(解 ADR-0021 PENDING)。最小连通画面 = S10a+S10b+S10d。

**触达**(新 `packages/web` Vue3+Vite,pnpm workspace 成员):
- `packages/web/package.json` — `@understand-book/web`(deps:`vue`;dev:`vite`/`@vitejs/plugin-vue`/`vue-tsc`/`typescript`);scripts `dev`/`build`(vue-tsc + vite)/`typecheck`
- `packages/web/tsconfig.json` — extends 根 tsconfig + `lib:[ES2022,DOM,DOM.Iterable]` + `types:[vite/client]`;`vite.config.ts` — `@vitejs/plugin-vue` + dev proxy `/api`→`127.0.0.1:8787`(同源无 CORS,承 ADR-0022;`UNDERSTAND_BOOK_ADDR` 可覆写)
- `packages/web/index.html` / `src/main.ts` / `src/env.d.ts`(`.vue` shim + vite/client)/ `src/style.css`(连续正文 CSS:段落自然流、无框/无 LID 标号、anchor 淡左缘、hl 高亮底色、note 行内徽标)
- `packages/web/src/api.ts:api` — 类型化命令面 REST 客户端 `[ADR-0028]`:`manifest/text/query/goto/scroll/highlight/note/state/recall` 九端点;import 生成 DTO `Manifest`/`QueryResponse`/`ToolError`;手定 `Viewport/ViewportEffect/ReaderState/MemoryRecord`(memory 类型未走 ts-rs);`ApiError` 携 §4.4 信封(category/error_code 供 UI 分流);`http<T>` 统一 GET/POST + 错误透传
- `packages/web/src/App.vue` — `<script setup>` 单组件:① 连续正文列(`GET /book/text` 逐 `visible_lids` 取真原文,段落承 `data-lid` 隐形锚、点击选中)② 四动作(上/下翻→`reader.scroll`、跳转 LID→`reader.goto`、🖍 高亮→`reader.highlight`、📝 笔记→`reader.note`,真经 HTTP)③ 标注 overlay(`memory.recall` 回显 highlight 底色 + note 徽标,**单源=记忆层**)④ 读位感条(章节=anchor 顶层段原文首行 + 进度%=anchor 在叶序下标/叶总数)⑤ 问答框(`book.query`,citations 可点跳原文、incomplete/scope/model_supplement 显式)`[ADR-0027/0028/0007]`

**入口**:`pnpm -C packages/web dev`(Vite dev server + proxy → tiny_http);需先 `cargo run -p server -- <真书目录>`。打包静态服留 S10e。
**测试**(确定性闸,守 A2 可验部分):`pnpm install`(workspace 加 web 成员,38 包)✓ + `pnpm -C packages/web typecheck`(vue-tsc 0 error)✓ + `pnpm -C packages/web build`(vue-tsc + vite,12 模块 → `dist/`)✓。`cargo test --workspace` 78 不受影响(纯前端刀)。
**实测落点回填**:① 连续正文取数粒度 = **逐 visible_lid 取**(窗口 ≤2*radius+1 叶,localhost 单用户可接受;整窗 `book.text` range 批取属优化,留实测)→ ADR-0028;② 标注 overlay 锚定 = 段落 `data-lid` + 客户端按 lid 过滤 recall(选区富文本 web-highlighter 留实测)→ ADR-0028。
**未闭(B2,浏览器人工验,需真书 + 跑 server [+ `.env` 做问答])**:浏览器加载书显连续正文 / 四动作真驱动后端 / 标注回显 / 章节+进度% / 问答返 citations 可跳——留有真书时人工验(类型 + 构建已确定性闸覆盖)。`memory.recall` 暂 `{}` 全取客户端过滤(book_id 精确作用域待前端拿到 book_id,留后);章节标题取 LID 首段(顶层非必为「章」,精确容器上溯留后)。

## 2026-06-24 · S10f /agent/chat 外层 E agent + reader 双向注入 + effects/trace 装配

> 切片1 第六刀;`[ADR-0030]` agent 阅读器形态后端落地。前端对话主入口 = 外层 E agent(非内层 book.query)。

**触达**:
- `crates/reader/src/lib.rs:Reader::{highlight,note}` — 加 `layer:&str` 入参(人走命令面默认 `long_term`、agent 提议态传 `session`)`[ADR-0030 决策4]`。人机对称无特供:agent 标注仍走 reader 闭环(content=原文片段逻辑复用),只是落 session 层;`§0.5` 对齐选 A(改 reader 公共签名)否决「orchestrator 绕过 reader 直写 memory」(破对称 + 重复取片段逻辑)
- `crates/runtime/src/orchestrator.rs:OuterOutcome` — 加 `effects:Vec<AgentEffect>` + `trace:Vec<TraceStep>`(runtime 内部结构,非冻结命令面,加 `#[derive(TS)]` 导出前端)`[ADR-0030 决策3/5]`
- `...:AgentEffect`(tagged enum `kind`)— `Goto{before_anchor,after_anchor}`(undo=goto(before))/ `Highlight{mem_id,lid}` / `Note{mem_id,lid,text}`(undo=memory.delete(mem_id));**视口变更按回合合并成单条 Goto**(事务性 undo,`with_goto` 比对回合首尾 anchor),highlight/note 每次一条
- `...:TraceStep{tool,args,result_digest}` — 查询踪迹(book.query 的 citations 链落 `result_digest`,`digest()` 截 200 字)
- `...:run` — **签名改注入 `reader:&mut Reader` + `messages:&mut Vec<Message>`**(删内部 `Reader::new`,与 server `AppState` 共享同一视口 + 跨回合 messages)`[ADR-0030 决策2]`;`dispatch` 改返 `(String, Option<AgentEffect>)`;终答入 messages(下一回合可见上轮回答);`new_session()` 产仅 system 的初始 messages(供 `/agent/new` 重置)
- `crates/runtime/src/main.rs` — `chat` 子命令适配新签名(自建 reader + new_session)
- `crates/server/src/lib.rs:AppState` — 加 `messages:Vec<Message>`;`route` 加 `POST /agent/chat`(body `{message}`→ 注入同一 book/store/reader/messages/adapter 调 `run`→ 返 `OuterOutcome`)+ `POST /agent/new`(清 messages 回 new_session);非 POST→405 `agent_method_not_allowed`、缺 message→400
- `crates/server/src/main.rs:main` — `AppState` 构造加 `messages:new_session()`
- `packages/web/src/generated/{OuterOutcome,AgentEffect,TraceStep}.ts` — ts-rs 新生成 3 个 agent DTO(S10g 前端消费)

**入口**:`POST /agent/chat`(前端对话区 S10g)/ `cargo run -p runtime -- <dir> chat <q>`(CLI 真跑)。
**测试**:`cargo test --workspace`(**87 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 25 + server 21**;runtime +2:视口变更合并单条 Goto + trace / messages 跨回合保留+重置;server +4:/agent/chat 驱动共享 reader 返 effects + 标注落 session 层、/agent/new 清 messages、GET 405、缺 message 400)+ `clippy -p reader -p runtime -p server` 净。**ChatStubAdapter** 脚本化外层 chat 把 /agent/chat 脱真 LLM 做成确定性可测(守 A2)。
**实测落点回填**(ADR-0030)：`AgentEffect`/`TraceStep` 字段定型(见上);视口 undo 粒度 = **按回合合并单条 Goto**(事务性);agent 标注 layer 注入点 = reader.* 加 layer 参数。
**未闭(B2,需真书 + `.env`)**:真跑 `POST /agent/chat` 一次「问→agent goto→高亮」后 `/reader/state` 见视口被 agent 改 + effects 可 undo(走 glm-5.1,真 LLM 不入自动测,守 A2/B2)由用户拍板跑。messages 累积上限/截断、idle 软提示留实测(ADR-0030 何时回头)。S10g 前端 agent UI(分屏+提议卡+踪迹)+ S10e 打包待做。

## 2026-06-24 · S10g agent 形态 UI(分屏对话 + 可撤销提议 + 踪迹)+ memory.delete 补全

> 切片1 第七刀;`[ADR-0030 决策1/5]` agent 阅读器形态前端落地。含 S10g-pre 后端补 `memory.delete`(完成 V3 §4.3 冻结契约第三命令,切片0 S6a 只实现了 save/recall)。

**触达**:
- `crates/memory/src/lib.rs:MemoryStore::delete` — **新增**:`retain` 删指定 mem_id + 落盘;找不到 → `MEMORY_NOT_FOUND`(not_found,禁静默降级 `[ADR-0015]`)。完成 V3 §4.3 `memory.delete(mem_id)→{ok}`(用户显式删,区别议题7 后台 usage 遗忘 `[ADR-0018]`)
- `crates/server/src/lib.rs:route_mut` — 加 `/memory/delete` 分支(body `{mem_id}`→ `store.delete`→ `{ok:true}`;缺 mem_id→400、找不到→404)
- `packages/web/src/api.ts:api` — 加 `save`(memory.save 客户端,「保留」升 long_term 用)/ `delete`(memory.delete)/ `agentChat`(POST /agent/chat→OuterOutcome)/ `agentNew`(POST /agent/new);import 生成 DTO `OuterOutcome`/`AgentEffect`/`TraceStep`
- `packages/web/src/App.vue` — 右侧 Q&A 框**改为 agent 对话区**(主入口 `[ADR-0030 决策1]`,直问 book.query 退为 agent 内部工具):`sendAgent`(接 `/agent/chat`,回来 `syncViewport` 同步被 agent 改的视口 + 刷新标注);**提议卡**渲 `effects[]`——`Goto`「↩ 返回 before_anchor」(`undoEffect`→reader.goto)/ `Highlight`·`Note`「保留(`keepEffect`→同内容 long_term re-save = 同 mem_id upsert 升层)/撤销(`undoEffect`→memory.delete)」;**踪迹**折叠渲 `trace[]`(tool+args+result_digest);对话末「凝练成笔记(`distill`→memory.save note,content=answer)」;「新对话」(`newChat`→agentNew 清前端 transcript);提议处置态 `handled` 防重复操作。AgentEffect 联合类型在 TS helper(`effLabel`/`isGoto`/`gotoBack`)里 narrow,避开模板收窄。`<style scoped>` 加 agent 区样式

**入口**:浏览器对话区 → `/agent/chat`;agent goto/highlight/note 即时驱动共享 reader,提议卡 undo 走反向命令(reader.goto / memory.delete)。
**测试**:`cargo test --workspace`(**89 = base-schema 11 + memory 7 + read-tools 14 + reader 10 + runtime 25 + server 22**;memory +1 delete 往返+MEMORY_NOT_FOUND、server +1 /memory/delete 端点 200+404)+ `clippy -p memory -p server` 净 + `pnpm -C packages/web typecheck`(vue-tsc 0)✓ + `build`(14 模块→dist)✓。
**实测落点回填**(ADR-0030)：提议确认 = **逐条**(保留/撤销 per-effect,非整回合事务);「保留」升级 = 同内容 long_term re-save 复用 content-addressed upsert(mem_id 不含 layer);Highlight 保留需 recall(layer=session) 取原文片段(effect 不载 content)。
**未闭(B2,浏览器人工验,需真书 + 跑 server + `.env`)**:对话分屏 / agent goto 后阅读区同步且可一键返回 / 高亮可保留·撤销 / 踪迹可展开 / 凝练成笔记——留有真书时人工验(类型+构建已确定性闸覆盖)。S10e 打包(tiny_http 服 dist 同源)= 切片1 收尾末刀。

## 2026-06-24 · 切片1 修缮 A:start.bat 修复 + agent 分屏布局回归 + 对话/笔记 MD+LaTeX(commit 13d6c4a)

> 切片1 阅读器修缮(非新子切片);三处独立修复 + 一个渲染增强。

**触达**:
- `start.bat`(新)— 双击启动器(dev:server :8787 + vite :5173)。修 base.json-missing 分支 for-loop 的 `^(...^)` 转义括号嵌在 if 块内**破坏块解析**、致双击「一闪就关」(哪怕 base.json 存在)→ 换 `dir /b /ad`(无括号嵌套陷阱)。
- `packages/web/src/style.css` / `App.vue` — 修 S10g 回归:右栏 class 从 `.qa` 改 `.agent` 时丢了 `width`,默认 flex 让 agent 回答后撑满全屏、按钮错位 → `.agent { flex: 0 0 24rem }` 固定侧栏 + `.transcript` 内容换行。
- `packages/web/src/md.ts`(新)`:renderMarkdown` = markdown-it(`html:false` 防 XSS)+ `@traptitech/markdown-it-katex`(`$..$` / `$$..$$`);`main.ts` import `katex/dist/katex.min.css`。
- `packages/web/src/App.vue` — agent 答案 + 笔记内容改 `v-html=renderMarkdown(...)`;`.md` 排版规则放**全局** `style.css`(scoped 够不到 v-html 注入内容,顺修上一刀潜在失效)。
- `packages/web/package.json` — 加 `markdown-it`/`katex`/`@traptitech/markdown-it-katex` + `@types/markdown-it`。

**入口**:`start.bat [book_dir]` 双击;浏览器对话区 / 笔记卡渲染。
**测试**:`pnpm typecheck`(vue-tsc 0)+ `build` 绿;md 渲染 10/10(粗体/列表/代码/`$E=mc^2$`/`$$\int$$`/XSS 转义/null);start.bat dry-run exitcode 0(修前 255 `( was unexpected`)。

## 2026-06-24 · 切片1 修缮 B:人自由高亮(段内字符区间)+ 笔记编辑器(H1/H2/N1)`[ADR-0031]`

> 自由高亮(段内任意文字、可删可改)+ 笔记专用编辑界面。跨后端(H1)+ 前端(H2/N1)。

**触达**:
- `docs/adr/0031-*`(新)— 自由高亮 = 段内 **UTF-16 字符区间**(相对 LID 文本);memory Record 加可选 `range`;兑现冻结 `reader.highlight(lid, range?)`(V3 §4.2)。
- `crates/memory/src/lib.rs` — `TextRange{start,end}`;`Record`/`SaveInput` 加 `range: Option<TextRange>`;`content_mem_id` **仅 Some 时**纳入 range(老 note 哈希不变=向后兼容;同段同子串不同位置=两条不同 mem_id)。
- `crates/reader/src/lib.rs:Reader::highlight` — 签名加 `range: Option<(u32,u32)>`:`Some` → `encode_utf16` 切子串作 content + 存 range,越界 → `INVALID_RANGE` 不降级;`None` → 整段(向后兼容 / agent)。
- `crates/runtime/src/orchestrator.rs` — agent `reader.highlight` 传 `None`(整段,人机对称无特供)+ memory.save `SaveInput` 补 `range:None`。
- `crates/server/src/lib.rs:route_mut` — `/reader/highlight` 解析 body `range:{start,end}` 传 reader;`/memory/save` 补 `range:None`。
- `packages/web/src/api.ts` — `TextRange` + `MemoryRecord.range` + `highlight(lid, range?)`。
- `packages/web/src/App.vue` — 选区捕获 `onProseMouseUp`(`Range.toString().length` 算段内 UTF-16 偏移)→ 浮动「🖍 高亮选区」`confirmHighlight`;`renderSeg`(段内高亮包 `<mark>`,合并重叠区间 + 转义防 XSS);高亮卡 `deleteHighlight`/`modifyHighlight`(改=移除后重选);**笔记模态编辑器**(`noteEditor` 状态 + `notePreview` 实时 MD/LaTeX 预览 + `saveNote`/`openEditNote`)替换 `window.prompt`。
- `packages/web/src/style.css` — `.hl-mark`/`.hl-card`(全局,v-html 用);App.vue scoped 加 `.hl-popover`/`.note-modal`/`.nd-*`。

**入口**:正文框选 → 浮动按钮 → `POST /reader/highlight {lid, range}`;高亮卡删除/改范围;📝 笔记 → 模态编辑器。
**测试**:`cargo test --workspace` **92**(memory 8 / reader 11 / server 23,各 +1)+ clippy 净;`typecheck`+`build` 绿;**真实 CJK 端到端**:`slice(2,6)`=`"由人民邮"` 前后端逐字一致 + 同段多条 + 删除 + 越界→400 全过(临时 memory 目录隔离,不碰真实库)。
**实测回填**(ADR-0031):UTF-16 三处同口径(DOM 选区 / JS `slice` / Rust `encode_utf16`)端到端一致;agent 高亮维持整段。
**未闭(B2,浏览器人工验)**:DOM 选区捕获 + 浮窗/模态视觉 + 高亮重绘——留浏览器肉眼确认(后端区间 + 渲染逻辑已确定性覆盖)。

## 2026-06-25 · S10e 打包形态:tiny_http 同端口服务 SPA dist + API `/api` 前缀归一

> 切片1「前端阅读器」打包收尾;兑现 ADR-0028 决策7:单进程单端口同源服务,无 CORS。

**触达**:
- `crates/server/src/main.rs:main` — 加 `web_dist_dir()`(`UNDERSTAND_BOOK_WEB_DIST` 覆盖,默认 `packages/web/dist`)并在 worker 中先判静态文件,静态 I/O 不持 `AppState` 锁。
- `crates/server/src/main.rs:static_response` — 非 API 的 `GET` 请求服务 `dist/<path>`;具体文件不存在则 fallback 到 `index.html`;路径拒绝 `..`、`.`、反斜杠和盘符冒号。
- `crates/server/src/main.rs:normalize_api_url` — 打包 SPA 的 `/api/*` 请求剥 `/api` 后再交给冻结命令面路由(`/book/*`、`/reader/*`、`/memory/*`、`/agent/*`),避免改 `server::route` 纯函数契约。
- `crates/server/src/main.rs:mime_for` — 覆盖 Vite 产物常见 MIME(html/js/css/json/svg/png/jpg/webp/ico/woff/woff2)。
- `skills/read/SKILL.md`(新) — `/understand-book:read` 启动说明:先 `pnpm -C packages\web build`,再 `cargo run -p server -- <book-dir>`,同端口打开 SPA+API。

**入口**:`pnpm -C packages\web build` → `cargo run -p server -- <book-dir>`;浏览器打开 server 打印的 `http://127.0.0.1:8787`。
**测试**:`cargo test -p server` 23(lib)+4(main) 全绿;`cargo build -p server` 绿;`pnpm -C packages\web build` 绿;真实同源 smoke(临时端口 18787,真书基座):`GET /` → HTML + app mount,`GET /api/book/manifest` → JSON + tree,`GET /not-a-real-spa-route` → HTML fallback。
**未闭**:浏览器人工完整验(S10c/f/g 的真 LLM + UI 操作)仍按前文 B2 清单执行;S10h/S10i 隐藏 LID 和自然定位入口未做。
## 2026-06-25 · start.bat 切到 S10e 打包单进程启动

> 切片1 启动入口修缮;把旧 dev 双进程(server + Vite)入口改为 S10e 产品形态入口。

**触达**:
- `start.bat` — 启动模式改为 packaged S10e:默认 `UNDERSTAND_BOOK_ADDR=127.0.0.1:8787`,默认 `UNDERSTAND_BOOK_WEB_DIST=%CD%\packages\web\dist`;校验 book `base.json`;默认 dist 缺失时自动 `pnpm -C packages\web build`;启动单个 `cargo run -p server -- <book_dir>` server 窗口并打开同端口 URL。
- `start.bat` — 移除 Vite dev server 常驻启动;`pnpm` 仅在 dist 缺失需要构建时才要求存在。

**入口**:双击 `start.bat` 或 `start.bat <book_dir>`;关闭 server 窗口停止服务。
**测试**:`cmd /c start.bat __missing_book_for_syntax_check__` 正常进入缺失 book 错误分支(exit 1),验证 batch 括号块解析与路径检查;未启动 server/浏览器。
## 2026-06-25 · asset 契约修订:Formula 升格为第四类核心 asset + FormulaSemantics

> 文档/契约切片;未改 schema/adapter 实现代码。把公式从长尾类型升格为核心 asset,并定义 agent 交互必须消费的公式语义剖面。

**触达**:
- `docs/adr/0029-*` — 修订 ADR-0029:`NodeKind` 目标从 Code/Table/Image 扩为 Code/Table/Image/Formula;新增 FormulaSemantics 决策(参数 parameters、组合含义 composition、上下文关系 context_links,均带 LID 证据);Formula 不再是长尾。
- `docs/切片方案-asset一等对象.md` — asset 刀总声明与 SA0-SA6 重写:SA0 统计 formula_count;SA1 加 Formula/FormulaSemantics schema;SA2/SA3 识别 md/epub 公式;SA4 禁止公式句切;SA5 专门抽公式语义剖面;SA6 真书验公式语义可检索。
- `CONTEXT.md` — `asset 叶子` 扩四类,新增 `公式语义剖面 (FormulaSemantics)` 术语。
- `docs/切片方案-切片1前端阅读器.md` — S10j 前端消费目标扩到 Formula,要求展示公式原文与公式语义剖面。
- `docs/adr/0032-*` — 粒度体检纳入 formula asset 候选数量,并规定 Formula 叶子固定不做句级展开。
- `docs/参考对照-文档世界状态-优化登记.md` / `需求文档-V3.md` — 同步四类 asset 与 agent 公式上下文要求。

**入口**:后续实现从 `docs/切片方案-asset一等对象.md` 的 SA0 开始;不得跳过粒度体检直接改 schema。
**测试**:`rg` 扫描 asset/Formula 相关旧表述,确认 Formula 已从长尾改为核心类型;本切片未改可执行代码,未跑 cargo/pnpm。

## 2026-06-25 · SA0 段/句粒度体检报告(GranularityProfile)

> asset 一等对象实现第一刀;只做正式构建前体检,不改 schema/adapter asset kind/正式 LID 构建。

**触达**:
- `packages/core/src/granularity.ts:buildGranularityProfile` — 新增确定性 `GranularityProfile`:段落数、估算句数、每段句数 avg/p50/p90/max、长段计数、`paragraph/hybrid/sentence` 三档 projected_lid_count 与 expansion_ratio、推荐档位。
- `packages/core/src/granularity.ts:countMarkdownAssetCandidates/countEpubAssetCandidates` — Markdown 源文本和 EPUB XHTML 的 code/table/image/formula 候选计数;不改变现有 `SourceBlock` 或 `NodeKind`。
- `skills/build/granularity-profile.ts` — SA0 CLI:`pnpm exec tsx skills/build/granularity-profile.ts <epub|md>` 输出人读摘要 + JSON。
- `packages/core/test/granularity.test.ts` — 覆盖句数估算、Markdown/XHTML/EPUB asset 候选计数、LID 膨胀估算和推荐逻辑。
- `skills/build/SKILL.md` — build 编排骨架加入第 0 步 SA0,明确用户确认粒度后才进入正式构建。

**入口**:`pnpm exec tsx skills/build/granularity-profile.ts <epub|md>`;输出推荐但不自动构建。
**测试**:`pnpm -C packages/core test`(38 例全绿)+ `pnpm -C packages/core typecheck` + `pnpm exec tsx skills/build/granularity-profile.ts packages/core/test/fixtures/sample.md`。

## 2026-06-25 · SA1 schema 扩:NodeKind asset 四值 + ManifestNode.kind + FormulaSemantics

> asset 一等对象第二刀;只做 schema/DTO/TS 导出,不碰 ingestion、segment 或 adapter 行为。

**触达**:
- `crates/base-schema/src/lib.rs:NodeKind` — 增 `Code/Table/Image/Formula` 四个 serde snake_case 枚举值,保持只增不改。
- `crates/base-schema/src/lib.rs:FormulaSemantics` — 新增公式语义剖面 schema:parameters/composition/context_links,均带 LID 证据字段;本刀先独立导出,未挂入 `ReadOnlyBase` 必填字段。
- `crates/read-tools/src/lib.rs:ManifestNode` — `book.manifest` 投影新增 `kind: NodeKind`,从 `LidNode.kind` 透传。
- `packages/core/src/generated/Formula*.ts` / `NodeKind.ts` — ts-rs 生成 FormulaSemantics 相关 TS 类型和扩展后的 NodeKind。
- `packages/web/src/generated/ManifestNode.ts` — ts-rs 生成 manifest 节点 `kind` 字段,前端类型通过。
- `packages/core/src/zod.ts` — 手写 zod 镜像同步 NodeKind 四值和 FormulaSemantics schema。

**入口**:`book.manifest()` 返回的 `tree[].kind`;FormulaSemantics schema 供 SA5 抽取/存储接入。
**测试**:`cargo test -p base-schema -p read-tools`;`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;`pnpm -C packages/web typecheck`;`cargo test --workspace`;`pnpm -C packages/web build`。

## 2026-06-25 · SA2 md-adapter 识别 code/table/image/formula asset leaf

> asset 一等对象第三刀;只做 Markdown ingestion 的 assetKind 标记,不改 EPUB、segment 透传或 read-tools 行为。

**触达**:
- `packages/core/src/segment.ts:SourceBlock` — 新增 `assetKind?: "code" | "table" | "image" | "formula"`;SA4 前 `segment()` 不消费该字段,仍产 paragraph。
- `packages/core/src/md-adapter.ts:markdownToBlocks` — 识别 fenced code、Markdown table、独立 image 行、块级公式和独立行内公式为带 `assetKind` 的 leaf;asset 文本保留源标记序列化。
- `packages/core/test/md-adapter.test.ts` — 覆盖五类 Markdown asset leaf 标记、代码块内空行不腰斩、表/图/公式原文保真,并确认当前 segment 行为和分区不变式不变。

**入口**:`markdownToBlocks(src)` 返回 `SourceBlock[]` 中的 `assetKind`;SA4 后才透传为 `LidNode.kind`。
**测试**:`pnpm -C packages/core test`(40 例全绿)+ `pnpm -C packages/core typecheck`;`pnpm exec tsx skills/build/split-lid.ts ...` 未跑通,当前环境报 `tsx` 不在 PATH。
## 2026-06-25 · SA3 epub-adapter 识别 pre/table/img/math asset leaf

> asset 一等对象第四刀;只做 EPUB ingestion 的 `assetKind` 标记与忠实源标记序列化,不改 `segment()` kind 透传、不做 SA4/SA5。
**触达**:
- `packages/core/src/epub-adapter.ts:RawBlock` — 新增 `assetKind?: AssetKind`,并在 `epubToSource` 透传到 `SourceBlock.assetKind`。
- `packages/core/src/epub-adapter.ts:xhtmlToBlocks` — `<pre>` 标为 `code` 并保留换行缩进;`<table>` 序列化为确定性表文本;`<img>` 合成为 `![alt](src)`;`<math>` 保留 MathML 源标记并标为 `formula`。
- `packages/core/test/epub-adapter.test.ts` — 覆盖 pre/table/img/math 四类 asset、当前 segment 仍产 paragraph、分区不变式 100%。
**入口**:`xhtmlToBlocks(html)` / `epubToSource(zip)` 返回带 `assetKind` 的 EPUB `SourceBlock[]`;SA4 后才透传为 `LidNode.kind`。
**测试**:`pnpm -C packages/core test`(41 例全绿)+ `pnpm -C packages/core typecheck`。
## 2026-06-25 · SA4 segment 透传 SourceBlock.assetKind 到 LidNode.kind

> asset 一等对象第五刀;只做 `segment()` 的 kind 透传,不改 Markdown/EPUB 识别规则、不做 FormulaSemantics。
**触达**:
- `packages/core/src/segment.ts:SourceBlock.assetKind` — 注释更新为 segment 会透传到 `LidNode.kind`。
- `packages/core/src/segment.ts:segment` — leaf 节点 kind 从固定 `paragraph` 改为 `b.assetKind ?? "paragraph"`,heading 标题叶仍为 `paragraph`。
- `packages/core/test/md-adapter.test.ts` — Markdown code/table/image/formula asset 经 segment 后产对应 `NodeKind`,分区不变式仍 100%。
- `packages/core/test/epub-adapter.test.ts` — EPUB pre/table/img/math asset 经 segment 后产对应 `NodeKind`,分区不变式仍 100%。
**入口**:`segment(markdownToBlocks(src))` / `segment(epubToSource(zip).blocks)` 现在生成 `code/table/image/formula` 叶子节点。
**测试**:`pnpm -C packages/core test`(41 例全绿)+ `pnpm -C packages/core typecheck`。
## 2026-06-25 · SA5 FormulaSemantics 候选证据闸 + agent prompt formatter

> asset 一等对象第六刀;只做 FormulaSemantics 的 TS 侧候选清洗/证据闸和 agent 上下文格式化,不把语义剖面挂进 `ReadOnlyBase` 必填字段,不做真书重建。
**触达**:
- `packages/core/src/formula-semantics.ts:gateFormulaSemanticsCandidate` — 新增确定性证据闸:校验 `formula_lid` 必须是 formula 节点,`source_lid` 必须指回公式,`evidence_lids` 必须存在且落在公式上下文 LID 集合内;悬空/越界/缺证据项进入 pending。
- `packages/core/src/formula-semantics.ts:formatFormulaSemanticsForPrompt` — 新增 agent 侧公式上下文格式化,把 composition、parameters、context_links 和证据 LID 展开为稳定文本。
- `packages/core/test/formula-semantics.test.ts` — 覆盖合法候选固化、悬空 LID 丢弃、上下文外证据丢弃、缺证据项 pending、composition 未锚到公式时不产只读语义剖面。
**入口**:LLM 抽取候选先调 `gateFormulaSemanticsCandidate(candidate, lidNodes, { contextLids })`;只有 `result.semantics` 可进入只读语义剖面,pending 项只能作为待确认/model_supplement。
**测试**:`pnpm -C packages/core test`(45 例全绿)+ `pnpm -C packages/core typecheck`。

## 2026-06-25 · ADR-0033 Core/Profile/Reader 解耦 + profile 深路径切片方案

> Grill 落档;只写架构决策和阶段切片方案,不改可执行代码。

**触达**:
- `docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md` — 冻结 Core Schema / Book Profile / Reader Profile 三层边界;当前内置 profile=`technical_learning`;暂不迁移 GraphNode envelope;Pass2/synthesize/consolidation/reader.* / adapter/provider/迁移的归属边界。
- `docs/切片方案-profile深路径.md` — P0-P6 阶段方案:ADR 落档、`technical_learning.pass2_longrange_v1`、`book.synthesize` 深路径、reader.* 全集与 agent policy、memory consolidation 四层、ReActAdapter/provider registry、增量构建与 profile artifact / memory 迁移。

**入口**:后续深路径实现从 `docs/切片方案-profile深路径.md` 的 P1 开始;P0 仅为本次文档落档。
**测试**:`rg` 扫描 ADR-0033 / profile 深路径关键边界;本切片未改可执行代码,未跑 cargo/pnpm。

## 2026-06-25 · P1 technical_learning.pass2_longrange_v1 + context/query far

> Profile 深路径第一刀;把 Pass2 长程边候选接入确定性 gate/audit sidecar,并让读时 context/query 消费 long_range 边。

**触达**:
- `packages/core/src/pass2.ts:gateTechnicalLearningPass2LongRange` — 新增 `technical_learning.pass2_longrange_v1` 候选闸:消费带 `ProfileArtifactHeader` 的 `TechnicalLearningPass2Output`;校验端点 graph node id、`scope="long_range"`、edge type 白名单、evidence_lids 真实存在;通过后降成 `GraphEdge(scope=long_range)` 并产带 header 的 audit sidecar。
- `packages/core/test/pass2.test.ts` — 覆盖合法 output 降级、audit header 保留、悬空端点/悬空证据/空证据/非法 scope/非法 type 确定性丢弃。
- `agents/pass2-longrange-linker.md` — 从占位改为真实 subagent 契约:输入/输出带 `ProfileArtifactHeader`,只产 long_range candidate,不产节点,必须输出 `scope="long_range"`,不重建悬空 LID。
- `crates/read-tools/src/lib.rs:Book::context` — 新增 `granularity=near|mid|far`;far 累积 near + concept occurrences(mid) + `scope=long_range` 边,保留 `context_near` wrapper。
- `crates/runtime/src/lib.rs:Scope/retrieve/query` — query ladder 扩为 `local→chapter→cross_chapter→global`;cross_chapter 消费 context far,global 加全书 anchored leaves。
- `crates/read-tools/src/main.rs` / `crates/server/src/lib.rs` / `crates/runtime/src/orchestrator.rs` — CLI、REST 和 agent tool spec/dispatch 接入 `granularity`。
- `packages/web/src/generated/Via.ts` — ts-rs 生成类型新增 `concept` via variant。

**入口**:Pass2 候选先调 `gateTechnicalLearningPass2LongRange(...)`;读时 `book.context?lid=...&granularity=far` 或 agent `book.context({granularity:"far"})` 可召回 long_range 指针;`book.query` 自动外扩到 cross_chapter/global。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;`cargo test -p read-tools -p runtime`;`cargo test --workspace`;`pnpm -C packages/web typecheck`。

## 2026-06-25 · profile 深路径契约补充:discourse / reader_profile / synthesize policy 形状

> 补齐 ADR-0033 落档时遗漏的具体 artifact 字段格式;仍只改文档,不改代码。

**触达**:
- `docs/切片方案-profile深路径.md` — 新增 `technical_learning v0 artifact contracts`: `ProfileArtifactHeader`、`TechnicalLearningDiscourseIndex`、`TechnicalLearningPass2Input/Output`、`ReaderProfile`、`SynthesizePolicy`、`TechnicalLearningAgentPolicy` 的 TS 形状与红线。

**入口**:P1+ 实现这些 artifact 时以该小节为输入契约草案;实现前仍需按切片重新 A1 声明。
**测试**:`rg` 扫描关键字段形状;本切片未改可执行代码,未跑 cargo/pnpm。

## 2026-06-25 · profile 深路径契约消费矩阵补充

> 补明每个 P 刀消费哪些 1.5.x 契约,并补出 `book.context` discourse projection 切片。

**触达**:
- `docs/切片方案-profile深路径.md` — 新增 `1.5.7 Contract consumption matrix`,明确 P1/P2/P2a/P3/P4/P5/P6 分别消费 `TechnicalLearningDiscourseIndex`、`SynthesizePolicy`、`ReaderProfile` 等契约的方式;新增 P2a `book.context` discourse projection。

**入口**:后续实现 P1/P2/P2a 时按矩阵确认输入契约,避免 profile artifact 写了但无人消费。
**测试**:`rg` 扫描 P2a 和 Contract consumption matrix;本切片未改可执行代码,未跑 cargo/pnpm。

## 2026-06-25 · P2 book.synthesize 接手收紧

**触达**:
- `crates/runtime/src/lib.rs:SynthesizeResponse` - 新增 `book.synthesize` 响应 DTO,导出到前端生成类型。
- `crates/runtime/src/lib.rs:synthesize` - 实现显式 LID 集综合,不外扩检索,过滤 citations 到输入/归并证据范围,支持确定性分批归并。
- `crates/runtime/src/lib.rs:tests::synthesize_*` - 覆盖悬空 citation 过滤、空/未知 LID、分批归并路径。
- `crates/runtime/src/orchestrator.rs:tool_specs/dispatch` - 外层 agent 暴露并分发 `book.synthesize` 工具。
- `crates/server/src/lib.rs:route_synthesize` - HTTP `POST /book/synthesize` 接入 runtime 并补路由测试。
- `packages/web/src/generated/SynthesizeResponse.ts` - ts-rs 生成前端响应类型。

**入口**:`POST /book/synthesize {lids, task?}` 或 agent tool `book.synthesize({lids, task?})`。
**测试**:`cargo test -p runtime`; `cargo test -p server`; `pnpm -C packages/web typecheck`。

## 2026-06-25 · P2 book.synthesize 消费 FormulaSemantics sidecar

**触达**:
- `crates/read-tools/src/lib.rs:Book::load` - 可选读取同目录 `formula_semantics.json`,缺失时为空,不改 `ReadOnlyBase`。
- `crates/read-tools/src/lib.rs:Book::formula_semantics` - 按公式 LID 查询 FormulaSemantics sidecar。
- `crates/runtime/src/lib.rs:formula_semantics_hint` - 将真实 FormulaSemantics 格式化进 `book.synthesize` prompt;缺 sidecar 时才保留占位提示。
- `crates/runtime/src/lib.rs:tests::synthesize_includes_formula_semantics_sidecar_in_prompt` - 验证公式组合含义和参数解释进入 prompt,且 citation 仍锁在输入 LID。
- `crates/read-tools/src/lib.rs:tests::load_reads_optional_formula_semantics_sidecar` - 验证 `formula_semantics.json` 可被加载。

**入口**:`formula_semantics.json` sidecar + `book.synthesize({lids:[formula_lid], task?})`。
**测试**:`cargo test -p read-tools`; `cargo test -p runtime`。

## 2026-06-25 · P2 book.synthesize 消费 discourse_index sidecar

**触达**:
- `crates/read-tools/src/lib.rs:TechnicalLearningDiscourseIndex` - 新增 technical_learning discourse sidecar 的 Rust 读时载体,不进入 `ReadOnlyBase`。
- `crates/read-tools/src/lib.rs:Book::load` - 可选读取同目录 `discourse_index.json`,缺失时为空。
- `crates/read-tools/src/lib.rs:Book::discourse_item` - 按 LID 查询 discourse item。
- `crates/runtime/src/lib.rs:discourse_hints` - 仅把输入 LID 集内部的 summary/function/relation 注入 `book.synthesize` prompt,过滤输入外 target/evidence。
- `crates/runtime/src/lib.rs:tests::synthesize_includes_only_input_scoped_discourse_hints` - 验证输入内 relation 进入 prompt,输入外 relation 被过滤。
- `crates/read-tools/src/lib.rs:tests::load_reads_optional_discourse_index_sidecar` - 验证 `discourse_index.json` 可被加载。

**入口**:`discourse_index.json` sidecar + `book.synthesize({lids, task?})`。
**测试**:`cargo test -p read-tools`; `cargo test -p runtime`。

## 2026-06-25 · P2 book.synthesize 编译收紧 + HTTP sidecar smoke

**触达**:
- `crates/runtime/src/lib.rs:retrieve` - 移除误入 query 检索路径的 `SynthesizeMode::from_task(task)` 引用,恢复 runtime 编译。
- `crates/runtime/src/lib.rs:synthesize` - 分批局部综合路径调用 `build_synthesize_prompt` 时传入已解析 `mode`,与单批/归并路径保持同一 SynthesizePolicy。
- `crates/server/src/lib.rs:tests::book_synthesize_loads_formula_and_discourse_sidecars` - 新增真实 `Book::load` HTTP smoke:临时 book dir 同时带 `formula_semantics.json` 与 `discourse_index.json`,经 `POST /book/synthesize` 验证两类 sidecar hint 进入 prompt,且响应仍锁定输入 `source_lids`。

**入口**:`POST /book/synthesize {lids, task?}`;本 smoke 覆盖从 book 目录 sidecar 加载到 HTTP route 再到 runtime prompt 的端到端路径。
**测试**:`cargo test -p read-tools`;`cargo test -p runtime`;`cargo test -p server`;`cargo test --workspace`;`pnpm -C packages/web typecheck`。

## 2026-06-26 · P2a book.context discourse projection

**触达**:
- `crates/read-tools/src/lib.rs:Via::Discourse` - 新增 `book.context` 的 discourse via 指针类型,携带 source/target/type/family/direction/confidence/evidence_lids,不携带原文。
- `crates/read-tools/src/lib.rs:Book::discourse_context_items` - 将 `TechnicalLearningDiscourseIndex.relations[]` 投影进 context;同父 LID 进 near,跨父 LID 进 far,并过滤悬空 target/evidence。
- `crates/read-tools/src/lib.rs:tests::context_projects_discourse_relations_to_near_and_far` - 覆盖 near/far 分层、输入外/悬空 relation 不投影。
- `packages/web/src/generated/Via.ts` - ts-rs 生成 `discourse` via 联合类型。

**入口**:`book.context(lid, granularity=near|far)`;HTTP 和 agent 既有 `book.context` 路径自动获得 discourse via。
**测试**:`cargo test -p read-tools`;`cargo test --workspace`;`pnpm -C packages/web typecheck`。

## 2026-06-26 · profile 预构建缺口切片落档

**触达**:
- `docs/切片方案-profile深路径.md:1.5.7` - 在契约消费矩阵补入 PB0-PB4,覆盖 profile metadata/header、FormulaSemantics sidecar、discourse_index sidecar、Pass2 audit sidecar、build smoke。
- `docs/切片方案-profile深路径.md:1.6` - 新增预构建缺口盘点,明确 P2/P2a 只完成读时消费,正式构建产物仍待 PB 刀补齐。
- `docs/切片方案-profile深路径.md:PB0-PB4` - 新增五个独立预构建切片,每刀声明做/不做/判据/触达。
- `docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md:影响` - 追加执行补记,说明 PB0-PB4 是 ADR-0033 sidecar 规则的构建落地,不改变边界决策。

**入口**:后续正式做预构建补齐时从 PB0 开始,不要把 discourse/Formula/Pass2 audit 写盘混入 P3/P4/P5。
**测试**:`rg "PB0|PB1|PB2|PB3|PB4|预构建缺口|执行补记" docs/切片方案-profile深路径.md docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md docs/代码链路.md`。

## 2026-06-26 · Book MCP 只读边界落档

**触达**:
- `docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md:决策12` - 冻结 Book MCP 只暴露只读 `book.*` 工具面,不继承 reader 当前视口,不暴露 reader/memory 写命令。
- `docs/切片方案-profile深路径.md:1.5.8` - 新增 Book MCP readonly boundary 与 `BookMcpTools` 接口草案。
- `docs/切片方案-profile深路径.md:P7` - 新增 Book MCP 只读工具面切片,要求 `book_query` 显式 `anchor_lid`。

**入口**:后续实现 MCP server 从 P7 开始;不要把 MCP 只读工具面混入 P3/P4/P5 reader/memory/provider 切片。
**测试**:`rg "Book MCP|BookMcpTools|anchor_lid|决策12|P7" docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-06-26 · PB0 profile metadata header writer

**触达**:
- `packages/core/src/profile-artifact.ts:buildProfileArtifactHeader` - 新增 technical_learning profile header 构造与必填字段校验。
- `packages/core/src/profile-artifact.ts:buildProfileMetadata` - 新增 `profile_metadata.json` 最小 `{header}` 形状。
- `packages/core/src/pass2.ts:ProfileArtifactHeader` - 改为复用 profile artifact 统一 header 类型并保持原导出兼容。
- `skills/build/pass1-batch.ts` - 在写 `base.json` / `source.txt` 同目录时同步写 `profile_metadata.json`。
- `packages/core/test/profile-artifact.test.ts` - 覆盖 header 默认值、metadata 形状与空字段失败。

**入口**:`pass1-batch.ts` 预构建写盘流程;后续 PB1-PB3 sidecar 复用 `buildProfileArtifactHeader` 的同一 header 来源。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;`.\node_modules\.bin\tsx.CMD skills\build\pass1-batch.ts packages\core\test\fixtures\sample.md skills\build\fixtures\pass1-sample-4windows.json "0"` 生成 `profile_metadata.json`。
## 2026-06-26 路 PB1 FormulaSemantics sidecar materialization

**触达**:
- `packages/core/src/formula-semantics.ts:buildFormulaSemanticsSidecar` - 将 gated FormulaSemantics 候选固化为带 `ProfileArtifactHeader` 的 `{header, items}` sidecar。
- `packages/core/src/zod.ts:FormulaSemanticsSidecarZ` - 增加预构建写盘前的 sidecar 形状自检。
- `skills/build/pass1-batch.ts` - 增加可选 `formula-candidates.json` 输入,复用 PB0 header 写出 `formula_semantics.json`。
- `crates/read-tools/src/lib.rs:parse_formula_semantics_sidecar` - `Book::load` 兼容 PB1 `{header, items}` 与旧数组格式。

**入口**:`pass1-batch.ts <book> <outputs.json> <idxs> [formula-candidates.json]` 写出 `formula_semantics.json`;读时 `Book::load` 自动加载。
**测试**:`packages/core/test/formula-semantics.test.ts` 覆盖 sidecar materializer;`crates/read-tools/src/lib.rs::tests::load_reads_headered_formula_semantics_sidecar` 覆盖新格式,旧格式测试保留;CLI smoke 写出 items=1/pending=0。
## 2026-06-26 路 PB2 TechnicalLearningDiscourseIndex sidecar materialization

**触达**:
- `packages/core/src/discourse-index.ts:buildTechnicalLearningDiscourseIndex` - 新增 discourse item/relation 确定性 gate,只校验 LID/enum/confidence/evidence,不判断标签语义质量。
- `packages/core/src/zod.ts:TechnicalLearningDiscourseIndexZ` - 增加 `discourse_index.json` 写盘前形状自检。
- `skills/build/pass1-batch.ts` - 增加可选 `discourse-candidates.json` 输入,复用 PB0 header 写出 `discourse_index.json`。
- `packages/core/test/discourse-index.test.ts` - 覆盖合法 sidecar、非法 relation 丢弃、非法 item 丢弃。

**入口**:`pass1-batch.ts <book> <outputs.json> <idxs> [formula-candidates.json] [discourse-candidates.json]` 写出 `discourse_index.json`。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;CLI smoke 写出 `discourse_index.json` items=1/dropped=1。

## 2026-06-26 · PB2b discourse extractor 两阶段 prompt 方案落档

**触达**:
- `docs/切片方案-profile深路径.md:PB2b` - 新增 discourse extractor two-stage prompt 切片:Step A 逐 LID 分类,Step B 基于分类连局部 relation。
- `docs/切片方案-profile深路径.md:1.5.7` - 契约消费矩阵补入 PB2b,明确 prompt 只产候选,gate 决定是否落 `discourse_index.json`。

**入口**:后续实现从 `agents/discourse-index-extractor.md` 开始;不要把 PB2b 混进 PB3 Pass2 build orchestration。
**测试**:`rg "PB2b|two-stage|少连边|evidence_lids.*source LID.*target LID" docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-06-27 · PB3 Pass2 prompt Grill 决策落档

**触达**:
- `docs/PB3-pass2-prompt-grill.md` - 新增 PB3 Pass2 prompt/build orchestration 决策记录,冻结 candidate-driven Pass2、accepted/pending/rejected、edge contracts、split evidence、support_level、soft compatibility、source-window packet、v1 seed signals、cross-window long_range gate。
- `docs/切片方案-profile深路径.md:PB3` - PB3 小节改为强引用 grill 文件,明确实现前必读,并把判据改成 `long_range_candidates.json` + candidate classification + `pass2_audit.json`。

**入口**:后续实现 PB3 前先读 `docs/PB3-pass2-prompt-grill.md`;不要回到开放式 LLM 抽长程边方案。
**测试**:`rg "PB3-pass2-prompt-grill|long_range_candidates|accepted_edges|pending_edges|rejected_candidates|support_level|source_evidence_lids|target_evidence_lids" docs/PB3-pass2-prompt-grill.md docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-06-28 · PB2b discourse extractor 两阶段 prompt + gate 收紧

> 范围含代码:除两阶段 prompt + fixture 外,把 PB2b 原则里的硬线由确定性 gate 兜底(B2),不靠 LLM 自觉。不扩 sidecar schema、不抽 long_range、不混 PB3。

**触达**:
- `packages/core/src/discourse-index.ts:relationError` — 加 `sourceLid` 参数 + 三条收紧:`low_confidence`(< `MIN_RELATION_CONFIDENCE`)、`evidence_missing_source`/`evidence_missing_target`(evidence 必须同时含 source=item.lid 与 target_lid)。
- `packages/core/src/discourse-index.ts:validItemShape` — 加 `summary_too_long`:`local_summary` 超 `MAX_LOCAL_SUMMARY_LEN` 整 item 丢弃(不可信标注整条拒)。
- `packages/core/src/discourse-index.ts` — 新增导出常数 `MIN_RELATION_CONFIDENCE=0.5`、`MAX_LOCAL_SUMMARY_LEN=200`(占位默认,实测回填);`DroppedDiscourseCandidateReason` 加 4 个 reason。
- `agents/discourse-index-extractor.md` — 新增两阶段 prompt:Step A 逐 LID 分类(只产 mode/local_function/rhetorical_move/local_summary,闭集 enum)、Step B 基于分类连局部 relation(少连边、evidence 含两端);只产 items,header 构建期注入。
- `skills/build/fixtures/discourse-sample.json` — 最小 fixture(sample.md LID),覆盖「定义+解释」(1.3→1.2 explains)、「例子」(1.4.2→1.2 exemplifies)、「相邻但不连」(1.5.2 弱 continues 被低置信丢弃)。
- `packages/core/test/discourse-index.test.ts` — 新增 `PB2b gate 收紧` 4 测(低置信丢弃 / evidence 缺 source 或 target / summary 超长整 item 丢 / 合法保留)。

**入口**:预构建 `pass1-batch.ts <book> <outputs> <idx> [formula] [discourse-candidates.json]` 第 5 位参数;subagent 实现从 `agents/discourse-index-extractor.md` 开始。gate 不变沿用 `buildTechnicalLearningDiscourseIndex`。
**测试**:`pnpm -C packages/core test`(58 全绿,discourse-index 7 测含 4 新)+ `pnpm -C packages/core typecheck`(tsc 0)。CLI smoke:`pass1-batch.ts sample.md … "0" "" discourse-sample.json` → `discourse index items=4 dropped=1`,写盘内容人工核对(1.3/1.4.2 relation 留、1.5.2 弱边丢)。schema(`TechnicalLearningDiscourseIndexZ`)保持不变。

## 2026-06-28 · PB3-1 Pass2 候选契约 + edge type 扩 11 类 + cross-window helper

> PB3 第一子刀(A4):仅落地 build-only 契约与跨窗口判定,**不**实现 candidate builder seed 信号 / gate / work packet / prompt / 编排。依据 `docs/PB3-pass2-prompt-grill.md` §2/§4/§12。

**触达**:
- `packages/core/src/pass2.ts:TechnicalLearningLongRangeEdgeType` — grill §4 扩为 11 类(加 `supports/rebuts/summarizes`);新增导出 `TECHNICAL_LEARNING_LONG_RANGE_EDGE_TYPES`(冻结列表,P1 gate 的 `LONG_RANGE_EDGE_TYPES` Set 由它构建);**未**加 `related_to/same_problem/reuses_formula`。
- `packages/core/src/pass2-build.ts` — 新文件(PB3 build-only,不被 `Book::load` 读)。`LongRangeCandidate`(grill §2 最小候选形状)+ `LongRangeCandidateIndex`;`buildLidToWindowIndex(windows)` 叶 LID→窗口序号;`isCrossWindow(sourceLids,targetLids,map)` 实现 grill §12 跨窗口硬定义(source/target 各有 LID 落不同窗口才为真;未解析 LID 不贡献信号)。
- `packages/core/test/pass2-build.test.ts` — 6 测:edge type 列表 11 类含新 3 类、不含禁用 3 类;LID→窗口映射;跨窗口 true/同窗 false/未解析 false。

**入口**:PB3-2 candidate builder 将消费 `buildLidToWindowIndex`/`isCrossWindow`;`LongRangeCandidate` 是 Pass2 work packet(PB3-4)与 prompt 输入。
**测试**:`pnpm -C packages/core test`(64 全绿,+6 pass2-build;pass2 P1 gate 3 测仍绿)+ `typecheck`(tsc 0)。

## 2026-06-28 · PB3-2a Pass2 确定性候选生成(shared-node bridge,grill §11 信号 4+1+2)

> PB3 第二子刀(A4)。决策:候选 v1 走 shared-node 路线,**不加**语义桥;意思相关但无共同概念的边接受预构建漏、读时 scope 外扩兜底(grill §11 已把语义桥列 deferred)。signal 3(formula 桥)拆 PB3-2b。

**触达**:
- `packages/core/src/pass2-build.ts:buildLongRangeCandidates` — 确定性候选生成。**signal 4**(同一 graph node occurrences 跨 ≥2 窗口)作 join 主干:取该 node 最早窗口为 source 侧,与其每个较晚窗口里共现的其他 node 配成有向候选;两端 LID 必过 `isCrossWindow`。**signal 1**(local_function definition/explanation → application/example/procedure_step ⇒ hint exemplifies/applies)、**signal 2**(rhetorical_move prerequisite/main_point → concept_elaboration/worked_example ⇒ hint builds_on/prerequisite)只定 `relation_hints` 不决定成对。按有向节点对去重合并(source/target LID、reason、hint 并集,seed_score 取大),输出按 candidate_id 排序。两端都复现时产双向候选(v1 高召回,精度交 LLM+gate)。
- `packages/core/src/pass2-build.ts:LongRangeCandidateInput` — `{graphNodes, lidToWindowIndex, discourseIndex?}`;辅助 `occLidsOf`(claim 取 source_lid,实体/概念取 occurrences)、`hintsFor`、`uniqSort`/`uniqHints`。
- `packages/core/test/pass2-build.test.ts` — +5 测:桥接+signal1 hint、无 discourse 时空 hint/score 0.4、signal2 rhetorical_move hint、无复现节点不产候选、同有向对跨多窗口合并(双向候选)。

**入口**:PB3-4 work packet / Pass2 prompt 消费 `buildLongRangeCandidates(...)` 的输出;seed_score=min(1,0.4+0.3×命中信号数)(占位,实测回填)。
**测试**:`pnpm -C packages/core test`(69 全绿,+5 pass2-build)+ `typecheck`(tsc 0)。
**不做(留后)**:signal 3 formula 桥(PB3-2b);PB3 gate/packet/prompt/编排;语义桥/二跳/标题匹配/same_problem(grill 禁)。

## 2026-06-28 · PB3-2b Pass2 候选 signal 3(formula 定义/使用桥)

> PB3 候选生成补齐 grill §11 第四信号。重构 `buildLongRangeCandidates` 为 `emit` 闭包统一合并,shared-node 与 formula 两路共用同一去重/排序。

**触达**:
- `packages/core/src/pass2-build.ts:LongRangeCandidateInput` — 加可选 `formulaSemantics?: FormulaSemantics[]`。
- `packages/core/src/pass2-build.ts:buildLongRangeCandidates` — 提取 `emit(srcId,tgtId,srcLids,tgtLids,hints,reasons,signals)` 闭包(自指/非跨窗口拒,跨信号合并取 score 大);新增 `lidToNodes`(LID→节点 id)解析 formula 桥端点;**signal 3 循环**:对每个 `FormulaSemantics`,`formula_lid` 与其 `context_links.target_lid` 跨窗口时,连两端 LID 上的 graph node,hint `applies/builds_on`,reason `signal3_formula_bridge:<formula_lid>`。
- `packages/core/test/pass2-build.test.ts` — +2 测:formula 跨窗口桥接产候选;同窗口 formula link 不产候选。

**入口**:`buildLongRangeCandidates({graphNodes, lidToWindowIndex, discourseIndex?, formulaSemantics?})` 现覆盖 grill §11 四信号全。
**测试**:`pnpm -C packages/core test`(71 全绿,+2 pass2-build)+ `typecheck`(tsc 0)。

## 2026-06-28 · PB3-3 PB3 gate(split evidence / support_level / 跨窗口硬校验)

> PB3 第三子刀。吃 Pass2 LLM 的 accepted/pending/rejected,确定性硬校验 accepted_edges(grill §9),过的降成 `GraphEdge(scope=long_range)`,split evidence/support_level/rationale 留 audit;pending+rejected+gate 丢弃只进 audit,绝不进 base.json。

**触达**:
- `packages/core/src/pass2-build.ts:gatePass2BuildOutput` — PB3 gate。硬校验顺序(grill §9):edge type ∈ 11 类、scope=long_range、source/target node 存在、source/target evidence 各非空、`evidence_lids` 覆盖 source∪target evidence、evidence 全真实、`support_level≠weak_inference`、`weight≥MIN_LONG_RANGE_WEIGHT`、source/target evidence 跨窗口(复用 `isCrossWindow`)。过 → `GraphEdge`(仅 Core 字段)+ audit accepted;否则 `gate_dropped` 带 `Pass2GateDropReason`。
- `packages/core/src/pass2-build.ts` 新类型:`SupportLevel`、`TechnicalLearningAcceptedEdge`(grill §7 split evidence)、`RejectedReason`/`RejectedCandidate`(grill §3)、`Pass2LlmOutput`、`Pass2GateDropReason`、`Pass2AuditEdge`、`Pass2BuildAuditSidecar`(header+accepted+pending+rejected+gate_dropped)、`Pass2BuildResult`;常数 `MIN_LONG_RANGE_WEIGHT=0.5`(占位)。
- `packages/core/test/pass2-build.test.ts` — +2 测:合法 accepted 降级 + pending/rejected 仅进 audit;11 种硬校验违例各自 `gate_dropped` 正确 reason。

**入口**:PB3-5 编排把 `buildLongRangeCandidates` → Pass2 subagent → `gatePass2BuildOutput` → 写 `base.json` long_range 边 + `pass2_audit.json`。注:P1 旧 gate `pass2.ts:gateTechnicalLearningPass2LongRange` 仍在(简版,不含 split evidence/support/跨窗口),PB3-5 用新 gate,旧 gate 后续评估是否退役。
**测试**:`pnpm -C packages/core test`(73 全绿,+2 pass2-build)+ `typecheck`(tsc 0)。

## 2026-06-28 · PB3-4 Pass2 work packet + edge type contracts + prompt 重写

> PB3 第四子刀。把「LLM 看到什么、必须怎么做」定下来:work packet 类型(grill §10)、每 edge type 的判定 rubric(grill §5/§6)、重写 subagent prompt 为 candidate-driven + 默认拒绝 + accepted/pending/rejected(grill §3/§13)。packet 实际装配(带正文)留 PB3-5。

**触达**:
- `packages/core/src/pass2-build.ts:EDGE_TYPE_CONTRACTS` — 11 类 edge 的 `{when, when_not, evidence, roles, direction}` rubric(grill §5);direction 按 §6:仅 `analogous_to` undirected,`contrasts` 默认 directed,余 directed。`EdgeTypeContract` 类型。
- `packages/core/src/pass2-build.ts:Pass2WorkPacket` + `CandidateNodeSnapshot` — grill §10 per-source-window 分类单元(source_window 正文/leaf_lids/title_path + source_nodes/discourse/formula + candidate_targets + edge_type_contracts)。
- `agents/pass2-longrange-linker.md` — 从「P1 自由抽边」重写为「PB3 逐候选分类」:输入 `Pass2WorkPacket`,默认拒绝流程(grill §13)、关键区分(prerequisite vs builds_on 等)、support_level 规则(grill §8)、输出三类 JSON;红线含证据分两侧/跨窗口/scope=long_range。
- `packages/core/test/pass2-build.test.ts` — +2 测:contracts 覆盖全 11 类且自洽;仅 analogous_to undirected。

**入口**:PB3-5 装配 `Pass2WorkPacket`(注入源窗口正文/title_path)喂 subagent;subagent 输出经 `gatePass2BuildOutput`。
**测试**:`pnpm -C packages/core test`(75 全绿,+2 pass2-build)+ `typecheck`(tsc 0)。

## 2026-06-28 · PB3-5 Pass2 预构建编排 + 写盘(PB3 收口)

> PB3 末刀。`pass1-batch` 接线:确定性候选 → (Pass2 subagent 输出 fixture)→ PB3 gate → 合并 long_range 边进 base + 写 `pass2_audit.json` + build-only `long_range_candidates.json`。仅 TS 改动,Rust/read-tools 未动(base.graph_edges 已支持 scope=long_range)。

**触达**:
- `skills/build/pass1-batch.ts` — 新增第 6 位参数 `[pass2-output.json]`;`profileHeader` 提前到 base 固化前;`buildLidToWindowIndex(windows)` → `buildLongRangeCandidates({graphNodes:nodes, lidToWindowIndex, discourseIndex?, formulaSemantics?})` 写 `long_range_candidates.json`(build-only);有 pass2 输出时 `gatePass2BuildOutput(...)` → `Pass2BuildAuditSidecarZ.parse` 自检 → long_range 边合并进 `base.graph_edges`(`[...edges, ...longRangeEdges]`)统一 `ReadOnlyBaseZ.parse` + 写 `pass2_audit.json`。
- `packages/core/src/zod.ts` — 新增 `TechnicalLearningLongRangeEdgeTypeZ`(11)、`SupportLevelZ`、`Pass2AuditEdgeZ`、`RejectedCandidateZ`、`Pass2GateDropZ`、`Pass2BuildAuditSidecarZ`(产出前自检)。
- `packages/core/test/pass2-build.test.ts` — +1 集成测:合成 2 窗口基座 `buildLongRangeCandidates`→手写 Pass2 输出→`gatePass2BuildOutput` 产 long_range `GraphEdge`,audit 过 `Pass2BuildAuditSidecarZ`。
- `skills/build/fixtures/pass2-sample.json` — Pass2 输出 fixture(accepted/pending/rejected 各一)。

**入口**:`pass1-batch.ts <book> <outputs> <idx> [formula] [discourse] [pass2-output.json]`。
**测试**:`pnpm -C packages/core test`(76 全绿,+1 pass2-build)+ `typecheck`(tsc 0)。CLI smoke(sample.md,1 窗口 0 节点):`long_range candidates=0` + `pass2_audit.json` accepted=0/pending=1/rejected=1/gate_dropped=1(c1 missing_source、c2 weak→pending、c3 rejected),zod 校验通过、写盘内容人工核对。成功写回长程边路径由集成测试确定性覆盖(sample.md 单窗口无法产跨窗口候选)。
**PB3 收口**:PB3-1..PB3-5 全完成。确定性骨架 `buildLongRangeCandidates`(grill §11 四信号)→ Pass2 subagent 分类(`agents/pass2-longrange-linker.md`)→ `gatePass2BuildOutput`(grill §9)→ base long_range 边 + `pass2_audit.json` 全打通。**未做(留 PB4)**:read-time smoke(`book.context far` 读写回后的 long_range 边);P1 旧 gate `gateTechnicalLearningPass2LongRange` 退役评估。

## 2026-06-28 · ADR-0034/0035 落档:route 导航原语 + 人主动带读/访客向导两投影 `[docs]`

> Grill 落档(读《参考_硅基天启:灭世之技术推演》insight)。演讲核心方法论 = mechanism/policy 分离,恰是 ADR-0033 我们的架构;两个 insight 各对一个故事(主动带读=教学状态机 / 引导外部 agent=世界模型+小解释)。只写架构决策与切片更新,不改可执行代码。八条决策已落 ADR。

**触达**:
- `docs/adr/0034-route导航原语-...md` — route 作为新 Core 导航原语 + 两投影:统一(一机制两投影)/确定性边界(route 零 LLM,LLM 住书里用 route 不止解释)/route_from 前沿内核 + route_to BFS/Core 结构排序 + policy 教学排序分层(架在 book.context 上)/前沿按 5 类导航语义分组(back/forward/concretize/cross/continue)/人投影默认逐停靠点带读 + 自动巡航 opt-in。
- `docs/adr/0035-book-mcp访客会话-...md` — 修订 P7 无状态 → 连接式访客会话;记忆三分(①世界模型可借 / ②读者私人不外借 / ③访客会话 ephemeral);TCP 式握手挥手 + 临时游标 cursor{at_lid,last_frontier};book_guide(返路线,book_query 姊妹);住户/访客 = 临时住户 lite。
- `docs/切片方案-profile深路径.md` — 新增 §1.5.9 navigation contracts;P3 加「agent 主动带读」+ 触达 0034;P4 标 reader_profile=② 绝不外借 + 触达 0034/0035;**P7 重写**为连接式访客向导面;**新增 P8** route Core 原语(P3/P7 共享底座);§3 完成判据加 route 行。
- `CONTEXT.md` — 新术语:route / route_from·route_to / 前沿(frontier)+导航类别 / 住户·访客 / 三类记忆 / 访客会话 / book_guide。

**入口**:后续实现从 **P8(route Core)** 起,再 P3(人带读)/ P7(访客向导)。
**测试**:`rg "route_from|route_to|导航类别|住户|访客|book_guide|访客会话|ADR-0034|ADR-0035" docs/ CONTEXT.md`;本切片未改可执行代码,未跑 cargo/pnpm。
**OPEN(未 grill / 待定)**:① **反馈信号**——agent 靠什么"反馈"调整带读(无眼动,候选:用户消息 / viewport 手动滚动 / memory / quiz),下一个 grill;② route 命令面落点 + 命名;③ route_to / book_guide / 访客会话 归 P7/P8 的具体边界。

## 2026-06-28 · ADR-0036 落档:带读反馈信号模型(解决 OPEN①) `[docs]`

> §0.5 Grill 四问收敛(承 OPEN①)。把"agent 靠什么反馈调整带读"定下来:显式 NL 提问为唯一主信号 / 反馈意图二维 / 裸信号结构兜底消歧 / 人访客两投影 / viewport 模式分裂。只写架构决策,零可执行代码改动。

**触达**:
- `docs/adr/0036-反馈信号模型-...md`(新)— 五决策:①显式 NL 提问唯一主信号(viewport 弱旁路/memory 慢先验/quiz 留后);②反馈意图二维(导航轴→route 5 类 / 讲法轴→policy 讲法层)+ 开放 NL 不立闭集词表,agent 据语义定 `{轴+类别+target}`,target 经 book.concept 解析(route 之外);③裸"没懂"= `route_from(at).back ∩ 未读前置` 结构收窄 + 可撤销提议 + 二次信号升级,不靠 LLM 神判;④人/访客同骨架两插槽(历史来源 ②/③、讲法整形 reader_profile/无)+ 终裁者不同;⑤viewport 偏离角色随模式分裂(默认 turn-based=静默 re-sync rebase `at`,巡航 opt-in=中断信号停+问)。命门=route 零 LLM,LLM 只产 `{轴+类别+target}` 不产路/LID。
- `docs/切片方案-profile深路径.md` — §1.5.9 红线加反馈信号行;P3 做/判据加 NL 提问→`{轴+类别}` + 裸信号结构兜底 + viewport 静默 re-sync,触达 +0036;P7 做/判据加访客反馈两插槽(③历史/讲法塌缩/访客自身终裁),触达 +0036。
- `CONTEXT.md` — 新术语:反馈信号 / 导航轴·讲法轴。

**入口**:实现仍从 **P8(route Core)** 起,P3 带读 loop 消费反馈走 ADR-0036;P7 访客面同骨架换插槽。
**测试**:`rg "反馈信号|导航轴|讲法轴|ADR-0036|结构兜底|静默 re-sync" docs/ CONTEXT.md`;本切片未改可执行代码,未跑 cargo/pnpm。
**OPEN 余下**:② route 命令面落点 + 命名;③ route_to / book_guide / 访客会话 归 P7/P8 的具体边界。(① 已由本 ADR 关闭)

## 2026-06-28 · OPEN② 收口:route 命令面落点 + 命名(更新 ADR-0034 影响段 + P8 判据) `[docs]`

> §0.5 Grill 两问收敛(承 OPEN②)。route 落点定为 book.* 一等叶子命令 + 两命令命名 + 参数/暴露形状。不新开 ADR(命名落点未到独立架构权衡份量,吝啬提议),更新 ADR-0034 自标的 OPEN + P8 判据收口。零代码。

**触达**:
- `docs/adr/0034-...md` 影响段 — 关掉"命令面落点 OPEN":归 `book.*` 一等叶子命令(确定性/零 LLM/只读/毫秒级,符 ADR-0014 叶子 + ADR-0007 人机对称);命名 `book.route_from(at,k?)`(返全 5 类、无 category 过滤参)+ `book.route_to(from,target,k?)`(BFS,target 先 book.concept 解析);三段落点(Core `read_tools::Book::route_*` 架在 context 上 / `orchestrator::tool_specs()`+dispatch 两命令均暴露 / REST 自动 GET);错误信封 ADR-0015。
- `docs/切片方案-profile深路径.md` P8 — 做/不做/判据加命令面落点(tool_specs 暴露、参数最小返全 5 类、不给 category 过滤参、错误信封);触达 `[ADR-0007/0013/0014/0028/0034]`。

**入口**:实现仍从 P8 起;`book.route_from` 内部吃 `Book::context(at,"far")` 的边按 `edge_type→NavCategory` 重组。
**测试**:`rg "book.route_from|book.route_to|一等叶子命令|category 过滤参" docs/`;本切片未改可执行代码。
**OPEN 余下**:③ route_to / book_guide / 访客会话 归 P7/P8 的具体边界。(①② 已关闭)

## 2026-06-28 · OPEN③ 收口:P7/P8 crate 边界 + 访客暴露双层(补 ADR-0035 决策7) `[docs]`

> §0.5 Grill 两问收敛(承 OPEN③)。Q1 层归属:route mechanism 共享兑现两投影、book_guide=runtime lite 命令、VisitorSession=server,不字面共享 run()。Q2 暴露边界:Tier 1 无状态只读 / Tier 2 book_guide 带会话,红线靠物理无路由。补 ADR-0035 决策7 + P7 判据。零代码。

**触达**:
- `docs/adr/0035-...md` — 决策2 限定作用域(连接式仅 Tier 2 book_guide);**新增决策7**(暴露双层 + crate 边界 + 红线焊法):Tier 1 无连接无状态只读(book_manifest/text/context/concept/query/synthesize,不建会话)/ Tier 2 带会话 book_guide(握手/挥手/GC 只压此层);crate 落点 route_*=read-tools Core 共享、book_guide=runtime lite LLM 命令(query 姊妹、不复用住户 run())、VisitorSession=server AppState;裸 route 不给访客(v1);红线靠访客 MCP dispatch 物理无 reader/memory 分支(非运行时权限判)。影响段:访客 N 会话=Tier 2 only,GC 只压 Tier 2。
- `docs/切片方案-profile深路径.md` P7 — 做加 Tier 双层 + crate 落点;不做加 裸 route 不给访客/物理无路由/Tier 1 不建会话/book_guide 不塞 server;判据加 Tier 1 不建会话 + dispatch 物理无 reader/memory 分支可测。

**入口**:P7 实现按 Tier 双层;route_* 从 P8 起;book_guide 在 runtime 复用 query/synthesize mini-loop + route_*。
**测试**:`rg "Tier 1|Tier 2|物理无路由|决策7|lite LLM 命令" docs/`;本切片未改可执行代码。
**OPEN 全部关闭**:①(ADR-0036 反馈信号)/ ②(ADR-0034 影响段 route 命令面)/ ③(ADR-0035 决策7 P7/P8 边界)。route 两投影带读设计线 grill 收敛,下一步进代码(P8 route Core 起)。

## 2026-06-28 · P8-1 route_from Core 前沿原语(NavCategory 映射 + 5 类分组 + 结构排序) `[Rust]`

> P8 第一子刀(A4 拆 P8-1 route_from / P8-2 route_to / P8-3 命令面)。承 ADR-0034 决策4/5。**仅 read-tools,未碰 runtime**(命令面暴露留 P8-3)。

**触达**:
- `crates/read-tools/src/lib.rs:NavCategory`(新 enum,5 类 back/forward/concretize/cross/continue,ts-rs 导出 packages/web)+ `RankedStep`(lid/edge_type/why/evidence_lids/score)+ `Frontier`(5 类 Vec,`continue_` serde/ts rename `continue`)`[ADR-0034 决策5]`。
- `crates/read-tools/src/lib.rs:nav_category_of`(自由函数)— `edge_type→NavCategory` 固定确定性映射表;back=prerequisite/depends_on、forward=builds_on/refines/elaborates/explains/prepares/causes/results_in、concretize=exemplifies/applies/answers、continue=next_sibling/continues、**未知 local 边→cross 兜底**(保覆盖全边类型不丢边)。
- `crates/read-tools/src/lib.rs:Book::route_from(at,k?)` — 调 `context(at,"far",MAX)` 拿全投影项 → `route_step` 逐项转 (NavCategory, RankedStep) → 分组 → 组内 `score=weight/(1+树距)` 降序(tiebreak lid→edge_type)→ 各类 top-K。零 LLM、纯结构。invalid at 透传 `context` 的 LID_NOT_FOUND(not_found);叶子无边返空 5 类非 error。
- `Book::route_step`(私)— Via→步映射:**Tree 仅取 next_sibling→continue**(parent/prev_sibling/child 不进前沿 v1);Edge 用 e.edge_type+weight;Concept→co_occurrence/cross(占位权重 `CONCEPT_COOCCURRENCE_WEIGHT=0.5`);Discourse 用 relation_type+confidence+真 evidence_lids。
- `Book::lid_tree_distance`(私)— 物化路径公共前缀外层数和(占位口径)。
- 常数 `DEFAULT_ROUTE_K=DEFAULT_NEAR_K`、`CONCEPT_COOCCURRENCE_WEIGHT`(占位,实测回填 ADR-0034 何时回头)。
- `packages/web/src/generated/{NavCategory,RankedStep,Frontier}.ts` — ts-rs 自动产出。

**入口**:`read_tools::Book::route_from`(库 API;命令面 `book.route_from` 暴露留 P8-3)。`route_to`(P8-2)、`nearest_valid_lid` 错误增强(需扩 ToolError 共享面,单独评估)、教学 reorder(P3 policy)**本刀不做**。
**测试**:`cargo test -p read-tools`(**29 绿**,+6:nav 映射全桶+未知兜底 / 远边+概念共现+sibling 分组 / 叶子空 5 类非 error / invalid at not_found / discourse 关系映射 / k 截断保高分;+3 ts-rs export NavCategory/RankedStep/Frontier)。
**两处自拍决定(待 P3 实测校)**:未知 local 边→cross 兜底;Tree 仅 next_sibling 进前沿。
**未闭(P8 余下)**:P8-2 `route_to` BFS 派生;P8-3 `orchestrator::tool_specs()`+dispatch 暴露两命令 + REST GET + nearest_valid_lid 错误增强评估。

## 2026-06-28 · P8-2 route_to BFS 派生(同批边路径式导航) `[Rust]`

> P8 第二子刀。承 ADR-0034 决策3(route_to = route_from 同批边上 BFS 的确定性组合)。仅 read-tools,命令面留 P8-3。

**触达**:
- `crates/read-tools/src/lib.rs:Book::route_to(from,target,k?)` — 在 route_from 同批边上跑确定性 BFS,返 `from→target` 导航步路径 `Vec<RankedStep>`(全真 LID/真边)。`target` 须已解析 LID(NL→入口在 route 之外,`book.concept`)。`k`=跳数预算。from/target 非真→LID_NOT_FOUND(not_found);from==target 或 预算内不可达→空路径非 error。BFS prev 回溯 + visited 防环,邻居按固定类序展开保确定性。
- `Book::route_neighbors`(私)— route_from 5 类前沿按 (back,forward,concretize,cross,continue) 序展平、按 lid 去重(保首现);BFS 内用 `route_from(lid, MAX)` 全量前沿避免 top-K 截断藏路。
- 常数 `DEFAULT_ROUTE_HOPS=8`(占位,实测回填 ADR-0034 决策3 预算全程)。

**入口**:`read_tools::Book::route_to`(库 API;命令面 `book.route_to` 暴露留 P8-3)。
**测试**:`cargo test -p read-tools`(**35 绿**,+6:直达 1 跳 / 多跳链 1.1→1.2→1.3 / from==target 空 / 不可达(章节 LID)空 / invalid 端点 not_found / 跳数预算 k=1 够不到空)。新增 `book_chain()` fixture(长程链无直边)。
**未闭(P8 余下)**:P8-3 `orchestrator::tool_specs()`+dispatch 暴露 `book.route_from`/`book.route_to` + REST GET + nearest_valid_lid 错误增强评估。

## 2026-06-28 · P8-3 route 命令面暴露(tool_specs + dispatch + REST GET)`[Rust]` —— P8 收口

> P8 末刀。把 `book.route_from`/`book.route_to` 暴露到三处命令面。承 ADR-0034 影响段(book.* 一等叶子命令)+ ADR-0007 人机对称 + ADR-0028 只读叶子 GET。

**触达**:
- `crates/runtime/src/orchestrator.rs:tool_specs()` — 加 `book.route_from(at,k?)` / `book.route_to(from,target,k?)` 两 ToolSpec(LLM 可见,bounded 非 manifest token 炸弹)。
- `crates/runtime/src/orchestrator.rs:dispatch()` — 加两分支:`book.route_from`(缺 at→INVALID_RANGE)、`book.route_to`(缺 from/target→INVALID_RANGE,返 `{from,target,path}` 信封);只读不产 effect;ToolError 原样回喂不降级(ADR-0015)。
- `crates/server/src/lib.rs:route_book()` — REST GET 加 `route_from`/`route_to` 两 leaf(mirror book.context:缺参→400 validation、非法 k→400、invalid LID→404 透传 ToolError)。
- `packages/web/src/generated/{NavCategory,RankedStep,Frontier}.ts` — 已于 P8-1 产出;`Frontier.continue` 键经 serde/ts rename 正确(非 continue_),守 §1.5.9 `Record<NavCategory,_>` 契约。

**入口**:外层 LLM 经 tool_specs 见两命令、dispatch 执行;HTTP `GET /book/route_from?at=&k=` / `GET /book/route_to?from=&target=&k=`。
**测试**:`cargo test -p runtime -p server -p read-tools`(**全绿**:read-tools 35 / runtime 34 +3(tool_specs 暴露两名 / route_from 返 5 类+invalid not_found / route_to 包 path+缺参 validation)/ server 27 +1(route_from/route_to GET 200+5键、400 缺参/非法k、404 invalid))。预存 `failed to parse serde attribute` ×4 警告非本刀引入(read-tools 单建为 0)。
**交叉验证发现**:ADR-0034 影响段写「REST 自动 GET」,实为 `route_book` 手工逐命令 wiring(非自动)——已按现实补手写分支;ADR 措辞待修(见下『待办』)。
**P8 收口**:P8-1 route_from + P8-2 route_to + P8-3 命令面 三子刀全完成。route Core 导航原语(确定性、零 LLM、人机对称)落地,P3 人带读 / P7 访客 book_guide 的共享底座就位。
**待办(非阻塞)**:① ADR-0034 影响段「REST 自动 GET」措辞修为「手工 wiring」;② nearest_valid_lid 错误增强(需扩共享 ToolError,单独评估);③ route 权重/距离/前沿规模实测回填(ADR-0034 何时回头)。

## 2026-06-28 · P3-1 带读骨架(prompt 驱动主动带读 + 管道回归测试)`[Rust]`

> P3 首刀(A4 拆 P3-1 骨架 / P3-2 反馈消歧兜底 / P3-3 policy / P3-4 前端)。承 ADR-0034 决策6 + ADR-0036。架构方向(§0 用户拍板):**复用 run()+prompt 驱动带读,Rust 仅确定性兜底**(本刀不含兜底)。

**触达**:
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加「主动带读」引导段:用户请求『带我读/一步步讲/接着讲』时逐停靠点——reader.state 取当前 anchor → book.route_from(anchor) 看 5 类前沿 → 按用户意图挑**一个**下一停靠点(LID 只取自 route_from,不可编造)→ reader.gotoLid 真翻(可撤销 Goto)→ book.synthesize([上一停靠点,新停靠点]) citation-gated 解释 → 讲完即停等人,一回合只进一个停靠点 `[ADR-0034 决策6/ADR-0036]`。
- `crates/runtime/src/orchestrator.rs:tests::guided_read_one_stop_pipeline`(新)— scripted FakeAdapter 演一个带读回合,断言管道串通。

**入口**:前端对话区『带我读这章』→ `/agent/chat` → `run()`;带读全靠现有外层 loop + 新 prompt 引导,**无新编排函数**(架构方向兑现处)。
**测试**:`cargo test -p runtime`(**35 绿**,+1:带读回合工具序列 reader.state→book.route_from→reader.gotoLid→book.synthesize + 单条 Goto(1.1→1.2)合并可撤销 + citations⊆输入 + 终答非空 + !incomplete)。
**B2 边界**:确定性测只覆盖「带读管道串得通」(回归保护);**prompt 带读智能(挑哪个停靠点 / 讲多细 / 何时回看)无法 cargo test,须靠真 LLM 后端手动验**。
**未闭(P3 余下)**:P3-2 NL→{轴+类别}消歧 + 裸「没懂」结构兜底(`route_from.back ∩ 未读前置`)+ viewport 静默 re-sync;P3-3 `TechnicalLearningAgentPolicy` 教学 reorder/过滤;P3-4 Vue 带读 UI。

## 2026-06-28 · P3-3 技术学习教学整形 policy + book.guided_route_from `[Rust]`

> P3 第三子刀。承 ADR-0037(§0.5 grill 落档)+ ADR-0034 决策4/7。**新增住户教学整形 route 投影**:= route_from(Core) + technical_learning 教学整形(runtime profile policy,与 `book.synthesize` Core+policy 同构,零 LLM 确定性)。§0.5 拍板:Y1 新增姊妹工具 + C 中性默认序占位。

**触达**:
- `crates/runtime/src/lib.rs:GuidedGroup`(新 `{category:NavCategory, steps:Vec<RankedStep>}`,ts-rs 导出)+ `TEACHING_ORDER`(中性默认教学序常量 `continue>back>concretize>forward>cross`,占位待 P4 reader_profile 回填)+ `technical_learning_reorder(Frontier)→Vec<GuidedGroup>`(按教学序重排+剔空组,确定性)+ `pub fn guided_route_from(book,at,k?)`(route_from + 整形)`[ADR-0037 决策1/2/4]`。
- `crates/runtime/src/orchestrator.rs:tool_specs()` — 加 `book.guided_route_from(at,k?)` ToolSpec(LLM 可见)。
- `crates/runtime/src/orchestrator.rs:dispatch()` — 加 `book.guided_route_from` 分支(缺 at→INVALID_RANGE;返 `{at, groups}` 信封;只读不产 effect;ToolError 原样回喂)。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 带读引导②改优先 `book.guided_route_from`(教学整形版;裸 route_from 降为底层/访客)。
- `crates/server/src/lib.rs:route_book()` — REST GET 加 `guided_route_from` leaf(mirror route_from:缺 at→400、非法 k→400、invalid at→404,返 `{at, groups}`)。
- `packages/web/src/generated/GuidedGroup.ts` — ts-rs 自动产出(`{category, steps}` 契约)。

**入口**:带读 LLM 经 tool_specs 见 `book.guided_route_from`、dispatch 执行;HTTP `GET /book/guided_route_from?at=&k=`。住户带读优先用本工具拿整形过的 route。
**测试**:`cargo test -p read-tools -p runtime -p server`(**全绿**:read-tools 35 / runtime 39 +4(reorder 中性序+剔空 / 空前沿无组 / tool_specs 暴露 guided / dispatch guided 返 {at,groups}+剔空+缺 at validation)/ server 27 含 guided GET 200+{at,groups}/400 缺参·非法k/404 invalid)。
**B2 边界**:教学整形 reorder/剔空**确定性可单测**(判据"policy 教学排序作用在分组上"达成);**带读用 guided_route_from 挑停靠点的智能靠真 LLM 手动验**。
**未闭(P3 余下)**:P3-2(NL→{轴+类别} + viewport re-sync,均 prompt;裸兜底已推迟 P4);P3-3 余项(reader_profile 个性化整形 = P4 接入;"何时建议 goto/highlight/note/公式"深度 prompt policy 未扩,P3-1 有基础动作引导);P3-4 Vue 带读 UI。

## 2026-06-28 · P4-1 确定性已读账本(reader 翻页 → memory 记账)`[Rust]`

> P4 首刀(ADR-0038 重定位:Claude 式透明账本)。承 ADR-0038 决策1「① 确定性账本(无 LLM,直存)= 真读过的 LID 历史」。§0 拍板数据模型:**复用 Record `type="read"`,内容寻址天然去重**;reader `goto/scroll` 落点 anchor 真叶记账。**解锁 P3-2 裸兜底真历史源**(`未读前置 = back ∩ (全集\已读集)`,消费方在 P3-2,本刀不写)。

**触达**:
- `crates/memory/src/lib.rs:MemoryStore::mark_read(book_id,lid,now)` — 新:已读记账,复用 `save(type="read", layer="long_term", content="")`,内容寻址 upsert(重读=同 mem_id、count++、generated_at 刷新);非 note/highlight ⇒ 无 citation。
- `crates/memory/src/lib.rs:MemoryStore::read_lids(book_id)→Vec<String>` — 新:已读集/reading journey,recall(type=read) 按 generated_at 触达序(tie-break mem_id)确定性排序、去重。
- `crates/reader/src/lib.rs:Reader::goto_lid` — 签名加 `store:&mut MemoryStore, now`;落点成功后 `mark_read(真叶 leaf_lids[i])`(goto 容器→记子树首叶,非传入容器 lid)。
- `crates/reader/src/lib.rs:Reader::scroll` — 签名加 `book, store, now`,**返回类型 `()`→`Result<ViewportEffect,ToolError>`**(记账=持久写,失败诚实传播不静默);落点 anchor 记账。
- `crates/server/src/lib.rs:route_mut()` — `/reader/goto`·`/reader/scroll` 分支传 `&mut state.store, now`;scroll 改 match Result。
- `crates/runtime/src/orchestrator.rs:dispatch()` — `reader.gotoLid`·`reader.scroll` 分支传 `store, now`;scroll 改 match Result。

**入口**:人/agent 经命令面翻页 → `reader.goto_lid`/`reader.scroll` → 委托 `memory.mark_read` 落持久账本(与 note/highlight 委托 memory.save 同构,标注/账本单源=记忆层)。HTTP `POST /reader/goto|/reader/scroll`;agent `reader.gotoLid|reader.scroll`。
**测试**:`cargo test -p memory -p reader -p server -p runtime`(**全绿**:memory 10 +2(read_ledger 标记/去重/触达序/跨书隔离/无citation·落盘持久)/ reader 27 +1(goto+scroll+容器记真叶+未翻到不入集)/ server 4 / runtime 39)。
**B2 边界**:已读集「读过 vs 没读过」+ 记账落盘**确定性可单测**(判据达成);记账触发点=anchor 落点(不近似、不记 visible 全部、不记容器 lid)。
**未闭(P4 余下)**:P4-2 reader_profile 确定性派生(已读集 + note/highlight/qa 的 LID,解锁 P3-3 已读降权)/ P4-3 用户主动 LLM 记忆(显式记·反复提及,读时 memory.save)/ P4-4 四层透明文件产物。**回收点**:P3-2 裸兜底现已有真历史源(read_lids),可捡起。

## 2026-06-28 · P4-2 reader_profile 确定性派生 + 已读降权接入(解锁 P3-3)`[Rust]`

> P4 第二刀,A1 拆两子刀连做:P4-2a 派生(memory)+ P4-2b reorder 接入(runtime)。承 ADR-0038 决策3(四层产物=确定性派生)+ ADR-0037(P3-3 reader_profile 留 P4)。§0 拍板已读降权语义 = **组内稳定降权排后**(未读在前·已读沉底,保留回看入口,不剔除)。

**触达**:
- `crates/memory/src/lib.rs:ReaderProfile`(新结构 `{book_id, read_lids, focus_lids, puzzle_lids}`,② 读者私有,无认知水平推断)+ `MemoryStore::derive_reader_profile(book_id)`(确定性聚合:已读集 + 关注点 note/highlight LID + 疑惑点 qa LID)+ `anchor_lids_of_type`(私有,去重·LID 序)`[ADR-0038 决策3]`。**qa 类型未落地 ⇒ puzzle_lids 诚实暂空**。
- `crates/runtime/src/lib.rs:technical_learning_reorder(f, read_set)` — 加 `read_set` 参数,组内 `sort_by_key(read_set.contains)` 稳定降权(未读 false<已读 true);`read_set` 空 ⇒ 退化纯 TEACHING_ORDER(向后兼容)`[ADR-0037/0038]`。
- `crates/runtime/src/lib.rs:guided_route_from(book, at, k, profile)` — 加 `&ReaderProfile` 参数,从 `profile.read_lids` 建 read_set 传 reorder(focus/puzzle 留后续)。import `memory::ReaderProfile` + `HashSet`。
- `crates/runtime/src/orchestrator.rs:dispatch()` — `book.guided_route_from` 分支 `store.derive_reader_profile(book_id)` 传入。
- `crates/server/src/lib.rs:route_book()` — 签名加 `store: &MemoryStore`(仅 guided 用);调用处 `route_book(&state.book, &state.store, ...)`;guided 分支派生 profile 传入。

**入口**:住户带读 LLM 经 `book.guided_route_from` / HTTP `GET /book/guided_route_from?at=` 拿**已读降权**后的教学整形前沿;profile 从 P4-1 持久账本派生(read_lids)。
**测试**:`cargo test --workspace`(**全绿**:memory 11 +1(derive 派生·去重·跨书隔离·qa 诚实空)/ runtime 40 +1(组内未读升首·已读沉底保原序·不剔除)/ server 27 / read-tools 35;两现有 reorder 测试随签名加 `&HashSet::new()`)。
**B2 边界**:派生 + 组内稳定降权**确定性可单测**(判据达成);账本→profile→降权两段拼接由类型系统保证,未加冗余闭环集成测试。
**未闭(P4 余下)**:P4-3 用户主动 LLM 记忆 / P4-4 四层透明文件。**qa 落地**(E loop 问答 save type=qa)未做 ⇒ 疑惑点维度待填。synthesize 的 `reader_profile=not_attached` prompt 表达层未接(留后续)。**可捡**:P3-2 裸兜底(read_lids 真历史源)/ P3-3 已读个性化现已通(guided 降权)。

## 2026-06-29 · P4-3 用户主动 LLM 记忆(agent judgment 触发 + context type + citation 闸)`[Rust]`

> P4 第三刀,一条线(ADR-0039 修正 ADR-0038:**砍确定性计数器**,改 agent 读时 judgment 主动记;memory=构建用户上下文 + 三护栏)。§0.5 两 PENDING 实现期拍板:**type 名=`context`**、**citation 闸=无效丢弃·零有效仍可存**。底层 memory crate 未动(`SaveInput.citations` / `Record.{citations,generated_at}` P4 前已就位)。

**触达**:
- `crates/runtime/src/orchestrator.rs:tool_specs()` `memory.save` — `type` enum 加 `"context"`;加可选 `citations:[string]` 参数;description 述明 context = 主动构建的用户上下文(背景/偏好/关注/卡点,认知诚实)`[ADR-0039]`。
- `crates/runtime/src/orchestrator.rs:dispatch()` `memory.save` 分支 — **citation 确定性闸**:仅当显式传 `citations` 时进闸,逐 lid 校验 `∈ book.base.lid_nodes`(承 reader.gotoLid 同款),无效**确定性丢弃·不阻断·零有效仍存**,有效转 `MemCitation{lid,book_id,note:None}` 传 `SaveInput.citations`(原硬编码 None);不传 → None(承 memory crate note/highlight 自动派生)。`context` 走既有 `ty!="position"⇒long_term` 分支,直接落 long_term`[ADR-0039 决策3]`。import 加 `MemCitation`。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加 judgment 主动记引导:显式「记住/记下」∨ judgment 值得构建进读者长期理解 → `memory.save(type='context',…,citations?)`;守三护栏(认知诚实带证据措辞 / citations 锚真 LID / 只记跨会话值得复用·记错可删)。

**入口**:住户带读/问答 LLM 在 E loop(`run()`)读时,据 SYSTEM_PROMPT judgment 主动调 `memory.save type=context`;HTTP `POST /memory/save`(承命令面 REST 投影)同面可达。
**测试**:`cargo test -p runtime`(**41 全绿**,+1 `dispatch_memory_save_context_gates_citations`:有效 1.1 保留 / 无效 9.9 丢弃 / 全无效零有效仍存 / 不传 citations 空仍存 / context 落 long_term / 不产 effect / recall 取回 3 条)+ `cargo build --workspace` 通过。
**B2 边界**:citation 闸 + long_term 落位 + 零有效仍存**确定性可单测**(判据达成);**agent judgment「该不该记」的智能留真 LLM 手动验**(本质无客观判据,守 B2 不靠 LLM 自评做闸)。
**未闭(P4 余下)**:P4-4 四层透明文件(context 连成成长时间线呈现)。**qa 落地**仍未做。context 记多/碎的 recall 裁剪策略(ADR-0039「何时回头」)留实测,**不复活计数器**。

## 2026-06-29 · P4-4 四层产物物化(只读派生 .md + 单向覆写)`[Rust]`

> P4 末刀(ADR-0040,§0.5 grill 拍两分支:**载体=物化 .md 文件**、**读写语义=纯派生只读快照·save/delete 后整页单向覆写**)。真相源唯一 memory.json;物化但单向,折中 ADR-0038(物化)× ADR-0012/0020(不双向)。**v1 两层零 LLM**(session 详档/raw 随 ADR-0038 砍 Phase1 失生产者,不做)。纯 memory crate 内,不动命令面/runtime/server。

**触达**:
- `crates/memory/src/lib.rs:MemoryStore::all_book_ids`(私有,distinct·排序)+ `context_timeline(book_id)`(私有,context 记忆按 generated_at 时间线序,tie mem_id)`[ADR-0039/0040]`。
- `crates/memory/src/lib.rs:MemoryStore::render_reader_profile_md` — 渲染 `reader-profile.md`(所有书分段:已读集 + 关注点 note/highlight + 卡点 qa[暂空诚实] + agent 记的 context 成长时间线带 cite),纯确定性无 LLM`[ADR-0040]`。
- `crates/memory/src/lib.rs:MemoryStore::render_handbook_md` — 渲染 `reading-handbook.md`(per-book 计数 + cross-book 读过哪些书/各自 context 条数;概念对齐留 ADR-0006),纯确定性`[ADR-0040]`。
- `crates/memory/src/lib.rs:MemoryStore::write_profile_files` — 把两 .md 物化到 memory.json 同目录(ASCII 文件名避 Windows 中文文件名工具坑);**best-effort**,失败不阻断账本。
- `crates/memory/src/lib.rs:MemoryStore::{save,delete}` — persist 后 `let _ = self.write_profile_files()`(账本变 → .md 单向覆写;mark_read 走 save 同触发)。

**入口**:用户/agent 直接看/grep `<memory 目录>/reader-profile.md` 与 `reading-handbook.md`(memory.json 同目录,默认 `<home>/.understand-book/memory/`);随 save/delete 自动刷新。
**测试**:`cargo test -p memory`(**12 全绿**,+1 `profile_files_materialize_and_overwrite_on_delete`:已读/关注/context 时间线+cite/qa 暂空/cross-book 落盘 + delete 后单向覆写旧 context 消失·已读仍在)+ `cargo test --workspace` 全绿(runtime 41/server 27/read-tools 35…,save/delete 新副作用 best-effort 不扰现有测试)。
**B2 边界**:渲染 + 落盘 + 单向覆写**确定性可单测**(判据达成);LLM 表达层摘要 v1 不做(守 B2,无 LLM 自评)。
**未闭(留后续)**:LLM 表达层摘要(讲成人话,不产事实);session 详档(需非后台确定性会话摘要源);cross-book 概念对齐(ADR-0006);reader-profile 体积膨胀裁剪(不复活计数器)。**qa 落地**仍未做 ⇒ 卡点维度恒空。**P4 大切片至此 P4-1~P4-4 全部完成**。

## 2026-06-29 · P3-2 裸「没懂」结构兜底(book.unvisited_back 确定性命令)`[Rust]`

> P3-2 末块(ADR-0036 决策3)。NL→{轴+类别} + viewport re-sync 早随 P3-1/P3-3 SYSTEM_PROMPT 落;**裸「没懂」兜底**当时依赖 P4 真历史源推迟,现 P4-1 `read_lids` 就位后捡起。§0.5 拍载体=**新独立确定性命令**(否决扩 guided 标 read 让 agent 心算交集,违 ADR-0036 命门)。无新 ADR(机制 ADR-0036 已钉)。

**触达**:
- `crates/runtime/src/lib.rs:unvisited_back(book, at, profile)` — 确定性原语:`route_from(at).back` 过滤 `read_lids` → 未读前置 RankedStep 列表(组内保 weight×距离 序,首项=最该回看)。未读过滤在 runtime policy 层消费 ② reader_profile,route Core 零 LLM 不破(guided_route_from 姊妹)`[ADR-0036 决策3]`。
- `crates/runtime/src/orchestrator.rs:tool_specs()` — 加 `book.unvisited_back` tool_spec(at,述明裸「没懂」用法)。
- `crates/runtime/src/orchestrator.rs:dispatch()` — `book.unvisited_back` 分支:派生 reader_profile → unvisited_back → `{at, unvisited_back}` 信封;缺 at→validation、invalid at→not_found(承 route_from),只读不产 effect。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加裸「没懂」(无 locus)编排引导:①调 unvisited_back ②空→synthesize 原地重讲(讲法轴)③非空→重讲+可撤销提议「先回看 首项」④二次「没懂」→ reader.gotoLid 升级带去回看;未读前置只能取自命令返回,不自判读没读。
- `crates/server/src/lib.rs:route_book()` — 加 `book.unvisited_back` GET 投影(派生 profile,`{at, unvisited_back}`);import 加 `unvisited_back`。

**入口**:住户带读 LLM 收到裸「没懂」→ `book.unvisited_back` / HTTP `GET /book/unvisited_back?at=` 拿确定性未读前置,据空/非空走讲法轴或回看提议。
**测试**:`cargo test --workspace` 全绿(runtime 41→43:+`unvisited_back_filters_read_prereqs_deterministically`(depends_on back fixture:全未读→[2.1,2.2]/读 2.1→[2.2]/全读→空/invalid→not_found)+ dispatch 信封接线;server 27 含新 GET 断言)。
**B2 边界**:`back ∩ 未读` 交集 + 空/非空判定**确定性可单测**(判据达成,守 ADR-0036 命门不靠 agent 心算);agent「是不是裸没懂」识别 + 编排(重讲/提议/升级)靠真 LLM 手动验。
**未闭(ADR-0036 何时回头)**:二次「没懂」升级前要不要先插一次反问;viewport re-sync「偏离」判定(完全跟随 vs 仅大跳)留实测;quiz 主动探测留后。

## 2026-06-29 · P4-5 qa-1 生产(qa 存 + puzzle_heat 派生)`[Rust]`

> P4-5 qa 落地首刀(ADR-0041,回收 P4-2 `puzzle_lids` 恒空缺口)。§0.5 grill 收敛:qa = LID 价值/提问热度信号(读者私人 ②),三消费方全锚 `anchor.lid`。本刀只生产(存 + 派生),消费(back 升权/recall/问题渲染)留 qa-2。

**触达**:
- `crates/memory/src/lib.rs:ReaderProfile.puzzle_lids: Vec<String>` → **`puzzle_heat: BTreeMap<String,u32>`**(lid→qa 记录条数;保留次数=价值热度信号)`[ADR-0041 决策3]`。
- `crates/memory/src/lib.rs:MemoryStore::qa_heat`(新私有)— 按 `anchor.lid` 聚合 qa 记录条数(内容寻址:不同问题=不同 record 计 1;重复同问题 upsert 不增),纯确定性无 LLM。
- `crates/memory/src/lib.rs:derive_reader_profile` — `puzzle_lids: anchor_lids_of_type(&["qa"])` → `puzzle_heat: qa_heat(book_id)`。
- `crates/memory/src/lib.rs:render_reader_profile_md` 卡点段 — 渲染 `- {lid} (×{count})`(真实问题文本渲染留 qa-2);`render_handbook_md` 卡点计数改 `puzzle_heat.len()`。
- `crates/runtime/src/orchestrator.rs:tool_specs()` `memory.save` — type enum 加 `"qa"`;description 述明 qa = book.query 答完后存(anchor=问题 LID、content=用户原问题)。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加 qa 记录引导:用 book.query 答完书内提问后 `memory.save(type='qa', anchor_lid=<query anchor>, content=<用户原问题>)`;事实记录不判断卡否;操作/闲聊/元问题不记。
- dispatch `memory.save` qa **无需改**:走既有 `ty!="position"⇒long_term`、非 note/highlight 不自动派生 citation,qa 直接流通。

**入口**:住户 E loop(`run()`)读时,据 SYSTEM_PROMPT 在 book.query 答题后调 `memory.save type=qa`;HTTP `POST /memory/save` 同面可达。puzzle_heat 经 `derive_reader_profile` 派生(消费方 qa-2 接)。
**测试**:`cargo test -p memory -p runtime` 全绿(memory 13 +1 `derive_reader_profile_qa_heat_aggregates`:不同问题聚合计数/重复 upsert 不增/跨书隔离/read·note 同 lid 不计入;runtime 44 +1 `dispatch_memory_save_qa_lands_longterm_and_feeds_heat`:qa 落 long_term+anchor 设+不产 effect+heat 聚合);`cargo build --workspace` + server 27/4 绿(struct 字段变更无外溢)。
**B2 边界**:qa 存储 + heat 派生**确定性可单测**(判据达成);**agent「是不是实质问题/该不该存 qa」的 judgment 靠真 LLM 手动验**(本质无客观判据,守 B2)。
**未闭(qa-2)**:`technical_learning_reorder` back 组按 heat 升权(压已读)+ `reader-profile.md` 卡点段渲染真实问题文本 + LID-local recall(`memory.recall(lid,type=qa)`)prompt 引导。

## 2026-06-29 · P4-5 qa-2 消费(back 组卡点升权 + 问题渲染 + LID-local recall)`[Rust]` —— qa 落地收口

> P4-5 qa 落地末刀(ADR-0041),消费 qa-1 的 `puzzle_heat`。三消费方全锚 `anchor.lid`:确定性 back 升权 / 透明渲染 / LID-local recall。

**触达**:
- `crates/runtime/src/lib.rs:technical_learning_reorder` — 加 `puzzle_heat: &BTreeMap<String,u32>` 参数;**仅 back 组** qa 卡点升权(压已读):组内 `sort_by_key((tier, Reverse(heat)))`,Tier A 问过(heat>0,heat 降序)/ B 未读+没问过 / C 读过+没问过沉底,卡点=读过+问过落 Tier A 冒顶;tiebreak 保 route_from score 序(稳定排序)。其余 4 组保持仅已读降权 `[ADR-0041 决策5/6/7]`。
- `crates/runtime/src/lib.rs:guided_route_from` — 传 `&profile.puzzle_heat` 给 reorder(消费 ② 读者私人 puzzle_heat;focus 留后续)。`book.guided_route_from` 命令面签名不变 ⇒ orchestrator dispatch / server route_book 零改动。
- `crates/memory/src/lib.rs:MemoryStore::qa_questions`(新私有)— 某书某 LID 的 qa 提问文本(generated_at 序),供透明展示读"问了什么"(区别 `qa_heat` 只数条数)。
- `crates/memory/src/lib.rs:render_reader_profile_md` 卡点段 — 升级为 `- {lid} (×{count})` + 嵌套真实问题文本(承 ADR-0040 透明 .md)。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加 LID-local recall 引导:带读/答某 LID 时先 `memory.recall(lid, type=qa)` 看读者历史提问,贴合其关心点(卡过的多讲)。

**入口**:住户带读 LLM 经 `book.guided_route_from` 拿 **back 组卡点升权**后的整形前沿(卡过的前置冒顶);经 `memory.recall(lid,type=qa)` 拿 LID 历史提问;用户 grep `reader-profile.md` 看卡点段真实问题。
**测试**:`cargo test --workspace` 全绿(runtime 45 +1 `..._back_promotes_qa_heat_over_read`:Tier A heat 降序/卡点压已读冒顶/未读居中/读过没问沉底/forward 不受 heat 影响;memory 14 +1 `reader_profile_md_renders_qa_questions`:lid×count + 嵌套问题文本;3 现有 reorder 测试随签名加 `&BTreeMap::new()`,断言不变=向后兼容)。
**B2 边界**:back 升权排序 + 渲染**确定性可单测**(判据达成);**agent「该不该 recall qa」「贴合卡点怎么讲」的 judgment 靠真 LLM 手动验**(守 B2)。
**P4-5 收口**:qa-1 生产 + qa-2 消费 两子刀全完成。qa = LID 价值/提问热度信号落地,回收 P4-2 `puzzle_lids` 恒空缺口;三消费方(确定性升权/LID-local recall/透明渲染)全锚 anchor.lid。
**未闭(ADR-0041 何时回头)**:count 噪声(3vs2)实测;re-ask 的 usage.count 要不要加权进 heat;concretize/cross 升权(back 不够时);跨读者"书内在价值"(多读者场景);reader-profile.md 体积裁剪(承 ADR-0040,不复活计数器)。

## 2026-06-29 · PB5-1 bookId 通用化(去硬编码 + 文件名 slug 派生)`[ADR-0042]`

**触达**:
- `packages/core/src/book-id.ts:deriveBookId` — 新增纯函数(零文件 IO/LLM):basename 去最后扩展名 → `slugify`(小写、空格/下划线/点→连字符、剥非 `[a-z0-9-]`、折叠 trim);`--book-id` override slug 化后采用;**非 ASCII 主导(nonAscii > asciiAlnum)或 ASCII 字母数字=0 或 slug 为空 → 抛错要 `--book-id`**(命门:不静默建 CJK/空目录坏工具链,承 [[windows-cjk-path-tooling]] + PB0 fail-fast)。
- `skills/build/pass1-batch.ts:48` — 去硬编码 `bookId="game-programming-patterns"` → `deriveBookId(book)`;`game-programming-patterns.md` 派生回同值,产物目录 `.understand-book/<bookId>/` 不变。

**入口**:`tsx skills/build/pass1-batch.ts <book> ...`(bookId 现从书路径派生);`deriveBookId` 三处共用(PB5-2 `build-status` / PB5-3 `emit-input` 后续接入)。
**测试**:`packages/core/test/book-id.test.ts`(6 例)— slug/path 剥离·ASCII-safe 规范化·override·非 ASCII 主导报错·空报错·ASCII 主导含少量非 ASCII 取 ASCII。`npx vitest run` 全量 82 测试绿(既有 81 不破)。
**偏离记录**:ADR-0042/切片方案草图写 `skills/build/`,实际落 `packages/core/src/`——纯确定性函数属 core 且测试基建(`pnpm -r test`/vitest)只覆盖 `packages/*`,放 `skills/build/` 无 runner 覆盖违 A2 测试契约。
**未闭(PB5-2/3)**:`--book-id` override 的 CLI 入口尚未接进 `pass1-batch`(本刀只 dehardcode,override 形参就位待 PB5-3 CLI 改造消费);`build-status`/`emit-input` 待建。

## 2026-06-29 · PB5-2 跨会话续建视图 build-status(存在性+content-hash 校验,无状态位)`[ADR-0042]`

**触达**:
- `packages/core/src/build-resume.ts:pass1ContentHash` — `sha256(buildPass1Input(window).text)` hex;命门=算自与抽取**同一份**正文,新鲜度锚。
- `packages/core/src/build-resume.ts:computeBuildStatus` — 纯函数(零文件 IO):逐窗重算 expected hash,对比磁盘侧 `existing: Map<id,{content_hash}>` → `{done, pending}`;在且一致=done,缺失/失配=pending(**位置 id 键、二值、无状态位/watermark/lock**)。
- `skills/build/build-status.ts` — 薄 CLI:载书(epub/md)→ `deriveBookId(book, --book-id?)` → 逐窗读 `.understand-book/<bookId>/.build/pass1/<id>.json` 取 content_hash → `computeBuildStatus` → 打印 done/pending ids。新会话冷启动第一步。

**入口**:`tsx skills/build/build-status.ts <book> [--book-id <id>]`(SKILL.md 续建 loop 第 1 步,PB5-3 写进契约)。
**测试**:`packages/core/test/build-resume.test.ts`(6 例)— 全缺→全 pending / 全抽一致→全 done / 删窗 json→该窗 pending / content_hash 失配→pending 不静默复用 / hash 确定性且对正文敏感。`npx vitest run` 全量 88 测试绿(既有 82 不破)。CLI 端到端烟测:2 窗书全 pending → 写窗 0 正确 hash → done=1/pending=1。
**B2 边界**:done/pending 判定**确定性可单测**(判据达成),零 LLM。
**未闭(PB5-3)**:`emit-input`(现算单窗输入喂 subagent,不落盘)+ `pass1-batch` 改造消费 `.build/pass1/*.json` + 逐窗原子写 + pending 拒绝收口(`--allow-partial`)+ SKILL.md 冷启动续建 loop 落地。

## 2026-06-29 · PB5-3 续建 loop 落地(emit-input + pass1-write 原子写 + pass1-batch 消费 + SKILL.md 契约)`[ADR-0042]`

**触达**:
- `packages/core/src/build-resume.ts:buildPass1Artifact` — 纯函数(PB5-3a):content_hash 从窗口正文 TS 重算 + 挂 subagent {nodes,edges} → `Pass1Artifact`。命门:hash 永远 TS 算,agent 不手算。
- `skills/build/load-book.ts:loadBookWindows/windowById` — 共享载书→重算 LID 树/窗口(确定性派生),续建各 CLI 复用。
- `skills/build/emit-input.ts` — (PB5-3b)现算窗口 id 的 `[LID]` 前缀正文打印 stdout 喂 subagent,不落盘;诊断走 stderr。
- `skills/build/pass1-write.ts` — (PB5-3b)`buildPass1Artifact` 重算 hash + 读 subagent {nodes,edges} + **临时文件+rename 原子写** `.build/pass1/<id>.json`。逐窗原子写=续建命根。
- `skills/build/pass1-batch.ts` — (PB5-3c)去 outputs.json/idxList 位置参数 → 消费 `.build/pass1/*.json` + `computeBuildStatus` 续建判定 + **pending 默认拒绝收口**(`--allow-partial` 兜底)+ flag 化 sidecar 路径;锚定率分母=done 窗口叶子。
- `skills/build/SKILL.md` — (PB5-3d)「跨会话续建」段命令名对齐实现(build-status/emit-input/pass1-write/pass1-batch)+ 命门 + 铁律;顶部状态注更新(PB5 已实现,余=真书真 LLM 端到端)。

**入口**:冷启动续建 loop(SKILL.md):`build-status` → 逐窗 `emit-input`+subagent 抽取+`pass1-write` → 全 done `pass1-batch` 收口。新 Claude 会话零上下文纯靠 `.build/` 接手。
**测试**:`packages/core/test/build-resume.test.ts`(7 例,+buildPass1Artifact);`npx vitest run` 全量 89 测试绿。CLI 端到端烟测:emit→write→status 翻 done;pass1-batch 三场景(pending 拒绝收口 exit1 / `--allow-partial` 部分收口 / 全 done 收口产 base.json);hash 跨管线(write↔status↔batch)确定性一致;非法 id 报错。
**B2 边界**:续建判定/原子写/拒绝收口**确定性可单测+烟测**(判据达成),零 LLM;subagent 抽取在环不变。
**未闭(ADR-0042 何时回头)**:内容寻址续建(书改了复用未变窗口)/ 跨版本增量([ADR-0019])/ Pass2·discourse 纳入同款续建 / 真书数十窗实测(build-status 重算开销、单会话能抽几窗、id 漂移 orphan)。

## 2026-06-29 · PB4 read-tools CLI context granularity bug 修复 `[ADR-0013/0014]`

> 真书读时烟测暴露:`context` CLI usage 宣传 `[near|mid|far] [k]`,但实现把 args[4] 当 k 解析、永远调 `context_near`,granularity 形同虚设。库侧 `Book::context` 早已实现且单测覆盖,纯属 CLI 接线漏接。

**触达**:
- `crates/read-tools/src/main.rs` `context` 分支 — args[4]=granularity(`Option<&str>`)、args[5]=k;`book.context_near(lid,k)` → `book.context(lid, granularity, k)`。granularity 缺省时库侧默认 near(向后兼容 `context <lid>`)。

**入口**:`read-tools <book_dir> context <lid> [near|mid|far] [k]`(headless 确定性叶子工具,读时驱动)。
**测试**:`cargo test -p read-tools` 35 绿(库侧 `context_far_accumulates...`/`context_rejects_unknown_granularity` 已覆盖语义)。CLI 解析内联于 `main()` 不单测;靠确定性烟测验(B2):`context 1.18.15.7 far all` → 9 条 `scope:long_range`/`layer:far`;判别性确认 `near` 同 LID → 0 条 long_range,证 granularity 真生效。
## 2026-06-30 · PB6 profile-sidecar 独立抽取趟方案落档

**触达**:
- `docs/切片方案-profile深路径.md:PB6` - 新增独立 profile-sidecar 抽取趟:复用 Pass1 window/input/hash,但单独 status/write/batch;一趟 subagent 同时产 discourse_items 与 formula_semantics 候选。

**入口**:后续实现从 `agents/profile-sidecar-extractor.md` 与 `skills/build/profile-sidecar-{input,status,write,batch}.ts` 开始;不要改 `pass1-batch` 职责边界。
**测试**:`rg "PB6|profile-sidecar|formula_lids|profile-sidecar-batch" docs/切片方案-profile深路径.md docs/代码链路.md`。
## 2026-06-30 · PB6 profile-sidecar 独立抽取趟骨架落地

**触达**:
- `packages/core/src/profile-sidecar-build.ts` - 新增独立 profile-sidecar artifact helper:复用 Pass1 window/input/hash,派生 `formula_lids`,计算 sidecar done/pending。
- `packages/core/test/profile-sidecar-build.test.ts` - 覆盖 `formula_lids` 确定性派生、artifact hash 口径、done/pending/hash 失配。
- `skills/build/profile-sidecar-input.ts` - 输出 `visible_lids`/`formula_lids` + `[LID]` 正文,喂合并 sidecar subagent。
- `skills/build/profile-sidecar-status.ts` - 扫 `.build/profile-sidecar/<id>.json` 做存在性 + content_hash 续建视图。
- `skills/build/profile-sidecar-write.ts` - 读取规范化 subagent JSON,容忍 UTF-8 BOM,原子写 `.build/profile-sidecar/<id>.json`。
- `skills/build/profile-sidecar-batch.ts` - 独立收口 sidecar 趟,默认拒绝 pending,只写 `discourse_index.json` / `formula_semantics.json`。
- `agents/profile-sidecar-extractor.md` - 新增合并抽取 prompt:Step A/B discourse,Step C formula,且公式 LID 只信确定性注入的 `formula_lids`。
- `skills/build/SKILL.md` - 新增 PB6 独立 sidecar 续建 loop,明确不改 Pass1 收口边界。

**入口**:`profile-sidecar-status` -> `profile-sidecar-input` + `profile-sidecar-extractor` + `profile-sidecar-write` -> `profile-sidecar-batch`。
**测试**:`pnpm -C packages/core test` 92 绿;`pnpm -C packages/core typecheck`;CLI smoke:input/status/write/batch on `packages/core/test/fixtures/sample.md`。
## 2026-06-30 · Pass2 消费 PB6 sidecar 前置关系补档

**触达**:
- `docs/PB3-pass2-prompt-grill.md` - 明确 Pass2 必须在 PB6 `profile-sidecar-batch` 收口后运行,从正式 `discourse_index.json` / `formula_semantics.json` 投影 `source_discourse` 与 `source_formula_semantics`。
- `docs/切片方案-profile深路径.md:PB6/P1` - 补充 PB6 是 Pass2 的 sidecar 前置输入,Pass2 不再从 Pass1 或临时 candidate JSON 读取 discourse/formula。

**入口**:后续实现 Pass2 编排时,先检查 PB6 sidecar 文件是否存在/新鲜,再装配 `Pass2WorkPacket`。
**测试**:`rg "PB6 profile-sidecar-batch|source_discourse|source_formula_semantics|Pass2 must run after" docs/PB3-pass2-prompt-grill.md docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-06-30 · PB6 true-book profile-sidecar 逐窗口抽取收口

**触达**:
- `.understand-book/quantification-essence/.build/profile-sidecar/*.json` - 真书 22 个窗口逐窗原子落盘,`profile-sidecar-status` 判定 done=22/pending=0。
- `.understand-book/quantification-essence/discourse_index.json` - `profile-sidecar-batch` 写出 1139 条 discourse items,确定性 gate dropped=25。
- `.understand-book/quantification-essence/formula_semantics.json` - `profile-sidecar-batch` 写出 22 条 formula semantics,pending=5。

**入口**:`profile-sidecar-status` -> 逐窗 `profile-sidecar-input` + `profile-sidecar-extractor` + `profile-sidecar-write` -> `profile-sidecar-batch --book-id quantification-essence`。
**测试**:`profile-sidecar-status` 22/22 done;`pnpm -C packages/core test` 92 绿;`pnpm -C packages/core typecheck`;`cargo test -p read-tools` 35 绿。

## 2026-06-30 · Pass2 编排补齐(正式 PB6 sidecar -> work packet -> gate writeback)

**触达**:
- `packages/core/src/pass2-orchestrate.ts` - 新增 Pass2 编排纯函数:正式 sidecar 参与候选生成,构建 source-window `Pass2WorkPacket`,packet hash,artifact/status。
- `packages/core/test/pass2-orchestrate.test.ts` - 覆盖 PB6 discourse/formula 投影进 packet、零候选窗口 skipped、hash stale 判定。
- `skills/build/pass2-common.ts` - 载入真书 build 上下文:`base.json` + `discourse_index.json` + `formula_semantics.json` + 重算窗口。
- `skills/build/pass2-status.ts` / `pass2-input.ts` / `pass2-write.ts` / `pass2-batch.ts` - 新增 Pass2 续建 loop;batch 写 `long_range_candidates.json`,替换 `base.json` long_range 边并写 `pass2_audit.json`。
- `skills/build/SKILL.md` - 追加 Pass2 长程边编排操作手册。

**入口**:`pass2-status` -> 逐 pending 窗口 `pass2-input` + `pass2-longrange-linker` + `pass2-write` -> `pass2-batch`。
**测试**:`pnpm -C packages/core test` 94 绿;`pnpm -C packages/core typecheck`;真书 smoke `pass2-status` 得 candidates=153 / pending=7 / skipped=15;`pass2-input 0` 含 candidates=37,source_nodes=39,discourse=74,formula=1;`pass2-batch` 在 pending 未清时拒绝收口。

## 2026-06-30 · Pass2 true-book candidate classification + writeback 收口

**触达**:
- `.understand-book/quantification-essence/.build/pass2/*.json` - 7 个有候选窗口完成 Pass2 分类落盘,`pass2-status` 判定 done=7/pending=0/skipped=15。
- `.understand-book/quantification-essence/long_range_candidates.json` - `pass2-batch` 写出 153 个确定性候选。
- `.understand-book/quantification-essence/base.json` - `pass2-batch` 替换 long_range 边,写入 13 条 gated long_range edges。
- `.understand-book/quantification-essence/pass2_audit.json` - 记录 accepted=13,pending=0,rejected=140,gate_dropped=0。

**入口**:`pass2-input` -> 保守分类 `pass2-longrange-linker` 输出 -> `pass2-write` -> `pass2-batch --book-id quantification-essence`。
**测试**:`pass2-status` done=7/pending=0/skipped=15;`pass2-batch` long_range_edges=13;`cargo test -p read-tools` 35 绿;`pnpm -C packages/core test` 94 绿;`pnpm -C packages/core typecheck`;`read-tools context 1.18.15.7 far all` 可见 `scope=long_range` 边。

## 2026-06-30 · S10j asset 原文渲染 + formula 语义剖面入口 `[Vue/TS + Rust]`

**触达**:
- `crates/server/src/lib.rs:route_book` — 新增 `GET /book/formula_semantics?lid=` 只读投影,返回 `Book::formula_semantics(lid)`,缺失返回 `FORMULA_SEMANTICS_NOT_FOUND`。
- `packages/web/src/api.ts` — 新增 `FormulaSemantics` 客户端类型与 `api.formulaSemantics(lid)`。
- `packages/web/src/App.vue` — `loadWindow` 合并 `ManifestNode.kind`,按 `code/table/image/formula` 分派渲染;公式叶子拉取并展示参数、组合含义、上下文关系;asset 保留 `data-lid` 可选中/高亮/回跳。
- `packages/web/src/style.css` — 新增 asset/code/table/image/formula 阅读流样式,长原文横向/换行约束不撑破布局。

**入口**:前端启动后 `book.manifest` 提供 kind,阅读流逐 `visible_lids` 调 `book.text`;公式 LID 额外调 `/book/formula_semantics?lid=` 展示剖面。
**测试**:`cargo test -p server` 28+4 绿;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-06-30 · S11 Mintlify docs 工作台化前端优化方案落档

**触达**:
- `docs/切片方案-切片1前端阅读器.md:§7` — 新增 S11 优化方向:Mintlify docs 工作台化、3 栏信息架构、视觉基线、LID 隐形/debug 折叠、左右 rail 数据源和 S11a-S11e 小切片顺序。

**入口**:后续从 S11a 开始,先做 layout shell + visual baseline,再做左 rail、右 context tabs、selection actions、responsive polish。
**测试**:`rg "S11|Mintlify docs 工作台化|layout shell \+ visual baseline" docs/切片方案-切片1前端阅读器.md docs/代码链路.md`。

## 2026-06-30 · S11b left rail navigation + debug foldout `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:buildOutline` — 从 `book.manifest()` 派生 chapter/section outline,后台用 `book.text(lid)` 建标题缓存。
- `packages/web/src/App.vue:doGoto` — 左 rail outline 点击复用既有 `reader.goto(lid)` 路径刷新 viewport。
- `packages/web/src/components/LeftRail.vue` — 渲染可搜索 outline、当前位置 active 高亮,普通模式不显示 raw LID/goto。
- `packages/web/src/style.css` — 增加 outline search/list/item 样式,保留 debug foldout 的 raw coordinate 工具。

**入口**:前端初始化 `api.manifest()` 后生成 outline;用户点击左侧 outline item -> `reader.goto(lid)`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-06-30 · S11c right context tabs `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:selectedFormula` — 从当前选中 reader segment 投影公式剖面,传给右 rail Formula tab。
- `packages/web/src/App.vue:latestTrace` — 从最近一次 agent outcome 投影 trace,传给右 rail Trace tab。
- `packages/web/src/App.vue:contextRecords` — 从当前 selected LID 与 viewport visible LIDs 派生附近 notes/highlights。
- `packages/web/src/components/RightRail.vue` — 启用 Agent/Trace/Formula/Notes tabs;Agent tab 保留既有 chat/proposal flow,其余 tabs 渲染对应上下文。

**入口**:右侧 rail tab 切换;Agent tab 继续走 `/agent/chat`,Trace/Formula/Notes 消费 App 已有状态。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;HTTP smoke `/`, `/api/book/manifest`, `/api/reader/state`。

## 2026-06-30 · S11d selection/actions `[Vue/TS]`

**触达**:
- `packages/web/src/components/TopBar.vue` — 移除旧的全局 Highlight/Note 操作,topbar 只保留翻页、新对话、debug。
- `packages/web/src/components/ReaderPane.vue` — 为 selected paragraph/asset 增加 inline block actions: Highlight block / Note。
- `packages/web/src/App.vue:highlightBlock` — block action 复用既有 `reader.highlight(lid)` 整段高亮路径。
- `packages/web/src/App.vue:noteSelection` — 段内选区 floating toolbar 增加 Note,用选中文本预填 anchored note。
- `packages/web/src/style.css` / `packages/web/src/App.vue` scoped style — 增加 block action pills 与双按钮 selection toolbar 样式。

**入口**:点击 reader 段落/asset 选中后出现 inline actions;鼠标选中单个 LID 内文本后出现 floating toolbar。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;HTTP smoke `/`, `/api/reader/state`。

## 2026-06-30 · S11e responsive polish `[Vue/TS]`

**触达**:
- `packages/web/src/style.css` — 增加 1023px 断点:取消三栏挤压,改为 left+reader 两栏、right rail 下置。
- `packages/web/src/style.css` — 增加 767px 断点:单列堆叠、topbar 换行、outline 限高、触控目标放大。
- `packages/web/src/style.css` — 移动端 reader prose/asset/code/notes 调整宽度、间距与换行,避免横向溢出。

**入口**:浏览器视口宽度 <=1023px / <=767px 时触发响应式布局。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;HTTP smoke `/`, `/api/reader/state`。
## 2026-06-30 · S11f Markdown 标题井号显示修复 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:renderSeg` — chapter/section 显示层剥离 Markdown 标题前缀,保留原文与 LID/range 锚点不变。
- `packages/web/src/App.vue:firstTitleLine` — outline/title cache 使用清洗后的标题文本,避免 `#`/`##` 泄漏到大纲。
- `packages/web/src/components/ReaderPane.vue` / `packages/web/src/style.css` — 为 chapter/section 段落加标题样式类。

**入口**:阅读流渲染与左侧 outline 标题缓存。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-06-30 · S11g 当前会话切换书入口 `[Rust + Vue/TS]`

**触达**:
- `crates/server/src/lib.rs:route_open_book` — 新增 `POST /book/open {dir}` 重新加载 Book、重建 Reader、重置 agent messages。
- `packages/web/src/api.ts:api.openBook` — 前端命令面客户端接入切书端点。
- `packages/web/src/components/TopBar.vue` / `packages/web/src/App.vue:openBook` — 顶栏 Open book 按钮输入本地 book dir,成功后清空当前阅读/对话状态并重载 manifest/state。

**入口**:TopBar `Open book` -> 输入 `.understand-book/<book_id>` 或 book 目录路径。
**测试**:`cargo test -p server`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11h 可折叠大纲与可调三栏宽度 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue` — 新增左侧 outline 开关、左右 rail 宽度状态与桌面端拖拽调整逻辑。
- `packages/web/src/components/TopBar.vue` — 新增 `Outline` 顶栏开关,用于隐藏/恢复左侧大纲。
- `packages/web/src/style.css` — workspace grid 改为 CSS 变量驱动栏宽,新增左右拖拽手柄,保留 tablet/mobile 响应式覆盖。

**入口**:TopBar `Outline` 按钮切换左 rail;桌面端拖拽左右分隔条调整左/中/右工作区宽度。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11i Agent 回答选区保存为笔记 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue` — 删除整段 agent answer 存笔记路径,新增 `questionAnchorLid` 冻结提问时阅读位置,`saveAgentSelection` 把用户选中的回答片段保存到该 LID。
- `packages/web/src/components/RightRail.vue` — 给 agent answer 增加选区捕获与浮动 `Note` 按钮,由用户自行选择要保存的句子或片段。

**入口**:右侧 Agent 回答文本中选中任意片段后点击浮动 `Note`,写入该问题发出时的 reader anchor 的 long_term note。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11j 正文选区 Ask AI 引用提问 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue` — 正文选区浮动工具条新增 `Ask AI`,保存 `{lid, quote}` 为 ask draft;发送 agent 消息时拼接引用原文与用户问题,用选区 LID 冻结 `questionAnchorLid`,并把 `questionQuote` 固化到该 chat turn。
- `packages/web/src/components/RightRail.vue` — Agent tab 收到 ask draft 时自动激活,输入区显示 `Quoted source` 引用块与独立问题输入框,支持清除引用;发送后引用块移动到用户问题上方作为对话记录。

**入口**:正文中选中文本 -> 浮动工具条 `Ask AI` -> 右侧 Agent composer 显示引用原文与问题输入 -> Send。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11k 正文行内 LaTeX 渲染 `[Vue/TS]`

**触达**:
- `packages/web/src/md.ts` — 新增 `renderInlineMarkdown`,复用现有 markdown-it + KaTeX 配置但不包外层 `<p>`。
- `packages/web/src/App.vue:renderSeg` — reader 段落/标题显示层改走 inline Markdown/LaTeX 渲染,段内高亮仍按原 UTF-16 range 分片后包 `<mark>`。

**入口**:ReaderPane 正文 `v-html=renderSeg(seg)`;普通段落中的 `$...$` 公式按 KaTeX 行内公式显示。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11l Agent 工具价值 prompt 补强 `[Rust]`

**触达**:
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 新增工具价值判断规则:引用原文优先直接解释,限制 `book.concept` fan-out,按任务类型选择 `book.text/context/query/synthesize/route`。

**入口**:`/agent/chat` 外层 E agent loop;前端 Ask AI 引用提问拼入 `引用原文 [LID: ...]` 后由 prompt 约束工具选择。
**测试**:`cargo test -p runtime`。
## 2026-07-01 · S11m 跨 LID 自由选区 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:selectionRanges` — 选区不再限制单个 `data-lid`,扫描阅读区内所有相交 LID 并计算各自 UTF-16 range。
- `packages/web/src/App.vue:confirmHighlight/noteSelection/askSelection` — 高亮按 LID 拆成多次 `reader.highlight(lid, range)`,Note/Ask AI 用完整选区文本锚到起点 LID。

**入口**:ReaderPane 正文任意跨段选中文本 -> floating toolbar `Highlight` / `Note` / `Ask AI`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11n Note Markdown/折叠/来源路径 `[Vue/TS]`

**触达**:
- `packages/web/src/components/ReaderPane.vue` — note 卡片改为可折叠 `details`,长 note 默认折叠,summary 显示来源 quote 或锚点 LID。
- `packages/web/src/components/RightRail.vue` — Notes tab 同步显示 note 来源路径并折叠长 note。
- `packages/web/src/App.vue:saveAgentSelection` — 从 Ask AI 回答选区保存 note 时,把原引用文本写成 Markdown blockquote 作为来源。
- `packages/web/src/style.css` — 补齐 note/Markdown 的 blockquote、table 与折叠 summary 样式。

**入口**:ReaderPane note 卡片、RightRail Notes tab、Agent answer selection `Note`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11o Agent 答案选区公式保真 `[Vue/TS]`

**触达**:
- `packages/web/src/components/RightRail.vue:markdownTextFromRange` — 保存 Agent 回答选区为 note 前,把选区内 KaTeX DOM 的 `annotation[encoding="application/x-tex"]` 还原为 `$...$`/`$$...$$`,避免生成 `Ft\mathcal{F}_tFt` 这类混合文本。

**入口**:RightRail Agent answer selection `Note`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11p Note 来源定位按钮与折叠预览 `[Vue/TS]`

**触达**:
- `packages/web/src/components/ReaderPane.vue` — note summary 来源改为按钮,点击通过 `goto` 事件定位来源 LID;长 note 折叠时显示三行 preview。
- `packages/web/src/components/RightRail.vue` — Notes tab 来源按钮同样触发 `goto`,长 note 折叠时保留 preview;继续保留 KaTeX 选区还原。
- `packages/web/src/App.vue` — 接入 ReaderPane/RightRail 的 `goto` 事件到既有 `doGoto`。
- `packages/web/src/style.css` — 增加 note source button 与 folded preview 样式。

**入口**:ReaderPane note source button / RightRail Notes tab source button。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11q Note 来源跳转临时高亮 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:focusSource/doGoto/renderSeg` — 来源跳转携带 quote，跳到 LID 后用 `sourceFocus` 临时高亮 quote 或整段。
- `packages/web/src/components/ReaderPane.vue` — reader 内 note source button 发出 `{lid, quote}`。
- `packages/web/src/components/RightRail.vue` — Notes tab source button 发出 `{lid, quote}`。
- `packages/web/src/style.css:.source-focus-mark` — 来源临时高亮使用 mint 轻量样式。

**入口**:ReaderPane note source button / RightRail Notes tab source button。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。



## 2026-07-01 S12 continuous reader model decision docs

**Touched**:

- `docs/adr/0043-reader连续滚动视口-后端区间窗口-前端虚拟流-note-overlay.md` - records the accepted continuous scrolling reader architecture.
- `docs/切片方案-S12连续滚动阅读模型.md` - decomposes implementation into S12a-S12e verifiable slices.

**Entry point**: future implementation starts at S12a reader viewport interval semantics.
**Test**: `rg "ADR-0043|S12a reader viewport interval semantics|连续滚动阅读模型" docs/adr docs/切片方案-S12连续滚动阅读模型.md docs/code-trail-S12-continuous-reader.md`.

## 2026-07-01 S12a reader viewport interval semantics `[Rust]`

**Touched**:

- `CONTEXT.md:interval viewport` - adds the S12 glossary terms for interval viewport, virtual reader buffer, and note overlay.
- `crates/reader/src/lib.rs:Viewport` - adds `top_lid`, `bottom_lid`, and `width` while retaining derived `anchor_lid`.
- `crates/reader/src/lib.rs:Reader` - replaces center-window state with `top_idx + width`; `scroll(delta)` moves the top interval, `goto_lid` lands the target leaf at top, and read ledger marks every visible LID.

**Entry point**: `Reader::new` now receives viewport width; HTTP `/reader/state`, `/reader/goto`, and `/reader/scroll` return the expanded viewport through existing server passthrough.
**Test**: `cargo test -p reader`; `cargo test -p server`.

## 2026-07-01 S12b viewport DTO and progress wiring `[Vue/TS]`

**Touched**:

- `packages/web/src/api.ts:Viewport` - mirrors the expanded reader viewport DTO with `top_lid`, `bottom_lid`, and `width`.
- `packages/web/src/App.vue:progressPct` - computes reading progress from `top_lid` instead of derived `anchor_lid`.
- `packages/web/src/App.vue:loadWindow` - selects and loads chapter context from `top_lid`; no-selection agent questions use `top_lid` as their default anchor.

**Entry point**: frontend initialization and reader command responses flow through `loadWindow(st.viewport)` / `loadWindow(effect.viewport)`.
**Test**: `pnpm -C packages/web typecheck`; `pnpm -C packages/web build`.

## 2026-07-01 S12c virtual reader buffer `[Vue/TS]`

**Touched**:

- `packages/web/src/components/ReaderPane.vue:onScroll` - emits `scroll-edge` when the native reader pane approaches the top or bottom of its scroll range.
- `packages/web/src/components/ReaderPane.vue:scrollRestoreId` - recenters the native scroll position after App swaps in the next interval buffer.
- `packages/web/src/App.vue:onScrollEdge` - locks concurrent edge loads, calls `reader.scroll(+/- width/2)`, reloads the interval viewport, and triggers scroll restoration.

**Entry point**: native scroll in `.reader-pane` drives `scroll-edge` -> `App.onScrollEdge` -> `/reader/scroll`.
**Test**: `pnpm -C packages/web typecheck`; `pnpm -C packages/web build`.
## 2026-07-01 S12d note overlay split `[Vue/TS]`

**Touched**:

- `packages/web/src/App.vue:visibleNotes` - derives notes for the current `visible_lids` interval only.
- `packages/web/src/components/ReaderPane.vue:note-overlay` - moves note cards out of the per-segment loop into an independent visible-notes overlay/list.
- `packages/web/src/style.css:note-overlay` - styles the overlay as a bounded independent layer so notes do not alter segment heights.

**Entry point**: ReaderPane receives `visibleNotes` from App and renders notes independently from the prose `seg` loop; highlights remain inline/segment-local.
**Test**: `pnpm -C packages/web typecheck`; `pnpm -C packages/web build`.
## 2026-07-01 S12e remove page buttons and keyboard fallback `[Vue/TS]`

**Touched**:

- `packages/web/src/components/TopBar.vue` - removes the old explicit up/down page buttons and `scroll` event from the top bar.
- `packages/web/src/App.vue` - removes the old topbar `doScroll` wiring; continuous scroll now enters through ReaderPane native scroll or keyboard.
- `packages/web/src/components/ReaderPane.vue:onKeydown` - adds ArrowUp/ArrowDown/PageUp/PageDown scrolling when the reader pane has focus, without stealing editable-field keys.
- `packages/web/src/style.css:reader-pane` - adds focus styling and removes unused `.icon-pill` button sizing.

**Entry point**: focus the reader pane and use Arrow/Page keys, or scroll natively; TopBar no longer exposes page controls.
**Test**: `pnpm -C packages/web typecheck`; `pnpm -C packages/web build`.
## 2026-07-02 S13a note 卡回正文 seg 循环 [Vue/TS]

**触达**:
- packages/web/src/components/ReaderPane.vue — note 卡从 <aside class="note-overlay"> 移回 seg 循环,hl-card 之后按
otesOf(seg.lid) 渲染;新增
otesByLid computed +
otesOf 函数;删除 <aside> overlay 块。
- packages/web/src/style.css — 删除 .note-overlay 和 .note-overlay .note-card 死样式。
- docs/adr/0043-reader连续滚动视口-后端区间窗口-前端虚拟流-note-overlay.md — 决策7 修订:note 卡从独立 overlay 回退为段内 seg 循环渲染。

**入口**:ReaderPane seg 循环每段 hl-card 之后渲染该 LID 的 notes;note 内容仍来自 App isibleNotes prop。
**测试**:pnpm -C packages/web typecheck;pnpm -C packages/web build。
## 2026-07-02 S13b note source 按钮只高亮不导航 [Vue/TS]

**触达**:
- packages/web/src/components/ReaderPane.vue — note source 按钮事件从 ocus-source 改为 ocus-source-local,不再触发 goto 导航。
- packages/web/src/App.vue — 新增 ocusLocalSource,只设 sourceFocus 高亮对应段,不调 pi.goto、不移动视口;RightRail 的 ocus-source 仍走 ocusSource/doGoto(那边 note 可能不在视口,需导航)。

**入口**:ReaderPane 内 note source button -> ocus-source-local -> App ocusLocalSource -> 设 sourceFocus。
**测试**:pnpm -C packages/web typecheck;pnpm -C packages/web build。
## 2026-07-02 S13b-fix note source 高亮全段不生效 [Vue/TS]

**触达**:
- packages/web/src/App.vue:focusLocalSource — quote 为 null(noting quote)时不再设 null 而是创 { lid, quote: null },使 sourceFocusRange 返回 [0, text.length] 高亮整段;有 quote 时精确匹配。

**入口**:ReaderPane note source button。
**测试**:pnpm -C packages/web typecheck;pnpm -C packages/web build。
## 2026-07-02 S13c hl-card 恢复可见 + sourceFocus 可清除 [Vue/TS]

**触达**:
- packages/web/src/style.css — .hl-actions 从合并规则(与 .note-card[open] .note-preview 共用 display:none)拆出,恢复 display:flex;.hl-ex 去掉 white-space:nowrap 改为 2 行 clamp,高亮文本正常换行显示。
- packages/web/src/App.vue — 新增 onSelectSeg,点击段时清 sourceFocus;onProseMouseUp collapsed 分支清 sourceFocus;@select 绑定从内联改为 onSelectSeg。

**入口**:ReaderPane 段点击 / 空白处 mouseup / 滚动 → sourceFocus 清除。
**测试**:pnpm -C packages/web typecheck;pnpm -C packages/web build。
## 2026-07-02 S13d 阅读位置/上次打开的书持久化(session.json 往返) [Rust]

**触达**:
- crates/server/src/main.rs:main — 启动时调 load_session(&session_path);argv 与 session book_dir 不同且可加载则用 session 目录;restore_top_lid 恢复 top;构造 AppState 后调 save_session(&guard,Some(dir)) 写初始 session.json(否则后续 save_session(None) 读不到旧 book_dir 永不写)。
- crates/server/src/main.rs:use — 加载 load_session,save_session;session_path = memory_dir/session.json。
- crates/server/src/lib.rs:AppState — 加 session_path:Option<PathBuf> 字段。
- crates/server/src/lib.rs:save_session(state,dir) — 用 serde+io 写 session.json{book_dir,top_lid};dir=Some 覆盖,None 沿用旧 book_dir(读现有文件)。
- crates/server/src/lib.rs:load_session — 静默读 session.json,缺/坏返 None。
- crates/server/src/lib.rs:route_open_book / route_mut(goto,scroll) — 成功后 save_session(state,Some(dir)或None)。
- crates/server/src/lib.rs:SessionState{pub book_dir, pub top_lid}。
- crates/reader/src/lib.rs:restore_top_lid(&mut self,_book,lid) — 按 leaf_lids 定位设 top_idx,不存在静默忽略(无写入)。

**入口**:server <book_dir> 启动 -> load_session 选目录 -> restore_top_lid -> save_session(Some) 写初始;后续 goto/scroll 持续更新;top_lid 写 ~/.understand-book/memory/session.json。
**测试**:cargo test -p server --lib(29 绿)。
**附带修复**:lib.rs 第20/32行 `n` PowerShell 转义损坏、.and_then 类型不匹配、main.rs Arc::new(Mutex::new(AppState { 构造头误删、load_session/save_session 可见性补全。

## 2026-07-02 S13e note/Ask quote 公式保真(选区 → markdown 源还原) [Vue/TS]
**触达**:
- packages/web/src/selection.ts — 新增 rangeToMarkdown(range):cloneContents 拿选区 DOM 片段,递归遍历遇 .katex 原子取其 <annotation encoding="application/x-tex"> 还原原始 LaTeX 包 `$...$`,其余取文本;块级元素拼尾加空格隔跨段。
- packages/web/src/App.vue — import rangeToMarkdown;onProseMouseUp 的 `const quote = range.toString()` 改为 `rangeToMarkdown(range)`,让 note 引用块(> ${quote})与 Ask AI 引用文本(「${quote}」)从源头带 `$...$`,后续 renderMarkdown 自然渲染出公式而非 \rho=0.05ρ=0.05 噪声。
**入口**:ReaderPane 正文选区 mouseup → onProseMouseUp → rangeToMarkdown → hlPopover → noteSelection 写 `> ${quote}` / askSelection 写 askDraft.quote。
**测试**:pnpm -C packages/web typecheck;pnpm -C packages/web build(均绿)。
**未覆盖**:正文段内自由高亮 selectionRanges 的 DOM 文本偏移 vs 后端 book.text UTF-16 偏移在含公式段仍会错位(同根因另一表现,留后续刀);leadingQuote 截断到 96 字符长公式超高会切断 $ 配对(安全降级整段高亮,留后续)。
- packages/web/src/selection.ts:fragmentToMarkdown — 修 DocumentFragment(nodeType 11)走子节点遍历分支(上版只接受 ELEMENT_NODE 导致 cloneContents 根被丢、rangeToMarkdown 永远返回空、popover/三按钮全消失);latex 取值加 .trim() 保证 `$` 紧贴内容(前导/尾随空格会让 katex 插件跳过行内公式);空公式退回 el.textContent 防 `$$` 碰成 display math 分隔符。
- packages/web/src/md.ts — 新增 normalizeDisplayMath(src):renderMarkdown 前把内联 `$$...$$`(和文字挤同一行)前后补换行规整成独占行;@traptitech/markdown-it-katex 的 `$$` display math 要求独占行,内联写法被插件跳过不渲染。不动已独占行的 `$$`、不动 `$...$` 行内公式。
**测试(追加)**:node 实测 normalizeDisplayMath + katex:内联 `$$` 规整后渲染 katex-display=true;已独占行不变;`$...$` 不受影响;blockquote 内联 `$$` 也修复。

## 2026-07-02 S13h agent answer note 保存后回到锚点 [Vue/TS]

**触达**:
- packages/web/src/App.vue:saveAgentSelection — 保存 agent 回答选区 note 后,若 note anchor 不在当前 `viewport.visible_lids`,调用 `reader.goto(anchor)` 并 `loadWindow`,让正文窗口回到该 LID 后渲染新 note;anchor 已可见时仍只 `refreshAnnotations`。

**入口**:RightRail agent answer 选区 mouseup → Note → App.saveAgentSelection → `/memory/save`;无 quoted source 时 anchor 来自发送问题时冻结的 selected LID。
**测试**:npm run typecheck --prefix packages/web;npm run build --prefix packages/web。

## 2026-07-02 S13i S12 连续滚动补窗锚点恢复 [Vue/TS]

**触达**:
- packages/web/src/components/ReaderPane.vue:captureScrollAnchor/restoreScrollAnchor — 补窗前按新旧窗口重叠 LID 捕获当前 DOM 相对位置,补窗渲染后把同一 LID 调回原相对位置,替代固定 35% scrollTop 恢复。
- packages/web/src/App.vue:onScrollEdge — 调 `reader.scroll` 后用旧 `visible_lids` 与新 `visible_lids` 交集作为候选锚点,`loadWindow` 后恢复该锚点。

**入口**:ReaderPane 原生滚动触底/触顶 → App.onScrollEdge → `/reader/scroll` → loadWindow → restoreScrollAnchor。
**测试**:npm run typecheck --prefix packages/web;npm run build --prefix packages/web。浏览器插件本轮无可用实例,未做真实滚动 smoke。

## 2026-07-02 P7 Book MCP 访客向导面(连接式访客会话) [Rust/MCP]

**触达**:
- crates/runtime/src/lib.rs:book_guide — 新增 `book.query` 姊妹命令:输入 `BookGuideRequest + BookGuideSessionContext`,用 Core `route_from` 组装入口/route/frontier,把 route evidence 交给 `ModelAdapter::complete`,输出 citations 只保留 route evidence 内真实 LID;不调用住户 `orchestrator::run()`。
- crates/server/src/mcp.rs — 新增访客 MCP dispatch 白名单:Tier 1 `book_manifest/text/context/concept/query/synthesize` 不建会话;Tier 2 `book_guide` 隐式握手建 `session_id`,支持 `不对` refine 换前沿分支,`action=close` 挥手删除,每次 dispatch 前超时 GC。
- crates/server/src/lib.rs:AppState — 增加 `visitor_sessions: VisitorSessions`,与住户 `reader/store/messages` 物理分离。
- crates/server/src/bin/book_mcp.rs — 新增 stdio JSON-RPC MCP server 入口,支持 `initialize` / `tools/list` / `tools/call`。
- crates/server/src/main.rs — HTTP server 初始化 `VisitorSessions::default()`。

**入口**:`cargo run -p server --bin book_mcp -- <book_dir>`;外部 MCP client 走 `tools/list` / `tools/call` 调 `book_guide` 或 Tier 1 只读工具。
**测试**:`cargo test -p server` 通过(37 例,含 Tier 1 不建会话、`reader.goto` 无路可达、open/refine/close、并发访客隔离、超时 GC、JSON-RPC tools/call smoke)。`cargo test -p runtime` 仍有既有 `orchestrator` 两例失败:reader scroll/goto 后 viewport anchor 保持 `1.5`,导致 Goto effect 数为 0,与本次 P7 MCP 路径无关。

## 2026-07-02 P7-fix server 默认二进制 [Rust]

**触达**:
- crates/server/Cargo.toml — `package.default-run = "server"`,新增 `book_mcp` 后恢复 `cargo run -p server` 默认启动 HTTP server。
- start.bat — 启动前按当前仓库 `target\debug\server.exe` 路径查杀旧进程,再用 `cargo run -p server --bin server -- "%BOOK%"` 显式启动 HTTP server。

**入口**:`cargo run -p server`;`start.bat [book_dir]`。
**测试**:`cargo test -p server` 通过;清理旧 `server.exe` 后 `cargo build -p server --bin server` 可覆盖编译;`cargo run -p server --bin server --` 进入 server usage(无二进制歧义/无 exe 占用)。

## 2026-07-02 S13j 正文长 note 折叠预览 [Vue/TS]

**触达**:
- packages/web/src/components/ReaderPane.vue — 长 note 的 `note-preview` 移入 `summary`,并用 `renderMarkdown(notePreview(note))` 渲染折叠态 markdown 预览。
- packages/web/src/style.css — 新增 `.note-summary-preview`,让折叠态 markdown 预览跨满 summary 网格并限制在三行高度。

**入口**:ReaderPane 正文段内长 note 折叠状态。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-07-02 P5 ReActAdapter + provider registry [Rust]

**触达**:
- crates/runtime/src/lib.rs:ProviderRegistry — 新增 `UNDERSTAND_BOOK_PROVIDER=native|react` 装配入口,默认 native,共享既有 `OPENCODE_API_KEY/OPENCODE_BASE_URL/FLUID_LLM_MODEL`。
- crates/runtime/src/lib.rs:ReActAdapter — 无原生 tool-calling provider 走 ReAct JSON 协议,解析 `final`/`tool_calls` 归一成 `AssistantTurn`;工具结果仍由 Runtime dispatch 执行。
- crates/runtime/src/orchestrator.rs:tests — 新增 native/ReAct 同工具集收敛测试和 ReAct 协议错误 → `PROVIDER_ERROR` 映射测试。
- crates/runtime/src/main.rs / crates/server/src/main.rs / crates/server/src/bin/book_mcp.rs — adapter 构造改走 `ProviderRegistry::adapter_from_env()`。

**入口**:`UNDERSTAND_BOOK_PROVIDER=react cargo run -p runtime -- <book_dir> chat <question...>`;server/book_mcp 同环境变量切换 provider。
**测试**:`cargo test -p server` 通过;`cargo test -p runtime` 48 过、2 个既有 viewport 测试失败(`guided_read_one_stop_pipeline`,`agent_viewport_change_merges_into_single_goto_effect`),P5 新增测试均通过。

## 2026-07-03 S13k 跨 LID quote source 高亮 [Vue/TS]

**触达**:
- packages/web/src/App.vue:sourceFocusRanges — 从 note anchor LID 起构造可见段落的 whitespace-normalized 文本流,把 quote source 覆盖区间映射回各 LID 的显示文本 range。
- packages/web/src/App.vue:renderSeg — 使用跨 LID source focus ranges,让 quote source 超过 anchor LID 时后续可见 LID 也被高亮。

**入口**:ReaderPane/RightRail 的 `Quote source` 按钮 → `sourceFocus` → `renderSeg`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-07-03 S13l agent 对话历史 [Rust/Vue]

**触达**:
- crates/server/src/lib.rs:AgentHistory — 新增按 book_id 分组的 resident agent 历史会话,保存 transcript turns + backend messages。
- crates/server/src/lib.rs:route_agent_history_select/delete — 新增 `/agent/history` 读取、select 恢复、delete 删除;`/agent/new` 改为创建新历史会话而非丢弃旧会话。
- crates/server/src/main.rs:main — 启动时从 `agent-history.json` 恢复当前书 active session messages。
- packages/web/src/api.ts — 新增 AgentHistoryResponse/AgentChatSession 类型与 history/select/delete API。
- packages/web/src/App.vue:applyAgentHistory — 从后端历史恢复 agent transcript,并在 send/new/select/delete 后同步。
- packages/web/src/components/RightRail.vue — Agent 面板新增历史选择器与删除按钮。

**入口**:RightRail Agent 面板 New/select/Delete -> `/agent/new` 或 `/agent/history*`;发送消息后 `/agent/chat` 持久化当前 active session。
**测试**:`cargo test -p server`;`npm run --prefix packages/web typecheck`;`npm run build`(packages/web,esbuild spawn 需授权);`cargo test -p runtime` 48 过、2 个既有 viewport effect 断言失败。

## 2026-07-03 S13m agent 历史弹窗 [Rust/Vue]

**触达**:
- crates/server/src/lib.rs:AgentChatSessionSummary — history summary 增加轻量 turns 摘要,供前端历史弹窗展示每轮问题和 question_anchor_lid。
- packages/web/src/api.ts:AgentChatTurnSummary — 对齐后端 summary turns 类型。
- packages/web/src/components/RightRail.vue — 移除原生 select,改为 History pill 按钮 + Mintlify 风格 modal;每个历史会话展示问题、Anchor LID、Goto/Open/Delete 操作。

**入口**:RightRail Agent 面板 History -> modal -> Goto/Open chat/Delete。
**测试**:`cargo test -p server`;`npm run --prefix packages/web typecheck`;`npm run build`(packages/web,esbuild spawn 需授权)。

## 2026-07-03 S13n Mintlify 主页面 + Apple 细节前端调色 [Vue/CSS]

**触达**:
- packages/web/src/style.css — 按 `DESIGN-mintlify.md` 保留三栏文档工具密度、黑白/mint accent 和细 hairline;按 `DESIGN-apple.md` 增加 off-white canvas、frosted topbar/left rail、44px 搜索触控目标、蓝色 focus ring、按钮按压反馈,并把显式 letter-spacing 收回到 0。
- packages/web/src/components/RightRail.vue — 右栏与 history modal 使用半透明 surface、blur backdrop、无重阴影、44px close 按钮、稳定的 History/Open/Goto 操作尺寸。
- packages/web/src/App.vue — selection popover 与 note modal 对齐同一遮罩/边界/按钮细节,不改 note 保存或高亮逻辑。

**入口**:主页面 app shell、reader pane、left/right rail、Agent History modal、note modal。
**测试**:`npm run --prefix packages/web typecheck`;`npm run build`(packages/web);`git diff --check` 仅 LF/CRLF warning。

## 2026-07-03 S14a 行内公式升级为 Formula LID [TS/Vue/Docs]

**触达**:
- packages/core/src/md-adapter.ts:inlineFormulaRanges — 普通 Markdown 段落内 `$...$` / 单行 `$$...$$` 拆成真实 `assetKind="formula"` leaf,前后文字保留为相邻 paragraph leaf。
- packages/core/src/epub-adapter.ts:leafBlocks — EPUB `<p>`/`li`/`blockquote` 内 MathML 拆成真实 formula leaf,前后文字保留为相邻 paragraph leaf。
- packages/core/test/{md-adapter,epub-adapter,profile-sidecar-build}.test.ts — 覆盖行内公式产出 formula 节点、分区不变式、PB6 `formula_lids` 派生。
- agents/profile-sidecar-extractor.md — FormulaSemantics 的 label/meaning/description 等自然语言解释字段强制中文;LID、数学符号和闭集 enum 保持原样。
- packages/web/src/components/{ReaderPane,RightRail}.vue — 公式剖面固定 UI 文案改为中文。
- CONTEXT.md / docs/adr/0029-* / docs/切片方案-asset一等对象.md — 回填“行内公式 LID”定型:真实 Formula LID,不是段内 occurrence/span sidecar。

**入口**:预构建 `markdownToBlocks` / `xhtmlToBlocks` -> `segment` -> PB6 `profile-sidecar-input` 的 `formula_lids`;读时前端选择 formula LID -> `/book/formula_semantics?lid=`。
**测试**:`pnpm -C packages/core typecheck`;`pnpm -C packages/core test`(沙箱 Vitest worker spawn EPERM 后授权重跑,95 例全绿);`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;`git diff --check` 仅 LF/CRLF warning。

## 2026-07-03 S14b 单美元公式块防卡死 [TS/Build]

**触达**:
- packages/core/src/md-adapter.ts:inlineFormulaRanges — 未闭合 `$` / `$$` 扫描时推进游标,避免真书状态检查在美元符号上死循环。
- packages/core/src/md-adapter.ts:markdownToBlocks — 识别成对的单独 `$` 行为 Markdown display formula leaf,保持公式原文进入真实 Formula LID。
- packages/core/test/md-adapter.test.ts — 覆盖单美元 display formula 和未匹配美元符号普通段落回归。

**入口**:真书预构建 `build-status` / `emit-input` 调用 `markdownToBlocks`。
**测试**:`pnpm -C packages/core test md-adapter`;`pnpm -C packages/core typecheck`;`build-status` 对 `quantification-essence` 正常返回 46 窗口状态。

## 2026-07-03 S14c 预构建子代理授权与 sidecar 质量红线 [Docs]

**触达**:
- `skills/build/SKILL.md` — 增加构建期直接调用 `profile-sidecar-extractor` / `pass2-longrange-linker` 的授权说明,并禁止 generic sidecar / reject-all 常规降级。
- `docs/预购建流程.md` — 将 sidecar 语义质量列为高优先级目标,说明子代理不可用时应停止报告而非产低质量文件。
- `agents/profile-sidecar-extractor.md` — 增加质量优先与禁止低信息填充的 prompt 红线。
- `SESSION_CHECKPOINT.md` — 把下一步从“可选质量提升”改为“已授权的语义重建”。

**入口**:`/understand-book:build` 预构建续跑;PB6/Pass2 pending window 处理流程。
**测试**:`rg "generic sidecar|reject-all|sidecar 的语义质量|Quality priority" skills docs agents SESSION_CHECKPOINT.md`;`git diff --check`。

## 2026-07-04 S14d 真书 PB6/Pass2 语义重建 [Build]

**触达**:
- `.understand-book/quantification-essence/.build/profile-sidecar/*.json` — 46 个窗口全部由 `profile-sidecar-extractor` 真实语义输出替换,不再使用 generic filler。
- `.understand-book/quantification-essence/discourse_index.json` — `profile-sidecar-batch` 收口写出 2582 条 discourse items。
- `.understand-book/quantification-essence/formula_semantics.json` — `profile-sidecar-batch` 收口写出 686 条 formula semantics。
- `.understand-book/quantification-essence/.build/pass2/*.json` — 21 个候选窗口全部由 `pass2-longrange-linker` 逐候选分类替换,不再使用 reject-all。
- `.understand-book/quantification-essence/base.json` / `pass2_audit.json` — `pass2-batch` 写入 509 条 gated long_range edges,审计 accepted=509/pending=119/rejected=1425/gate_dropped=0。

**入口**:`profile-sidecar-status` -> `profile-sidecar-input` + `profile-sidecar-extractor` + `profile-sidecar-write` -> `profile-sidecar-batch`;随后 `pass2-status` -> `pass2-input` + `pass2-longrange-linker` + `pass2-write` -> `pass2-batch`。
**测试**:`profile-sidecar-status` done=46 pending=0;`pass2-status` done=21 pending=0 skipped=25;`pass2-batch` gate_dropped=0。

## 2026-07-04 S14e 公式 sidecar 改为点击查看 [Vue]

**触达**:
- `packages/web/src/components/ReaderPane.vue` — 相邻 paragraph/formula LID 合并为连续正文 flow;公式以行内 chip 按钮紧贴上下文,点击发出 `open-formula`,不再在正文展开 sidecar。
- `packages/web/src/App.vue` — 增加 `formulaDialog` 状态与公式剖面 modal,复用已加载的 `Segment.formula` 展示 composition / parameters / context_links。
- `packages/web/src/style.css` — 增加 flow 内 LID 状态与行内公式 chip 样式,移除旧正文公式 profile 展开样式。

**入口**:读时 `loadWindow` 预取 `/book/formula_semantics?lid=` -> `ReaderPane` formula 按钮 -> `App.formulaDialog` 弹窗查看 sidecar。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-04 S14f 正文虚拟滚动边界防抖 [Vue]

**触达**:
- `packages/web/src/components/ReaderPane.vue` — 取消无方向 `scroll` 事件触发虚拟翻窗;仅在 `wheel` / 键盘带明确方向且到达物理边界时发出 `scroll-edge`,并在滚轮边界处 `preventDefault` 防止浏览器边界滚动与锚点恢复互相拉扯。

**入口**:ReaderPane 正文滚轮/键盘滚动 -> 物理顶/底边界 -> `scroll-edge` -> `App.onScrollEdge`。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-04 S14g 正文连续滚动缓冲 [Vue]

**触达**:
- `packages/web/src/App.vue` — 将正文 `loadWindow` 拆为 hydrate/merge,普通 goto/sync 仍 replace,滚动到边界时按全书 leaf 顺序 append/prepend 下一批 LID,不再用后端 20-LID viewport 直接替换正文 DOM。
- `packages/web/src/App.vue:visibleNotes` — 正文内 note 按已加载 `segments` 过滤,避免连续缓冲后旧窗口正文的 note 消失。
- `packages/web/src/style.css:.reader-pane/.formula-open` — 为 reader pane 增加 `min-height:0` 与纵向 overscroll containment;公式按钮改为可换行 inline-block,避免 display formula 破坏正文流。

**入口**:ReaderPane 滚轮/键盘到达缓冲边界 -> `App.onScrollEdge` -> `api.goto(targetLid)` 同步后端位置 -> `loadWindow(..., append/prepend)` 扩展正文。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web);Chrome headless smoke on `http://127.0.0.1:8791` 验证 LID 从 20 追加到 180,追加后底部 last LID 可完整进入 pane。

## 2026-07-04 S14h 正文预取不推进 reader [Vue]

**触达**:
- `packages/web/src/components/ReaderPane.vue` — 恢复 `scroll` 监听,按距离顶/底约 2 个屏幕触发正文缓冲请求;`segments` 首尾 LID 变化后自动复查,初始内容不足一屏时也会继续补正文。
- `packages/web/src/App.vue:onScrollEdge` — 不再调用 `api.goto()` 做预取;改为按 `leafOrder` 切出下一批/上一批 LID,直接 `hydrateSegments` 后 append/prepend 到正文,从而不改后端 reader session。

**入口**:ReaderPane `scroll`/`wheel`/键盘滚动 -> near-edge buffer request -> App 直接读取 `book.text` / `formula_semantics` 扩展正文缓冲。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web);Chrome headless 真实滚轮 smoke on packaged server `127.0.0.1:8792` 显示 LID 60->140->200->280 且 bottomGap 保持约 2 屏,临时 session `top_lid` 保持 `1.1`。

## 2026-07-04 S14i Outline 跟随本地阅读 LID [Vue]

**触达**:
- `packages/web/src/components/ReaderPane.vue` — 在 scroll/wheel/keyboard/segments 变化后用正文 pane 内 28% 高度探针计算当前可见 `data-lid`,发出 `current-lid`。
- `packages/web/src/App.vue` — 新增 `currentReadingLid` / `readingAnchorLid`,让 TopBar 进度、LeftRail active、ReaderPane anchor 使用本地阅读 LID;replace/goto/正文点击/公式点击同步该状态。
- `packages/web/src/components/LeftRail.vue` — 监听 active outline 变化并 `scrollIntoView({ block: "nearest" })`,让左侧列表跟随正文位置滚入可见区域。

**入口**:ReaderPane 本地滚动 -> `current-lid` -> App `readingAnchorLid` -> LeftRail active outline/progress。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-04 S14j 正文 Markdown 标题段显示 [Vue]

**触达**:
- `packages/web/src/App.vue:markdownHeadingLevel/displayText` — 识别 paragraph/chapter/section 的 ATX 标题行,显示层剥掉 `#`/`##` 并保留 range offset 修正。
- `packages/web/src/components/ReaderPane.vue:isFlowSegment` — Markdown 标题 paragraph 不再进入连续正文 flow,改为单独段落并附加 `heading-markdown-*` class。
- `packages/web/src/style.css:.prose p.heading-markdown-*` — 为 `#` 到 `######` 标题段补阅读标题层级样式。

**入口**:ReaderPane 渲染正文 `segments` -> `markdownHeadingLevel` 判断标题段 -> `renderSeg` 输出剥掉 Markdown 标题符号后的文本。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web);Chrome headless packaged smoke on `127.0.0.1:8794` 验证 `1.1`/`1.2.1` 带 `heading-markdown-*` 且裸 `#` 计数为 0。

## 2026-07-04 S14k Quote source 与公式 LID 匹配归一化 [Vue]

**触达**:
- `packages/web/src/App.vue:normalizeSourceMatchText` — Quote source 匹配前统一归一化 Markdown 转义标点,让 `0.1\%` 与 `0.1%` 同口径比较,且不吞 `\rho` 这类 LaTeX 命令。
- `packages/web/src/App.vue:sourceFocusStream` — 按正文行内 flow 规则拼接 paragraph/formula LID,公式相邻处不额外插空格。
- `packages/web/src/App.vue:selectionRanges` — 自由高亮按当前 `segments` 源文本坐标拆分;公式 LID 作为原子 leaf 高亮整条源文本。
- `packages/web/src/components/ReaderPane.vue:leadingQuote` — Quote source 传完整引用文本,不再用 96 字截断值做匹配。
- `packages/web/src/selection.ts` — 更新注释,说明公式 LID 的 range 映射已迁到 `App.selectionRanges`。

**入口**:ReaderPane note `Quote source` -> `focus-source-local` -> App `sourceFocusRanges` -> `renderSeg` 包 `source-focus-mark`。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web);Chrome headless packaged smoke on `127.0.0.1:8796` 验证源文本 `0.1\%` 可被 note quote `0.1%` 完整高亮。

## 2026-07-04 S14l BookStructure 结构地图决策落档 [Docs]

**触达**:
- `docs/adr/0044-bookstructure预构建结构地图-带读先总览再停靠.md` — 冻结 BookStructure 为预构建结构地图 sidecar,三层骨架为 `spine + throughlines + key_stops`。
- `CONTEXT.md:BookStructure` — 新增 BookStructure / spine / throughline / key_stop / structure unit card 术语。
- `docs/切片方案-profile深路径.md:PB7` — 新增 PB7 BookStructure sidecar 切片,并把 P3 带读改为先结构地图再逐停靠点。

**入口**:后续从 PB7 实现 `book_structure.json` 抽取流程、工具、prompt、gate/write/batch/status,再由 P8 接读时消费与带读路线投影。
**测试**:`rg "BookStructure|book_structure|spine|throughline|key_stop|PB7|ADR-0044" CONTEXT.md docs/adr docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-07-04 S14m BookStructure sidecar schema/gate [TS]

**触达**:
- `packages/core/src/book-structure.ts:buildBookStructureSidecar` — 新增 BookStructure sidecar 类型与确定性 gate,校验 LID/evidence/enum/shape 并过滤悬空引用。
- `packages/core/src/zod.ts:BookStructureSidecarZ` — 新增 BookStructure 产物自检 schema。
- `packages/core/test/book-structure.test.ts` — 覆盖合法 sidecar、非法条目丢弃、锚定证据必填。
- `docs/技术方案-架构蓝图.md:2.1` — 将 BookStructure sidecar 加入预构建管线组件表。

**入口**:后续 PB7 CLI/prompt 产出的候选先进入 `buildBookStructureSidecar(...)`,再写 `book_structure.json`。
**测试**:`npm run typecheck`(packages/core);`npm run test -- book-structure.test.ts`(packages/core,提升权限重跑;沙箱内 worker spawn EPERM)。

## 2026-07-04 S14n BookStructure build-loop tools [TS/Build]

**触达**:
- `packages/core/src/book-structure.ts:buildBookStructureUnitSources` -> 新增 PB7 unit-card 输入组装,从公共 base/discourse/formula/pass2 artifacts 投影结构单元输入。
- `packages/core/src/book-structure.ts:computeBookStructureStatus` -> 新增 unit/stitch content-hash 续建状态判定。
- `skills/build/book-structure-{status,input,write,batch}.ts` -> 新增 PB7 status/input/write/batch CLI,原子写 `.build/book-structure` 并收口 `book_structure.json`。
- `agents/book-structure-extractor.md` / `skills/build/SKILL.md` -> 新增 unit-card + stitching subagent 合同与 `/understand-book:build` 编排说明。

**入口**:`book-structure-status` -> `book-structure-input unit:<lid>` + `book-structure-extractor` + `book-structure-write unit:<lid>` -> `book-structure-input stitch` + `book-structure-write stitch` -> `book-structure-batch`。
**测试**:`npm run test -- book-structure`(packages/core,7 tests);`npm run typecheck`(packages/core);`tsc --noEmit ... skills/build/book-structure-*.ts`;`book-structure-status .understand-book/quantification-essence/source.txt --book-id quantification-essence` 返回 25 个 H2 级 pending unit。

## 2026-07-04 S14o quantification-essence BookStructure 真书收口 [Build]

**触达**:
- `.understand-book/quantification-essence/.build/book-structure/raw/unit-*.json` -> 25 个结构单元的 `book-structure-extractor` raw unit-card 输出。
- `.understand-book/quantification-essence/.build/book-structure/units/*.json` -> `book-structure-write` 写入带 content hash 的正式 unit artifacts。
- `.understand-book/quantification-essence/.build/book-structure/raw/stitch.json` / `stitch.json` -> 全书 stitching 输出与 hash 绑定 artifact。
- `.understand-book/quantification-essence/book_structure.json` -> `book-structure-batch` 生成并 gate 通过的 BookStructure sidecar,`spine=25 throughlines=7 key_stops=73 dropped=0`。

**入口**:`book-structure-status .understand-book/quantification-essence/source.txt --book-id quantification-essence` 返回 `units=25 done=25 pending=0 stitch=done`;读时可由 P8 在 Rust/read-tools 层开始加载 `book_structure.json`。
**测试**:`book-structure-batch .understand-book/quantification-essence/source.txt --book-id quantification-essence` 写出 `book_structure.json` 且 `dropped=0`;`npm run test -- book-structure`(packages/core,7 tests);`npm run typecheck`(packages/core)。

## 2026-07-04 S14p BookStructure 读时消费与 MCP 投影落档 [Docs]

**触达**:
- `docs/adr/0045-bookstructure读时消费与mcp投影.md` -> 新增 BookStructure 读时工具面、guide_path 路线、自检跳转与 MCP 投影决策。
- `CONTEXT.md:结构投影` -> 新增 structure projection / guide path / guide target self-check 术语。
- `docs/切片方案-profile深路径.md:P8` -> 新增 BookStructure 读时消费 + 带读路线投影总切片,覆盖人/访客两投影、`guide_path`、非机械跳转上下文自检。
- `docs/切片方案-profile深路径.md:P3/P7/P9` -> P3/P7 收窄为消费者;原 route Core 切片顺延为 P9。
- 历史代码链路中 2026-06-28 的 “P8 route Core” 保留旧编号语境;现行切片方案统一称 P9。

**入口**:后续 Rust 实现从 **P8** 起:先在 `read-tools::Book` 加载可选 `book_structure.json`,再暴露 `book.structure` / `book.guide_path`;P3 执行 `reader.gotoLid`,P7/MCP 只读面映射为 `book_structure` / `book_guide_path`,局部绕路复用 P9 route Core。
**测试**:`rg "P8 · BookStructure|P9 · route Core|book\\.structure|book\\.guide_path|book_structure|book_guide_path|guide_path -> text/context -> gotoLid" docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-07-04 S14q BookStructure 读时消费与只读投影 [Rust]

**触达**:
- `crates/read-tools/src/lib.rs:Book::load` — 加载可选 `book_structure.json`;缺失显式降级,存在但坏 schema/悬空引用 fail-fast。
- `crates/read-tools/src/lib.rs:Book::structure` / `Book::guide_path` — 新增结构投影与宏观带读路线 DTO,导出前端 TS 类型。
- `crates/runtime/src/orchestrator.rs:tool_specs/dispatch/SYSTEM_PROMPT` — 接入 `book.structure` / `book.guide_path`,主动带读改为先结构地图再逐停靠点。
- `crates/server/src/lib.rs:route_book` — 新增 REST `GET /book/structure?at=` 与 `GET /book/guide_path?at=`。
- `crates/server/src/mcp.rs:tools_list_result/dispatch_mcp_tool` — 新增 MCP 只读 `book_structure` / `book_guide_path`,保持访客面不触 reader/memory/session。
- `packages/web/src/generated/BookStructure*.ts` / `GuidePath*.ts` / `StructureProjection.ts` — 由 ts-rs 导出 P8 DTO。

**入口**:`Book::load(root)` 读入 sidecar -> runtime `book.structure` / `book.guide_path` -> REST `/book/structure` `/book/guide_path` 或 MCP `book_structure` `book_guide_path`。
**测试**:`cargo test` 覆盖 read-tools sidecar 缺失/坏文件/有效投影、runtime tool dispatch、REST 路由、MCP 只读访客投影。

## 2026-07-04 S14r 右栏全量 Notes 与 Source Preview [Vue]

**触达**:
- `packages/web/src/App.vue:allNotes/allHighlights` — 右栏 Notes tab 改用全书全部 note/highlight,按 LID 阅读顺序排序;ReaderPane 内联 note 仍只显示当前可见段。
- `packages/web/src/App.vue:openSourcePreview/renderSourcePreviewSeg` — `Go to source` / `Quote source` 打开临时 source 阅读弹窗,加载目标 LID 前后 leaf,把 source 滚到视觉中心并高亮来源 quote。
- `packages/web/src/components/RightRail.vue:notesExpanded` — Notes tab 增加全量展开/折叠控制;每条 note 默认折叠并显示摘要。
- `packages/web/src/components/RightRail.vue:leadingQuote` — Quote source 传完整引用文本,不再用摘要截断值做匹配。

**入口**:RightRail `Notes` tab -> note `Go to source` / `Quote source` -> App source preview modal;弹窗 `Open in reader` 才执行真实 `reader.gotoLid`。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-05 PP0 content_profile 显式 technical_learning [TS/Build]

**触达**:
- `packages/core/src/content-profile.ts:resolveContentProfile` — 新增 content profile 解析,当前只接受 `technical_learning`,未知 profile fail-fast。
- `packages/core/src/profile-artifact.ts:buildProfileArtifactHeader` — header 接受 `content_profile`,默认与显式 `technical_learning` 等价。
- `skills/build/content-profile-options.ts:parseContentProfileArgsOrExit` — build CLI 统一解析 `--content-profile technical_learning`。
- `skills/build/*-{status,input,write,batch}.ts` / `pass1-*` — Pass1、profile-sidecar、Pass2、BookStructure 链路显式接受 profile;batch 产物 header 写入解析后的 profile。
- `packages/core/test/profile-artifact.test.ts` — 覆盖默认/显式 technical_learning 等价与 unsupported profile 拒绝。

**入口**:`build-status|emit-input|pass1-write|pass1-batch|profile-sidecar-*|pass2-*|book-structure-* <book> ... --content-profile technical_learning`。
**测试**:`pnpm -C packages/core test`(106 tests);`pnpm -C packages/core typecheck`;`.\\node_modules\\.bin\\tsc.ps1 --noEmit --pretty false`;`emit-input` 默认与显式 `technical_learning` stdout 逐字一致;`build-status --content-profile paper` fail-fast。

## 2026-07-05 PP0.5 paper_subtype overlay resolver + survey skeleton [TS/Core]

**触达**:
- `packages/core/src/content-profile.ts:resolveContentProfile/resolvePaperRulePack` — 新增 `content_profile=paper` 规则解析,默认 `paper_subtype=research_article`。
- `packages/core/src/content-profile.ts:SURVEY_OVERLAY` — 新增 `survey` overlay skeleton,只 patch metadata/argument/graph/book_structure/reading_guide/validators 固定 slot。
- `packages/core/src/profile-artifact.ts:buildProfileArtifactHeader` — profile header 可解析 `paper` profile/version,但不存 subtype。
- `skills/build/content-profile-options.ts:parseContentProfileArgs` — build CLI 能识别 `--paper-subtype`,但在 PP1 前拒绝执行 `content_profile=paper`,避免静默套 technical rules。
- `packages/core/test/content-profile.test.ts` — 覆盖 research_article 默认、survey overlay、base invariant 保留、`review_says` 默认 claim source、禁止顶层 `survey`。

**入口**:规则层调用 `resolveContentProfile("paper", { paper_subtype: "survey" })` 得到 paper base + survey overlay 的 effective rules。
**测试**:`pnpm -C packages/core test`(112 tests);`.\\node_modules\\.bin\\tsc.ps1 --noEmit --pretty false`;`build-status --content-profile paper --paper-subtype survey` 明确拒绝执行到 PP1。

## 2026-07-05 PP1 Markdown canonical source + optional PDF attachment manifest [TS/Build]

**触达**:
- `packages/core/src/source-manifest.ts:buildSourceManifest` — 新增 canonical source / original PDF attachment / optional PDF source map manifest,固定 LID 为唯一 citation anchor。
- `packages/core/src/zod.ts:SourceManifestZ` — 新增 `source_manifest.json` 产出前自检 schema,确保 PDF/source map 不能成为 citation evidence。
- `skills/build/content-profile-options.ts:parseContentProfileArgs` — 增加 `allowPaperExecution` 门禁,仅 Pass1 阶段允许 `content_profile=paper` 执行。
- `skills/build/pass1-batch.ts` — 写出 `source_manifest.json`,支持 `--original-pdf` 与 `--pdf-source-map`,并保留 `source.txt` 作为 canonical truth。
- `packages/core/test/source-manifest.test.ts` — 覆盖无 PDF、有 PDF、source map 扩展点和非法 PDF wiring。

**入口**:`pass1-batch <paper.md> --content-profile paper [--paper-subtype survey] [--original-pdf <paper.pdf>] [--pdf-source-map <map.json>]`。
**测试**:`pnpm -C packages/core test`(116 tests);`pnpm -C packages/core typecheck`;`.\\node_modules\\.bin\\tsc.ps1 --noEmit --pretty false`;`build-status --content-profile paper --paper-subtype survey` 可执行;`pass1-batch --allow-partial --content-profile paper --original-pdf ... --pdf-source-map ...` 写出 `source_manifest.json`;`profile-sidecar-status --content-profile paper` 仍 fail-fast。

## 2026-07-05 PP2 paper_metadata sidecar build flow [TS/Build]

**触达**:
- `packages/core/src/paper-metadata.ts:buildPaperMetadataSidecar` — 新增 `paper_metadata.json` 聚合/gate,字段统一 `MetadataField<T>` envelope,正文来源必须带真实 LID evidence。
- `packages/core/src/paper-metadata.ts:buildPaperMetadataArtifact/computePaperMetadataStatus` — 新增逐窗 metadata artifact 与 content-hash 续建状态。
- `packages/core/src/zod.ts:PaperMetadataZ` — 新增 paper metadata 产出前 zod 自检 schema。
- `skills/build/paper-metadata-{status,input,write,batch}.ts` — 新增 PP2 独立 build loop,只允许 `--content-profile paper`。
- `agents/paper-metadata-extractor.md` — 新增 paper metadata extractor I/O 契约,禁止裸字段、消歧和跨论文关系。
- `skills/build/fixtures/paper-metadata-sample.json` — 新增最小 CLI smoke fixture。
- `docs/技术方案-架构蓝图.md:2.1` / `skills/build/SKILL.md` — 登记 PaperMetadata sidecar 与 PP2 编排命令。

**入口**:`paper-metadata-status` -> `paper-metadata-input` + `paper-metadata-extractor` + `paper-metadata-write` -> `paper-metadata-batch`。
**测试**:`pnpm -C packages/core test`(121 tests);`pnpm -C packages/core typecheck`;`.\\node_modules\\.bin\\tsc.ps1 --noEmit --pretty false`;`paper-metadata-status/input/write/batch` 对 `sample.md --content-profile paper` smoke 产出 `paper_metadata.json`;默认 technical profile 调 `paper-metadata-status` fail-fast。

## 2026-07-05 PP3 paper_lexicon sidecar build flow [TS/Build]

**触达**:
- `packages/core/src/paper-lexicon.ts:buildPaperLexiconSidecar` — 新增 `paper_lexicon.json` 聚合/gate,强制 `occurrences_lids` 非空且真实,`defined_at_lid` 必须属于 occurrences。
- `packages/core/src/paper-lexicon.ts:buildPaperLexiconArtifact/computePaperLexiconStatus` — 新增逐窗 lexicon artifact 与 content-hash 续建状态。
- `packages/core/src/zod.ts:PaperLexiconZ` — 新增 paper lexicon 产出前 zod 自检 schema。
- `skills/build/paper-lexicon-{status,input,write,batch}.ts` — 新增 PP3 独立 build loop,只允许 `--content-profile paper`。
- `agents/paper-lexicon-extractor.md` — 新增 paper lexicon extractor I/O 契约,禁止普通英语词典和长中文解释。
- `skills/build/fixtures/paper-lexicon-sample.json` — 新增最小 CLI smoke fixture。
- `docs/技术方案-架构蓝图.md:2.1` / `skills/build/SKILL.md` — 登记 PaperLexicon sidecar 与 PP3 编排命令。

**入口**:`paper-lexicon-status` -> `paper-lexicon-input` + `paper-lexicon-extractor` + `paper-lexicon-write` -> `paper-lexicon-batch`。
**测试**:`pnpm -C packages/core test`(126 tests);`.\\node_modules\\.bin\\tsc.ps1 --noEmit --pretty false`;`paper-lexicon-status/input/write/batch` 对 `sample.md --content-profile paper` smoke 产出 `paper_lexicon.json`;默认 technical profile 调 `paper-lexicon-status` fail-fast。

## 2026-07-05 PP4 paper Pass1 profile-aware rules [TS/Build]

**触达**:
- `packages/core/src/pass1-profile-input.ts:buildProfiledPass1Input` — 新增 profile-aware Pass1 输入;technical 路径逐字保持旧输入,paper 路径注入 `PAPER_PASS1_RULES`。
- `packages/core/src/build-resume.ts:buildPass1Artifact/computeBuildStatus` — Pass1 content hash 改为绑定 profile-aware input,避免 technical artifact 被 paper 构建复用。
- `skills/build/emit-input.ts` / `pass1-write.ts` / `pass1-batch.ts` / `build-status.ts` — Pass1 CLI 全链路使用 profile-aware input/hash。
- `agents/pass1-local-extractor.md` — 说明 `PAPER_PASS1_RULES` 规则头与 paper 抽取关注点,仍输出现有 entity/concept/claim/edge shape。
- `packages/core/test/pass1-profile-input.test.ts` — 覆盖 technical 输入不变、paper 规则头和 profile hash 隔离。

**入口**:`emit-input <paper.md> <windowId> --content-profile paper [--paper-subtype survey]` 输出规则头 + LID 正文;`pass1-write/pass1-batch/build-status` 使用同一 profile-aware hash。
**测试**:`pnpm -C packages/core test pass1-profile-input build-resume`;`pnpm -C packages/core typecheck`;`.\\node_modules\\.bin\\tsc.ps1 --noEmit --pretty false`;CLI smoke 验证默认与显式 `technical_learning` 输入等价、paper 输入含 `PAPER_PASS1_RULES` / survey slots。

## 2026-07-05 PP5 paper discourse sidecar rules [TS/Build]

**触达**:
- `packages/core/src/discourse-index.ts:LocalFunction/RhetoricalMove` — 扩展 paper discourse enum,覆盖 research question、method、experiment、evidence、result、future work 等段落功能。
- `packages/core/src/profile-sidecar-build.ts:buildProfileSidecarWindowInput` — paper profile 下向 profile-sidecar input 注入 `PAPER_DISCOURSE_RULES`,并将 content hash 绑定到 profile-aware input。
- `skills/build/profile-sidecar-{status,input,write,batch}.ts` — PP5 起允许 `--content-profile paper` 执行 profile-sidecar 链路。
- `agents/profile-sidecar-extractor.md` — 新增 paper discourse 输入规则和红线,禁止 metadata/lexicon/cross-paper 输出。
- `skills/build/fixtures/profile-sidecar-paper-sample.json` — 新增最小 paper discourse CLI smoke fixture。
- `packages/core/test/discourse-index.test.ts` / `profile-sidecar-build.test.ts` — 覆盖 paper enum gate、规则头和 hash 隔离。

**入口**:`profile-sidecar-status` -> `profile-sidecar-input --content-profile paper` + `profile-sidecar-extractor` + `profile-sidecar-write` -> `profile-sidecar-batch` 产出 `discourse_index.json`。
**测试**:`pnpm -C packages/core test`(131 tests);`.\\node_modules\\.bin\\tsc.ps1 --noEmit --pretty false`;`profile-sidecar-status/input/write/batch` 对 `sample.md --content-profile paper --paper-subtype survey` smoke 产出 paper header 的 `discourse_index.json`。
## 2026-07-05 PP6 paper Pass2 edge contracts [TS/Build]

**触达**:
- `packages/core/src/pass2.ts:TechnicalLearningLongRangeEdgeType` — 扩展 Pass2 long_range 闭集,新增 `claim_supported_by_evidence`、`method_supports_result`、`hypothesis_tested_by_experiment`、`related_work_contrasts`、`related_work_builds_on`、`limitation_motivates_future_work`。
- `packages/core/src/pass2-build.ts:hintsFor/EDGE_TYPE_CONTRACTS` — paper discourse 标签产生 paper relation hints,并为每个新增 edge type 提供 when/when_not/evidence/roles contract;deterministic gate 继续只降 accepted 且过证据门禁的边。
- `packages/core/src/zod.ts:TechnicalLearningLongRangeEdgeTypeZ` — Pass2 audit 自检 schema 接受 PP6 paper edge types。
- `packages/core/src/content-profile.ts:PAPER_BASE_RULES` — paper rule pack 的 graph edge rules 对齐 PP6 实际 edge type 名称。
- `skills/build/pass2-common.ts:parseBookArgs` / `pass2-{status,input,write,batch}.ts` — Pass2 CLI 允许 `--content-profile paper`,batch 写 paper profile header。
- `agents/pass2-longrange-linker.md` / `skills/build/SKILL.md` — 文档化 paper Pass2 edge contracts 与 agent 分类边界。
**入口**:`pass2-status/input/write/batch <book> --content-profile paper [--paper-subtype survey]` 装配 `Pass2WorkPacket`,交 `pass2-longrange-linker` 分类候选,再由 `gatePass2BuildOutput` 写 `base.json` / `pass2_audit.json`。
**测试**:`pnpm -C packages/core test pass2-build content-profile`;`pnpm -C packages/core test`(133 tests);`.\node_modules\.bin\tsc.ps1 --noEmit --pretty false`;`pp6-smoke` 跑通 paper `pass1-batch --allow-partial` -> `profile-sidecar-write/batch` -> `pass2-status` -> `pass2-batch`,audit header 为 `profile_id=paper`。
## 2026-07-05 PP7 paper BookStructure rules [TS/Build]

**触达**:
- `packages/core/src/book-structure.ts:BookStructureProfileRules` — 为 BookStructure unit/stitch packet 新增 paper profile rules 输入,technical 默认不带该字段以保持旧路径输入稳定。
- `packages/core/src/book-structure.ts:buildBookStructureUnitSources/buildBookStructureStitchPacket` — paper profile 下写入 `PAPER_BOOK_STRUCTURE_RULES`,覆盖 section mapping、spine/throughline/key_stop 策略和 metadata 禁区;输出仍复用 `book_structure.json`。
- `skills/build/book-structure-common.ts:parseBookStructureArgs/loadBookStructureBuildContext` — BookStructure build-loop 允许 `--content-profile paper`,并将 profile 传入 unit/stitch hash。
- `skills/build/book-structure-{status,input,write,batch}.ts` — usage 扩展为 `technical_learning|paper`,paper batch 写 profile header。
- `agents/book-structure-extractor.md` / `skills/build/SKILL.md` — 文档化 paper BookStructure 规则边界:不新增 `paper_structure.json`,不把 metadata 塞进 BookStructure。
- `skills/build/fixtures/book-structure-*.json` — 新增最小 unit/stitch smoke 输出 fixture。
**入口**:`book-structure-status/input/write/batch <book> --content-profile paper [--paper-subtype survey]` 从公共 artifacts 装配 paper-aware unit/stitch packet,经 `book-structure-extractor` 输出共享 BookStructure candidate,再由 gate 写 `book_structure.json`。
**测试**:`pnpm -C packages/core test book-structure`;`pnpm -C packages/core test`(134 tests);`.\node_modules\.bin\tsc.ps1 --noEmit --pretty false`;`pp6-smoke` 跑通 paper `book-structure-status/input/write/batch`,`book_structure.json` 写出 `profile_id=paper` 且 dropped=0。

## 2026-07-05 PP8 PaperReadingGuide projection [Rust/Read]

**触达**:
- `crates/read-tools/src/lib.rs:PaperReadingGuide` — 新增 paper 只读 projection DTO,包含 reading mode/stage、论文十问、PaperCodebook、AbstractReadingAid 和 answer slot 标记。
- `crates/read-tools/src/lib.rs:Book::load` — 读取并校验可选 `paper_metadata.json` / `paper_lexicon.json`,坏 LID fail-fast,缺 sidecar 显式 unavailable。
- `crates/read-tools/src/lib.rs:Book::paper_reading_guide` — 组合 BookStructure、graph、discourse、paper metadata/lexicon 与 `book.text`,不新增 `paper_questions.json` 或持久 Codebook。
- `crates/server/src/lib.rs:route_book` — 暴露 `GET /book/paper_reading_guide?mode=&stage=`。
- `crates/runtime/src/orchestrator.rs:tool_specs/dispatch` — 暴露 runtime tool `book.paper_reading_guide`,并在系统提示中引导论文十问/Codebook/摘要辅助先走该投影。
- `crates/server/src/mcp.rs:tools_list_result/dispatch_mcp_tool` — 暴露单书 MCP tool `book_paper_reading_guide`,不创建 visitor session。
- `packages/web/src/generated/PaperReadingGuide.ts` — ts-rs 导出 PP8 投影的前端类型契约。
- `skills/build/SKILL.md` — 登记 PP8 REST/runtime/MCP 入口和 projection 边界。

**入口**:`/book/paper_reading_guide?mode=skim|close|deep&stage=passive|active|critical|creative`;runtime `book.paper_reading_guide`;MCP `book_paper_reading_guide`。
**测试**:`cargo test -p read-tools`;`cargo test -p runtime`;`cargo test -p server`;`.\node_modules\.bin\tsc.ps1 --noEmit --pretty false`。

## 2026-07-05 PP9 single-paper MCP dossier boundary [Rust/MCP]

**触达**:
- `crates/read-tools/src/lib.rs:PaperMetadataProjection` — 新增单篇 paper metadata projection,保留 value/source/evidence_lids/confidence,缺 `paper_metadata.json` 时 explicit unavailable。
- `crates/read-tools/src/lib.rs:PaperLexiconProjection` — 新增单篇 paper lexicon projection,复用 codebook term shape,缺 `paper_lexicon.json` 时 explicit unavailable。
- `crates/read-tools/src/lib.rs:Book::paper_metadata_projection/Book::paper_lexicon_projection` — 从已加载 sidecar 生成只读 dossier 对齐键,不生成跨论文关系。
- `crates/server/src/lib.rs:route_book` — 暴露 `GET /book/paper_metadata` 与 `GET /book/paper_lexicon`。
- `crates/runtime/src/orchestrator.rs:tool_specs/dispatch` — 暴露 runtime tools `book.paper_metadata` / `book.paper_lexicon`,系统提示把作者、年份、数据集、术语查询导向这些 projection。
- `crates/server/src/mcp.rs:tools_list_result/dispatch_mcp_tool` — 暴露 MCP tools `book_paper_metadata` / `book_paper_lexicon`,并测试工具名单不含 `cross_paper` / `corpus`。
- `packages/web/src/generated/PaperMetadataProjection.ts` / `PaperLexiconProjection.ts` — ts-rs 导出单篇 dossier 前端类型契约。
- `skills/build/SKILL.md` — 登记 PP9 单篇 MCP dossier 出口和多论文编排边界。

**入口**:`/book/paper_metadata`,`/book/paper_lexicon`;runtime `book.paper_metadata` / `book.paper_lexicon`;MCP `book_paper_metadata` / `book_paper_lexicon`。多论文比较由外部 MCP client 同时连接多个单篇服务后运行时完成。
**测试**:`cargo test -p read-tools -p runtime -p server`;`.\node_modules\.bin\tsc.ps1 --noEmit --pretty false`。

## 2026-07-05 PF1 Profile Manifest 与 Reader Layout 合约 [Rust/TS]

**触达**:
- `crates/read-tools/src/lib.rs:ProfileManifest` — 新增 profile manifest 静态能力契约,覆盖 projections、ui slots、layout presets、allowed layout actions、agent tools、guided reading policy 与 defaults。
- `crates/read-tools/src/lib.rs:ReaderLayoutState/ReaderLayoutAction` — 新增 reader layout 会话态与 action 闭集 DTO,按 ADR-0060 使用 snake_case JSON tag。
- `crates/read-tools/src/lib.rs:ReaderLayoutEffect/ReaderLayoutProposal` — 新增 layout 直执 effect 与高风险 proposal 承载类型。
- `crates/runtime/src/orchestrator.rs:AgentEffect` — 扩展 `Layout` / `LayoutProposal` effect variant,先只接合约不接 reducer。
- `packages/web/src/generated/ProfileManifest.ts` / `ReaderLayoutAction.ts` / `ReaderLayoutState.ts` / `ReaderLayoutEffect.ts` / `ReaderLayoutProposal.ts` — ts-rs 导出的前端共享合约。

**入口**:PF2 后端 profile registry 后续返回 `ProfileManifest`;PF4/PF5 后续由 `reader.layout.apply` 产出 `ReaderLayoutEffect` / `ReaderLayoutProposal`。
**测试**:`cargo test -p read-tools -p runtime -p server`;`.\node_modules\.bin\tsc.ps1 --noEmit --pretty false`。

## 2026-07-05 PF2 Profile Registry 与 Manifest 查询面 [Rust/REST]

**触达**:
- `crates/read-tools/src/lib.rs:technical_learning_profile_manifest/paper_profile_manifest` — 新增后端 profile registry,提供 `technical_learning` 与 `paper` manifest。
- `crates/read-tools/src/lib.rs:Book::content_profile_id/profile_manifest/profile_summary` — 当前 book 默认 technical,加载 paper artifacts 时推断为 paper,并返回 agent-facing summary。
- `crates/runtime/src/orchestrator.rs:tool_specs/dispatch` — 新增 runtime tool `profile.manifest`,并让 `reader.state` 返回 profile summary。
- `crates/server/src/lib.rs:route_profile_manifest/reader_state_response` — 新增 REST `GET /profile/manifest`,并扩展 `/reader/state` profile 字段。
- `packages/web/src/api.ts:profileManifest/ReaderState` — 前端 API 增加 manifest 获取和 profile summary 类型。
- `packages/web/src/generated/ProfileSummary.ts` — ts-rs 导出的 reader.state profile summary 契约。

**入口**:REST `GET /profile/manifest[?profile_id=technical_learning|paper]`;runtime tool `profile.manifest`;POST `/reader/state` 返回 `{..., profile}`。
**测试**:`cargo test -p read-tools -p runtime -p server`;`.\node_modules\.bin\tsc.ps1 --noEmit --pretty false`。
## 2026-07-05 PF4 ReaderLayoutState reducer 与 proposal lifecycle [Rust/REST]

**触达**:
- `crates/read-tools/src/lib.rs:ReaderLayoutApplyOutcome` — 新增 layout apply 直执 effect / 高风险 proposal 的共享返回合约并导出 TS。
- `crates/reader/src/lib.rs:Reader::apply_layout_actions/apply_layout_proposal/undo_layout_effect` — 新增后端 session layout reducer,覆盖 profile manifest 校验、rev bump、低风险直执、高风险 proposal、stale 拒绝和 undo rev 保护。
- `crates/reader/src/lib.rs:Reader::state` — `reader.state` 增加 `layout: ReaderLayoutState`,后端继续作为布局真相源。
- `crates/server/src/lib.rs:route_mut(/reader/layout.apply)` — 新增 REST `POST /reader/layout.apply`,支持 `actions[]` 与 `{proposal_id,base_layout_rev}` 两种请求形态。
- `crates/runtime/src/orchestrator.rs:reader_state_value` — runtime `reader.state` 同步返回 layout,但暂不暴露 `reader.layout.apply` 给 agent。
- `packages/web/src/api.ts:ReaderState/layoutApply` — 前端命令客户端补齐 layout state 与 apply outcome 类型。
- `packages/web/src/generated/ReaderLayoutApplyOutcome.ts` — ts-rs 导出的 layout apply 响应契约。

**入口**:REST `POST /reader/layout.apply` with `{actions:[ReaderLayoutAction...]}` 或 `{proposal_id,base_layout_rev}`;POST `/reader/state` 返回 `{..., layout, profile}`。
**测试**:`cargo test -p read-tools -p reader -p runtime -p server`;`.\node_modules\.bin\tsc.ps1 --noEmit --pretty false`。
## 2026-07-05 PF5 Agent layout tool/effect 集成 [Rust/Runtime]

**触达**:
- `crates/runtime/src/orchestrator.rs:tool_specs` — 新增 agent tool `reader.layout.apply`,schema 只接收 typed `actions[]`。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 明确 layout 操作必须调用 `reader.layout.apply`,使用 manifest slot id 与 snake_case action kind,高风险动作等待 proposal 确认。
- `crates/runtime/src/orchestrator.rs:dispatch(reader.layout.apply)` — 调用 PF4 reducer,将 direct outcome 映射为 `AgentEffect::Layout`,将 high-risk outcome 映射为 `AgentEffect::LayoutProposal`。
- `crates/runtime/src/orchestrator.rs:dispatch_reader_layout_apply_returns_effect_or_proposal` — 覆盖 runtime dispatch 的 direct/proposal 两条路径。
- `crates/runtime/src/orchestrator.rs:agent_loop_layout_apply_emits_direct_and_proposal_effects` — 覆盖脚本化 agent 回合 effects/trace。

**入口**:runtime tool `reader.layout.apply({actions:[...]})`;`OuterOutcome.effects[]` 返回 `Layout` 或 `LayoutProposal`。
**测试**:`cargo test -p runtime`;总体验证同 PF4。
## 2026-07-05 PF6 前端 manifest/layout sync 与通用 slot shell [Vue]

**触达**:
- `packages/web/src/App.vue:profileSummary/profileManifest/readerLayout` — `api.state()` 同步 profile summary 与后端 layout,并按 profile id/version 拉取完整 manifest。
- `packages/web/src/App.vue:profile-shell` — 新增通用 profile workspace shell,显示 manifest/presets/open slots/focus/panel size/pending proposal。
- `packages/web/src/App.vue:applyLayoutActions/applyPendingLayoutProposal` — 前端 layout 操作统一走 `api.layoutApply`,proposal Apply 用 `{proposal_id,base_layout_rev}` 回后端复验。
- `packages/web/src/App.vue:effLabel/keepEffect/undoEffect` — Agent `LayoutProposal` 在消息区显示 Apply/Dismiss;direct `Layout` 不复用 note/highlight 的 Keep/Undo 语义。
- `packages/web/src/components/RightRail.vue:effectPrimaryLabel/effectSecondaryLabel` — effect 操作按钮文案改由父层按 effect kind 决定。
- `packages/web/src/style.css:profile-shell` — 新增通用 slot shell 紧凑样式,不实现 paper 专属内容。
- `packages/web/src/api.ts:layoutApply/ReaderState` — 前端 API 增加 layout state、layout action/outcome/proposal 类型导出。

**入口**:页面初始化/agent 同步时 `api.state()` -> `api.profileManifest(profile_id)`;profile shell preset/slot 操作 -> `POST /reader/layout.apply`;Agent proposal card -> Apply/Dismiss。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-05 PF7 paper workbench slots [Vue]

**触达**:
- `packages/web/src/api.ts:api.paperReadingGuide/paperMetadata/paperLexicon/structure` - 暴露 paper workbench 需要消费的现有 REST projection 客户端与生成类型。
- `packages/web/src/App.vue:loadPaperProjectionData` - 按当前 paper profile、layout preset 和阅读锚点同步 PaperReadingGuide、metadata、lexicon、BookStructure projection。
- `packages/web/src/App.vue:profile-shell paper slot renderers` - 为 `paper.structure_map`、`paper.codebook`、`paper.abstract_aid`、`paper.ten_questions`、`paper.evidence`、`paper.agent` 渲染工作台内容与 LID evidence 操作。
- `packages/web/src/style.css:paper-slot-body` - 增加 paper slot 内部列表、证据按钮和状态文本样式。

**入口**:paper profile 页面初始化/跳转/agent 同步 -> `api.state()` + manifest -> paper slot projection 拉取;slot 内 LID 按钮 -> `reader.goto`;Pin -> `POST /reader/layout.apply` 的 `pin_evidence` action。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-05 PF8 end-to-end regression [Verify]

**触达**:
- `crates/read-tools/src/lib.rs:tests` - 回归 profile manifest、paper projection、ts-rs export、layout contract。
- `crates/reader/src/lib.rs:tests` - 回归 layout reducer、proposal apply/stale、undo 与 action validation。
- `crates/runtime/src/orchestrator.rs:tests` - 回归 `reader.layout.apply` agent tool、layout effect/proposal 与 paper projection tools。
- `crates/server/src/lib.rs:tests` - 回归 REST manifest、reader state/layout apply、paper projection endpoints 与 MCP边界。
- `packages/web/src/App.vue` / `packages/web/src/api.ts` - 回归 manifest/layout sync、paper workbench projection consumption 与构建链路。

**入口**:PF 总回归命令集合:Rust packages、root TS、web typecheck/build、diff check。
**测试**:`cargo test -p read-tools -p reader -p runtime -p server`;`.\node_modules\.bin\tsc.ps1 --noEmit --pretty false`;`npm run typecheck`(packages/web);`npm run build`(packages/web);`git diff --check`(仅 LF/CRLF warning)。

## 2026-07-05 PF9 profile API static fallback bugfix [Rust/Server]

**触达**:
- `crates/server/src/main.rs:is_api_url` - 把 `/api/profile/*` 纳入 API 路由识别，避免 GET `/api/profile/manifest` 被 SPA `index.html` fallback 吃掉。
- `crates/server/src/main.rs:api_paths_are_not_static_fallback_candidates` - 增加 `/api/profile/manifest` 回归断言。

**入口**:打包/同源 server 启动前端 -> `api.profileManifest()` -> `GET /api/profile/manifest` -> `normalize_api_url` -> `/profile/manifest` JSON route。
**测试**:`cargo test -p server`。

## 2026-07-05 PF10 hide technical profile workspace shell [Frontend]

**触达**:
- `packages/web/src/App.vue:profile-shell` - 将 profile workspace shell 限定为 `paper` profile 显示,避免普通 `technical_learning` 书籍暴露 `Structure map`/`Focus`/`book.structure` 这类内部调试壳。

**入口**:普通书启动/打开后 `reader.state.profile_id=technical_learning` -> `isPaperProfile=false` -> 不渲染 profile workspace;paper 书仍渲染 paper workbench。
**测试**:`npm run typecheck`;`npm run build`。

## 2026-07-05 PF11 agent history modal teleport and selectable cards [Frontend]

**触达**:
- `packages/web/src/components/RightRail.vue:historyOpen/history-backdrop` - 用 Vue `Teleport` 将 agent history 弹窗挂到 `body`,避免被右侧栏 `overflow/backdrop-filter` 裁剪成只在 rail 内显示。
- `packages/web/src/components/RightRail.vue:openHistorySessionFromCard` - 让历史会话卡片本体可点击/键盘切换,并阻止 Goto/Delete/Open 按钮事件冒泡误触发会话切换。
- `packages/web/src/components/RightRail.vue:.history-card/.history-backdrop` - 提升弹窗层级并补充卡片 hover/focus/cursor 状态。

**入口**:右侧 Agent 面板 `History` -> 全窗口 history modal -> 点击历史卡片或 `Open chat` -> `select-chat` -> `api.agentHistorySelect(sessionId)`。
**测试**:`npm run typecheck`;`npm run build`。

## 2026-07-05 PF12 right rail answer note popover teleport [Frontend]

**触达**:
- `packages/web/src/components/RightRail.vue:onAnswerMouseUp` - 选中 Agent 回答后计算 `Note` 浮层坐标时夹在视口内,避免贴右侧边缘不可见。
- `packages/web/src/components/RightRail.vue:answer-popover` - 用 Vue `Teleport` 将选区 `Note` 浮层挂到 `body`,避免被右侧栏 `overflow:hidden`/层叠上下文裁剪。
- `packages/web/src/components/RightRail.vue:.answer-popover` - 提升 z-index,确保浮层显示在右侧栏和其它普通内容之上。

**入口**:右侧 Agent 面板选中回答文本 -> `onAnswerMouseUp` -> `answerSelection` -> `Note` popover -> `save-answer-selection` -> `saveAgentSelection`。
**测试**:`npm run typecheck`;`npm run build`。

## 2026-07-05 PF13 source preview continuous quote context [Frontend]

**触达**:
- `packages/web/src/App.vue:sourcePreviewHtml/sourcePreviewSingleHtml/sourcePreviewFlowHtml` - 将 Source preview 的邻近 LID 片段拼成连续只读 Markdown/LaTeX 正文块,不再逐段显示 LID/kind 元信息。
- `packages/web/src/App.vue:centerSourcePreview` - Source preview 打开后滚动到 `source-focus-mark`,即被引用/匹配的句子位置。
- `packages/web/src/App.vue:source-preview modal template/CSS` - 标题改为用户语义的 `Quote source`,正文改为连续阅读样式,保留代码/表格/图片块级展示。

**入口**:点击笔记里的 `Quote source` -> `openSourcePreview` 拉取中心 LID 前后邻近 LID -> `sourcePreviewHtml` 连续渲染 -> quote 通过既有 `sourceFocusRangesIn` 高亮。
**测试**:`npm run typecheck`;`npm run build`。

## 2026-07-05 PF14 quoted source send collapse and transcript autoscroll [Frontend]

**触达**:
- `packages/web/src/App.vue:sendAgent` - 创建 pending 回合并保存 `questionQuote` 后立即清空 `askDraft`,让 quoted source 从输入区并入已发送消息。
- `packages/web/src/components/RightRail.vue:transcriptRef/scrollTranscriptToBottom` - 监听新回合、pending/answer 状态和 askDraft 高度变化,自动滚动 Agent transcript 到最新对话。

**入口**:右侧 Agent 面板带 `Quoted source` 草稿点击 `Send` -> `sendAgent` 推入 pending turn -> 输入区引用块消失,transcript 中该 turn 保留引用并滚到底部显示 `Agent is thinking…`。
**测试**:`npm run typecheck`;`npm run build`。

## 2026-07-05 PF15 formula parameter symbol math rendering [Frontend]

**触达**:
- `packages/web/src/App.vue:inlineMathSource/renderFormulaSymbol` - 新增公式参数符号 inline math 渲染 helper,兼容裸 LaTeX、`$...$` 和 `\\(...\\)` 输入。
- `packages/web/src/App.vue:formula-dialog parameters template` - 参数列表的 `p.symbol` 从纯文本改为 KaTeX inline math HTML,标签仍作为普通文本展示。
- `packages/web/src/App.vue:.formula-param-symbol/.formula-param-label` - 调整参数行布局,让公式符号和中文标签基线对齐并可换行。

**入口**:点击正文公式块 -> `openFormulaDialog` -> formula sidecar modal -> 参数 `dt` 调 `renderFormulaSymbol(p.symbol)`。
**测试**:`npm run typecheck`;`npm run build`。

## 2026-07-05 PF16 agent answer note markdown preservation [Frontend]

**触达**:
- `packages/web/src/selection.ts:rangeToMarkdown/fragmentToMarkdown` - 将选区 DOM 转 Markdown 从纯文本升级为结构化转换,保留标题、加粗/斜体、列表、blockquote、链接、代码、表格和 KaTeX。
- `packages/web/src/components/RightRail.vue:onAnswerMouseUp` - 右侧 Agent 回答选区保存 Note 改用共享 `rangeToMarkdown`,避免表格、标题、分点在 Note 中被压平成纯文本。

**入口**:右侧 Agent 回答选中文本 -> `onAnswerMouseUp` -> `rangeToMarkdown` -> `save-answer-selection` -> `saveAgentSelection` -> note 使用 Markdown 保存并再渲染。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-05 PF17 right rail note preview markdown rendering [Frontend]

**触达**:
- `packages/web/src/components/RightRail.vue:notePreviewMarkdown` - 右侧 Notes 预览改为返回去掉开头 source quote 后的 Markdown 原文,不再用纯文本压缩破坏标题、列表、表格和公式结构。
- `packages/web/src/components/RightRail.vue:notes panel template` - 折叠态 Note preview 改用 `props.renderMarkdown(...)` 和 `.md` 渲染,与展开正文共享 Markdown 渲染能力。
- `packages/web/src/components/RightRail.vue:.note-preview` - 为预览块增加最大高度、内部滚动和表格/标题/列表/KaTeX 的紧凑样式,避免 Markdown 预览撑爆右栏。

**入口**:右侧 Notes tab -> note memory card collapsed preview -> `notePreviewMarkdown` -> `renderMarkdown` -> `.note-preview.md`。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-05 PF18 right rail collapsed note preview visibility [Frontend]

**触达**:
- `packages/web/src/components/RightRail.vue:notes panel template` - 将 `.note-preview.md` 移入 `<summary>`,修复 `<details>` 折叠时非 summary 子节点被浏览器隐藏导致预览完全消失的问题。
- `packages/web/src/components/RightRail.vue:.note-preview` - 让折叠态 Markdown 预览占满 summary 一行并限制为 7.5rem 高度,保留一部分内容用于快速扫读。

**入口**:右侧 Notes tab -> 折叠 note memory card -> `<summary>` 内 `.note-preview.md` 始终可见;展开后 `.note-memory-card[open] .note-preview` 隐藏并显示完整 Markdown 正文。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-05 PF19 raw asset code block rendering [Frontend]

**触达**:
- `packages/web/src/App.vue:isRawAssetSegment/renderSegmentText` - code/table/image asset segment 改为原始文本 HTML escape,不再进入 `renderInlineMarkdown`,避免 fenced code 的换行和缩进被 inline 渲染压平。
- `packages/web/src/App.vue:renderSegWithFocus` - 段内高亮和 Source preview focus 仍复用同一套 range 包 `<mark>`,但 asset 文本切片按 raw escaped text 输出。
- `packages/web/src/style.css:.asset-code/.md pre` 与 `packages/web/src/App.vue:.source-preview-code` - code 块使用浅色 code panel、低对比边框、深灰代码字色,并用 `white-space: pre`、横向滚动保留代码布局。

**入口**:正文 code asset -> `ReaderPane` `<pre.asset-code>` -> `renderSeg`;Source preview code asset -> `sourcePreviewSingleHtml` -> `renderSourcePreviewSeg`。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。

## 2026-07-05 PF20 cross-LID highlight grouping [Frontend/Reader]

**触达**:
- `packages/web/src/App.vue:confirmHighlight/newHighlightGroupId` - 跨多个 LID 的一次选区高亮写入同一个 `highlight-group:*` 标识;正文 range 仍按 LID 分段保存,保证锚点和 mark 精确。
- `crates/server/src/lib.rs:/reader/highlight` 与 `crates/reader/src/lib.rs:Reader::highlight` - `/reader/highlight` 接收可选 `source_session_id` 并透传到 memory record,用于前端识别同一次跨 LID 高亮。
- `packages/web/src/App.vue:highlightCardsOf/highlightGroupMembers/deleteHighlight` 与 `packages/web/src/components/ReaderPane.vue:highlightCardsOf` - 正文和右栏 Highlight 卡片按 group 只展示一个代表;删除/修改代表时作用于整组。
- `packages/web/src/api.ts:MemoryRecord/highlight` - 暴露 `source_session_id` 并允许 highlight 请求携带 group 标识。

**入口**:正文跨 LID 选区 -> `confirmHighlight` -> 多次 `/reader/highlight` 带同一 group id -> `memory.recall` -> `highlightCardsOf/allHighlights` 聚合展示。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web);`cargo test -p reader -p runtime -p server`;`git diff --check`。

## 2026-07-05 PF21 Claude-inspired reader visual comfort [Frontend]

**触达**:
- `packages/web/src/style.css::root` - 将阅读相关色板调整为 Claude 参考的暖 cream/coral/dark warm ink 体系,新增 `--reader-*` tokens 供正文、批注和公式块复用。
- `packages/web/src/style.css:.reader-pane/.prose` - 收紧正文行宽、增大行高和段距,标题改为轻量 serif 层级,让长文阅读更接近 editorial pacing。
- `packages/web/src/style.css:.formula-open/.formula-inline-source/.hl-mark` - 公式从偏蓝按钮块改为暖色 inline math chip,高亮 mark 改为低噪 amber。
- `packages/web/src/style.css:.note-card/.hl-card/.block-actions/.asset-block` - note/highlight/action/asset 与正文分层:note 用 cream+coral,highlight 用 amber,操作按钮弱化为辅助控件。
- `packages/web/src/App.vue:.source-preview-code` - Source preview code block 同步去除蓝色强调,使用 coral-tinted code panel。

**入口**:主阅读区 `ReaderPane` -> `.reader-pane/.prose` 正文、公式、Note/Highlight 卡片、asset/code 块;Source preview code block 共享视觉语义。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web);`git diff --check`;本地 Vite `http://127.0.0.1:5173/` 返回 200。

## 2026-07-05 PF22 provider transport retry once [Runtime]

**触达**:
- `crates/runtime/src/lib.rs:NativeAdapter::post_chat_completions` - OpenAI-compatible `/chat/completions` 请求遇到传输层失败时短退避后重试一次;重试后仍失败继续返回 `PROVIDER_ERROR` 路径,不静默降级。
- `crates/runtime/src/lib.rs:is_retriable_chat_completion_error` - retry 边界限定为 `ureq::Error::Transport` 的 DNS/连接/IO/坏状态行/代理连接类错误;已拿到 HTTP status 的 provider 响应不重试。
- `crates/runtime/src/lib.rs:native_adapter_retries_once_after_transport_disconnect/native_adapter_does_not_retry_http_status_error` - 用本地 TCP 假 provider 覆盖断连后成功和 HTTP 500 不重试两条路径。

**入口**:右侧 Agent/`book.query` -> `NativeAdapter::complete/chat` -> `post_chat_completions` -> provider transport error retry once。
**测试**:`cargo test -p runtime`。

## 2026-07-05 PF23 native provider tool-name compatibility [Runtime]

**触达**:
- `crates/runtime/src/lib.rs:provider_tool_name/native_message_to_json` - Native tool-calling 对 provider 暴露 `^[A-Za-z0-9_-]+$` 风格工具名,将内部 `book.text` / `reader.goto` 映射为 `book_text` / `reader_goto`;历史 assistant tool_calls 同步按映射序列化。
- `crates/runtime/src/lib.rs:NativeAdapter::chat` - 请求前建立 provider-safe 名字表,响应解析时把 provider 返回的 tool_call 名称映射回内部 dotted 名,保持工具执行层和 ReAct 路径不变。
- `crates/runtime/src/lib.rs:adapter_http_error_message/truncate_provider_error_body` - HTTP status 错误不重试,但读取 provider 错误响应体并截断后带入 `PROVIDER_ERROR`,让 DeepSeek/OpenAI-compatible 400 能显示具体原因。
- `crates/runtime/src/lib.rs:provider_tool_name_sanitizes_dotted_names_and_collisions/native_adapter_maps_provider_safe_tool_names_back_to_runtime_names/native_adapter_does_not_retry_http_status_error` - 覆盖 dotted name、碰撞、历史消息映射、返回名反解和 status body passthrough。

**入口**:右侧 Agent 外层 loop -> `NativeAdapter::chat` -> provider-safe tool schema/messages -> provider tool_call response -> 内部 `ToolCall.name=book.text` -> deterministic tool dispatch。
**测试**:`cargo test -p runtime`。

## 2026-07-06 PF24 image asset render bundle [Build/Server/Frontend]

**触达**:
- `packages/core/src/segment.ts:SourceImageRef/SourceBlock.image` 与 `packages/core/src/md-adapter.ts` / `packages/core/src/epub-adapter.ts` - image 叶子除 `source.txt` 的 `![alt](src)` 原文外,保留图片引用元数据;EPUB 解析 XHTML 相对路径。
- `packages/core/src/asset-manifest.ts` 与 `skills/build/pass1-batch.ts` - 构建收口时复制 Markdown/EPUB/data-uri 图片到 `.understand-book/<bookId>/assets/images/`,写 `asset_manifest.json` 映射 image LID、原 src、mime、sha256、URL 和缺失原因。
- `crates/server/src/lib.rs:route_asset_manifest/route_book_asset_file` 与 `crates/server/src/main.rs` - `/book/asset_manifest` 返回图片索引;`/book/assets/...` 只在当前书目录 `assets/` 下服务二进制图片并拒绝路径穿越。
- `packages/web/src/api.ts`、`packages/web/src/App.vue`、`packages/web/src/components/ReaderPane.vue`、`packages/web/src/style.css` - 前端加载 asset manifest,正文与 Source preview 的 image LID 优先渲染真实 `<img>`,缺失时保留 alt/src/warning 占位。
- `docs/adr/0029-*` 与 `docs/切片方案-asset一等对象.md` - 回填图片文件是 render asset bundle,不改变 `source.txt` / LID citation truth。

**入口**:预构建 `pass1-batch.ts <book.md|epub>` -> `asset_manifest.json` + `assets/images/*`;阅读器 init -> `/book/asset_manifest`;正文 image LID -> `/api/book/assets/images/*`。
**测试**:`npm run test -- asset-manifest`;`npm run test`(packages/core);`npm run typecheck`(packages/core/web);`npm run build`(packages/web);`cargo test -p server`;`pass1-batch.ts understand-book.md --book-id understand-book-image-smoke --allow-partial` 产 `images=1 available=1`。

## 2026-07-06 PF25 open book library picker [Server/Frontend]

**触达**:
- `crates/server/src/lib.rs:route_book_library/list_book_library` - 新增 `GET /book/library`,从当前书目录的 `.understand-book` 父目录或工作目录 `.understand-book` 扫描带 `base.json` 的子目录,返回 `name/book_id/dir`。
- `packages/web/src/api.ts:api.bookLibrary` - 前端命令面新增 typed client,供打开书弹窗加载候选书目录。
- `packages/web/src/App.vue:openBook/loadBookLibrary/submitOpenBook` - TopBar Open book 从 `window.prompt` 改为 modal 选择器,列出 `.understand-book` 子目录,保留手输路径和刷新兜底,成功后沿用原状态重置和 `init()` 重载路径。

**入口**:TopBar `Open book` -> `/book/library` -> 选择 `.understand-book/<book_id>` -> `/book/open`。
**测试**:`cargo test -p server book_library`;`npm run typecheck`(packages/web)。
## 2026-07-06 PF26 per-book reading progress session [Server]

**触达**:
- `crates/server/src/lib.rs:SessionState/SessionBookProgress/load_session/save_session` - `session.json` 从单个 `{book_dir, top_lid}` 升级为 `{current_book_dir, books:{dir:{top_lid}}}`,保存当前书进度时保留其他书记录,并兼容读取旧格式。
- `crates/server/src/lib.rs:route_open_book` - 打开目标书时先查询该书已保存的 `top_lid`,重建 Reader 后恢复到对应位置,再写回当前书。
- `crates/server/src/lib.rs:select_start_book` 与 `crates/server/src/main.rs` - 启动时仍优先恢复最后打开且可加载的书,但 `top_lid` 从对应书的进度表读取;请求目录也能恢复自己的历史进度。

**入口**:`/reader/goto`/`/reader/scroll` -> `save_session`;TopBar `Open book` -> `/book/open` -> per-book top restore;server 启动 -> `select_start_book`。
**测试**:`cargo test -p server session_`;`cargo test -p server book_open`。

## 2026-07-06 PF27 paper minimap left rail [Frontend]

**触达**:
- `packages/web/src/App.vue:loadPaperProjectionData` - paper 首页消费层收窄为 reading guide + structure projection,不再为顶部 workbench 拉 metadata/lexicon 详情。
- `packages/web/src/App.vue:LeftRail props` - 删除横向 `profile-shell` workbench,把 paper mode/preset/proposal/structure rows/pinned evidence 传入左栏。
- `packages/web/src/components/LeftRail.vue:paper-minimap` - 新增 paper 小地图,提供阅读模式切换、当前位置摘要、结构跳转和 pinned evidence 跳转。
- `packages/web/src/style.css:paper-minimap` - 新增左栏紧凑小地图样式,保持正文主阅读区不被 paper workbench 挤占。

**入口**:paper profile 初始化 -> `api.state()` + `api.profileManifest()` + `loadPaperProjectionData()` -> `LeftRail` paper minimap;mode button -> `POST /reader/layout.apply` 的 `set_layout_preset` action/proposal。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web);`git diff --check`。

## 2026-07-06 PF28 paper author superscript rendering [Frontend]

**触达**:
- `packages/web/src/md.ts:renderInlineMarkdown/renderMarkdown` - 将 `$ ^{1,2,3} $` 这类清洗后作者单位裸上标渲染为安全 `<sup>`,普通 `$ x^2 $` 仍走 KaTeX。
- `packages/web/src/md.test.ts` - 覆盖作者上标、普通行内数学和普通美元文本三种路径。
- `packages/web/src/style.css:inline-citation-sup` - 为正文中的作者单位上标补齐稳定右上标样式。

**入口**:正文/Source preview/Note/Agent Markdown -> `renderInlineMarkdown` 或 `renderMarkdown` -> `$ ^{...} $` 裸上标显示为右上标。
**测试**:`tsx` 直接验证三类输入输出;`npm run typecheck`(packages/web);`npm run build`(packages/web);`git diff --check`。Vitest 在当前沙箱内 worker spawn 受限,未完成运行。
## 2026-07-06 PF29 raw HTML image parsing [Core/Frontend]

**触达**:
- `packages/core/src/md-adapter.ts:parseHtmlImageLine` - Markdown 输入中的独立 raw HTML `<img ...>` 行会解析为 image SourceBlock,支持远程 `https` URL、query string、`alt`,以及 PDF 转 Markdown 常见的 `<div style="text-align: center;"><img ... /></div>` 包裹。
- `packages/core/test/md-adapter.test.ts` 与 `packages/core/test/asset-manifest.test.ts` - 覆盖 div-wrapped raw HTML 图片被识别为 image leaf,并在 manifest 中以 `status: external` 记录原始 URL。
- `packages/web/src/App.vue:imageRenderSrc/sourcePreviewSingleHtml` - Source preview 对外部图片使用 `original_src` 直接渲染,本地打包图片仍优先使用 `url_path`。
- `packages/web/src/components/ReaderPane.vue:imageRenderSrc` - 正文 image LID 同步支持 external 图片显示,缺失或失败资源仍保留占位信息。

**入口**:网页/Markdown 导入含 `<div ...><img src="https://...jpg?..." /></div>` -> `parseHtmlImageLine` -> image LID -> `asset_manifest.json` external entry -> Reader/Source preview `<img src=original_src>`。
**测试**:`npm run test -- --pool=threads --poolOptions.threads.singleThread=true md-adapter asset-manifest`(packages/core);`npm run typecheck`(packages/core/web);`npm run build`(packages/web);`git diff --check`。

## 2026-07-09 PH0 paper PDF-first reconciled source docs [Docs]

**触达**:
- `docs/adr/0062-paper-pdf-first-hybrid-source-markdown-semantic-pdf-visual-map.md` - 标记旧 Markdown-as-truth 假设被 ADR-0063 supersede。
- `docs/adr/0063-paper-pdf-first-reconciled-source-build-workbench.md` - 新增 PDF-first paper 的 reconciled `source.txt`、source reconciliation、PDF map、Build Workbench 和决策持久化边界。
- `docs/adr/0064-paper-pdf-first-grill-traceability-gate.md` - 新增 Grill traceability gate,要求每个 Grill 决策都有 covered/superseded/deferred 落点。
- `docs/切片方案-paper-pdf-first-hybrid.md` - 重写为 v2 实施切片,覆盖 source reconciliation -> hybrid foundation -> Workbench -> PDF reader surface,并追加 Grill Q1-Q68 覆盖表。
- `CONTEXT.md` - 更新 Hybrid paper source / PDF map 术语,新增 Source reconciliation、PDF selection map、Build Workbench、BuildDecisionRequest。

**入口**:后续 PDF-first paper 实现从 ADR-0063 和 `docs/切片方案-paper-pdf-first-hybrid.md` 开工,不再按 ADR-0062 的 raw Markdown truth 假设实现。
**测试**:`rg -n "G68|G67|G1 text geometry" docs/切片方案-paper-pdf-first-hybrid.md`;`git diff --check`。

## 2026-07-09 PH1 PDF-first schema fixtures [Core]

**触达**:
- `packages/core/src/source-reconciliation.ts:SourceReconciliationReport` - 新增 `source_reconciliation_report.v1` 类型、空 summary helper 和 trusted gate helper。
- `packages/core/src/source-manifest.ts:buildSourceManifestV2` - 新增 `source_manifest.v2` builder,保留 legacy manifest 路径,显式表达 PDF capability 状态。
- `packages/core/src/pdf-source-map.ts:PdfSourceMap` - 新增 `pdf_source_map.v1` 与 backend-only `pdf_selection_map` manifest/page shard 类型。
- `packages/core/src/zod.ts:SourceManifestV2Z/PdfSourceMapZ/PdfSelectionMapManifestZ/SourceReconciliationReportZ` - 新增 PH1 schema 自检,`SourceManifestZ` 兼容 legacy 与 v2。
- `packages/core/test/source-manifest.test.ts` / `packages/core/test/pdf-source-map.test.ts` - 覆盖 verified、auto_repaired、needs_review、stale、degraded/unavailable capability 状态。

**入口**:PH2-PH5 build code 通过这些 schema 产出并校验 source reconciliation、source manifest、PDF source map 和 selection map artifacts。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`。

## 2026-07-09 PH2 clean-room PDF geometry adapter [Core]

**触达**:
- `packages/core/package.json` / `pnpm-lock.yaml` - 新增官方 `pdfjs-dist` 依赖,仅通过公开 package API 读取 PDF。
- `packages/core/src/pdf-geometry.ts:extractPdfTextGeometry` - 新增 clean-room adapter,抽取 page metadata、chars、words、lines 和 PDF user-space bbox,不推断语义结构。
- `packages/core/test/pdf-geometry.test.ts` - 用测试内生成的最小 born-digital PDF 验证页面尺寸、字符、词、行和 bbox。

**入口**:后续 PH3 source reconciliation 调 `extractPdfTextGeometry(pdfBytes)` 获取 PDF text geometry,再由 deterministic aligner 决定可信 source。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`。

## 2026-07-09 PH3 source reconciliation gate [Core/Build]

**触达**:
- `packages/core/src/source-reconciliation.ts:reconcilePaperSource` - 新增 monotonic forward fuzzy v1 reconciliation,exact/safe repair 才可信,fuzzy 内容差异进入 review。
- `packages/core/src/source-reconciliation.ts:writeSourceReconciliationArtifacts` - 写 `.build/source-reconciliation/{report,review-draft,review-decisions,source.txt?}`,unresolved 时不写 trusted stage source。
- `packages/core/test/source-reconciliation.test.ts` - 覆盖 verified、auto_repaired、needs_review 阻断和 source.txt 写入门禁。
- `skills/build/source-reconcile.ts` - 新增 CLI:`paper.md + paper.pdf` 调 PH2 geometry adapter 后写 source reconciliation stage artifacts,unresolved 非零退出 1。

**入口**:`pnpm exec tsx skills/build/source-reconcile.ts <paper.md> <paper.pdf> [--book-id <id>] [--out-dir <dir>]`。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;临时 PDF CLI smoke 验证 trusted exit=0/blocked exit=1。

## 2026-07-09 PH4 review candidate gate [Core/Build]

**触达**:
- `packages/core/src/source-reconciliation.ts:contentEquivalent` - 新增 LLM format repair 的 content-equivalence 签名,防止内容改写进入 trusted source。
- `packages/core/src/source-reconciliation.ts:reviewCandidateAndReconcile` - LLM/manual reviewed draft 都重新进入 PH3 reconciliation gate,只有 unresolved=0 才 accepted。
- `packages/core/src/source-reconciliation.ts:writeSourceReconciliationArtifacts` - 支持单独写 `reviewed-draft.md`,不替代 `review-draft.md` 或绕过 trusted `source.txt` 门禁。
- `packages/core/test/source-reconciliation.test.ts` - 覆盖 LLM 格式修复接受、LLM 内容改写拒绝、manual draft 重新对齐和 reviewed artifact 写入。
- `skills/build/source-reconcile.ts` - CLI 新增 `--reviewed-draft` / `--review-kind llm_format_repair|manual_review`。

**入口**:`source-reconcile.ts ... --reviewed-draft reviewed.md --review-kind manual_review`。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;临时 PDF CLI smoke 验证 manual reviewed draft exit=0 且写 `reviewed-draft.md/source.txt`。

## 2026-07-09 PH5 hybrid foundation artifacts [Core/Build]

**触达**:
- `packages/core/src/hybrid-foundation.ts:buildHybridFoundation` - 从 trusted `source.txt` + PDF geometry 生成 `base.json`、`source_manifest.v2`、`pdf_source_map.v1`、`pdf_selection_map` shards 和 `alignment_report.v1`。
- `packages/core/src/hybrid-foundation.ts:assertHybridFoundationHardGates` - 校验 source map LID 引用、PDF region page bounds 和 selection shard hash。
- `packages/core/src/hybrid-foundation.ts:writeHybridFoundationArtifacts` - 写 reader 可加载的 PH5 artifact 集合,不运行 paper metadata/lexicon/discourse/Pass2/BookStructure。
- `packages/core/src/zod.ts:AlignmentReportZ` - 新增 alignment report schema 自检。
- `packages/core/test/hybrid-foundation.test.ts` - 覆盖 artifact schema、degraded PDF capability、hard gates 和文件写出。
- `skills/build/hybrid-foundation.ts` - 新增 CLI:`trusted source.txt + paper.pdf` 收口到 hybrid foundation artifacts。

**入口**:`pnpm exec tsx skills/build/hybrid-foundation.ts <source.txt> <paper.pdf> [--book-id <id>] [--out-dir <dir>]`。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;临时 PDF CLI smoke 验证 7 个 PH5 artifact 写出。

## 2026-07-09 PH6 Build Workbench minimum shell [Core]

**触达**:
- `packages/core/src/build-workbench.ts:BUILD_STAGE_DAG` - 显式声明 source reconciliation -> hybrid foundation -> pass1 -> paper projection 的阶段 DAG,把 BookStructure/PaperReadingGuide 作为后续投影阶段挂在 DAG 尾部。
- `packages/core/src/build-workbench.ts:readBuildWorkbenchSnapshot/detectBuildReadiness` - 从书目录读取 `report.json`、`source.txt`、`base.json`、`source_manifest.json`、PDF map/selection/alignment hash,判定 reader vs Build Workbench;missing、needs_review、stale、incomplete 都不会进入 normal reader。
- `packages/core/src/build-workbench.ts:createBuildJob/reuseOrCreateBuildJob/markJobStaleIfInputChanged` - 新增 `BuildJobState` 最小状态机,同 input fingerprint 的未完成 job 复用,不同 fingerprint 的旧 job 标记 `stale_input`。
- `packages/core/src/build-workbench.ts:setActiveExecutor/requestBuildDecision/requestExecutorPermission` - 记录 active executor telemetry,并把 build-direction decision 与 executor permission 分成两类请求和事件。
- `packages/core/test/build-workbench.test.ts` - 覆盖 Workbench 路由、可信 foundation 进 reader、hash stale、同输入 job 复用、输入变更 stale、决策/权限事件分离。

**入口**:`readBuildWorkbenchSnapshot(bookDir,{current_input_fingerprint}) -> detectBuildReadiness(snapshot)`;构建编排层用 `reuseOrCreateBuildJob` 管 job shell,但 reader trust gate 只信 artifact/hash/schema recomputation。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`。

## 2026-07-09 PH7a PDF reader backend endpoints [Server]

**触达**:
- `crates/server/src/lib.rs:route_source_manifest/route_pdf_source_map` - 新增 `GET /book/source_manifest` 与 `GET /book/pdf_source_map`,从当前书目录读取 runtime artifact,并按 `source_manifest.v2` capability 阻断 unavailable/stale map。
- `crates/server/src/lib.rs:route_book_asset_file/route_original_pdf_file` - 扩展现有 binary bypass,支持 `GET /book/pdf/original`,只服务 `source_manifest.json` 声明的 original PDF。
- `crates/server/src/lib.rs:route_pdf_selection_resolve` - 新增 `POST /reader/pdf_selection.resolve`,从 backend-only `pdf_selection_map` page shard 将 PDF rect 解析为 LID + UTF-16 range,quote 重新从 `Book::text` 切出。
- `crates/server/src/lib.rs:route_pdf_ranges_project` - 新增 `POST /reader/pdf_ranges.project`,把 semantic LID range 投影到 `pdf_source_map` region;line/block fallback 明确返回 `lid_region_fallback`。
- `crates/server/src/lib.rs:pdf_runtime_endpoints_expose_manifest_map_original_selection_and_range_projection` - 覆盖 manifest/map/PDF binary/selection resolve/range project 的端到端 server route 行为。

**入口**:`/book/source_manifest`;`/book/pdf/original`;`/book/pdf_source_map`;`/reader/pdf_selection.resolve`;`/reader/pdf_ranges.project`。
**测试**:`cargo test -p server`。

## 2026-07-09 PH7b PDF reader center surface [Frontend]

**触达**:
- `packages/web/src/api.ts:SourceManifestV2/PdfSourceMap/api.sourceManifest/api.pdfSourceMap/api.pdfOriginalUrl` - 前端新增 PH7 PDF runtime artifact 类型与端点 client。
- `packages/web/src/components/PdfReaderPane.vue` - 新增互斥中心 PDF surface:原 PDF native frame + 全量 page shell + `pdf_source_map` LID region overlay;active LID 跳转滚动到对应 page shell。
- `packages/web/src/App.vue:loadPdfRuntimeArtifacts/pdfReaderAvailable` - 初始化时读取 `source_manifest.v2` 与完整 `pdf_source_map`;PDF capability 可用时中心区切换为 `PdfReaderPane`。
- `packages/web/src/App.vue:doGoto` - PDF surface 下 LID 无 mapped region 时打开 Markdown Source preview,不把 page/bbox 当 citation anchor。

**入口**:reader init -> `/book/source_manifest` -> `/book/pdf_source_map` -> `PdfReaderPane`;TopBar/outline/agent goto -> PDF region overlay 或 Source preview fallback。
**测试**:`npm run typecheck`(packages/web);`npm run build`(packages/web)。
## 2026-07-09 PH8 paper projection chain on trusted source [Core/Build/Read]

**触达**:
- `packages/core/src/paper-projection-chain.ts:assertTrustedPaperProjectionSource/buildPaperProjectionChainPlan` - 新增 PH8 trusted source gate,要求 `source_manifest.v2` canonical source 为 `source.txt`、sha256 匹配、source reconciliation report 无 unresolved,并把 projection failure 与 source truth 明确隔离。
- `packages/core/test/paper-projection-chain.test.ts` - 覆盖 projection plan 阶段顺序、全部 batch 参数指向 reconciled `source.txt`、stale source hash 拦截、unresolved report 拦截和 workspace run target guard。
- `skills/build/paper-projection-chain.ts` - 新增 build runner:默认写 `.build/paper-projection-chain/plan.json`;显式 `--run` 时依次调用现有 `paper-metadata-batch`、`paper-lexicon-batch`、`profile-sidecar-batch`、`pass2-batch`、`book-structure-batch`,并跑 PaperReadingGuide smoke。
- `crates/read-tools/src/main.rs:paper_reading_guide` - CLI 增加读时 projection smoke 入口,直接加载同一 book dir 的 `source.txt/base.json/sidecars`,不落地新的 paper truth。

**入口**:`pnpm exec tsx skills/build/paper-projection-chain.ts .understand-book/<book_id> [--allow-partial] [--run]`;BookStructure smoke fixture 可显式加 `--seed-book-structure-smoke`,正常生产路径仍依赖已接受的 `.build/book-structure` artifacts。
**测试**:`pnpm -C packages/core test -- paper-projection-chain`;`pnpm -C packages/core typecheck`;`cargo test -p read-tools`;`.\\node_modules\\.bin\\tsx.ps1 skills/build/paper-projection-chain.ts .understand-book/ph8-smoke --allow-partial --run --seed-book-structure-smoke`。
## 2026-07-09 PH9 natural-language sidecar planning follow-up [Core/Build]

**触达**:
- `packages/core/src/sidecar-plan.ts:draftSidecarPlan/confirmSidecarPlan/compileSidecarBuildSpec` - 新增 Q68 两步 sidecar planning contract:自然语言只能先生成 confirmable `sidecar_plan.v1` + `form_draft.v1`,未确认时 `compileSidecarBuildSpec` 拒绝进入 custom sidecar generation。
- `packages/core/src/sidecar-plan.ts:DEFAULT_SIDECAR_OPTIONS` - 内置 timeline、concept_map、comparison_table、argument_map 默认选项,`custom` 保留为最后 escape hatch;所有默认 output contract 都要求 `lid_required` evidence。
- `packages/core/test/sidecar-plan.test.ts` - 覆盖默认选项顺序、自然语言 draft 推断、未确认阻断、确认后编译 spec、custom 仍需确认和 LID evidence gate。
- `skills/build/sidecar-plan.ts` - 新增 Build Workbench extension CLI,在 `.build/sidecar-plan/` 写 `sidecar_plan.json` / `form_draft.json`;显式 `--confirm` 才额外写 `sidecar_build_spec.json`。
- `docs/切片方案-paper-pdf-first-hybrid.md:G68` - 从 deferred 改为 covered,落点为 PH9 confirmable plan/form。

**入口**:`pnpm exec tsx skills/build/sidecar-plan.ts <book_dir> --request <text> [--target-view ...] [--lids ...] [--confirm]`。
**测试**:`pnpm -C packages/core test -- sidecar-plan`;`pnpm -C packages/core typecheck`;`.\\node_modules\\.bin\\tsx.ps1 skills/build/sidecar-plan.ts .understand-book/ph8-smoke --request "Compare the datasets and methods" --lids 1.1,2.1`;同命令加 `--confirm` 写出 `sidecar_build_spec.json`。

## 2026-07-11 WD1 Windows desktop reader distribution [Desktop/Server/Web/Plugin]

**触达**:
- `apps/desktop/src-tauri/src/main.rs` / `plugin_manager.rs` - Tauri Reader host、维护命令、Codex PATH/App 探测、marketplace/plugin 安装和所有权回执卸载。
- `apps/desktop/src-tauri/tauri.conf.json` / `windows/installer-hooks.nsh` - currentUser NSIS、sidecar、Reader 资源、安装同意与按回执卸载钩子。
- `crates/server/src/host.rs` / `crates/server/src/lib.rs` - 可复用随机端口 host、无书 bootstrap、稳定默认书库根和外部 workspace 注册表。
- `packages/web/src/App.vue` / `components/TopBar.vue` - 无书时先打开书库;仅桌面显示 Codex plugin 状态/重试设置。
- `skills/build/sidecar-entry.ts` / `automatic-build.ts` - Bun 单 exe dispatcher、内嵌 extractor prompt、自指 input/write/close/attempt 命令。
- `.agents/plugins/marketplace.json` / `plugins/understand-book` - 公开 Git marketplace 的精简 Codex plugin 包。
- `apps/desktop/scripts/` - sidecar 编译、公共 Git release 门禁和 `UnderstandBookSetup.exe` 导出。

**入口**:`UnderstandBookSetup.exe` -> Reader 安装 -> 可选 Codex plugin 安装;后续 `UnderstandBook.exe` -> random localhost Reader;Codex `$understand-book-build <target>` -> installed sidecar `next` loop。
**测试**:`cargo test -p server`(83);`cargo test -p understand-book-desktop --offline`(3);`pnpm -C packages/web test`(49);Web typecheck/build;sidecar next/prompt/emit-input smoke;plugin/skill validator;Tauri debug no-bundle 与 NSIS bundle。

## 2026-07-11 WD2a persisted desktop library root [Desktop/Server]

**触达**:
- `apps/desktop/src-tauri/src/library_settings.rs:LibrarySettingsStore` - 解析父目录或现有 `.understand-book`,验证可写性并原子持久化用户书库根目录。
- `apps/desktop/src-tauri/src/main.rs:set_desktop_library_directory` - 验证和写入成功后切换当前 server 的书库根目录。
- `crates/server/src/host.rs:RunningServer.set_library_root/bootstrap_book` - 支持运行时切换,已保存目录不可用时仍以纯内存 bootstrap 启动且不重建路径。
- `crates/server/src/lib.rs:route` - `/desktop/status` 暴露 `library_root_available`,供桌面 UI 阻断不可用书库。

**入口**:Tauri `set_desktop_library_directory` command -> `LibrarySettingsStore.apply_selection` -> `RunningServer.set_library_root`。
**测试**:`cargo test -p understand-book-desktop --offline`;`cargo test -p server desktop_ --offline`;`cargo test -p server book_library --offline`。

## 2026-07-11 WD2b desktop Provider persistence and hot reload [Desktop/Runtime]

**触达**:
- `crates/runtime/src/lib.rs:ProviderConfig.from_values/ProviderMode.as_str` - 统一校验 native/react、非空 Key/Model 与 http(s) Base URL,供环境变量和桌面设置共用。
- `apps/desktop/src-tauri/src/library_settings.rs:PersistedProviderSettings/LibrarySettingsStore.apply_provider` - 在同一用户设置中原子保存明文 Provider 配置,并与书库根互相保留。
- `apps/desktop/src-tauri/src/main.rs:desktop_provider_status/save_desktop_provider_settings` - 状态不回传 Key;空白 Key 保留既有值;写入成功后替换当前 server adapter。
- `crates/server/src/host.rs:RunningServer.set_provider_config` - 进程内原子替换 `AppState.adapter`,不重启 server 或阅读会话。
- `apps/desktop/src-tauri/build.rs` / `capabilities/default.json` - 为 Provider 与既有 desktop commands 生成并授予 Tauri ACL 权限。

**入口**:Tauri `desktop_provider_status` / `save_desktop_provider_settings` -> `ProviderConfig.from_values` -> `RunningServer.set_provider_config`。
**测试**:`cargo test -p understand-book-desktop --offline`(11);`cargo test -p runtime provider_config --offline`(2);`cargo test -p server book_query_provider_error_502 --offline`。

## 2026-07-11 WD2c desktop library and Provider settings UI [Web/Desktop]

**触达**:
- `packages/web/src/App.vue:openDesktopSettings/chooseDesktopLibraryDirectory` - 用 Tauri 原生文件夹选择器切换书库,不可用持久化路径会阻断内容初始化并要求重新选择。
- `packages/web/src/App.vue:refreshDesktopProviderStatus/saveDesktopProviderSettings` - 设置页编辑 Native/ReAct、Base URL、Model 与密码输入;保存后热应用,空白 Key 保留旧值。
- `packages/web/src/desktop-library.ts:desktopLibraryNeedsSelection` - 纯函数判定不可用书库启动门禁。
- `packages/web/src/desktop-provider.ts:desktopProviderDraft/desktopProviderStatusLabel` - 只用无密钥状态投影表单与用户反馈。
- `apps/desktop/src-tauri/capabilities/default.json` / `permissions/autogenerated` - localhost WebView 获得目录、Provider 与 Codex plugin command ACL。

**入口**:TopBar 设置 -> 书库“选择目录”或 Provider“保存并应用” -> Tauri command -> 当前 localhost Reader server。
**测试**:`pnpm -C packages/web test`(53);`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;`cargo test -p understand-book-desktop --offline`(11);`pnpm -C apps/desktop package:windows` 生成 `dist/UnderstandBookSetup.exe`(SHA-256 `F7E666CE0B3209CA4BE7A29E417933F775F6341A60DCD150FF05B760E1BF88DB`)。

## 2026-07-11 Reader trust 自动进入阅读 [Web]

**触达**:
- `packages/web/src/surface-selection.ts:chooseAppSurface` - 以 artifact `readiness.route` 作为主界面唯一权威,移除 sessionStorage Workbench 恢复和 job 状态阻塞。
- `packages/web/src/App.vue:showSurfaceForWorkbenchSnapshot/applyWorkbenchAction/refreshBuildWorkbench` - 阶段动作或轮询首次看到可信 snapshot 时运行完整 reader 初始化;用户主动打开时用当前会话内存标记保留诊断 Workbench。
- `packages/web/src/App.vue:enterReader` / `components/BuildWorkbenchPane.vue:canEnterReader` - 进入按钮不再被残留 running/needs_user/interrupted job 禁用或静默拦截。
- `packages/web/src/surface-selection.test.ts` / `components/BuildWorkbenchPane.test.ts` - 覆盖可信后自动选 reader 和 interrupted job 不阻塞按钮。

**入口**:Workbench 阶段动作或自动轮询 -> snapshot `readiness.route=reader` -> `init()` -> reader;可信 Workbench 的“进入阅读”走同一路径。
**测试**:`pnpm -C packages/web test`(10 files / 54 tests);`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;`pnpm -C apps/desktop package:windows` 生成 `dist/UnderstandBookSetup.exe`(SHA-256 `FFF4D8E9076BD7C4AA5830B98EE6FA23596C7DE50DE97D9F9C8B2C52B8A0C1D2`)。

## 2026-07-12 AM1 Agentic paper minimap contracts [Read/Reader/Web]

**触达**:
- `crates/read-tools/src/lib.rs:PaperMinimapBase/PaperRegion/PaperLandmark/PaperArgumentRelation` - 新增只读地图、区域、地标、关系、局部论证槽位及 availability/provenance 闭集。
- `crates/reader/src/lib.rs:ReaderPaperMinimapState/PaperMinimapAction/SavedUserOverlayAction/PaperMinimapEffect/PaperMinimapProposal` - 新增 reader 会话态、session/saved command、effect/proposal/noop wire contract,尚未接 reducer。
- `packages/web/src/generated/PaperMinimapBase.ts:PaperMinimapBase` - ts-rs 导出只读地图依赖图。
- `packages/web/src/generated/PaperMinimapApplyOutcome.ts:PaperMinimapApplyOutcome` - ts-rs 导出 reader minimap action outcome 依赖图。
- `crates/read-tools/src/lib.rs:paper_minimap_base_contract_round_trips_closed_types` - 覆盖 base JSON shape、snake_case 枚举、关系 `type` 字段与缺失/未知值拒绝。
- `crates/reader/src/lib.rs:paper_minimap_reader_contract_round_trips_commands_and_outcome/paper_minimap_saved_actions_are_proposal_safe_and_closed` - 覆盖 session/saved tagged commands、state/effect/proposal round-trip 与非法 action 拒绝。

**入口**:AM1 仅建立 Rust 权威 DTO -> ts-rs generated TypeScript contract;runtime/HTTP/UI 尚无可调用入口。
**测试**:`cargo test -p read-tools -p reader`(read-tools 116 / reader 38);`pnpm -C packages/web typecheck`。

## 2026-07-12 AM2 Agentic paper minimap topology projector [Read]

**触达**:
- `crates/read-tools/src/lib.rs:load_paper_minimap_artifacts` - 宽容读取 `source_manifest.json`/`pdf_source_map.json`;缺失、坏 JSON 或 IO 错误只进入地图 warnings,不改变 `Book::load` 成败。
- `crates/read-tools/src/lib.rs:validated_paper_minimap_artifacts` - 严格校验 artifact version/book_id/capability/config hash/page identity/leaf LID/source span/page reference,任一失配使地图 topology unavailable。
- `crates/read-tools/src/lib.rs:select_paper_region_units/classify_paper_heading` - 按 structural root/section 规则选择区域,heading-first 映射标准标题及别名,其余明确为 unknown。
- `crates/read-tools/src/lib.rs:Book::paper_minimap` - 聚合 section subtree 叶 LID 的 PDF 页范围,生成稳定 region ids、LID spans、page spans 与 FNV-1a identity fingerprint;AM3 前 landmarks/arguments 独立 unavailable,整体 degraded。
- `crates/read-tools/src/lib.rs:paper_minimap_projects_heading_alias_unknown_and_cross_page_region/paper_minimap_stale_config_degrades_without_breaking_book_load` - 覆盖标准标题、Methods 别名、unknown、跨页聚合、稳定 fingerprint 与 stale config 降级。

**入口**:`Book::load(workspace)` -> optional minimap artifacts -> `Book::paper_minimap()`;尚未暴露 HTTP/runtime/UI。
**测试**:`cargo fmt --package read-tools -- --check`;`cargo test -p read-tools -p reader`(read-tools 118 / reader 38);`pnpm -C packages/web typecheck`。

## 2026-07-12 AM3 Agentic paper minimap semantic projector [Read]

**触达**:
- `crates/read-tools/src/lib.rs:RuntimePass2Audit/load_paper_minimap_artifacts` - 宽容挂载 `pass2_audit.json`;只读取 accepted,缺失/坏文件不阻断 Book 或 topology。
- `crates/read-tools/src/lib.rs:discourse_landmark_kind/book_structure_key_stop_kind/book_structure_spine_kind` - 把 paper discourse、key stop 与明确 spine role 规范化到地标闭集,模糊输入保守为 other 或不投影。
- `crates/read-tools/src/lib.rs:normalize_graph_relation/normalize_discourse_relation` - 只接受冻结关系映射;paper supports/tests 依语义反转端点,未知边不进入地图。
- `crates/read-tools/src/lib.rs:Book::project_paper_minimap_semantics` - 按 `{kind,anchor_lid}` 去重并合并 BookStructure/discourse/graph/Pass2 provenance;统一解析 PDF 页坐标后再解析关系端点。
- `crates/read-tools/src/lib.rs:Book::project_graph_minimap_relations` - Pass2 header、graph endpoint、split evidence 覆盖与 dangling LID 硬门禁;pending/rejected/gate_dropped 不消费。
- `crates/read-tools/src/lib.rs:paper_minimap_projects_deduped_landmarks_provenance_and_closed_relations` 等 - 覆盖 provenance 合并、paper edge 反转、pending 排除、悬空 evidence、语义 sidecar 缺失与独立 layer degradation。

**入口**:`Book::paper_minimap()` topology 完成后调用 semantic projector,输出同一 `PaperMinimapBase.landmarks/relations/layer_status`。
**测试**:`cargo fmt --package read-tools -- --check`;`cargo test -p read-tools -p reader`(read-tools 121 / reader 38);`pnpm -C packages/web typecheck`。

## 2026-07-12 AM4 Agentic paper minimap ModeLens [Reader/Web]

**触达**:
- `crates/reader/src/lib.rs:PaperArgumentSlotBinding/PaperMinimapLensProjection` - 新增只引用 base IDs 的 lens 结果契约,由 ts-rs 生成 Web 类型。
- `crates/reader/src/lib.rs:minimap_slot_for_landmark` - 固定 abstract/introduction/method/results/discussion/conclusion/related_work grammar;unknown/references 无局部槽位。
- `crates/reader/src/lib.rs:project_paper_minimap_lens` - 纯函数应用 skim/abstract/deep,按槽位、PDF page、anchor、ID 稳定排序;全局 5 点、局部 4 点、关系 3 条硬预算。
- `crates/reader/src/lib.rs:paper_minimap_lens_enforces_global_local_and_relation_budgets` 等 - 覆盖 5/4/3、results 四槽、unknown 空投影、abstract 缺失 fallback 与输出 ID 不伪造。
- `packages/web/src/generated/PaperMinimapLensProjection.ts` - lens wire contract。

**入口**:`project_paper_minimap_lens(base,mode,focus_region_id)`;尚未接 reducer/HTTP/UI。
**测试**:`cargo fmt --package read-tools --package reader -- --check`;`cargo test -p read-tools -p reader`(read-tools 121 / reader 43);`pnpm -C packages/web typecheck`。

## 2026-07-12 AM5 Agentic paper minimap reducer [Reader/Web]

**触达**:
- `crates/reader/src/lib.rs:PaperMinimapActor` - 明确 user/agent actor wire contract。
- `crates/reader/src/lib.rs:default_paper_minimap_state` - Reader 创建时绑定 base fingerprint,默认 collapsed/skim 与空 session/saved overlays。
- `crates/reader/src/lib.rs:apply_minimap_session_action/apply_minimap_saved_action` - 校验 presentation 权限、region/landmark ID、5/4 预算、region grammar、layer 闭集、saved LID/override/preference。
- `crates/reader/src/lib.rs:Reader::apply_paper_minimap_commands` - 整批 scratch-state 原子验证;user session direct effect,agent presentation forbidden,agent mode 与全部 saved commands 生成 proposal;重复状态返回 noop。
- `crates/reader/src/lib.rs:Reader::apply_paper_minimap_proposal/undo_paper_minimap_effect` - 复验 base fingerprint/state rev,保存 before/after 并支持 stale-safe undo。
- `crates/reader/src/lib.rs:paper_minimap_reducer_*` - 覆盖 effect/noop/undo、权限、saved/mode proposal、stale、坏 ID/grammar/预算。
- `packages/web/src/generated/PaperMinimapActor.ts` - actor wire contract。

**入口**:`Reader::paper_minimap_state/apply_paper_minimap_commands/apply_paper_minimap_proposal/undo_paper_minimap_effect`;尚未接 server persistence/HTTP。
**测试**:`cargo fmt --package read-tools --package reader -- --check`;`cargo test -p read-tools -p reader`(read-tools 121 / reader 47);`pnpm -C packages/web typecheck`。

## 2026-07-12 AM6 Agentic paper minimap private overlay store [Reader/Server]

**触达**:
- `crates/server/src/lib.rs:PaperMinimapOverlayStore/StalePaperMinimapOverlayItem` - `paper_minimap_overlays.v1` envelope 只承载 SavedUserOverlay 与 stale records。
- `crates/server/src/lib.rs:paper_minimap_overlay_path/load_paper_minimap_overlay_store/write_paper_minimap_overlay_store` - store 与 session.json 同 user-data 目录,missing 默认空,corrupt/version mismatch 显式错误,temp+rename 提交。
- `crates/server/src/lib.rs:reanchor_saved_paper_minimap_overlay` - 旧 book version 仅保留新 base 中仍存在的 custom LID 和稳定 landmark ID;失败条目记录 stale,不猜邻近 LID。
- `crates/server/src/lib.rs:load_saved_paper_minimap_overlay/save_saved_paper_minimap_overlay` - 按 book_id+book_version 精确取存,无精确版本时迁移并物化当前版本。
- `crates/reader/src/lib.rs:Reader::restore_saved_user_overlay` - Reader 注入前复验 book/version、custom LID 与 base landmark IDs。
- `crates/server/src/host.rs:start_server` / `crates/server/src/lib.rs:route_book_open` - 冷启动和换书时恢复 saved overlay;corrupt store 阻断并报告,不覆盖原文件。
- `crates/server/src/lib.rs:paper_minimap_overlay_store_*` - 覆盖 roundtrip、无 session/base 字段、restart、version migration/stale 与 corruption isolation。

**入口**:`<user-data>/paper-minimap-overlays.json` -> `restore_saved_paper_minimap_overlay` -> Reader;confirmed proposal 的保存将在 AM7 HTTP apply 后调用。
**测试**:`cargo fmt --package read-tools --package reader --package server -- --check`;`cargo test -p read-tools -p reader -p server`(121 / 47 / 88);`pnpm -C packages/web typecheck`。

## 2026-07-12 AM7 Agentic paper minimap HTTP/runtime surface [Runtime/Server/Web]

**触达**:
- `crates/server/src/lib.rs:route_book("paper_minimap")` - `GET /book/paper_minimap` 返回只读 base。
- `crates/server/src/lib.rs:route_mut("/reader/paper_minimap.state")` - 返回 `{base,state,lens}`;base unavailable 时仍返回 state 与 typed lens_error。
- `crates/server/src/lib.rs:route_mut("/reader/paper_minimap.apply")` - commands/proposal_id 共用 reducer;confirmed saved effect 成功后写 AM6 store;corrupt store 在 mutation 前拒绝。
- `crates/server/src/lib.rs:status_for` - 增加 conflict=409、permission=403、unavailable=503 业务映射。
- `crates/runtime/src/orchestrator.rs:tool_specs/dispatch("reader.paper_minimap.apply")` - resident agent 以 actor=agent 调同一 reducer,不新增 loop。
- `crates/runtime/src/orchestrator.rs:AgentEffect::PaperMinimap/PaperMinimapProposal` - effect/proposal 进入既有 `OuterOutcome.effects`;`reader.state` 同步暴露 minimap revision/state。
- `crates/server/src/lib.rs:paper_minimap_http_base_state_apply_proposal_and_saved_persistence` - 覆盖三 endpoint、confirm、saved store 与 stale 409。
- `crates/runtime/src/orchestrator.rs:agent_loop_paper_minimap_tool_emits_effect_and_mode_proposal` - fake provider 覆盖 direct effect 与 agent mode proposal。

**入口**:`GET /book/paper_minimap`;`POST /reader/paper_minimap.state`;`POST /reader/paper_minimap.apply`;runtime `reader.paper_minimap.apply`。
**测试**:`cargo fmt --package read-tools --package reader --package runtime --package server -- --check`;`cargo test -p read-tools -p reader -p runtime -p server`(121 / 47 / 61 / 89);`pnpm -C packages/web typecheck`。

## 2026-07-12 AM8 Agentic paper minimap passive frontend [Web]

**触达**:
- `packages/web/src/components/PaperMinimap.vue:PaperMinimap` - collapsed 仅渲染 region 坐标带、viewport marker 与 landmark dots;expanded 才显示 region title、page span 和真实 LID span。
- `packages/web/src/App.vue:loadPaperProjectionData/togglePaperMinimap` - 并发读取 minimap state;展开/收起发送 user `set_presentation` typed command 后回读 authoritative state。
- `packages/web/src/api.ts:paperMinimap/paperMinimapState/paperMinimapApply` - Web 只消费 AM7 wire surface,不本地推断 base 或绕过 reducer。
- `packages/web/src/components/LeftRail.vue` - 论文 profile 左栏挂载新组件;旧 BookStructure 摘要块不再作为“论文地图”渲染。
- `packages/web/playwright/paper-minimap.spec.ts` - Chromium 断言桌面展开前后 PDF surface bounding box 完全一致、地图不越过左栏;移动 overlay 不越出视口。
- `docs/screenshots/am8-paper-minimap-desktop.png` / `am8-paper-minimap-mobile.png` - 1440x900 与 390x844 视觉证据。

**入口**:`App.loadPaperProjectionData()` -> `POST /reader/paper_minimap.state` -> `LeftRail` -> `PaperMinimap`;user toggle -> `POST /reader/paper_minimap.apply`。
**测试**:`pnpm -C packages/web test`(11 files / 57);`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;`pnpm -C packages/web test:e2e`(2 passed)。

## 2026-07-12 AM9 Agentic paper minimap PDF viewport sync [Reader/Server/Web]

**触达**:
- `crates/reader/src/lib.rs:PaperMinimapAction::UpdateViewport/SetSelectedLid` - 补齐 position triad 的 typed mutation;仅 user 可执行,校验 PDF page bounds、finite ratio、真实 LID 与 center region。
- `crates/reader/src/lib.rs:apply_minimap_session_action` - viewport/selection mutation 不触碰 `map_focus`;Agent 导航被 `PAPER_MINIMAP_ACTION_FORBIDDEN` 拒绝。
- `packages/web/src/components/PdfReaderPane.vue:measureViewport/nearestMappedLid/scheduleViewportChange` - scroll 每帧最多测量一次;用可见页中心和 PDF region 视觉中心确定真实 anchor LID。
- `packages/web/src/App.vue:onPdfViewportChange/flushPaperPositionSync` - marker 先本地更新;180ms idle 后合并 viewport/selection 为一次 HTTP command,409 时回读 authoritative state 后重试。
- `packages/web/src/components/PaperMinimap.vue:onTrackPointer*/onTrackKeydown` - 轨道为可键盘访问 slider;drag 只预览,release/click/方向键才通过既有 `doGoto` 导航 PDF。
- `crates/server/src/lib.rs:paper_minimap_http_syncs_pdf_position_without_granting_agent_navigation` - HTTP roundtrip 与 user/agent 权限边界。

**入口**:PDF scroll -> rAF `viewport-change` -> local minimap marker -> idle `update_viewport`;map track/region -> `doGoto` -> PDF `scrollIntoView` -> 同一 sync loop。
**测试**:`cargo test -p reader -p runtime -p server`(49 / 61 / 90);`pnpm -C packages/web test`(11 files / 59);typecheck/build;Playwright 2 passed。

## 2026-07-12 AM10 Agentic paper minimap ModeLens interactions [Reader/Server/Web]

**触达**:
- `crates/reader/src/lib.rs:PaperAbstractCorrespondence/project_paper_minimap_lens` - abstract 四槽与 abstract 外同类正文地标确定性配对,最多 3 条,只引用 base IDs 且不冒充 relation。
- `crates/reader/src/lib.rs:paper_minimap_effects/undo_paper_minimap_effect_by_id` - 仅服务端保存可撤销 effect 原件;position/selection effect 不入 history,history 上限 64,revision stale 即拒绝。
- `crates/server/src/lib.rs:route_mut("/reader/paper_minimap.apply")` - `undo_effect_id` 只查 Reader 内部快照;忽略客户端附带 effect 内容,并沿用 saved overlay corruption/persistence 边界。
- `packages/web/src/components/PaperMinimap.vue` - expanded mode segmented control、layer checkbox、5/4/3 global/local/relation、abstract correspondence、pin、effect reason/undo 与 degraded/unknown 空态;collapsed 密度不变。
- `packages/web/src/App.vue:applyPaperMinimapCommands/undoLastPaperMinimapEffect` - mode/layer/pin/undo 全走 HTTP reducer;与 viewport sync 串行,每次 action 后回读 state+lens。
- `packages/web/playwright/paper-minimap.spec.ts` - Chromium 切 deep 后断言 local=4、relations<=3、global<=5 且 PDF bounding box 不变。
- `docs/screenshots/am10-paper-minimap-desktop.png` / `am10-paper-minimap-mobile.png` - deep desktop 与移动 overlay 视觉证据。

**入口**:expanded controls -> typed session command -> reducer effect -> state/lens refresh;undo icon -> `undo_effect_id` -> server-owned before snapshot。
**测试**:`cargo test -p reader -p runtime -p server`(50 / 61 / 91);`pnpm -C packages/web test`(11 files / 61);typecheck/build;Playwright 2 passed。

## 2026-07-12 AM11 Agentic paper minimap resident feedback [Runtime/Server/Web]

**触达**:
- `crates/runtime/src/orchestrator.rs:PaperMinimapAgentContext/paper_minimap_agent_context` - 仅 topology、position/mode、最多 12 个 global/overlay 地标状态、goal/feedback 与合法 action 名;base unavailable 时不注入。
- `crates/runtime/src/orchestrator.rs:classify_paper_minimap_feedback/SYSTEM_PROMPT` - deterministic orientation/interest/confusion/density/correction/persistence signal 与 effect/proposal/noop/clarify policy;禁止 expand/navigation/direct persistence。
- `crates/runtime/src/orchestrator.rs:run/reader_state_value` - 自然语言 user message 附紧凑 context;`reader.state` 可刷新无 goal context。PDF scroll 不调用 resident loop。
- `crates/reader/src/lib.rs:dismiss_paper_minimap_proposal` / `crates/server/src/lib.rs:dismiss_proposal_id` - ignore 复验 map/state revision 后删除 proposal,不改 state。
- `packages/web/src/App.vue:sendAgent/undoEffect/keepEffect` - PaperMinimap direct effect 显示原因并按 server effect ID 撤销;proposal 应用或 dismiss 后刷新 authoritative state+lens。
- `crates/runtime/src/orchestrator.rs:agent_loop_paper_minimap_policy_emits_effect_proposal_noop_and_clarify` - 真实 paper fixture + fixed provider 覆盖 4 direct effects、2 proposals、1 noop 和 clarification。

**入口**:`POST /agent/chat` natural-language turn -> injected context -> resident tool call -> reducer -> `OuterOutcome.effects` -> RightRail apply/ignore/undo -> minimap refresh。
**测试**:`cargo test -p reader -p runtime -p server`(50 / 63 / 92);`pnpm -C packages/web test`(11 files / 61);typecheck/build。真实 provider 仅在 AM13 smoke,不替代 fixed assertions。

## 2026-07-12 AM12 Agentic paper minimap legacy migration [Reader/Web]

**触达**:
- `packages/web/src/App.vue:loadPaperProjectionData` - paper 左栏只请求 `paperMinimapState`;删除旧 PaperReadingGuide/BookStructure 并发请求、rows/presets/pins/warnings 计算及无消费 props。
- `packages/web/src/components/LeftRail.vue` - 删除 legacy paper props/computed/label adapters/preset/proposal events;模板仅挂一个 `PaperMinimap`。
- `packages/web/src/style.css` - 删除 `.paper-minimap/.paper-preset-row/.paper-map-list/.paper-layout-proposal` 等无 DOM owner 的旧样式块。
- `packages/web/src/paper-minimap-migration.test.ts` - 静态守卫 legacy identifiers/重复 surface,并锁定 layout/minimap 两个 endpoint 与 mode handler。
- `crates/reader/src/lib.rs:paper_layout_preset_and_minimap_mode_are_independent_control_planes` - `paper_deep_read` proposal 改 layout slots 后 minimap 仍 skim;minimap 改 abstract 后 active preset 仍不变。

**入口**:paper profile -> App 仅 `paperMinimapState` -> single LeftRail surface;layout preset 继续经 `reader.layout.apply`,minimap mode 经 `reader.paper_minimap.apply`。
**测试**:`cargo test -p reader paper_layout_preset_and_minimap_mode_are_independent_control_planes`;`pnpm -C packages/web test`(12 files / 63);typecheck/build;Playwright 2 passed。

## 2026-07-12 Agentic paper minimap release handoff [Desktop]

**触达**:`apps/desktop/package.json:package:windows` - 注入仓库 README 冻结的公开 marketplace source,执行 Web production build、Rust release build、Tauri NSIS bundle 与 installer export。

**入口**:`pnpm -C apps/desktop package:windows` -> `target/release/bundle/nsis/Understand Book_0.1.0_x64-setup.exe` -> `dist/UnderstandBookSetup.exe`。
**验证**:package command exit 0;Setup 为 33,882,263 bytes,SHA-256 `58667CF27446412FEE65A060F3AC05649192D531C2CF039858C850DC219AF813`。按用户要求未运行 AM13、未启动或安装 Setup,由用户自行测试。

## 2026-07-12 Paper minimap navigation bounds [Web]

**触达**:
- `packages/web/src/paper-minimap-navigation.ts:normalizePaperViewportForMinimap` - 把 PDF 可见页规范化到小地图可信区域页域并重算进度,避免封面/相邻页触发 viewport validation。
- `packages/web/src/paper-minimap-navigation.ts:shouldOpenPdfSourcePreview` - 同时检查请求 LID 与服务端解析后的叶 LID,已有 PDF 落点时不打开来源 fallback。
- `packages/web/src/App.vue:onPdfViewportChange/doGoto` - viewport 回写与地图跳转接入上述确定性规则。

**入口**:地图区域/地标 click -> `doGoto` -> resolved viewport;PDF scroll -> `onPdfViewportChange` -> minimap reducer sync。
**测试**:`pnpm -C packages/web test -- paper-minimap-navigation.test.ts`(2 passed);`pnpm -C packages/web typecheck`。

## 2026-07-12 Paper minimap Chinese display layer [Server/Web]

**触达**:
- `crates/server/src/lib.rs:route_paper_minimap_localize` - 首次请求查版本缓存,未命中时用现有 Provider 执行有界结构化翻译;严格复验 region/landmark IDs 与标签长度,失败返回中文 fallback。
- `crates/server/src/lib.rs:PaperMinimapLocalizationCache` - `paper-minimap-localizations.json` 仅保存按 book/version/fingerprint 失效的显示标签,不进入 paper workspace 或 evidence。
- `packages/web/src/App.vue:loadPaperProjectionData` - 每个 base fingerprint 只请求一次中文显示层,导航与滚动复用内存/磁盘缓存。
- `packages/web/src/components/PaperMinimap.vue:regionDisplayLabel/landmarkDisplayLabel` - 章节、重点位置、局部链、摘要对应和论证关系统一显示中文,专有名词可保留英文。

**入口**:paper reader 初始化 -> `paper_minimap.state` -> `paper_minimap.localize` -> LeftRail `PaperMinimap`;后续 viewport/mode action 不触发 LLM。
**测试**:`cargo test -p server paper_minimap_localization --offline`(3 passed);Server full 95;Web full 13 files / 66;typecheck/build;Playwright desktop/mobile 2 passed,截图 `docs/screenshots/paper-minimap-chinese-*.png`。

## 2026-07-12 Paper minimap Chinese release package [Desktop]

**触达**:`apps/desktop/package.json:package:windows` - Web production build、Rust release、Tauri NSIS 与 installer export 包含导航修复和中文显示层。

**入口**:`pnpm -C apps/desktop package:windows` -> `dist/UnderstandBookSetup.exe`。
**验证**:33,889,606 bytes;SHA-256 `1748718451FE4DE002E82AD463BDAA1FC938165F53FD484BD2F012561305FCBC`;按用户约定未运行 Setup smoke。

## 2026-07-12 Paper minimap mode differentiation and readable labels [Reader/Web]

**触达**:
- `crates/reader/src/lib.rs:project_paper_minimap_lens` - skim 只投影全局主线关系,abstract 只投影摘要局部关系,deep 关系必须触达当前 focus region 的局部地标。
- `packages/web/src/components/PaperMinimap.vue:lensFocusedRegionIds` - 固定章节坐标不重排,但按 skim 路线、abstract 摘要/正文对应和 deep 当前章节显示独立 lens 焦点。
- `packages/web/src/components/PaperMinimap.vue:paper-map-mode-section` - 下方内容互斥呈现全文主线、摘要结构或当前章节深读;移除内部 LID、ellipsis 与 line clamp,长标签和关系改为完整换行。
- `packages/web/src/paper-minimap-visual.ts:makeLens` / `packages/web/playwright/paper-minimap.spec.ts` - 使用身份不同的三模式 fixture 和长中文/英文专名验证切换、无裁切、移动滚动及 PDF 几何稳定。

**入口**:expanded minimap mode button -> `reader.paper_minimap.apply` -> mode-specific lens -> region focus + exclusive mode section。
**测试**:`cargo test -p reader --offline`(51 passed);`cargo test -p server paper_minimap --offline`(11 passed);`pnpm -C packages/web test`(13 files / 67 passed);typecheck/build;Playwright desktop/mobile 2 passed,截图 `docs/screenshots/paper-minimap-chinese-*.png`。

## 2026-07-12 Paper minimap mode/readability release package [Desktop]

**触达**:`apps/desktop/package.json:package:windows` - Web production build、Rust release、Tauri NSIS 与 installer export 包含三模式差异化和完整文字布局。

**入口**:`UNDERSTAND_BOOK_MARKETPLACE_SOURCE=adaelon/undertand-book` -> `pnpm -C apps/desktop package:windows` -> `dist/UnderstandBookSetup.exe`。
**验证**:33,879,612 bytes;SHA-256 `4F33DCDAF18DD45EE5D894B131573653727DB85ABC6A71F38B15DC865DB9EF28`;按既有约定未运行 Setup smoke。

## 2026-07-12 PDF LID 对齐诊断与大纲导航边界 [Core/Web]

**触达**:
- `packages/core/src/hybrid-foundation.ts:pageLinesInReadingOrder/findLinesForBlock` - PDF 行先按页内跨栏/左栏/右栏空间顺序排列;过滤越界和异常高几何,在 240 词窗口内支持跨行 token、行中起点与连字符归一化,短碎片只允许整行精确匹配,避免侧栏远跳和吞标题。
- `packages/core/src/hybrid-foundation.ts:assertHybridFoundationHardGates` / `packages/core/src/zod.ts:AlignmentReportZ` - `monotonic_windowed_blocks_v2` 新增正文 60% 与标题 80% 覆盖率门禁,保留 v1 report 读取兼容。
- `packages/core/scripts/rebuild-hybrid-foundation-temp.ts` - 校验正式 source/PDF hash 后在仓库唯一临时目录重建,输出候选 map/行几何/报告与摘要,对比 LID identity、页码倒退和正式核心文件 hash;不写 `.understand-book/<book_id>`。
- `packages/core/scripts/apply-hybrid-foundation-candidate.ts` - 复验候选 schema/hard gates/shard hash 与当前 source/PDF/LID identity,事务式替换正式 foundation artifacts;失败回滚,成功保留 `.build/hybrid-foundation-backup-*`。
- `packages/core/test/hybrid-foundation.test.ts` - 覆盖晚处重复标题、跨行段落、断词/语义连字符、行中锚点、双栏内容流乱序、越界几何、短碎片误命中和覆盖率拒绝。
- `packages/web/src/App.vue:doGoto` / `packages/web/src/paper-minimap-navigation.ts:hasMappedPdfNavigationTarget` - 大纲 goto 只检查请求 LID 或 resolved leaf 是否有 PDF region;无映射时保留当前 PDF 页面并提示,不再自动调用 `openSourcePreview`。
- `packages/web/src/paper-minimap-migration.test.ts` - 静态锁定 `doGoto` 不含 `openSourcePreview`;来源正文只保留显式按钮/引用操作入口。

**入口**:`.\node_modules\.bin\tsx.ps1 packages\core\scripts\rebuild-hybrid-foundation-temp.ts .understand-book\1` -> 独立候选目录;outline/minimap goto -> `doGoto` -> PDF region 或原页提示。
**验证**:正式写回前重建候选 `.tmp-hybrid-foundation-v2-tZ5sAV`:正文 166/258(64.34%)、标题 29/29、outline 29/29、页码倒退 0、LID identity 相同、正式核心文件 hash 未变;`2.47.23.1` 与 `2.47.24.1` 均定位 `pageIndex=2`。写回后全部 artifact hash 与候选一致,旧版备份在 `.understand-book/1/.build/hybrid-foundation-backup-2026-07-12T15-02-12-080Z`;Core 36 files / 206 tests;Web 13 files / 68 tests;Core/Web typecheck 与 Web production build 通过。桌面 Reader 未运行,未做在线 API/安装包验收。

## 2026-07-12 PDF LID 映射与导航修复 release package [Desktop]

**触达**:`apps/desktop/package.json:package:windows` - 重新执行 Web production build、installed build sidecar、Rust release、Tauri NSIS 和 installer export,包含 windowed PDF aligner 与大纲不自动打开来源正文的前端修复。

**入口**:`UNDERSTAND_BOOK_MARKETPLACE_SOURCE=adaelon/undertand-book` -> `pnpm -C apps/desktop package:windows` -> `dist/UnderstandBookSetup.exe`。
**验证**:package exit 0;导出包与 Tauri bundle 均为 33,881,407 bytes,SHA-256 `92EAC8CE8F0E0415D99089F19EDA42A6B518ED4A723DB62AC3AB64CB5C3004BF`,两者 hash 一致。尚未安装或启动新 Reader smoke。

## 2026-07-13 论文地图长章节标题防重叠 [Web]

**触达**:
- `packages/web/src/components/PaperMinimap.vue:.paper-map-region-list/.paper-map-region-copy` - 章节行改为按内容自动增高,长标题与页码上下分行,色条从行顶部对齐。
- `packages/web/src/paper-minimap-visual.ts:localization.region_labels` - 六个视觉 fixture 章节改用多行长标题,覆盖窄侧栏真实压力场景。
- `packages/web/playwright/paper-minimap.spec.ts:expectRegionLabelsSeparated` - 浏览器几何断言相邻标题不相交且页码不与长标题共线。

**入口**:展开论文地图 -> 竖直彩色地图右侧章节列表 -> 长中文章节标题自然换行。
**测试**:`PaperMinimap.test.ts` 8 passed;Playwright desktop/mobile 2 passed;Web typecheck 与 production build 通过。

## 2026-07-13 PDF 自动映射罩层移除 [Web]

**触达**:
- `packages/web/src/components/PdfReaderPane.vue:page template/active LID watcher` - 不再为全部 source-map regions 或当前大纲 LID 绘制半透明罩层,并移除仅供罩层使用的 range projection 请求;page/bbox 映射数据继续用于点击命中、滚动同步与页级跳转。
- `packages/web/src/components/PdfReaderPane.test.ts:keeps source-map geometry interactive without painting automatic PDF masks` - 锁定有映射时罩层 DOM 为零,同时 bbox 点击仍发出原 LID。

**入口**:paper reader 打开 PDF -> 原版 canvas + 透明 PDF.js text layer;大纲 goto -> page-level scroll,不绘制自动映射区域。
**测试**:`PdfReaderPane.test.ts` 3 passed;Web full 13 files / 69 tests;Web typecheck 与 production build 通过。

## 2026-07-13 论文地图可读性与 PDF 纯净阅读 release package [Desktop]

**触达**:`apps/desktop/package.json:package:windows` - Web production build、installed build sidecar、Rust release、Tauri NSIS 与 installer export 包含长章节标题防重叠和 PDF 自动映射罩层移除。

**入口**:`UNDERSTAND_BOOK_MARKETPLACE_SOURCE=adaelon/undertand-book` -> `pnpm -C apps/desktop package:windows` -> `dist/UnderstandBookSetup.exe`。
**验证**:package exit 0;导出包与 Tauri bundle 均为 33,890,152 bytes,SHA-256 `5490278D226CD85C1A757666AF85F6DDC60381C0D5AFF1777413BEFE532328CE`,两者 hash 一致;未执行安装 smoke。

## 2026-07-13 PDF 选区动作与用户标注投影领域对齐 [Docs]

**触达**:
- `CONTEXT.md:PDF 临时选区快照/PDF 用户标注投影/PDF 行内 Note 标记/Ask AI 选区上下文/Memory selection context/Memory replace` - 固化选区动作、精确投影、双 quote、Note 锚与原子编辑术语。
- `docs/adr/0074-pdf-selection-actions-and-exact-user-annotation-projection.md` - 冻结显式三动作、resolved/partial 门禁、仅用户标注可见和 exact-only 投影边界。
- `docs/切片方案-pdf-selection-actions.md:PS0-PS7` - 以共享 schema、原子 replace、char-level 反向投影、NoteCard、选区状态机、PDF annotation projection 和 AskDraft provenance 拆成独立可验收切片。

**入口**:PDF 原生拖选 -> request-safe selection draft -> 用户显式选择 Highlight/Note/Ask AI;本切片只落决策与计划。
**验证**:术语零未解析;ADR/计划互链;`git diff --check` 通过。

## 2026-07-13 PDF 选区实现切片方案扩写 [Docs]

**触达**:
- `docs/切片方案-pdf-selection-actions.md:PS0-PS7/Hard gates` - 增加依赖图、共同执行规则、真实代码落点、跨层契约、实施顺序、测试矩阵、失败不变量与逐刀退出证据。

**入口**:PS0 提交后按 `PS1 -> PS2/PS3/PS4 -> PS5 -> PS6 -> PS7` 的文件态依赖逐刀实施。
**验证**:未跟踪文件 `git diff --no-index --check` 通过;代码围栏 58 个且成对;文档引用的 crate、Web scripts 与现有文件落点已核对。

## 2026-07-13 PS1 Selection/Memory schema [Memory/Server/Web]

**触达**:
- `crates/memory/src/lib.rs:SelectionResolution/SelectedRange/SelectionContext/Record/SaveInput/content_mem_id/MemoryStore::save` - 增加兼容 serde 字段、context validation、canonical mem_id 输入与按阅读顺序去重 citation。
- `crates/reader/src/lib.rs:Reader::highlight/Reader::note`、`crates/runtime/src/orchestrator.rs:dispatch_tool` - 旧 SaveInput 路径显式保持无 selection context。
- `crates/server/src/lib.rs:/memory/save` - 结构化解析 selection context 并沿用 memory validation。
- `packages/web/src/api.ts:SelectionContext/MemoryRecord/api.save` - 增加同构 TS 类型与可选 save payload。

**入口**:`POST /memory/save` 可选携带 `selection_context`;旧调用缺省字段时保持原 JSON/hash/citation 行为。
**测试**:`cargo test -p memory -p reader -p runtime -p server`(17 / 51 / 63 / 96);`pnpm -C packages/web typecheck`;`git diff --check`。`cargo fmt --check` 仍命中 memory 文件既有全文件格式差异,未在本功能切片引入格式化噪声。

## 2026-07-13 PS2 原子 memory.replace [Memory/Server/Web]

**触达**:
- `crates/memory/src/lib.rs:ReplaceInput/MemoryStore::persist_records_atomically/MemoryStore::replace` - 候选 records 经临时文件与 backup 切换成功后才替换内存;缺失、空内容、目标 ID 冲突和写失败不改变旧记录。
- `crates/server/src/lib.rs:/memory/replace` - 新增单命令 route,结构化解析可选 reselection context,不复用 delete+save。
- `packages/web/src/api.ts:api.replace` - 增加 content edit/reselection 的同构 client contract。

**入口**:`POST /memory/replace {mem_id,content,selection_context?}`;缺省 context 继承旧 anchor/context/citations,显式 context 重派生 anchor 与 citations。
**测试**:`cargo test -p memory`(21);`cargo test -p server`(97);`pnpm -C packages/web typecheck`;目标文件 `git diff --check`。

## 2026-07-13 PS3 精确 PDF range 反向投影 [Server/Web]

**触达**:
- `crates/server/src/lib.rs:ExactPdfRect/PdfRangeProjection/selection_page_shards/selection_chars_for_source_range/route_pdf_ranges_project` - range 必填并校验 UTF-16 边界;扫描 char-level selection shards,返回 exact/partial/unmapped、连续 covered range 与 exact terminal rect。
- `crates/server/src/lib.rs:write_projection_selection_pages/pdf_range_projection_*` - 覆盖同页跨行、跨页、90/180/270 度、请求顺序、coverage gap、缺末字符、unmapped 与 source-map fallback 拒绝。
- `packages/web/src/api.ts:PdfRangesProjectResponse/api.pdfRangesProject` - 同构 exact rect 响应并移除 `lid_region_fallback`。

**入口**:`POST /reader/pdf_ranges.project {ranges:[{lid,range}]}` -> selection shard chars -> caller-order projections;`pdf_source_map` 不再参与 range geometry。
**测试**:`cargo test -p server`(100);`pnpm -C packages/web typecheck`;目标文件 `git diff --check`。
