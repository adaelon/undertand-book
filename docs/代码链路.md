# 代码链路(改动账本)

> 每个切片完成时当场追加一条,精确到 文件:符号。供日后反查「当时为什么改了这个文件」。

## 2026-06-22 · S0 monorepo 骨架 + 插件外壳 + TS↔Rust 基座 schema 链路

**触达**:
- `Cargo.toml` / `pnpm-workspace.yaml` / `package.json` / `tsconfig.json` — monorepo 三面骨架(cargo workspace + pnpm workspace 并置)`[ADR-0021]`
- `.claude-plugin/plugin.json` — 本地 Claude Code 插件清单(name understand-book)`[ADR-0022]`
- `skills/build/SKILL.md` — `/understand-book:build` 入口(插件 skill 命名空间 `/插件名:skill文件夹名`)+ 7 段管线编排骨架(占位,实编排留 S1–S3)`[ADR-0022]`
- `agents/pass1-local-extractor.md` / `agents/pass2-longrange-linker.md` — subagent 占位(I/O 契约已声明,实现留 S3)`[ADR-0010]`
- `crates/base-schema/src/lib.rs` — Rust 权威基座 schema:`Span/NodeKind/LidNode/GraphNodeType/GraphNode/EdgeScope/GraphEdge/ReadOnlyBase`(serde+ts-rs+schemars)+ `sample_base()` `[ADR-0021]`
- `crates/base-schema/tests/roundtrip.rs` — `rust_sample_roundtrips`(Rust 自洽)+ `ts_fixture_deserializes_zero_mismatch`(跨语言闸:读 TS fixture 零失配 + 逐字段比对)
- `packages/core/src/generated/*.ts` — ts-rs 生成的 8 个 TS 类型(权威产物,`export_to` 相对 src 目录三级)
- `packages/core/src/zod.ts` — 镜像 Rust 定义的 zod 自检 schema(产出前运行时闸)
- `packages/core/src/sample.ts` — `buildSampleBase()` 按生成类型构造样例基座
- `packages/core/test/roundtrip.test.ts` — vitest:zod 自检 + 产 `crates/base-schema/fixtures/sample-base.json`

**入口**:`/understand-book:build <epub|md>`(预构建,插件 slash 命令;`claude --plugin-dir <repo>` 加载);基座 schema 重生成 = `cargo test -p base-schema`(触发 ts-rs export)。
**测试**(三道确定性闸,全绿 = S0 判据① 闭环):
- `cargo test -p base-schema` — 8 export_bindings + rust_sample_roundtrips + ts_fixture_deserializes_zero_mismatch
- `pnpm -C packages/core test` — zod 自检 + 产 fixture
- `pnpm -C packages/core typecheck` — tsc --noEmit,验 sample 符合 ts-rs 生成类型

**未闭**:判据②(`/understand-book:build` 被 Claude Code 识别为命令)需在 harness 内加载插件验证。

## 2026-06-22 · S1(核心)段级 LID 切分器 + 分区不变式闸 + md 适配器

**触达**:
- `packages/core/src/segment.ts:segment` — Model A 切分(章/节=纯结构容器 span=子并集;标题=容器首个叶子段)→ `LidNode[]` `[ADR-0008]`
- `packages/core/src/partition.ts:checkPartitionInvariant` — 分区不变式闸:LID 唯一 / 同级数值递增 / 父⊇子 / 叶子全覆盖·无重叠 / 非内容字节显式归类 + 切段覆盖率
- `packages/core/src/md-adapter.ts:markdownToBlocks` — md → 忠实块映射 `SourceBlock[]`(`#` 行=heading,span 含 marker;空行分段)
- `packages/core/test/fixtures/sample.md` — 测试样书
- `packages/core/test/segment.test.ts` — vitest:切分→不变式→Model A 结构→span 回切→`LidNodeZ` 校形状(5 例)
- `packages/core/src/epub-adapter.ts:xhtmlToBlocks/epubToSource` — epub 解 zip(fflate)+ spine 顺序解析 xhtml(node-html-parser),忠实块映射(h1-6/p/li/blockquote/pre;嵌套块不重复下钻)→ `{source, blocks}`
- `packages/core/test/epub-adapter.test.ts` — vitest:块抽取 + 合成 epub 全链路分区不变式 100%(2 例)
- `skills/build/split-lid.ts` — S1 CLI(经 tsx 运行):epub/md→segment→分区不变式闸,打印报告、违例非零退出
- `.npmrc` — `node-linker=hoisted`(Windows 无符号链接权限,避 pnpm EPERM symlink)

**入口**:`pnpm exec tsx skills/build/split-lid.ts <epub|md>`(SKILL.md 编排第 1 段调它)。
**测试**:`pnpm -C packages/core test`(9 例:S0 2 + segment 5 + epub 2,覆盖率 100%)+ `pnpm -C packages/core typecheck`(tsc 0)+ `cargo test -p base-schema`。
**真书实测**:《游戏编程模式》epub → 3047 块 → 3399 节点(3047 叶 + 352 容器),source 25 万字,**分区不变式 ok、切段覆盖率 100.0000%** ⇒ S1 判据达成。
**未闭**:产物写盘 `.understand-book/`(留 S3/S7 整合);字节精确 span(中文 UTF-16↔字节,影响 S4 `book.text` 取真原文)留精化。

## 2026-06-22 · S2 窗口切分(LID 子树窗口 + 双约束预算 + 确定性 token 近似)

**触达**:
- `packages/core/src/window.ts:splitWindows` — 按 LID 章/节子树切窗口:atomic 子树整窗、超限子树内逐叶贪心细分(吸附 LID 边界不腰斩)、过小由贪心累加自然合并、顶层间封口(不跨卷);单叶超硬闸自成一窗标 `overBudget`(诚实不腰斩)`[ADR-0009]`
- `packages/core/src/window.ts:estimateTokens` — 确定性近似 token(CJK `/[一-鿿]/` 按 1、其余 0.25,ceil),零依赖
- `packages/core/src/window.ts` — `WindowBudget`(maxInputTokens 硬闸 + maxLeavesSoft 软闸)/ `DEFAULT_BUDGET`(12000/80 占位,留实测)/ `Window`(leafLids/tokens/spans/overBudget)
- `packages/core/test/window.test.ts` — vitest 8 例:estimateTokens + 划分(无遗漏无重叠)+ token 一致 + 细分 + 过小合并 + 软闸 + 不跨卷 + 单叶超限不腰斩
- `skills/build/window-cli.ts` — S2 CLI(tsx):适配器→segment→splitWindows→窗口数/token 分布 + 窗口层划分校验,违例非零退出

**入口**:`pnpm exec tsx skills/build/window-cli.ts <epub|md> [maxInputTokens] [maxLeavesSoft]`(SKILL.md 编排第 2 段;消费 S1 `LidNode[]` + source)。
**测试**:`pnpm -C packages/core test`(17 例:S0 2 + segment 5 + epub 2 + window 8,全绿)+ `typecheck`(tsc 0)。
**真书实测**:《游戏编程模式》3047 叶/正文≈174k tok。默认 12000/80→**64 窗口**(利用率 22.7%,median 3233),小预算 2000/40→152 窗口(57.3%);两者 **partition.ok、单叶超限=0、每窗 ≤ 硬闸** ⇒ S2 判据达成。完整预算扫描(软闸/硬闸敏感性 + 三层约束根因)见 `docs/实测-S2窗口利用率.md`;结论回填 ADR-0009「何时回头」。
**未闭**:软闸/硬闸最终数字待 S3 接 Pass1 抽取质量实测后定(当前为占位);融合批次打包进单次 subagent 调用 + 并发数留 S3。

## 2026-06-22 · S3 语义边两遍抽取确定性骨架(Pass1 输入 + merge+闸 + 目录 + schema 补 direction)

**触达**:
- `crates/base-schema/src/lib.rs:Direction` — 新增边方向枚举(directed/undirected),merge 去重键成员 `[ADR-0011/0023]`;`GraphEdge` 加 `direction` 字段;**不补 description**(收窄 ADR-0010,见 ADR-0023)。重生成 `generated/{Direction,GraphEdge}.ts`,同步 `zod.ts`/`sample.ts`/`sample_base()`/roundtrip fixture。
- `packages/core/src/pass1-input.ts:buildPass1Input/buildPass1Inputs` — Window → 每段前缀 `[LID]` 标注的抽取输入正文(回填红线物理前提 `[ADR-0004]`),纯确定性 `[ADR-0010]`
- `packages/core/src/merge.ts:mergeAndGate` — 节点按类型合并 occurrences(实体/概念并集 name keep-last、断言 keep-last)+ 确定性图谱闸(悬空丢不重建 + 最小连坐:边端缺失只丢边/断言锚悬空丢断言连坐边/实体部分 occ 悬空剔锚、全悬空丢节点)+ 边去重 (source,target,type,direction) keep higher weight(undirected 规范化端点)+ 可观测报告(锚定率/每条丢弃可见)`[ADR-0010/0011]`
- `packages/core/src/catalog.ts:projectCatalog` — merge 后 nodes 确定性投影全局目录(零幽灵,切片0 不带 summary)`[ADR-0010]`
- `agents/pass1-local-extractor.md` — Pass1 subagent 真实 prompt(抽实体/概念/断言+局部边,严格 JSON,回填红线/单一职责/不产长程边)
- `skills/build/SKILL.md` — 编排骨架第 2–5 段指向真实模块(window/pass1-input/merge/catalog)
- `skills/build/pass1-smoke.ts` — Pass1 小样真实抽取 smoke(list/emit/verify)
- `packages/core/test/{pass1-input,merge,catalog}.test.ts` — vitest 新增 17 例(pass1-input 4 + merge 9 + catalog 4)

**入口**:`buildPass1Inputs(windows,nodes,source)` → subagent `pass1-local-extractor`(harness 供 LLM,5 并发)→ `mergeAndGate(outputs, lidNodes)` → `projectCatalog(nodes)`。smoke:`pnpm exec tsx skills/build/pass1-smoke.ts <书> [<idx> emit|verify <json>]`。
**测试**:`pnpm -C packages/core test`(34 例全绿)+ `typecheck`(tsc 0)+ `cargo test -p base-schema`(11,含 ts_fixture 零失配,direction 贯通 Rust→TS)。
**小样真实抽取 smoke**:窗口 #12「2.4 类风格化还是函数风格化」(18 段)真实抽取 13 节点/8 边 → **回填红线窗口外 LID=0**、闸后全留、目录 13 条零幽灵 ⇒ pass1 prompt + 回填红线 + 确定性闸 链路打通。
**未闭**:全书 64 窗口真实 LLM Pass1 抽取 + 图谱锚定率≥90% 实测 + 固化 `.understand-book/` 留下一刀(S3 收尾);Pass2 长程边切片0 砍、留切片1+。

## 2026-06-22 · S3.5 小规模 Pass1 实测(4 窗口手抽 + 产小基座)

**触达**:
- `skills/build/pass1-batch.ts` — 消费多窗口抽取结果 → `mergeAndGate` → 锚定率(全书分母 + 已抽窗口分母)+ 固化 `.understand-book/<book_id>/base.json`(`ReadOnlyBaseZ` 产出前校验)
- `skills/build/fixtures/pass1-sample-4windows.json` — 窗口 #9/#12/#14/#17 的手工真实抽取结果(可复现基座 + S4 夹具来源)

**入口**:`pnpm exec tsx skills/build/pass1-batch.ts <书> skills/build/fixtures/pass1-sample-4windows.json "9,12,14,17"`(PowerShell 注意给索引列表加引号,逗号否则被当数组操作符)。
**实测**:41 抽取节点→merge 跨窗口合并 1(`entity:flyweight_pattern` 跨 #12/#14 occ 并集)→闸后 40 节点/26 边/目录 40 零幽灵/零丢弃;基座 zod 校验通过。**已抽窗口锚定率 55.36%(31/56)**,根因(非内容叶+召回)+ 全书口径建议见 `docs/实测-S3小规模锚定率.md`,回填 ADR-0011。
**未闭**:`.understand-book/` 基座 gitignore(生成物,由 fixture + pass1-batch 重建);全书锚定率定量仍留下一刀。

## 2026-06-23 · S4 读时确定性叶子工具(Rust:manifest/text/context-near/concept)

**触达**:
- `skills/build/pass1-batch.ts` — 固化 base.json 时同写 `.understand-book/<id>/source.txt`(原文旁路,book.text 取真原文用)`[ADR-0024]`
- `crates/base-schema/src/lib.rs:Span` — 注释口径修正「字节区间」→「UTF-16 code unit 区间」(消 schema↔segment.ts 实现矛盾);重生成 `generated/Span.ts` `[ADR-0024]`
- `crates/read-tools/src/lib.rs:Book` — 新 crate(纯函数库)。`load/new`(serde 读 base.json + source.txt,原文存 `Vec<u16>`)/ `text`(按 UTF-16 span 切真原文,单 LID 或 LID 区间)/ `manifest`(tree + stats_by_lid:child/leaf/anchored)/ `context_near`(树邻接 parent·child·prev·next + scope=local 边,自指边跳过、weight 降序、同 LID 去重、top-K)/ `concept`(全量 occurrences + related_entities);helpers `nodes_anchored_at`/`lids_of_node`/`parent_lid`;`ToolError` 信封(LID_NOT_FOUND/CONCEPT_NOT_FOUND,禁宽松降级)`[ADR-0014/0015/0024]`
- `crates/read-tools/src/main.rs` — CLI:`manifest [lid]` / `text <lid> [end_lid]` / `context <lid> [k]` / `concept <name>`
- `crates/read-tools/Cargo.toml` — 依赖 base-schema(path)+ serde/serde_json

**入口**:`cargo run -p read-tools -- <book_dir> <cmd>`(headless 库+CLI;localhost/HTTP 暴露留 S7)。`<book_dir>` = `.understand-book/<id>`。S5+ 直接调 `Book::{context_near,text,...}`。
**测试**:`cargo test -p read-tools`(6:text 2 / manifest 1 / context 2 / concept 1);全 workspace `cargo test` 全绿(17 = base-schema 11 + read-tools 6)。
**真基座实测**(《游戏编程模式》小基座 3399 LID / 40 图谱节点):`text 11.18.4` 逐字 == TS 预览(**UTF-16 跨语言一致**,证 ADR-0024 切法);`concept 享元模式` occ 跨 11.19/12.10 多锚;`context 12.10.2` near = 树邻接3(parent/prev/next)+ local边2(11.19.5 exemplifies 0.8 / 12.10.5 about 0.7),自指边(享元↔共享实例同锚 12.10.2)正确跳过。
**未闭**:context mid/far(概念二跳 / long_range)+ via `concept` kind 留切片1+;`book.query`(S5);`range` 仅支 LID 区间(段内字符 range 留后);near 默认 K=10 占位待 S8 回填;manifest leaf_count O(n²) 暂可接受(慢再优化)。

## 2026-06-23 · S5a book.query 内层 mini-loop 确定性骨架 + ModelAdapter trait + FakeAdapter(Rust)

**触达**:
- `crates/runtime/Cargo.toml` — 新 crate(模块 E 自建运行时),依赖 `read-tools`(检索骨架)+ `base-schema` `[ADR-0025]`
- `crates/runtime/src/lib.rs:Scope` — scope 两档枚举(Local/Chapter,顶档 chapter,无 global)`[ADR-0016/0025]`
- `...:ModelAdapter`(trait)— `complete(CompletionRequest{system,user}) → ParsedResponse{sufficient,answer?,citations,model_supplement}`,loop 与后端之间 provider 无关薄层 `[ADR-0016]`
- `...:retrieve` — 确定性档位检索(local=anchor∪`context_near`;chapter=local∪章内 anchored 叶);`chapter_root` 用 `NodeKind∈{Section,Chapter}` 上溯定位章(非物化路径段数)、`anchored_leaves_under` 收章内有锚定的叶 LID `[ADR-0025]`
- `...:query` — 内层 loop:合一轮 + **确定性交叉验停**(`citations⊆证据集` 悬空滤净 = 结构红线)+ 零有效/不足外扩(local→chapter)+ 触顶 `incomplete`+`CONTEXT_BUDGET_EXCEEDED` `[ADR-0004/0016]`
- `...:QueryResponse/Citation/SupplementOut` — 对外响应(符 V3 §4.1 核心子集;citations 全真 LID、`model_supplement` 无 LID 物理隔离)
- `...:#[cfg(test)] FakeAdapter` — 脚本化确定性测试替身(loop 每轮调一次 complete,按序吐)

**入口**:`runtime::query(book, q, anchor, adapter)`(S6 外层 E loop 调它;S5b NativeAdapter 真实对接)。
**测试**:`cargo test -p runtime`(4:留证 local 收口 / 零有效强制外扩+悬空滤净 / 触顶 incomplete / retrieve 含 anchor 真原文);全 workspace `cargo test` 全绿(21 = base-schema 11 + read-tools 6 + runtime 4)。
**未闭**:**S5b** = NativeAdapter(读 `.env` OpenAI-兼容端点 + CLI `query` 子命令 + 真实 glm-5.1 端到端裸调人工验);scope 档检索体积上限待实测;chapter 档真基座行为待 S5b 真跑观察。

## 2026-06-23 · S5b NativeAdapter(读 .env OpenAI 兼容)+ CLI + 真实 glm-5.1 端到端验

**触达**:
- `crates/runtime/Cargo.toml` — 加 `ureq`(features json,同步 HTTP)+ `dotenvy` 依赖(切片0 不上 tokio/async,承 `[ADR-0024]` 精神)
- `crates/runtime/src/lib.rs:NativeAdapter` — `from_env`(读 `OPENCODE_API_KEY/OPENCODE_BASE_URL/FLUID_LLM_MODEL`)+ `complete`(POST `{base}/chat/completions`,`response_format=json_object` + system 拼 `OUTPUT_CONTRACT`,`strip_fence` 剥 markdown 容错,`LlmOut` 中间解析 → `ParsedResponse`)`[ADR-0003/0025]`
- `...:OUTPUT_CONTRACT`(const)— JSON 输出契约;**S5b 真跑调优**:澄清 `sufficient` 语义 + `answer`/`model_supplement` 分工(否则 answer 错塞 supplement + 误判 incomplete,见 ADR-0025 S5b 回填)
- `crates/runtime/src/main.rs` — CLI:`runtime <book_dir> query <anchor_lid> <question...>`(读 .env,真实后端裸调 query 输出 JSON)

**入口**:`cargo run -p runtime -- <book_dir> query <anchor_lid> <question...>`(S6 外层 E loop 改调 `runtime::query`)。
**测试**:全 workspace `cargo test` 全绿(21)+ `clippy --workspace` 零警告;NativeAdapter 真实调用走人工验(真实 LLM 不可确定性复现,不入自动测,守 A2/B2)。
**真跑实测**(glm-5.1,anchor `11.18.4`):契约调优后 `answer` 完整正确(命令模式↔闭包)+ `citations` 3 条全真 LID(`11.18.2/4/5`,逐字原文)+ `model_supplement` 物理隔离(仅 GoF 定义)+ `scope_used=local` 收口不过度外扩 + `incomplete=false`。**结构红线 100%**。
**未闭**:scope 档检索体积上限 / chapter 真基座行为待更多查询观察;`book.query` 经 provider 抽象多后端留切片1+;S6 外层 E loop。

## 2026-06-23 · S6a 最小 memory 模块(Record + save/recall + 独立 JSON 落盘)

**触达**:
- `crates/runtime/src/memory.rs` — 新模块(读时运行时状态,非冻结基座)。`Record/Anchor/MemCitation/Usage`(符 V3 §4.3/`[ADR-0015]`,`type` serde rename)+ `SaveInput`(mem_id/citations 可缺)+ `RecallQuery`(切片0 维度);`MemoryStore::{open,default_path,save,recall}` —— `save`=内容寻址 mem_id(`fnv1a` 稳定哈希,`mem_{book|type|anchor|content}`)upsert + note/highlight citation 自动派生(锚回 anchor.lid)+ count 累加 + 整文件重写;`recall`=book_id/lid/type/layer 精确 + text 子串合取过滤、按 mem_id 排序(确定性);时间戳/路径调用方注入(可测)`[ADR-0026/0015/0006]`
- `...default_path` — `UNDERSTAND_BOOK_MEMORY_DIR` env 覆盖,否则 `<home>/.understand-book/memory/memory.json`;**绝不落进 `.understand-book/<book_id>/`(只读基座),守物理隔离 `[ADR-0006]`**
- `crates/runtime/src/lib.rs` — 加 `pub mod memory;`;错误信封复用 `read_tools::ToolError`(IO/解析失败 → `INTERNAL_ERROR`,守统一信封 `[ADR-0015]`)

**入口**:`MemoryStore::open(path)` → `save(SaveInput, now)` / `recall(&RecallQuery)`(S6b 外层 loop 的 `memory.save/recall` 工具调它;落盘默认走 `default_path()`)。
**测试**:`cargo test -p runtime`(10:4 query + 6 memory)全绿;全 workspace `cargo test`(27 = base-schema 11 + read-tools 6 + runtime 10)+ `clippy --workspace` 零警告。memory 6 测:save→recall 往返 / citation 自动派生 / position 不派生 / 内容寻址 upsert 幂等(count 累加不重复)/ recall 维度过滤合取 / 落盘隔离持久化重载。
**未闭**:concept recall 维度 + 跨书 + Phase1/2 consolidation + 四层产物 + 遗忘留议题7/切片1+;memory.* 接进外层 loop tool 集留 **S6b**;真实 home 目录落盘走 S6c 真跑(单测用 temp 隔离)。

## 2026-06-23 · S6b 外层 E 编排 loop + ModelAdapter.chat + 双重停机(FakeAdapter 确定性测)

**触达**:
- `crates/runtime/src/lib.rs` — `ModelAdapter` trait 加 `chat(&[Message],&[ToolSpec])→AssistantTurn`(`complete` 内层不动);新协议类型 `Role/Message/ToolCall/ToolSpec/AssistantTurn`;`message_to_json`(Message→OpenAI 请求体形:assistant tool_calls 嵌套 `{id,type,function}`、tool 带 tool_call_id);`NativeAdapter::chat`(POST `/chat/completions` 带 `tools` schema,解析 `choices[0].message.{content,tool_calls}` + `usage.total_tokens`→AssistantTurn)`[ADR-0026]`;内层 query 测的 FakeAdapter 补 `chat` 桩(unimplemented,该测不涉外层)
- `crates/runtime/src/orchestrator.rs` — 新模块。`run(book,store,adapter,question,now,cfg)→OuterOutcome`:messages 会话态 loop——`chat`→无 tool_calls 即终答(incomplete=false)/有则追加 assistant 回合 + 逐个 `dispatch` 回填 `role:tool` 结果 → 双重停机(`turns≥max_turns ∨ spent>token_budget`,`usage.total_tokens` 累加、无 usage 退 `estimate_tokens` 兜底)触顶标 `incomplete`+`CONTEXT_BUDGET_EXCEEDED`;`tool_specs()` 7 工具 JSON-Schema(book.query/text/context/concept/manifest + memory.save/recall);`dispatch` 派发到 `crate::query`/`Book::*`/`MemoryStore::*`,**错误原样回喂 ToolError 信封(不降级)**`[ADR-0026/0015/0016]`;`OuterConfig`(max_turns=12/token_budget=120k 占位)
- `crates/runtime/src/lib.rs` — 加 `pub mod orchestrator;`

**入口**:`runtime::orchestrator::run(...)`(S6c CLI `chat` 子命令 + 真实后端驱动它)。
**测试**:`cargo test -p runtime`(14:4 query + 6 memory + 4 orchestrator)全绿;全 workspace `cargo test`(31)+ `clippy --workspace --all-targets` 零警告。orchestrator 4 测(FakeAdapter 双队列脚本化 chat+complete):多跳收敛(query→memory.save→终答,内层 complete 联动、citation 自动锚回)/ max_turns 触顶标 incomplete / 工具错误回喂 LID_NOT_FOUND 信封非静默 / 报错后 loop 继续并收敛。
**未闭**:`NativeAdapter::chat` 真实 glm-5.1 验证(原生 tools 是否稳)+ CLI `chat` 子命令留 **S6c**(真实 LLM 不入自动测,守 A2/B2);max_turns/token_budget 实测回填 ADR-0016。

## 2026-06-23 · S6c CLI chat 子命令 + 真实 glm-5.1 外层 loop 端到端人工验

**触达**:
- `crates/runtime/src/main.rs` — CLI 重构为子命令分发:`query`(S5b 内层,不变)+ 新 `chat <question...>`(读 `.env` NativeAdapter + `MemoryStore::open(default_path())`,驱动 `orchestrator::run`,输出 `OuterOutcome` JSON)`[ADR-0026]`;`now_ts()`(epoch 秒,memory 时间戳)`[ADR-0025/0026]`
- (`NativeAdapter::chat` 实现已随 S6b 落地;S6c 只加 CLI 入口 + 真跑验)

**入口**:`cargo run -p runtime -- <book_dir> chat <question...>`(读 `.env`,真实后端驱动外层 E loop)。
**测试**:全 workspace `cargo test`(31)+ `clippy` 零警告不变;`chat` 走真实 LLM 人工验(不入自动测,守 A2/B2,镜像 S5b)。
**真跑实测**(glm-5.1,书 `game-programming-patterns`,问「查证命令模式核心思想并记笔记」):外层 loop **10 轮自主多跳**(concept/query/text + memory.save)收敛、`incomplete=false`、tokens 31608(<120k budget)。确定性核查 memory 库:落 1 条 note,`anchor.lid="11"` + `citations=[{lid:"11",book_id}]`,LID 11 = 真 LID(`text 11`→"第2章 命令模式",可跳原文)。**多跳收敛 + 触顶诚实 + recall 带可验证 LID citation 三判据兑现**;回填 ADR-0026 何时回头(glm-5.1 tools 稳 / usage 口径 / max_turns 偏近上限待 S8)。
**未闭**:max_turns/token_budget 实测数(单次 10/12 偏近,待 S8 金标准集观察)回填 ADR-0016;reader.* 四动作 + 闭环留 S7;consolidation/跨书/concept recall 维度留议题7/切片1+。

## 2026-06-23 · S7 命令优先阅读器 + 闭环四动作(memory 抽 crate 拆环 + reader crate + 接入外层 loop)

**触达**:
- `crates/memory/{Cargo.toml,src/lib.rs}` — **S7a 从 runtime 抽出的独立 crate**(deps: read-tools/serde/serde_json):内容原样搬 `runtime/src/memory.rs`(`Record/Anchor/MemCitation/Usage/SaveInput/RecallQuery/MemoryStore` + 6 测),零行为变更。拆 `runtime↔reader` 循环依赖(crate 强制 DAG)+ 兑现 memory 独立模块地位 `[ADR-0027/0006]`
- `crates/runtime/src/memory.rs` — **删除**(搬至 memory crate);`runtime/src/lib.rs` 删 `pub mod memory;`;`runtime/Cargo.toml` 加 `memory`+`reader` 依赖;`orchestrator.rs`/`main.rs` 的 `crate::memory::`/`runtime::memory::` → `memory::` `[ADR-0027]`
- `crates/reader/{Cargo.toml,src/lib.rs}` — **S7b 新 crate**(deps: read-tools/memory/serde;dev: base-schema)。`Reader`(会话态:leaf_lids 叶序 + anchor_idx + radius + selection)/ `Viewport{anchor_lid,visible_lids}` + `ViewportEffect/HighlightEffect/NoteEffect/ReaderState`(返 effect 非裸 ack)。`new`(叶序=lid_nodes 过滤 children 空)/ `viewport`(**叶序滑动窗口** anchor±radius saturating)/ `goto_lid`(叶→锚该叶、容器→子树首叶、不存在→LID_NOT_FOUND 不降级)/ `scroll`(delta clamp)/ `highlight`(委托 memory.save,content=book.text 原文片段)/ `note`(委托 memory.save,content=text)/ `state` / `render`(读 memory.recall 画标注,**标注单源=记忆层**)`[ADR-0007/0015/0027]`
- `crates/runtime/src/orchestrator.rs` — **S7c reader 接入外层 loop**:`tool_specs()` 加 reader.gotoLid/scroll/highlight/note/state 五工具、**移出 book.manifest**(真跑实测全树 token 炸弹);`dispatch` 加 reader.* 分支 + `reader:&mut Reader` 参数;`run` 建会话态 `Reader::new(book,DEFAULT_RADIUS)` 贯穿 loop;SYSTEM_PROMPT 强化「要求翻页/高亮/记笔记必须调对应 reader 工具」;新增 `UB_TRACE` env-gated loop trace(诊断设施)`[ADR-0026/0027]`

**入口**:`reader::Reader::{new,goto_lid,scroll,highlight,note,state,render}`(headless 库);经外层 `runtime::orchestrator::run` 的 reader.* 工具由 agent 驱动;CLI `runtime <book_dir> chat <q>`(`UB_TRACE=1` 开 trace)。
**测试**:`cargo test --workspace`(**42 = base-schema 11 + memory 6 + read-tools 6 + reader 10 + runtime 9**)+ `clippy --workspace --all-targets` 零警告。reader 10 测:叶序窗口/scroll clamp/goto 叶·容器·不存在/note·highlight 委托 memory 单源/highlight 取原文/render 读 recall 画标注/标注跨 reader 实例可见(证单源)。orchestrator +1:`closed_loop_query_goto_highlight_note`(FakeAdapter 脚本化「问→跳转→高亮→记笔记」,标注真落 memory、citation 锚回真 LID)。
**真跑实测**(glm-5.1,书 game-programming-patterns,临时 memory 隔离):给定 LID「把 11.18.2 高亮并记笔记」→ 外层 loop **turn1 并行调 reader.highlight+reader.note → turn2 终答**,incomplete=false、turns=2、tokens 2644。memory 库落 highlight(content=11.18.2 真原文片段)+ note(content=笔记),**两条 citation 均锚回真 LID 11.18.2**。**闭环判据兑现**(切片0 总判据第 3 条)。诊断发现回填 ADR-0027:① book.manifest 全树 token 炸弹(一次 38k→205k 触顶)→ 移出外层工具集;② glm 不给 LID 时倾向 book.* 反复探索不调 reader → prompt 强化。
**未闭**:DEFAULT_RADIUS(=3)叶序窗口半径 / max_turns / token_budget 实测回填 ADR-0016/0027(S8);不给明确 LID 时的定位语义(book.query/concept 探索收敛)属切片0 已有部分,与 reader 闭环正交;reader 全集(面板系统)+ 段内字符 range + 真 GUI 渲染层留切片1+;memory 默认落 `~/.understand-book/memory/`(真跑用 UNDERSTAND_BOOK_MEMORY_DIR 隔离)。

## 2026-06-23 · S8 金标准集 + 验收闸(内层 book.query 主度量;真跑结构红线 100%)

**选址决策**:金标准集针对**内层 `book.query`**(每条 `{q, anchor_lid, expect_cite[]}`),非外层 E loop——结构红线度量最干净、可复现、省 token,anchor 预供;直接消费 `runtime::query`。外层 loop 闭环已于 S7 兑现(切片0 总判据第 3 条),不在 S8 重复度量。

**触达**:
- `crates/runtime/src/goldset.rs` — 新模块 `[ADR-0004]`。`GoldItem{id,q,anchor_lid,expect_cite}` / `ItemResult` / `GoldReport`;**纯函数闸**:`structural_check`(返回 citation 全是真 LID 无悬空=结构红线,列悬空 LID)+ `semantic_metrics`(recall=|返回∩期望|/|期望|、precision=|返回∩期望|/|返回|,**人工评的客观信号非自动判定**)+ `build_report`(聚合结构红线率/均值/incomplete 数);runner `evaluate_item`/`run_goldset`(逐条 `crate::query`,返回去重 citation LID)。自动验收只到结构红线;语义达阈+人工试读由人裁(守 B2)`[ADR-0004]`
- `crates/runtime/src/lib.rs` — 加 `pub mod goldset;`
- `crates/runtime/src/main.rs` — CLI 加 `goldset <file.json>` 子命令:读金标准集 JSON → `run_goldset`(NativeAdapter 真跑)→ 输出 `GoldReport` JSON + stderr 一行汇总;**结构红线 <100% 非零退出**(确定性闸,违 ADR-0004 即 fail);errored 显式告警
- `crates/runtime/goldset/game-programming-patterns.json` — **12 条金标准集**(从真基座 40 节点确定性推导),覆盖 4 章区:命令模式/闭包/撤销/头等函数(11.18–11.19)、享元/工厂方法/对象池/状态(11.19/12.10)、简单性/抽象解耦(9.x)、观察者/数据绑定(13.12);每条 expect_cite = 锚定 claim 的真 LID
- `goldset.rs` runner **逐条容错**:provider 偶发错(空响应)重试一次,仍失败记 `ItemResult.error`+`GoldReport.errored` 单列(不算结构红线分母,显式记录非静默降级 `[ADR-0015]`);真跑首跑遇 1 条 glm 空响应即此设计的由来

**入口**:`cargo run -p runtime -- <book_dir> goldset crates/runtime/goldset/game-programming-patterns.json`(读 `.env`,真实后端)。
**测试**:`cargo test --workspace`(**47 = base-schema 11 + memory 6 + read-tools 6 + reader 10 + runtime 14**;runtime +5 goldset:结构红线判悬空 / 语义 recall·precision / 聚合 / errored 排除 / run_goldset 经 FakeAdapter 确定性端到端)+ `clippy -p runtime` 零警告。真 LLM 不入自动测(守 A2/B2)。
**真跑实测**(glm-5.1,真基座 game-programming-patterns):**结构红线 100.0%(12/12,0 悬空)** + mean_recall 1.00 + mean_precision 0.75 + incomplete 0 + errored 0;12 条全 local 档收口;answer 抽查贴原文正确。**切片0 总判据①(结构红线 100%)兑现** + 语义信号达基线(recall≥0.9 建议阈)。回填 ADR-0004(结构红线兑现 + 语义阈值建议 + 金标准集规模 12)、ADR-0016(内层 scope 全 local 收口)。
**未闭(切片0 收尾,需用户裁)**:① **人工试读认可**(语义质量最终拍板,人类动作);② **命令面契约冻结为基线**(V3 §6,切片0 完成充要)→ 冻结后切片0 完成,可起切片1+。

## 2026-06-23 · S9 ModelAdapter 确定性产物 JSON 规范性硬化(鲁棒抽取 + 抽不到诚实报错)

> 切片0 收尾后第一刀(不在切片0 判据内);**内部硬化,不动命令面契约**。

**触达**:
- `crates/runtime/src/lib.rs:extract_json_object` — 新增鲁棒 JSON 对象抽取:`strip_fence` 剥围栏 → 扫描第一个**平衡 `{}` 子串**(跳过字符串字面量内的括号 + `\` 转义,引号内 `{`/`}` 不计深度)→ 抽不到返 `None`。只负责「形状」抽取,值正确性仍由内层 `query` 的 `citations⊆证据集` 确定性交叉验停再校验 `[ADR-0016 决策5/ADR-0004]`;仅扫 ASCII 字节,切片落 char 边界(注释证安全)
- `crates/runtime/src/lib.rs:NativeAdapter::complete` — `serde_json::from_str(strip_fence(content))` → 先 `extract_json_object(content)`,**抽不到合法 JSON / 空响应 → 显式 `AdapterError`**(经 `query` 映射 `PROVIDER_ERROR`,不静默成功,守禁宽松降级 `[ADR-0015]`);外层 `chat`(原生 tool_calls)不动
- **不改** `OUTPUT_CONTRACT`/prompt(强后端走 `json_object` 模式,鲁棒抽取纯防御偶发;放宽 prompt 自由推理属 ReActAdapter/切片1+);**不动** `ParsedResponse`/命令面契约/错误信封/值校验

**入口**:`NativeAdapter::complete`(内层 `query` 唯一确定性产物路径调它)。
**测试**:`cargo test --workspace`(**50 = base-schema 11 + memory 6 + read-tools 6 + reader 10 + runtime 17**;runtime +3 S9:三形态抽对 / 字符串内括号·转义跳过 / 空·纯散文·不平衡→None)+ `clippy -p runtime --all-targets` 零警告。真 LLM 不入自动测(守 A2/B2)。
**判据**:① 三形态单测全过;② 抽不到/空内容 → `PROVIDER_ERROR` 非静默(`None`→`AdapterError`→`query` 映射);③ 真跑回归结构红线 100% **待人工验**(走 `.env`/glm-5.1,B2)。
**未闭**:判据③ 真跑回归(glm-5.1 偶发空/啰嗦响应消化验证)由用户拍板跑;内层 `query` 是否需逐条重试(goldset runner 已有重试,内层裸调暂无)留实测 `[ADR-0016]`;ReActAdapter 完整解析回填(注模板/多字段宽松容错)留切片1+。

## 2026-06-24 · S10a 新 server crate:book.* 只读 GET REST 投影 + API DTO ts-rs 导出 packages/web

> 切片1「前端阅读器」第一刀(A4 子切片);`[ADR-0028]` 命令面 REST 投影落地。本刀只投影 `book.*`(reader/memory→S10b、query→S10c、agent→S10f、静态资源→S10e)。

**触达**:
- `crates/server/Cargo.toml` — 新 crate(`tiny_http=0.12`,依赖 `read-tools`;`base-schema` 仅 dev 供 `sample_base`)。坐 DAG 顶端(S10a 只需 read-tools)`[ADR-0028 决策2]`
- `crates/server/src/lib.rs:route` — **纯函数路由** `route(&AppState, method, url) -> Reply`(脱 socket 可单测,守 A2):`/book/manifest`(GET)+ `/book/text?lid=&end=` + `/book/context?lid=&k=` + `/book/concept?name=` 1:1 投影 `Book` 四叶子 `[ADR-0028 决策3]`;`AppState{book}`(S10b 扩 reader/store)
- `crates/server/src/lib.rs:err_reply/status_for` — `ToolError` §4.4 信封**原样透传** + category→HTTP status(validation 400/not_found 404/provider 502/budget 429/internal 500);非 GET→405 `METHOD_NOT_ALLOWED`、未知路由→404 `ROUTE_NOT_FOUND`(传输层码,非冻结 command 枚举)
- `crates/server/src/lib.rs:parse_query/percent_decode` — 最小 query 解析 + `%XX`/`+` 解码(支持 CJK 概念名);`INVALID_RANGE`(缺必填参)/`INVALID_K`(k 非整数)
- `crates/server/src/main.rs:main` — tiny_http 同步、4 worker 线程共享 `Arc<Mutex<AppState>>`(单会话单书,不上 tokio `[ADR-0024/0028]`);`Book::load(<dir>)`,`UNDERSTAND_BOOK_DIR`/`UNDERSTAND_BOOK_ADDR`(默认 127.0.0.1:8787)
- `crates/read-tools/src/lib.rs` — `Manifest/ManifestNode/LidStats/Context/ContextItem/Via/Concept/ToolError` 补 `#[derive(TS)]` + `#[ts(export_to="../../../packages/web/src/generated/")]`(+ Cargo `ts-rs=10`)`[ADR-0028 决策6]`
- `crates/runtime/src/lib.rs` — `QueryResponse/Citation/SupplementOut` 补 `#[derive(TS)]` 同导出目标(+ Cargo `ts-rs=10`);S10a 先导出类型,query 端点 S10c 接
- `packages/web/src/generated/*.ts` — ts-rs 新生成 11 个 API DTO TS 类型(`ManifestNode.span` 跨指 base-schema → `import { Span } from "../../../core/src/generated/Span"`,跨 crate 相对路径实测正确)

**入口**:`cargo run -p server -- <book_dir>`(localhost REST);前端经 `GET /book/*` 消费(S10d)。API TS 类型重生成 = `cargo test -p read-tools`/`-p runtime`(触发 ts-rs export)。
**测试**:`cargo test --workspace`(**71 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 20 + server 10**;server 10:四端点正确/错误透传 status 映射/未知路由 404/非 GET 405/percent 解码 CJK·空格/query 解析)+ `clippy -p server -p read-tools -p runtime` 净。
**实测落点回填**:① ts-rs 跨 crate 导出目标 = `packages/web/src/generated/`,跨指 base 类型由 ts-rs 自动算相对 import,可行(`[ADR-0028/0021]`);② tiny_http 路由 = `url().split('?')` 手解 query + 自写 `percent_decode`(无额外依赖)。
**未闭(B2,需真书)**:手动 curl 各 GET 返正确 JSON / 错误返信封——需一本预构建真书目录,留有真书时验(路由逻辑已由纯函数单测确定性覆盖)。`ToolError` 暂无 `recovery` 字段(各 command 真产 recovery 时再补,不擅扩冻结契约)。

## 2026-06-24 · S10b reader.*/memory.* POST 端点 + Reader/MemoryStore 会话状态

> 切片1 第二刀;`[ADR-0028 决策3]` 可变命令 → POST 投影。承 S10a 的 server crate。

**触达**:
- `crates/server/Cargo.toml` — 加 `reader`/`memory` 依赖(route 需 Reader/MemoryStore/SaveInput/RecallQuery/Anchor)
- `crates/server/src/lib.rs:AppState` — 扩 `{book, reader: Reader, store: MemoryStore}`(单会话:只读书 + 会话态阅读器 + 用户私有记忆库,物理隔离 `[ADR-0006]`)
- `crates/server/src/lib.rs:Req` — 新增请求输入结构 `{method, url, body, now}`;`route` 签名改 `route(&mut AppState, Req) -> Reply`(POST 带 JSON body + 注入时间戳,守 A2 确定性)
- `crates/server/src/lib.rs:route/route_book/route_mut` — 按命名空间前缀分派:`/book/*`→GET(route_book,S10a 逻辑抽出)、`/reader/*`+`/memory/*`→POST(route_mut),错方法→405、未知路径→404。字段级不相交借用(reader mut + book shared + store mut 同时)
- `crates/server/src/lib.rs:route_mut` — POST 七端点:`/reader/{goto,scroll,highlight,note,state}`(reader.* 返 effect;highlight/note 委托 `memory.save`,标注单源 `[ADR-0015]`)+ `/memory/{save,recall}`(直读写记忆层;save layer 缺省按 type 默认 position→session 否则 long_term `[ADR-0006]`;recall 各维度 Some 过滤、book_id 缺即跨书)
- `crates/server/src/lib.rs:body_value` — JSON body 解析(空 body→空对象供 reader.state 无字段端点;非法→400 INVALID_RANGE)
- `crates/server/src/main.rs:main` — 构建 `Reader::new(&book, DEFAULT_RADIUS)` + `MemoryStore::open(default_path())`(物理隔离库)入 AppState;worker 读 POST body(`rq.as_reader().read_to_string`)+ 生成 `now_ts`(epoch 毫秒)+ 传 `Req`
- `crates/server/src/main.rs:now_ts` — memory 时间戳(epoch 毫秒串;ISO 化留后)

**入口**:`cargo run -p server -- <book_dir>`;前端经 `POST /reader/*`、`POST /memory/*` 驱动(S10d)。标注回显 = 客户端调 `POST /memory/recall`(标注单源 memory,无独立 render 端点)。
**测试**:`cargo test --workspace`(**73 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 20 + server 12**;server +2:reader goto/scroll/highlight/note/state effect + 非法 LID 404 不降级 + memory save/recall 往返 + 缺字段 400 + 坏 body 400 + 错方法 405)+ `clippy -p server` 净。
**未闭(B2,需真书)**:手动 POST 驱动真 reader/标注落盘/recall 回显——留有真书时验(纯函数 route 已确定性覆盖全部分支)。query 端点(`/book/query`)= S10c。

## 2026-06-24 · S10c book.query POST 端点(接 runtime::query + adapter 注入)

> 切片1 第三刀;`[ADR-0014/0016/0028]` LLM 命令 REST 投影。承 S10b 的 server crate。

**触达**:
- `crates/server/Cargo.toml` — 加 `runtime` 依赖(route_query 需 `runtime::query` + `ModelAdapter`/`NativeAdapter`)
- `crates/server/src/lib.rs:AppState` — 扩 `adapter: Box<dyn ModelAdapter + Send>`(`+ Send` 供 `Arc<Mutex<_>>` 跨 worker 线程;LLM 后端经它触模型)`[ADR-0028]`
- `crates/server/src/lib.rs:route` — 单列 `POST /book/query` 分支于 GET-only `route_book` 之前(`book.*` 命名空间但 LLM 命令、秒级非确定性叶子,方法分派对它例外)`[ADR-0014]`;GET→`query_method_not_allowed` 405(文案单列免误导)
- `crates/server/src/lib.rs:route_query` — body `{q, anchor_lid?}` → 直调 `runtime::query`(确定性档位检索 + LLM 合一轮 + 确定性交叉验停 = 结构红线 `[ADR-0004/0016]`)→ 返 `QueryResponse`;缺 q→400 `INVALID_RANGE`;**anchor 缺省取 `reader.state().viewport.anchor_lid`**(读到哪问到哪);`scope` 入参暂不接(内层切片0 固定 local→chapter auto,无 scope 旋钮,留切片1+);provider 错经 query 映射 `PROVIDER_ERROR`(category=provider→502)透传不降级 `[ADR-0015]`
- `crates/server/src/lib.rs:UnconfiguredAdapter` — `.env` 缺失兜底 adapter(complete/chat 均返 `AdapterError`);book/reader/memory 浏览不被 LLM 配置阻塞,仅 query 触模型时诚实报错 `[ADR-0015]`
- `crates/server/src/main.rs:main` — `NativeAdapter::from_env()` 装配 adapter,失败则 eprintln 警告 + 兜底 `UnconfiguredAdapter` 入 `AppState`

**入口**:`cargo run -p server -- <book_dir>`(读 `.env`);前端经 `POST /book/query`(body `{q, anchor_lid?}`)消费(S10d 问答框)。
**测试**:`cargo test --workspace`(**78 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 20 + server 17**;server +5:StubAdapter 确定性返 QueryResponse(scope local/全真 LID citation)+ 显式 anchor + 缺 q 400 + GET 405 + provider 错 502)+ `clippy -p server` 净。**StubAdapter 实现 `ModelAdapter`** 把 query 路由层脱真 LLM 做成确定性可测(守 A2,超出原 checkpoint 的"仅 B2")。
**未闭(B2,需真书 + `.env`)**:真跑 `POST /book/query` 返结构红线 100% 的 `QueryResponse`(走 glm-5.1,真 LLM 不入自动测,守 A2/B2,镜像 S5b/S8)由用户拍板跑。`scope` 入参待 `runtime::query` 长出 scope 旋钮(切片1+)。

## 2026-06-24 · S10d Vue/Vite 前端 app(连续正文 + 四动作 + 标注 overlay + 读位感 + 问答框)

> 切片1 第四刀;`[ADR-0028]` 前端栈落地(解 ADR-0021 PENDING)。最小连通画面 = S10a+S10b+S10d。

**触达**(新 `packages/web` Vue3+Vite,pnpm workspace 成员):
- `packages/web/package.json` — `@understand-book/web`(deps:`vue`;dev:`vite`/`@vitejs/plugin-vue`/`vue-tsc`/`typescript`);scripts `dev`/`build`(vue-tsc + vite)/`typecheck`
- `packages/web/tsconfig.json` — extends 根 tsconfig + `lib:[ES2022,DOM,DOM.Iterable]` + `types:[vite/client]`;`vite.config.ts` — `@vitejs/plugin-vue` + dev proxy `/api`→`127.0.0.1:8787`(同源无 CORS,承 ADR-0022;`UNDERSTAND_BOOK_ADDR` 可覆写)
- `packages/web/index.html` / `src/main.ts` / `src/env.d.ts`(`.vue` shim + vite/client)/ `src/style.css`(连续正文 CSS:段落自然流、无框/无 LID 标号、anchor 淡左缘、hl 高亮底色、note 行内徽标)
- `packages/web/src/api.ts:api` — 类型化命令面 REST 客户端 `[ADR-0028]`:`manifest/text/query/goto/scroll/highlight/note/state/recall` 九端点;import 生成 DTO `Manifest`/`QueryResponse`/`ToolError`;手定 `Viewport/ViewportEffect/ReaderState/MemoryRecord`(memory 类型未走 ts-rs);`ApiError` 携 §4.4 信封(category/error_code 供 UI 分流);`http<T>` 统一 GET/POST + 错误透传
- `packages/web/src/App.vue` — `<script setup>` 单组件:① 连续正文列(`GET /book/text` 逐 `visible_lids` 取真原文,段落承 `data-lid` 隐形锚、点击选中)② 四动作(上/下翻→`reader.scroll`、跳转 LID→`reader.goto`、🖍 高亮→`reader.highlight`、📝 笔记→`reader.note`,真经 HTTP)③ 标注 overlay(`memory.recall` 回显 highlight 底色 + note 徽标,**单源=记忆层**)④ 读位感条(章节=anchor 顶层段原文首行 + 进度%=anchor 在叶序下标/叶总数)⑤ 问答框(`book.query`,citations 可点跳原文、incomplete/scope/model_supplement 显式)`[ADR-0027/0028/0007]`

**入口**:`pnpm -C packages/web dev`(Vite dev server + proxy → tiny_http);需先 `cargo run -p server -- <真书目录>`。打包静态服留 S10e。
**测试**(确定性闸,守 A2 可验部分):`pnpm install`(workspace 加 web 成员,38 包)✓ + `pnpm -C packages/web typecheck`(vue-tsc 0 error)✓ + `pnpm -C packages/web build`(vue-tsc + vite,12 模块 → `dist/`)✓。`cargo test --workspace` 78 不受影响(纯前端刀)。
**实测落点回填**:① 连续正文取数粒度 = **逐 visible_lid 取**(窗口 ≤2*radius+1 叶,localhost 单用户可接受;整窗 `book.text` range 批取属优化,留实测)→ ADR-0028;② 标注 overlay 锚定 = 段落 `data-lid` + 客户端按 lid 过滤 recall(选区富文本 web-highlighter 留实测)→ ADR-0028。
**未闭(B2,浏览器人工验,需真书 + 跑 server [+ `.env` 做问答])**:浏览器加载书显连续正文 / 四动作真驱动后端 / 标注回显 / 章节+进度% / 问答返 citations 可跳——留有真书时人工验(类型 + 构建已确定性闸覆盖)。`memory.recall` 暂 `{}` 全取客户端过滤(book_id 精确作用域待前端拿到 book_id,留后);章节标题取 LID 首段(顶层非必为「章」,精确容器上溯留后)。

## 2026-06-24 · S10f /agent/chat 外层 E agent + reader 双向注入 + effects/trace 装配

> 切片1 第六刀;`[ADR-0030]` agent 阅读器形态后端落地。前端对话主入口 = 外层 E agent(非内层 book.query)。

**触达**:
- `crates/reader/src/lib.rs:Reader::{highlight,note}` — 加 `layer:&str` 入参(人走命令面默认 `long_term`、agent 提议态传 `session`)`[ADR-0030 决策4]`。人机对称无特供:agent 标注仍走 reader 闭环(content=原文片段逻辑复用),只是落 session 层;`§0.5` 对齐选 A(改 reader 公共签名)否决「orchestrator 绕过 reader 直写 memory」(破对称 + 重复取片段逻辑)
- `crates/runtime/src/orchestrator.rs:OuterOutcome` — 加 `effects:Vec<AgentEffect>` + `trace:Vec<TraceStep>`(runtime 内部结构,非冻结命令面,加 `#[derive(TS)]` 导出前端)`[ADR-0030 决策3/5]`
- `...:AgentEffect`(tagged enum `kind`)— `Goto{before_anchor,after_anchor}`(undo=goto(before))/ `Highlight{mem_id,lid}` / `Note{mem_id,lid,text}`(undo=memory.delete(mem_id));**视口变更按回合合并成单条 Goto**(事务性 undo,`with_goto` 比对回合首尾 anchor),highlight/note 每次一条
- `...:TraceStep{tool,args,result_digest}` — 查询踪迹(book.query 的 citations 链落 `result_digest`,`digest()` 截 200 字)
- `...:run` — **签名改注入 `reader:&mut Reader` + `messages:&mut Vec<Message>`**(删内部 `Reader::new`,与 server `AppState` 共享同一视口 + 跨回合 messages)`[ADR-0030 决策2]`;`dispatch` 改返 `(String, Option<AgentEffect>)`;终答入 messages(下一回合可见上轮回答);`new_session()` 产仅 system 的初始 messages(供 `/agent/new` 重置)
- `crates/runtime/src/main.rs` — `chat` 子命令适配新签名(自建 reader + new_session)
- `crates/server/src/lib.rs:AppState` — 加 `messages:Vec<Message>`;`route` 加 `POST /agent/chat`(body `{message}`→ 注入同一 book/store/reader/messages/adapter 调 `run`→ 返 `OuterOutcome`)+ `POST /agent/new`(清 messages 回 new_session);非 POST→405 `agent_method_not_allowed`、缺 message→400
- `crates/server/src/main.rs:main` — `AppState` 构造加 `messages:new_session()`
- `packages/web/src/generated/{OuterOutcome,AgentEffect,TraceStep}.ts` — ts-rs 新生成 3 个 agent DTO(S10g 前端消费)

**入口**:`POST /agent/chat`(前端对话区 S10g)/ `cargo run -p runtime -- <dir> chat <q>`(CLI 真跑)。
**测试**:`cargo test --workspace`(**87 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 25 + server 21**;runtime +2:视口变更合并单条 Goto + trace / messages 跨回合保留+重置;server +4:/agent/chat 驱动共享 reader 返 effects + 标注落 session 层、/agent/new 清 messages、GET 405、缺 message 400)+ `clippy -p reader -p runtime -p server` 净。**ChatStubAdapter** 脚本化外层 chat 把 /agent/chat 脱真 LLM 做成确定性可测(守 A2)。
**实测落点回填**(ADR-0030)：`AgentEffect`/`TraceStep` 字段定型(见上);视口 undo 粒度 = **按回合合并单条 Goto**(事务性);agent 标注 layer 注入点 = reader.* 加 layer 参数。
**未闭(B2,需真书 + `.env`)**:真跑 `POST /agent/chat` 一次「问→agent goto→高亮」后 `/reader/state` 见视口被 agent 改 + effects 可 undo(走 glm-5.1,真 LLM 不入自动测,守 A2/B2)由用户拍板跑。messages 累积上限/截断、idle 软提示留实测(ADR-0030 何时回头)。S10g 前端 agent UI(分屏+提议卡+踪迹)+ S10e 打包待做。

## 2026-06-24 · S10g agent 形态 UI(分屏对话 + 可撤销提议 + 踪迹)+ memory.delete 补全

> 切片1 第七刀;`[ADR-0030 决策1/5]` agent 阅读器形态前端落地。含 S10g-pre 后端补 `memory.delete`(完成 V3 §4.3 冻结契约第三命令,切片0 S6a 只实现了 save/recall)。

**触达**:
- `crates/memory/src/lib.rs:MemoryStore::delete` — **新增**:`retain` 删指定 mem_id + 落盘;找不到 → `MEMORY_NOT_FOUND`(not_found,禁静默降级 `[ADR-0015]`)。完成 V3 §4.3 `memory.delete(mem_id)→{ok}`(用户显式删,区别议题7 后台 usage 遗忘 `[ADR-0018]`)
- `crates/server/src/lib.rs:route_mut` — 加 `/memory/delete` 分支(body `{mem_id}`→ `store.delete`→ `{ok:true}`;缺 mem_id→400、找不到→404)
- `packages/web/src/api.ts:api` — 加 `save`(memory.save 客户端,「保留」升 long_term 用)/ `delete`(memory.delete)/ `agentChat`(POST /agent/chat→OuterOutcome)/ `agentNew`(POST /agent/new);import 生成 DTO `OuterOutcome`/`AgentEffect`/`TraceStep`
- `packages/web/src/App.vue` — 右侧 Q&A 框**改为 agent 对话区**(主入口 `[ADR-0030 决策1]`,直问 book.query 退为 agent 内部工具):`sendAgent`(接 `/agent/chat`,回来 `syncViewport` 同步被 agent 改的视口 + 刷新标注);**提议卡**渲 `effects[]`——`Goto`「↩ 返回 before_anchor」(`undoEffect`→reader.goto)/ `Highlight`·`Note`「保留(`keepEffect`→同内容 long_term re-save = 同 mem_id upsert 升层)/撤销(`undoEffect`→memory.delete)」;**踪迹**折叠渲 `trace[]`(tool+args+result_digest);对话末「凝练成笔记(`distill`→memory.save note,content=answer)」;「新对话」(`newChat`→agentNew 清前端 transcript);提议处置态 `handled` 防重复操作。AgentEffect 联合类型在 TS helper(`effLabel`/`isGoto`/`gotoBack`)里 narrow,避开模板收窄。`<style scoped>` 加 agent 区样式

**入口**:浏览器对话区 → `/agent/chat`;agent goto/highlight/note 即时驱动共享 reader,提议卡 undo 走反向命令(reader.goto / memory.delete)。
**测试**:`cargo test --workspace`(**89 = base-schema 11 + memory 7 + read-tools 14 + reader 10 + runtime 25 + server 22**;memory +1 delete 往返+MEMORY_NOT_FOUND、server +1 /memory/delete 端点 200+404)+ `clippy -p memory -p server` 净 + `pnpm -C packages/web typecheck`(vue-tsc 0)✓ + `build`(14 模块→dist)✓。
**实测落点回填**(ADR-0030)：提议确认 = **逐条**(保留/撤销 per-effect,非整回合事务);「保留」升级 = 同内容 long_term re-save 复用 content-addressed upsert(mem_id 不含 layer);Highlight 保留需 recall(layer=session) 取原文片段(effect 不载 content)。
**未闭(B2,浏览器人工验,需真书 + 跑 server + `.env`)**:对话分屏 / agent goto 后阅读区同步且可一键返回 / 高亮可保留·撤销 / 踪迹可展开 / 凝练成笔记——留有真书时人工验(类型+构建已确定性闸覆盖)。S10e 打包(tiny_http 服 dist 同源)= 切片1 收尾末刀。

## 2026-06-24 · 切片1 修缮 A:start.bat 修复 + agent 分屏布局回归 + 对话/笔记 MD+LaTeX(commit 13d6c4a)

> 切片1 阅读器修缮(非新子切片);三处独立修复 + 一个渲染增强。

**触达**:
- `start.bat`(新)— 双击启动器(dev:server :8787 + vite :5173)。修 base.json-missing 分支 for-loop 的 `^(...^)` 转义括号嵌在 if 块内**破坏块解析**、致双击「一闪就关」(哪怕 base.json 存在)→ 换 `dir /b /ad`(无括号嵌套陷阱)。
- `packages/web/src/style.css` / `App.vue` — 修 S10g 回归:右栏 class 从 `.qa` 改 `.agent` 时丢了 `width`,默认 flex 让 agent 回答后撑满全屏、按钮错位 → `.agent { flex: 0 0 24rem }` 固定侧栏 + `.transcript` 内容换行。
- `packages/web/src/md.ts`(新)`:renderMarkdown` = markdown-it(`html:false` 防 XSS)+ `@traptitech/markdown-it-katex`(`$..$` / `$$..$$`);`main.ts` import `katex/dist/katex.min.css`。
- `packages/web/src/App.vue` — agent 答案 + 笔记内容改 `v-html=renderMarkdown(...)`;`.md` 排版规则放**全局** `style.css`(scoped 够不到 v-html 注入内容,顺修上一刀潜在失效)。
- `packages/web/package.json` — 加 `markdown-it`/`katex`/`@traptitech/markdown-it-katex` + `@types/markdown-it`。

**入口**:`start.bat [book_dir]` 双击;浏览器对话区 / 笔记卡渲染。
**测试**:`pnpm typecheck`(vue-tsc 0)+ `build` 绿;md 渲染 10/10(粗体/列表/代码/`$E=mc^2$`/`$$\int$$`/XSS 转义/null);start.bat dry-run exitcode 0(修前 255 `( was unexpected`)。

## 2026-06-24 · 切片1 修缮 B:人自由高亮(段内字符区间)+ 笔记编辑器(H1/H2/N1)`[ADR-0031]`

> 自由高亮(段内任意文字、可删可改)+ 笔记专用编辑界面。跨后端(H1)+ 前端(H2/N1)。

**触达**:
- `docs/adr/0031-*`(新)— 自由高亮 = 段内 **UTF-16 字符区间**(相对 LID 文本);memory Record 加可选 `range`;兑现冻结 `reader.highlight(lid, range?)`(V3 §4.2)。
- `crates/memory/src/lib.rs` — `TextRange{start,end}`;`Record`/`SaveInput` 加 `range: Option<TextRange>`;`content_mem_id` **仅 Some 时**纳入 range(老 note 哈希不变=向后兼容;同段同子串不同位置=两条不同 mem_id)。
- `crates/reader/src/lib.rs:Reader::highlight` — 签名加 `range: Option<(u32,u32)>`:`Some` → `encode_utf16` 切子串作 content + 存 range,越界 → `INVALID_RANGE` 不降级;`None` → 整段(向后兼容 / agent)。
- `crates/runtime/src/orchestrator.rs` — agent `reader.highlight` 传 `None`(整段,人机对称无特供)+ memory.save `SaveInput` 补 `range:None`。
- `crates/server/src/lib.rs:route_mut` — `/reader/highlight` 解析 body `range:{start,end}` 传 reader;`/memory/save` 补 `range:None`。
- `packages/web/src/api.ts` — `TextRange` + `MemoryRecord.range` + `highlight(lid, range?)`。
- `packages/web/src/App.vue` — 选区捕获 `onProseMouseUp`(`Range.toString().length` 算段内 UTF-16 偏移)→ 浮动「🖍 高亮选区」`confirmHighlight`;`renderSeg`(段内高亮包 `<mark>`,合并重叠区间 + 转义防 XSS);高亮卡 `deleteHighlight`/`modifyHighlight`(改=移除后重选);**笔记模态编辑器**(`noteEditor` 状态 + `notePreview` 实时 MD/LaTeX 预览 + `saveNote`/`openEditNote`)替换 `window.prompt`。
- `packages/web/src/style.css` — `.hl-mark`/`.hl-card`(全局,v-html 用);App.vue scoped 加 `.hl-popover`/`.note-modal`/`.nd-*`。

**入口**:正文框选 → 浮动按钮 → `POST /reader/highlight {lid, range}`;高亮卡删除/改范围;📝 笔记 → 模态编辑器。
**测试**:`cargo test --workspace` **92**(memory 8 / reader 11 / server 23,各 +1)+ clippy 净;`typecheck`+`build` 绿;**真实 CJK 端到端**:`slice(2,6)`=`"由人民邮"` 前后端逐字一致 + 同段多条 + 删除 + 越界→400 全过(临时 memory 目录隔离,不碰真实库)。
**实测回填**(ADR-0031):UTF-16 三处同口径(DOM 选区 / JS `slice` / Rust `encode_utf16`)端到端一致;agent 高亮维持整段。
**未闭(B2,浏览器人工验)**:DOM 选区捕获 + 浮窗/模态视觉 + 高亮重绘——留浏览器肉眼确认(后端区间 + 渲染逻辑已确定性覆盖)。

## 2026-06-25 · S10e 打包形态:tiny_http 同端口服务 SPA dist + API `/api` 前缀归一

> 切片1「前端阅读器」打包收尾;兑现 ADR-0028 决策7:单进程单端口同源服务,无 CORS。

**触达**:
- `crates/server/src/main.rs:main` — 加 `web_dist_dir()`(`UNDERSTAND_BOOK_WEB_DIST` 覆盖,默认 `packages/web/dist`)并在 worker 中先判静态文件,静态 I/O 不持 `AppState` 锁。
- `crates/server/src/main.rs:static_response` — 非 API 的 `GET` 请求服务 `dist/<path>`;具体文件不存在则 fallback 到 `index.html`;路径拒绝 `..`、`.`、反斜杠和盘符冒号。
- `crates/server/src/main.rs:normalize_api_url` — 打包 SPA 的 `/api/*` 请求剥 `/api` 后再交给冻结命令面路由(`/book/*`、`/reader/*`、`/memory/*`、`/agent/*`),避免改 `server::route` 纯函数契约。
- `crates/server/src/main.rs:mime_for` — 覆盖 Vite 产物常见 MIME(html/js/css/json/svg/png/jpg/webp/ico/woff/woff2)。
- `skills/read/SKILL.md`(新) — `/understand-book:read` 启动说明:先 `pnpm -C packages\web build`,再 `cargo run -p server -- <book-dir>`,同端口打开 SPA+API。

**入口**:`pnpm -C packages\web build` → `cargo run -p server -- <book-dir>`;浏览器打开 server 打印的 `http://127.0.0.1:8787`。
**测试**:`cargo test -p server` 23(lib)+4(main) 全绿;`cargo build -p server` 绿;`pnpm -C packages\web build` 绿;真实同源 smoke(临时端口 18787,真书基座):`GET /` → HTML + app mount,`GET /api/book/manifest` → JSON + tree,`GET /not-a-real-spa-route` → HTML fallback。
**未闭**:浏览器人工完整验(S10c/f/g 的真 LLM + UI 操作)仍按前文 B2 清单执行;S10h/S10i 隐藏 LID 和自然定位入口未做。
## 2026-06-25 · start.bat 切到 S10e 打包单进程启动

> 切片1 启动入口修缮;把旧 dev 双进程(server + Vite)入口改为 S10e 产品形态入口。

**触达**:
- `start.bat` — 启动模式改为 packaged S10e:默认 `UNDERSTAND_BOOK_ADDR=127.0.0.1:8787`,默认 `UNDERSTAND_BOOK_WEB_DIST=%CD%\packages\web\dist`;校验 book `base.json`;默认 dist 缺失时自动 `pnpm -C packages\web build`;启动单个 `cargo run -p server -- <book_dir>` server 窗口并打开同端口 URL。
- `start.bat` — 移除 Vite dev server 常驻启动;`pnpm` 仅在 dist 缺失需要构建时才要求存在。

**入口**:双击 `start.bat` 或 `start.bat <book_dir>`;关闭 server 窗口停止服务。
**测试**:`cmd /c start.bat __missing_book_for_syntax_check__` 正常进入缺失 book 错误分支(exit 1),验证 batch 括号块解析与路径检查;未启动 server/浏览器。
## 2026-06-25 · asset 契约修订:Formula 升格为第四类核心 asset + FormulaSemantics

> 文档/契约切片;未改 schema/adapter 实现代码。把公式从长尾类型升格为核心 asset,并定义 agent 交互必须消费的公式语义剖面。

**触达**:
- `docs/adr/0029-*` — 修订 ADR-0029:`NodeKind` 目标从 Code/Table/Image 扩为 Code/Table/Image/Formula;新增 FormulaSemantics 决策(参数 parameters、组合含义 composition、上下文关系 context_links,均带 LID 证据);Formula 不再是长尾。
- `docs/切片方案-asset一等对象.md` — asset 刀总声明与 SA0-SA6 重写:SA0 统计 formula_count;SA1 加 Formula/FormulaSemantics schema;SA2/SA3 识别 md/epub 公式;SA4 禁止公式句切;SA5 专门抽公式语义剖面;SA6 真书验公式语义可检索。
- `CONTEXT.md` — `asset 叶子` 扩四类,新增 `公式语义剖面 (FormulaSemantics)` 术语。
- `docs/切片方案-切片1前端阅读器.md` — S10j 前端消费目标扩到 Formula,要求展示公式原文与公式语义剖面。
- `docs/adr/0032-*` — 粒度体检纳入 formula asset 候选数量,并规定 Formula 叶子固定不做句级展开。
- `docs/参考对照-文档世界状态-优化登记.md` / `需求文档-V3.md` — 同步四类 asset 与 agent 公式上下文要求。

**入口**:后续实现从 `docs/切片方案-asset一等对象.md` 的 SA0 开始;不得跳过粒度体检直接改 schema。
**测试**:`rg` 扫描 asset/Formula 相关旧表述,确认 Formula 已从长尾改为核心类型;本切片未改可执行代码,未跑 cargo/pnpm。

## 2026-06-25 · SA0 段/句粒度体检报告(GranularityProfile)

> asset 一等对象实现第一刀;只做正式构建前体检,不改 schema/adapter asset kind/正式 LID 构建。

**触达**:
- `packages/core/src/granularity.ts:buildGranularityProfile` — 新增确定性 `GranularityProfile`:段落数、估算句数、每段句数 avg/p50/p90/max、长段计数、`paragraph/hybrid/sentence` 三档 projected_lid_count 与 expansion_ratio、推荐档位。
- `packages/core/src/granularity.ts:countMarkdownAssetCandidates/countEpubAssetCandidates` — Markdown 源文本和 EPUB XHTML 的 code/table/image/formula 候选计数;不改变现有 `SourceBlock` 或 `NodeKind`。
- `skills/build/granularity-profile.ts` — SA0 CLI:`pnpm exec tsx skills/build/granularity-profile.ts <epub|md>` 输出人读摘要 + JSON。
- `packages/core/test/granularity.test.ts` — 覆盖句数估算、Markdown/XHTML/EPUB asset 候选计数、LID 膨胀估算和推荐逻辑。
- `skills/build/SKILL.md` — build 编排骨架加入第 0 步 SA0,明确用户确认粒度后才进入正式构建。

**入口**:`pnpm exec tsx skills/build/granularity-profile.ts <epub|md>`;输出推荐但不自动构建。
**测试**:`pnpm -C packages/core test`(38 例全绿)+ `pnpm -C packages/core typecheck` + `pnpm exec tsx skills/build/granularity-profile.ts packages/core/test/fixtures/sample.md`。

## 2026-06-25 · SA1 schema 扩:NodeKind asset 四值 + ManifestNode.kind + FormulaSemantics

> asset 一等对象第二刀;只做 schema/DTO/TS 导出,不碰 ingestion、segment 或 adapter 行为。

**触达**:
- `crates/base-schema/src/lib.rs:NodeKind` — 增 `Code/Table/Image/Formula` 四个 serde snake_case 枚举值,保持只增不改。
- `crates/base-schema/src/lib.rs:FormulaSemantics` — 新增公式语义剖面 schema:parameters/composition/context_links,均带 LID 证据字段;本刀先独立导出,未挂入 `ReadOnlyBase` 必填字段。
- `crates/read-tools/src/lib.rs:ManifestNode` — `book.manifest` 投影新增 `kind: NodeKind`,从 `LidNode.kind` 透传。
- `packages/core/src/generated/Formula*.ts` / `NodeKind.ts` — ts-rs 生成 FormulaSemantics 相关 TS 类型和扩展后的 NodeKind。
- `packages/web/src/generated/ManifestNode.ts` — ts-rs 生成 manifest 节点 `kind` 字段,前端类型通过。
- `packages/core/src/zod.ts` — 手写 zod 镜像同步 NodeKind 四值和 FormulaSemantics schema。

**入口**:`book.manifest()` 返回的 `tree[].kind`;FormulaSemantics schema 供 SA5 抽取/存储接入。
**测试**:`cargo test -p base-schema -p read-tools`;`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;`pnpm -C packages/web typecheck`;`cargo test --workspace`;`pnpm -C packages/web build`。
