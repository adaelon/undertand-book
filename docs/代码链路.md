# 代码链路(改动账本)

> 每个切片完成时当场追加一条,精确到 文件:符号。供日后反查「当时为什么改了这个文件」。

## 2026-06-22 · S0 monorepo 骨架 + 插件外壳 + TS↔Rust 基座 schema 链路

**触达**:
- `Cargo.toml` / `pnpm-workspace.yaml` / `package.json` / `tsconfig.json` — monorepo 三面骨架(cargo workspace + pnpm workspace 并置)`[ADR-0021]`
- `.claude-plugin/plugin.json` — 本地 Claude Code 插件清单(name understand-book)`[ADR-0022]`
- `skills/build/SKILL.md` — `/understand-book:build` 入口(插件 skill 命名空间 `/插件名:skill文件夹名`)+ 7 段管线编排骨架(占位,实编排留 S1–S3)`[ADR-0022]`
- `agents/pass1-local-extractor.md` / `agents/pass2-longrange-linker.md` — subagent 占位(I/O 契约已声明,实现留 S3)`[ADR-0010]`
- `crates/base-schema/src/lib.rs` — Rust 权威基座 schema:`Span/NodeKind/LidNode/GraphNodeType/GraphNode/EdgeScope/GraphEdge/ReadOnlyBase`(serde+ts-rs+schemars)+ `sample_base()` `[ADR-0021]`
- `crates/base-schema/tests/roundtrip.rs` — `rust_sample_roundtrips`(Rust 自洽)+ `ts_fixture_deserializes_zero_mismatch`(跨语言闸:读 TS fixture 零失配 + 逐字段比对)
- `packages/core/src/generated/*.ts` — ts-rs 生成的 8 个 TS 类型(权威产物,`export_to` 相对 src 目录三级)
- `packages/core/src/zod.ts` — 镜像 Rust 定义的 zod 自检 schema(产出前运行时闸)
- `packages/core/src/sample.ts` — `buildSampleBase()` 按生成类型构造样例基座
- `packages/core/test/roundtrip.test.ts` — vitest:zod 自检 + 产 `crates/base-schema/fixtures/sample-base.json`

**入口**:`/understand-book:build <epub|md>`(预构建,插件 slash 命令;`claude --plugin-dir <repo>` 加载);基座 schema 重生成 = `cargo test -p base-schema`(触发 ts-rs export)。
**测试**(三道确定性闸,全绿 = S0 判据① 闭环):
- `cargo test -p base-schema` — 8 export_bindings + rust_sample_roundtrips + ts_fixture_deserializes_zero_mismatch
- `pnpm -C packages/core test` — zod 自检 + 产 fixture
- `pnpm -C packages/core typecheck` — tsc --noEmit,验 sample 符合 ts-rs 生成类型

**未闭**:判据②(`/understand-book:build` 被 Claude Code 识别为命令)需在 harness 内加载插件验证。

## 2026-06-22 · S1(核心)段级 LID 切分器 + 分区不变式闸 + md 适配器

**触达**:
- `packages/core/src/segment.ts:segment` — Model A 切分(章/节=纯结构容器 span=子并集;标题=容器首个叶子段)→ `LidNode[]` `[ADR-0008]`
- `packages/core/src/partition.ts:checkPartitionInvariant` — 分区不变式闸:LID 唯一 / 同级数值递增 / 父⊇子 / 叶子全覆盖·无重叠 / 非内容字节显式归类 + 切段覆盖率
- `packages/core/src/md-adapter.ts:markdownToBlocks` — md → 忠实块映射 `SourceBlock[]`(`#` 行=heading,span 含 marker;空行分段)
- `packages/core/test/fixtures/sample.md` — 测试样书
- `packages/core/test/segment.test.ts` — vitest:切分→不变式→Model A 结构→span 回切→`LidNodeZ` 校形状(5 例)
- `packages/core/src/epub-adapter.ts:xhtmlToBlocks/epubToSource` — epub 解 zip(fflate)+ spine 顺序解析 xhtml(node-html-parser),忠实块映射(h1-6/p/li/blockquote/pre;嵌套块不重复下钻)→ `{source, blocks}`
- `packages/core/test/epub-adapter.test.ts` — vitest:块抽取 + 合成 epub 全链路分区不变式 100%(2 例)
- `skills/build/split-lid.ts` — S1 CLI(经 tsx 运行):epub/md→segment→分区不变式闸,打印报告、违例非零退出
- `.npmrc` — `node-linker=hoisted`(Windows 无符号链接权限,避 pnpm EPERM symlink)

**入口**:`pnpm exec tsx skills/build/split-lid.ts <epub|md>`(SKILL.md 编排第 1 段调它)。
**测试**:`pnpm -C packages/core test`(9 例:S0 2 + segment 5 + epub 2,覆盖率 100%)+ `pnpm -C packages/core typecheck`(tsc 0)+ `cargo test -p base-schema`。
**真书实测**:《游戏编程模式》epub → 3047 块 → 3399 节点(3047 叶 + 352 容器),source 25 万字,**分区不变式 ok、切段覆盖率 100.0000%** ⇒ S1 判据达成。
**未闭**:产物写盘 `.understand-book/`(留 S3/S7 整合);字节精确 span(中文 UTF-16↔字节,影响 S4 `book.text` 取真原文)留精化。

## 2026-06-22 · S2 窗口切分(LID 子树窗口 + 双约束预算 + 确定性 token 近似)

**触达**:
- `packages/core/src/window.ts:splitWindows` — 按 LID 章/节子树切窗口:atomic 子树整窗、超限子树内逐叶贪心细分(吸附 LID 边界不腰斩)、过小由贪心累加自然合并、顶层间封口(不跨卷);单叶超硬闸自成一窗标 `overBudget`(诚实不腰斩)`[ADR-0009]`
- `packages/core/src/window.ts:estimateTokens` — 确定性近似 token(CJK `/[一-鿿]/` 按 1、其余 0.25,ceil),零依赖
- `packages/core/src/window.ts` — `WindowBudget`(maxInputTokens 硬闸 + maxLeavesSoft 软闸)/ `DEFAULT_BUDGET`(12000/80 占位,留实测)/ `Window`(leafLids/tokens/spans/overBudget)
- `packages/core/test/window.test.ts` — vitest 8 例:estimateTokens + 划分(无遗漏无重叠)+ token 一致 + 细分 + 过小合并 + 软闸 + 不跨卷 + 单叶超限不腰斩
- `skills/build/window-cli.ts` — S2 CLI(tsx):适配器→segment→splitWindows→窗口数/token 分布 + 窗口层划分校验,违例非零退出

**入口**:`pnpm exec tsx skills/build/window-cli.ts <epub|md> [maxInputTokens] [maxLeavesSoft]`(SKILL.md 编排第 2 段;消费 S1 `LidNode[]` + source)。
**测试**:`pnpm -C packages/core test`(17 例:S0 2 + segment 5 + epub 2 + window 8,全绿)+ `typecheck`(tsc 0)。
**真书实测**:《游戏编程模式》3047 叶/正文≈174k tok。默认 12000/80→**64 窗口**(利用率 22.7%,median 3233),小预算 2000/40→152 窗口(57.3%);两者 **partition.ok、单叶超限=0、每窗 ≤ 硬闸** ⇒ S2 判据达成。完整预算扫描(软闸/硬闸敏感性 + 三层约束根因)见 `docs/实测-S2窗口利用率.md`;结论回填 ADR-0009「何时回头」。
**未闭**:软闸/硬闸最终数字待 S3 接 Pass1 抽取质量实测后定(当前为占位);融合批次打包进单次 subagent 调用 + 并发数留 S3。

## 2026-06-22 · S3 语义边两遍抽取确定性骨架(Pass1 输入 + merge+闸 + 目录 + schema 补 direction)

**触达**:
- `crates/base-schema/src/lib.rs:Direction` — 新增边方向枚举(directed/undirected),merge 去重键成员 `[ADR-0011/0023]`;`GraphEdge` 加 `direction` 字段;**不补 description**(收窄 ADR-0010,见 ADR-0023)。重生成 `generated/{Direction,GraphEdge}.ts`,同步 `zod.ts`/`sample.ts`/`sample_base()`/roundtrip fixture。
- `packages/core/src/pass1-input.ts:buildPass1Input/buildPass1Inputs` — Window → 每段前缀 `[LID]` 标注的抽取输入正文(回填红线物理前提 `[ADR-0004]`),纯确定性 `[ADR-0010]`
- `packages/core/src/merge.ts:mergeAndGate` — 节点按类型合并 occurrences(实体/概念并集 name keep-last、断言 keep-last)+ 确定性图谱闸(悬空丢不重建 + 最小连坐:边端缺失只丢边/断言锚悬空丢断言连坐边/实体部分 occ 悬空剔锚、全悬空丢节点)+ 边去重 (source,target,type,direction) keep higher weight(undirected 规范化端点)+ 可观测报告(锚定率/每条丢弃可见)`[ADR-0010/0011]`
- `packages/core/src/catalog.ts:projectCatalog` — merge 后 nodes 确定性投影全局目录(零幽灵,切片0 不带 summary)`[ADR-0010]`
- `agents/pass1-local-extractor.md` — Pass1 subagent 真实 prompt(抽实体/概念/断言+局部边,严格 JSON,回填红线/单一职责/不产长程边)
- `skills/build/SKILL.md` — 编排骨架第 2–5 段指向真实模块(window/pass1-input/merge/catalog)
- `skills/build/pass1-smoke.ts` — Pass1 小样真实抽取 smoke(list/emit/verify)
- `packages/core/test/{pass1-input,merge,catalog}.test.ts` — vitest 新增 17 例(pass1-input 4 + merge 9 + catalog 4)

**入口**:`buildPass1Inputs(windows,nodes,source)` → subagent `pass1-local-extractor`(harness 供 LLM,5 并发)→ `mergeAndGate(outputs, lidNodes)` → `projectCatalog(nodes)`。smoke:`pnpm exec tsx skills/build/pass1-smoke.ts <书> [<idx> emit|verify <json>]`。
**测试**:`pnpm -C packages/core test`(34 例全绿)+ `typecheck`(tsc 0)+ `cargo test -p base-schema`(11,含 ts_fixture 零失配,direction 贯通 Rust→TS)。
**小样真实抽取 smoke**:窗口 #12「2.4 类风格化还是函数风格化」(18 段)真实抽取 13 节点/8 边 → **回填红线窗口外 LID=0**、闸后全留、目录 13 条零幽灵 ⇒ pass1 prompt + 回填红线 + 确定性闸 链路打通。
**未闭**:全书 64 窗口真实 LLM Pass1 抽取 + 图谱锚定率≥90% 实测 + 固化 `.understand-book/` 留下一刀(S3 收尾);Pass2 长程边切片0 砍、留切片1+。

## 2026-06-22 · S3.5 小规模 Pass1 实测(4 窗口手抽 + 产小基座)

**触达**:
- `skills/build/pass1-batch.ts` — 消费多窗口抽取结果 → `mergeAndGate` → 锚定率(全书分母 + 已抽窗口分母)+ 固化 `.understand-book/<book_id>/base.json`(`ReadOnlyBaseZ` 产出前校验)
- `skills/build/fixtures/pass1-sample-4windows.json` — 窗口 #9/#12/#14/#17 的手工真实抽取结果(可复现基座 + S4 夹具来源)

**入口**:`pnpm exec tsx skills/build/pass1-batch.ts <书> skills/build/fixtures/pass1-sample-4windows.json "9,12,14,17"`(PowerShell 注意给索引列表加引号,逗号否则被当数组操作符)。
**实测**:41 抽取节点→merge 跨窗口合并 1(`entity:flyweight_pattern` 跨 #12/#14 occ 并集)→闸后 40 节点/26 边/目录 40 零幽灵/零丢弃;基座 zod 校验通过。**已抽窗口锚定率 55.36%(31/56)**,根因(非内容叶+召回)+ 全书口径建议见 `docs/实测-S3小规模锚定率.md`,回填 ADR-0011。
**未闭**:`.understand-book/` 基座 gitignore(生成物,由 fixture + pass1-batch 重建);全书锚定率定量仍留下一刀。

## 2026-06-23 · S4 读时确定性叶子工具(Rust:manifest/text/context-near/concept)

**触达**:
- `skills/build/pass1-batch.ts` — 固化 base.json 时同写 `.understand-book/<id>/source.txt`(原文旁路,book.text 取真原文用)`[ADR-0024]`
- `crates/base-schema/src/lib.rs:Span` — 注释口径修正「字节区间」→「UTF-16 code unit 区间」(消 schema↔segment.ts 实现矛盾);重生成 `generated/Span.ts` `[ADR-0024]`
- `crates/read-tools/src/lib.rs:Book` — 新 crate(纯函数库)。`load/new`(serde 读 base.json + source.txt,原文存 `Vec<u16>`)/ `text`(按 UTF-16 span 切真原文,单 LID 或 LID 区间)/ `manifest`(tree + stats_by_lid:child/leaf/anchored)/ `context_near`(树邻接 parent·child·prev·next + scope=local 边,自指边跳过、weight 降序、同 LID 去重、top-K)/ `concept`(全量 occurrences + related_entities);helpers `nodes_anchored_at`/`lids_of_node`/`parent_lid`;`ToolError` 信封(LID_NOT_FOUND/CONCEPT_NOT_FOUND,禁宽松降级)`[ADR-0014/0015/0024]`
- `crates/read-tools/src/main.rs` — CLI:`manifest [lid]` / `text <lid> [end_lid]` / `context <lid> [k]` / `concept <name>`
- `crates/read-tools/Cargo.toml` — 依赖 base-schema(path)+ serde/serde_json

**入口**:`cargo run -p read-tools -- <book_dir> <cmd>`(headless 库+CLI;localhost/HTTP 暴露留 S7)。`<book_dir>` = `.understand-book/<id>`。S5+ 直接调 `Book::{context_near,text,...}`。
**测试**:`cargo test -p read-tools`(6:text 2 / manifest 1 / context 2 / concept 1);全 workspace `cargo test` 全绿(17 = base-schema 11 + read-tools 6)。
**真基座实测**(《游戏编程模式》小基座 3399 LID / 40 图谱节点):`text 11.18.4` 逐字 == TS 预览(**UTF-16 跨语言一致**,证 ADR-0024 切法);`concept 享元模式` occ 跨 11.19/12.10 多锚;`context 12.10.2` near = 树邻接3(parent/prev/next)+ local边2(11.19.5 exemplifies 0.8 / 12.10.5 about 0.7),自指边(享元↔共享实例同锚 12.10.2)正确跳过。
**未闭**:context mid/far(概念二跳 / long_range)+ via `concept` kind 留切片1+;`book.query`(S5);`range` 仅支 LID 区间(段内字符 range 留后);near 默认 K=10 占位待 S8 回填;manifest leaf_count O(n²) 暂可接受(慢再优化)。

## 2026-06-23 · S5a book.query 内层 mini-loop 确定性骨架 + ModelAdapter trait + FakeAdapter(Rust)

**触达**:
- `crates/runtime/Cargo.toml` — 新 crate(模块 E 自建运行时),依赖 `read-tools`(检索骨架)+ `base-schema` `[ADR-0025]`
- `crates/runtime/src/lib.rs:Scope` — scope 两档枚举(Local/Chapter,顶档 chapter,无 global)`[ADR-0016/0025]`
- `...:ModelAdapter`(trait)— `complete(CompletionRequest{system,user}) → ParsedResponse{sufficient,answer?,citations,model_supplement}`,loop 与后端之间 provider 无关薄层 `[ADR-0016]`
- `...:retrieve` — 确定性档位检索(local=anchor∪`context_near`;chapter=local∪章内 anchored 叶);`chapter_root` 用 `NodeKind∈{Section,Chapter}` 上溯定位章(非物化路径段数)、`anchored_leaves_under` 收章内有锚定的叶 LID `[ADR-0025]`
- `...:query` — 内层 loop:合一轮 + **确定性交叉验停**(`citations⊆证据集` 悬空滤净 = 结构红线)+ 零有效/不足外扩(local→chapter)+ 触顶 `incomplete`+`CONTEXT_BUDGET_EXCEEDED` `[ADR-0004/0016]`
- `...:QueryResponse/Citation/SupplementOut` — 对外响应(符 V3 §4.1 核心子集;citations 全真 LID、`model_supplement` 无 LID 物理隔离)
- `...:#[cfg(test)] FakeAdapter` — 脚本化确定性测试替身(loop 每轮调一次 complete,按序吐)

**入口**:`runtime::query(book, q, anchor, adapter)`(S6 外层 E loop 调它;S5b NativeAdapter 真实对接)。
**测试**:`cargo test -p runtime`(4:留证 local 收口 / 零有效强制外扩+悬空滤净 / 触顶 incomplete / retrieve 含 anchor 真原文);全 workspace `cargo test` 全绿(21 = base-schema 11 + read-tools 6 + runtime 4)。
**未闭**:**S5b** = NativeAdapter(读 `.env` OpenAI-兼容端点 + CLI `query` 子命令 + 真实 glm-5.1 端到端裸调人工验);scope 档检索体积上限待实测;chapter 档真基座行为待 S5b 真跑观察。

## 2026-06-23 · S5b NativeAdapter(读 .env OpenAI 兼容)+ CLI + 真实 glm-5.1 端到端验

**触达**:
- `crates/runtime/Cargo.toml` — 加 `ureq`(features json,同步 HTTP)+ `dotenvy` 依赖(切片0 不上 tokio/async,承 `[ADR-0024]` 精神)
- `crates/runtime/src/lib.rs:NativeAdapter` — `from_env`(读 `OPENCODE_API_KEY/OPENCODE_BASE_URL/FLUID_LLM_MODEL`)+ `complete`(POST `{base}/chat/completions`,`response_format=json_object` + system 拼 `OUTPUT_CONTRACT`,`strip_fence` 剥 markdown 容错,`LlmOut` 中间解析 → `ParsedResponse`)`[ADR-0003/0025]`
- `...:OUTPUT_CONTRACT`(const)— JSON 输出契约;**S5b 真跑调优**:澄清 `sufficient` 语义 + `answer`/`model_supplement` 分工(否则 answer 错塞 supplement + 误判 incomplete,见 ADR-0025 S5b 回填)
- `crates/runtime/src/main.rs` — CLI:`runtime <book_dir> query <anchor_lid> <question...>`(读 .env,真实后端裸调 query 输出 JSON)

**入口**:`cargo run -p runtime -- <book_dir> query <anchor_lid> <question...>`(S6 外层 E loop 改调 `runtime::query`)。
**测试**:全 workspace `cargo test` 全绿(21)+ `clippy --workspace` 零警告;NativeAdapter 真实调用走人工验(真实 LLM 不可确定性复现,不入自动测,守 A2/B2)。
**真跑实测**(glm-5.1,anchor `11.18.4`):契约调优后 `answer` 完整正确(命令模式↔闭包)+ `citations` 3 条全真 LID(`11.18.2/4/5`,逐字原文)+ `model_supplement` 物理隔离(仅 GoF 定义)+ `scope_used=local` 收口不过度外扩 + `incomplete=false`。**结构红线 100%**。
**未闭**:scope 档检索体积上限 / chapter 真基座行为待更多查询观察;`book.query` 经 provider 抽象多后端留切片1+;S6 外层 E loop。

## 2026-06-23 · S6a 最小 memory 模块(Record + save/recall + 独立 JSON 落盘)

**触达**:
- `crates/runtime/src/memory.rs` — 新模块(读时运行时状态,非冻结基座)。`Record/Anchor/MemCitation/Usage`(符 V3 §4.3/`[ADR-0015]`,`type` serde rename)+ `SaveInput`(mem_id/citations 可缺)+ `RecallQuery`(切片0 维度);`MemoryStore::{open,default_path,save,recall}` —— `save`=内容寻址 mem_id(`fnv1a` 稳定哈希,`mem_{book|type|anchor|content}`)upsert + note/highlight citation 自动派生(锚回 anchor.lid)+ count 累加 + 整文件重写;`recall`=book_id/lid/type/layer 精确 + text 子串合取过滤、按 mem_id 排序(确定性);时间戳/路径调用方注入(可测)`[ADR-0026/0015/0006]`
- `...default_path` — `UNDERSTAND_BOOK_MEMORY_DIR` env 覆盖,否则 `<home>/.understand-book/memory/memory.json`;**绝不落进 `.understand-book/<book_id>/`(只读基座),守物理隔离 `[ADR-0006]`**
- `crates/runtime/src/lib.rs` — 加 `pub mod memory;`;错误信封复用 `read_tools::ToolError`(IO/解析失败 → `INTERNAL_ERROR`,守统一信封 `[ADR-0015]`)

**入口**:`MemoryStore::open(path)` → `save(SaveInput, now)` / `recall(&RecallQuery)`(S6b 外层 loop 的 `memory.save/recall` 工具调它;落盘默认走 `default_path()`)。
**测试**:`cargo test -p runtime`(10:4 query + 6 memory)全绿;全 workspace `cargo test`(27 = base-schema 11 + read-tools 6 + runtime 10)+ `clippy --workspace` 零警告。memory 6 测:save→recall 往返 / citation 自动派生 / position 不派生 / 内容寻址 upsert 幂等(count 累加不重复)/ recall 维度过滤合取 / 落盘隔离持久化重载。
**未闭**:concept recall 维度 + 跨书 + Phase1/2 consolidation + 四层产物 + 遗忘留议题7/切片1+;memory.* 接进外层 loop tool 集留 **S6b**;真实 home 目录落盘走 S6c 真跑(单测用 temp 隔离)。

## 2026-06-23 · S6b 外层 E 编排 loop + ModelAdapter.chat + 双重停机(FakeAdapter 确定性测)

**触达**:
- `crates/runtime/src/lib.rs` — `ModelAdapter` trait 加 `chat(&[Message],&[ToolSpec])→AssistantTurn`(`complete` 内层不动);新协议类型 `Role/Message/ToolCall/ToolSpec/AssistantTurn`;`message_to_json`(Message→OpenAI 请求体形:assistant tool_calls 嵌套 `{id,type,function}`、tool 带 tool_call_id);`NativeAdapter::chat`(POST `/chat/completions` 带 `tools` schema,解析 `choices[0].message.{content,tool_calls}` + `usage.total_tokens`→AssistantTurn)`[ADR-0026]`;内层 query 测的 FakeAdapter 补 `chat` 桩(unimplemented,该测不涉外层)
- `crates/runtime/src/orchestrator.rs` — 新模块。`run(book,store,adapter,question,now,cfg)→OuterOutcome`:messages 会话态 loop——`chat`→无 tool_calls 即终答(incomplete=false)/有则追加 assistant 回合 + 逐个 `dispatch` 回填 `role:tool` 结果 → 双重停机(`turns≥max_turns ∨ spent>token_budget`,`usage.total_tokens` 累加、无 usage 退 `estimate_tokens` 兜底)触顶标 `incomplete`+`CONTEXT_BUDGET_EXCEEDED`;`tool_specs()` 7 工具 JSON-Schema(book.query/text/context/concept/manifest + memory.save/recall);`dispatch` 派发到 `crate::query`/`Book::*`/`MemoryStore::*`,**错误原样回喂 ToolError 信封(不降级)**`[ADR-0026/0015/0016]`;`OuterConfig`(max_turns=12/token_budget=120k 占位)
- `crates/runtime/src/lib.rs` — 加 `pub mod orchestrator;`

**入口**:`runtime::orchestrator::run(...)`(S6c CLI `chat` 子命令 + 真实后端驱动它)。
**测试**:`cargo test -p runtime`(14:4 query + 6 memory + 4 orchestrator)全绿;全 workspace `cargo test`(31)+ `clippy --workspace --all-targets` 零警告。orchestrator 4 测(FakeAdapter 双队列脚本化 chat+complete):多跳收敛(query→memory.save→终答,内层 complete 联动、citation 自动锚回)/ max_turns 触顶标 incomplete / 工具错误回喂 LID_NOT_FOUND 信封非静默 / 报错后 loop 继续并收敛。
**未闭**:`NativeAdapter::chat` 真实 glm-5.1 验证(原生 tools 是否稳)+ CLI `chat` 子命令留 **S6c**(真实 LLM 不入自动测,守 A2/B2);max_turns/token_budget 实测回填 ADR-0016。

## 2026-06-23 · S6c CLI chat 子命令 + 真实 glm-5.1 外层 loop 端到端人工验

**触达**:
- `crates/runtime/src/main.rs` — CLI 重构为子命令分发:`query`(S5b 内层,不变)+ 新 `chat <question...>`(读 `.env` NativeAdapter + `MemoryStore::open(default_path())`,驱动 `orchestrator::run`,输出 `OuterOutcome` JSON)`[ADR-0026]`;`now_ts()`(epoch 秒,memory 时间戳)`[ADR-0025/0026]`
- (`NativeAdapter::chat` 实现已随 S6b 落地;S6c 只加 CLI 入口 + 真跑验)

**入口**:`cargo run -p runtime -- <book_dir> chat <question...>`(读 `.env`,真实后端驱动外层 E loop)。
**测试**:全 workspace `cargo test`(31)+ `clippy` 零警告不变;`chat` 走真实 LLM 人工验(不入自动测,守 A2/B2,镜像 S5b)。
**真跑实测**(glm-5.1,书 `game-programming-patterns`,问「查证命令模式核心思想并记笔记」):外层 loop **10 轮自主多跳**(concept/query/text + memory.save)收敛、`incomplete=false`、tokens 31608(<120k budget)。确定性核查 memory 库:落 1 条 note,`anchor.lid="11"` + `citations=[{lid:"11",book_id}]`,LID 11 = 真 LID(`text 11`→"第2章 命令模式",可跳原文)。**多跳收敛 + 触顶诚实 + recall 带可验证 LID citation 三判据兑现**;回填 ADR-0026 何时回头(glm-5.1 tools 稳 / usage 口径 / max_turns 偏近上限待 S8)。
**未闭**:max_turns/token_budget 实测数(单次 10/12 偏近,待 S8 金标准集观察)回填 ADR-0016;reader.* 四动作 + 闭环留 S7;consolidation/跨书/concept recall 维度留议题7/切片1+。

## 2026-06-23 · S7 命令优先阅读器 + 闭环四动作(memory 抽 crate 拆环 + reader crate + 接入外层 loop)

**触达**:
- `crates/memory/{Cargo.toml,src/lib.rs}` — **S7a 从 runtime 抽出的独立 crate**(deps: read-tools/serde/serde_json):内容原样搬 `runtime/src/memory.rs`(`Record/Anchor/MemCitation/Usage/SaveInput/RecallQuery/MemoryStore` + 6 测),零行为变更。拆 `runtime↔reader` 循环依赖(crate 强制 DAG)+ 兑现 memory 独立模块地位 `[ADR-0027/0006]`
- `crates/runtime/src/memory.rs` — **删除**(搬至 memory crate);`runtime/src/lib.rs` 删 `pub mod memory;`;`runtime/Cargo.toml` 加 `memory`+`reader` 依赖;`orchestrator.rs`/`main.rs` 的 `crate::memory::`/`runtime::memory::` → `memory::` `[ADR-0027]`
- `crates/reader/{Cargo.toml,src/lib.rs}` — **S7b 新 crate**(deps: read-tools/memory/serde;dev: base-schema)。`Reader`(会话态:leaf_lids 叶序 + anchor_idx + radius + selection)/ `Viewport{anchor_lid,visible_lids}` + `ViewportEffect/HighlightEffect/NoteEffect/ReaderState`(返 effect 非裸 ack)。`new`(叶序=lid_nodes 过滤 children 空)/ `viewport`(**叶序滑动窗口** anchor±radius saturating)/ `goto_lid`(叶→锚该叶、容器→子树首叶、不存在→LID_NOT_FOUND 不降级)/ `scroll`(delta clamp)/ `highlight`(委托 memory.save,content=book.text 原文片段)/ `note`(委托 memory.save,content=text)/ `state` / `render`(读 memory.recall 画标注,**标注单源=记忆层**)`[ADR-0007/0015/0027]`
- `crates/runtime/src/orchestrator.rs` — **S7c reader 接入外层 loop**:`tool_specs()` 加 reader.gotoLid/scroll/highlight/note/state 五工具、**移出 book.manifest**(真跑实测全树 token 炸弹);`dispatch` 加 reader.* 分支 + `reader:&mut Reader` 参数;`run` 建会话态 `Reader::new(book,DEFAULT_RADIUS)` 贯穿 loop;SYSTEM_PROMPT 强化「要求翻页/高亮/记笔记必须调对应 reader 工具」;新增 `UB_TRACE` env-gated loop trace(诊断设施)`[ADR-0026/0027]`

**入口**:`reader::Reader::{new,goto_lid,scroll,highlight,note,state,render}`(headless 库);经外层 `runtime::orchestrator::run` 的 reader.* 工具由 agent 驱动;CLI `runtime <book_dir> chat <q>`(`UB_TRACE=1` 开 trace)。
**测试**:`cargo test --workspace`(**42 = base-schema 11 + memory 6 + read-tools 6 + reader 10 + runtime 9**)+ `clippy --workspace --all-targets` 零警告。reader 10 测:叶序窗口/scroll clamp/goto 叶·容器·不存在/note·highlight 委托 memory 单源/highlight 取原文/render 读 recall 画标注/标注跨 reader 实例可见(证单源)。orchestrator +1:`closed_loop_query_goto_highlight_note`(FakeAdapter 脚本化「问→跳转→高亮→记笔记」,标注真落 memory、citation 锚回真 LID)。
**真跑实测**(glm-5.1,书 game-programming-patterns,临时 memory 隔离):给定 LID「把 11.18.2 高亮并记笔记」→ 外层 loop **turn1 并行调 reader.highlight+reader.note → turn2 终答**,incomplete=false、turns=2、tokens 2644。memory 库落 highlight(content=11.18.2 真原文片段)+ note(content=笔记),**两条 citation 均锚回真 LID 11.18.2**。**闭环判据兑现**(切片0 总判据第 3 条)。诊断发现回填 ADR-0027:① book.manifest 全树 token 炸弹(一次 38k→205k 触顶)→ 移出外层工具集;② glm 不给 LID 时倾向 book.* 反复探索不调 reader → prompt 强化。
**未闭**:DEFAULT_RADIUS(=3)叶序窗口半径 / max_turns / token_budget 实测回填 ADR-0016/0027(S8);不给明确 LID 时的定位语义(book.query/concept 探索收敛)属切片0 已有部分,与 reader 闭环正交;reader 全集(面板系统)+ 段内字符 range + 真 GUI 渲染层留切片1+;memory 默认落 `~/.understand-book/memory/`(真跑用 UNDERSTAND_BOOK_MEMORY_DIR 隔离)。

## 2026-06-23 · S8 金标准集 + 验收闸(内层 book.query 主度量;真跑结构红线 100%)

**选址决策**:金标准集针对**内层 `book.query`**(每条 `{q, anchor_lid, expect_cite[]}`),非外层 E loop——结构红线度量最干净、可复现、省 token,anchor 预供;直接消费 `runtime::query`。外层 loop 闭环已于 S7 兑现(切片0 总判据第 3 条),不在 S8 重复度量。

**触达**:
- `crates/runtime/src/goldset.rs` — 新模块 `[ADR-0004]`。`GoldItem{id,q,anchor_lid,expect_cite}` / `ItemResult` / `GoldReport`;**纯函数闸**:`structural_check`(返回 citation 全是真 LID 无悬空=结构红线,列悬空 LID)+ `semantic_metrics`(recall=|返回∩期望|/|期望|、precision=|返回∩期望|/|返回|,**人工评的客观信号非自动判定**)+ `build_report`(聚合结构红线率/均值/incomplete 数);runner `evaluate_item`/`run_goldset`(逐条 `crate::query`,返回去重 citation LID)。自动验收只到结构红线;语义达阈+人工试读由人裁(守 B2)`[ADR-0004]`
- `crates/runtime/src/lib.rs` — 加 `pub mod goldset;`
- `crates/runtime/src/main.rs` — CLI 加 `goldset <file.json>` 子命令:读金标准集 JSON → `run_goldset`(NativeAdapter 真跑)→ 输出 `GoldReport` JSON + stderr 一行汇总;**结构红线 <100% 非零退出**(确定性闸,违 ADR-0004 即 fail);errored 显式告警
- `crates/runtime/goldset/game-programming-patterns.json` — **12 条金标准集**(从真基座 40 节点确定性推导),覆盖 4 章区:命令模式/闭包/撤销/头等函数(11.18–11.19)、享元/工厂方法/对象池/状态(11.19/12.10)、简单性/抽象解耦(9.x)、观察者/数据绑定(13.12);每条 expect_cite = 锚定 claim 的真 LID
- `goldset.rs` runner **逐条容错**:provider 偶发错(空响应)重试一次,仍失败记 `ItemResult.error`+`GoldReport.errored` 单列(不算结构红线分母,显式记录非静默降级 `[ADR-0015]`);真跑首跑遇 1 条 glm 空响应即此设计的由来

**入口**:`cargo run -p runtime -- <book_dir> goldset crates/runtime/goldset/game-programming-patterns.json`(读 `.env`,真实后端)。
**测试**:`cargo test --workspace`(**47 = base-schema 11 + memory 6 + read-tools 6 + reader 10 + runtime 14**;runtime +5 goldset:结构红线判悬空 / 语义 recall·precision / 聚合 / errored 排除 / run_goldset 经 FakeAdapter 确定性端到端)+ `clippy -p runtime` 零警告。真 LLM 不入自动测(守 A2/B2)。
**真跑实测**(glm-5.1,真基座 game-programming-patterns):**结构红线 100.0%(12/12,0 悬空)** + mean_recall 1.00 + mean_precision 0.75 + incomplete 0 + errored 0;12 条全 local 档收口;answer 抽查贴原文正确。**切片0 总判据①(结构红线 100%)兑现** + 语义信号达基线(recall≥0.9 建议阈)。回填 ADR-0004(结构红线兑现 + 语义阈值建议 + 金标准集规模 12)、ADR-0016(内层 scope 全 local 收口)。
**未闭(切片0 收尾,需用户裁)**:① **人工试读认可**(语义质量最终拍板,人类动作);② **命令面契约冻结为基线**(V3 §6,切片0 完成充要)→ 冻结后切片0 完成,可起切片1+。

## 2026-06-23 · S9 ModelAdapter 确定性产物 JSON 规范性硬化(鲁棒抽取 + 抽不到诚实报错)

> 切片0 收尾后第一刀(不在切片0 判据内);**内部硬化,不动命令面契约**。

**触达**:
- `crates/runtime/src/lib.rs:extract_json_object` — 新增鲁棒 JSON 对象抽取:`strip_fence` 剥围栏 → 扫描第一个**平衡 `{}` 子串**(跳过字符串字面量内的括号 + `\` 转义,引号内 `{`/`}` 不计深度)→ 抽不到返 `None`。只负责「形状」抽取,值正确性仍由内层 `query` 的 `citations⊆证据集` 确定性交叉验停再校验 `[ADR-0016 决策5/ADR-0004]`;仅扫 ASCII 字节,切片落 char 边界(注释证安全)
- `crates/runtime/src/lib.rs:NativeAdapter::complete` — `serde_json::from_str(strip_fence(content))` → 先 `extract_json_object(content)`,**抽不到合法 JSON / 空响应 → 显式 `AdapterError`**(经 `query` 映射 `PROVIDER_ERROR`,不静默成功,守禁宽松降级 `[ADR-0015]`);外层 `chat`(原生 tool_calls)不动
- **不改** `OUTPUT_CONTRACT`/prompt(强后端走 `json_object` 模式,鲁棒抽取纯防御偶发;放宽 prompt 自由推理属 ReActAdapter/切片1+);**不动** `ParsedResponse`/命令面契约/错误信封/值校验

**入口**:`NativeAdapter::complete`(内层 `query` 唯一确定性产物路径调它)。
**测试**:`cargo test --workspace`(**50 = base-schema 11 + memory 6 + read-tools 6 + reader 10 + runtime 17**;runtime +3 S9:三形态抽对 / 字符串内括号·转义跳过 / 空·纯散文·不平衡→None)+ `clippy -p runtime --all-targets` 零警告。真 LLM 不入自动测(守 A2/B2)。
**判据**:① 三形态单测全过;② 抽不到/空内容 → `PROVIDER_ERROR` 非静默(`None`→`AdapterError`→`query` 映射);③ 真跑回归结构红线 100% **待人工验**(走 `.env`/glm-5.1,B2)。
**未闭**:判据③ 真跑回归(glm-5.1 偶发空/啰嗦响应消化验证)由用户拍板跑;内层 `query` 是否需逐条重试(goldset runner 已有重试,内层裸调暂无)留实测 `[ADR-0016]`;ReActAdapter 完整解析回填(注模板/多字段宽松容错)留切片1+。

## 2026-06-24 · S10a 新 server crate:book.* 只读 GET REST 投影 + API DTO ts-rs 导出 packages/web

> 切片1「前端阅读器」第一刀(A4 子切片);`[ADR-0028]` 命令面 REST 投影落地。本刀只投影 `book.*`(reader/memory→S10b、query→S10c、agent→S10f、静态资源→S10e)。

**触达**:
- `crates/server/Cargo.toml` — 新 crate(`tiny_http=0.12`,依赖 `read-tools`;`base-schema` 仅 dev 供 `sample_base`)。坐 DAG 顶端(S10a 只需 read-tools)`[ADR-0028 决策2]`
- `crates/server/src/lib.rs:route` — **纯函数路由** `route(&AppState, method, url) -> Reply`(脱 socket 可单测,守 A2):`/book/manifest`(GET)+ `/book/text?lid=&end=` + `/book/context?lid=&k=` + `/book/concept?name=` 1:1 投影 `Book` 四叶子 `[ADR-0028 决策3]`;`AppState{book}`(S10b 扩 reader/store)
- `crates/server/src/lib.rs:err_reply/status_for` — `ToolError` §4.4 信封**原样透传** + category→HTTP status(validation 400/not_found 404/provider 502/budget 429/internal 500);非 GET→405 `METHOD_NOT_ALLOWED`、未知路由→404 `ROUTE_NOT_FOUND`(传输层码,非冻结 command 枚举)
- `crates/server/src/lib.rs:parse_query/percent_decode` — 最小 query 解析 + `%XX`/`+` 解码(支持 CJK 概念名);`INVALID_RANGE`(缺必填参)/`INVALID_K`(k 非整数)
- `crates/server/src/main.rs:main` — tiny_http 同步、4 worker 线程共享 `Arc<Mutex<AppState>>`(单会话单书,不上 tokio `[ADR-0024/0028]`);`Book::load(<dir>)`,`UNDERSTAND_BOOK_DIR`/`UNDERSTAND_BOOK_ADDR`(默认 127.0.0.1:8787)
- `crates/read-tools/src/lib.rs` — `Manifest/ManifestNode/LidStats/Context/ContextItem/Via/Concept/ToolError` 补 `#[derive(TS)]` + `#[ts(export_to="../../../packages/web/src/generated/")]`(+ Cargo `ts-rs=10`)`[ADR-0028 决策6]`
- `crates/runtime/src/lib.rs` — `QueryResponse/Citation/SupplementOut` 补 `#[derive(TS)]` 同导出目标(+ Cargo `ts-rs=10`);S10a 先导出类型,query 端点 S10c 接
- `packages/web/src/generated/*.ts` — ts-rs 新生成 11 个 API DTO TS 类型(`ManifestNode.span` 跨指 base-schema → `import { Span } from "../../../core/src/generated/Span"`,跨 crate 相对路径实测正确)

**入口**:`cargo run -p server -- <book_dir>`(localhost REST);前端经 `GET /book/*` 消费(S10d)。API TS 类型重生成 = `cargo test -p read-tools`/`-p runtime`(触发 ts-rs export)。
**测试**:`cargo test --workspace`(**71 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 20 + server 10**;server 10:四端点正确/错误透传 status 映射/未知路由 404/非 GET 405/percent 解码 CJK·空格/query 解析)+ `clippy -p server -p read-tools -p runtime` 净。
**实测落点回填**:① ts-rs 跨 crate 导出目标 = `packages/web/src/generated/`,跨指 base 类型由 ts-rs 自动算相对 import,可行(`[ADR-0028/0021]`);② tiny_http 路由 = `url().split('?')` 手解 query + 自写 `percent_decode`(无额外依赖)。
**未闭(B2,需真书)**:手动 curl 各 GET 返正确 JSON / 错误返信封——需一本预构建真书目录,留有真书时验(路由逻辑已由纯函数单测确定性覆盖)。`ToolError` 暂无 `recovery` 字段(各 command 真产 recovery 时再补,不擅扩冻结契约)。

## 2026-06-24 · S10b reader.*/memory.* POST 端点 + Reader/MemoryStore 会话状态

> 切片1 第二刀;`[ADR-0028 决策3]` 可变命令 → POST 投影。承 S10a 的 server crate。

**触达**:
- `crates/server/Cargo.toml` — 加 `reader`/`memory` 依赖(route 需 Reader/MemoryStore/SaveInput/RecallQuery/Anchor)
- `crates/server/src/lib.rs:AppState` — 扩 `{book, reader: Reader, store: MemoryStore}`(单会话:只读书 + 会话态阅读器 + 用户私有记忆库,物理隔离 `[ADR-0006]`)
- `crates/server/src/lib.rs:Req` — 新增请求输入结构 `{method, url, body, now}`;`route` 签名改 `route(&mut AppState, Req) -> Reply`(POST 带 JSON body + 注入时间戳,守 A2 确定性)
- `crates/server/src/lib.rs:route/route_book/route_mut` — 按命名空间前缀分派:`/book/*`→GET(route_book,S10a 逻辑抽出)、`/reader/*`+`/memory/*`→POST(route_mut),错方法→405、未知路径→404。字段级不相交借用(reader mut + book shared + store mut 同时)
- `crates/server/src/lib.rs:route_mut` — POST 七端点:`/reader/{goto,scroll,highlight,note,state}`(reader.* 返 effect;highlight/note 委托 `memory.save`,标注单源 `[ADR-0015]`)+ `/memory/{save,recall}`(直读写记忆层;save layer 缺省按 type 默认 position→session 否则 long_term `[ADR-0006]`;recall 各维度 Some 过滤、book_id 缺即跨书)
- `crates/server/src/lib.rs:body_value` — JSON body 解析(空 body→空对象供 reader.state 无字段端点;非法→400 INVALID_RANGE)
- `crates/server/src/main.rs:main` — 构建 `Reader::new(&book, DEFAULT_RADIUS)` + `MemoryStore::open(default_path())`(物理隔离库)入 AppState;worker 读 POST body(`rq.as_reader().read_to_string`)+ 生成 `now_ts`(epoch 毫秒)+ 传 `Req`
- `crates/server/src/main.rs:now_ts` — memory 时间戳(epoch 毫秒串;ISO 化留后)

**入口**:`cargo run -p server -- <book_dir>`;前端经 `POST /reader/*`、`POST /memory/*` 驱动(S10d)。标注回显 = 客户端调 `POST /memory/recall`(标注单源 memory,无独立 render 端点)。
**测试**:`cargo test --workspace`(**73 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 20 + server 12**;server +2:reader goto/scroll/highlight/note/state effect + 非法 LID 404 不降级 + memory save/recall 往返 + 缺字段 400 + 坏 body 400 + 错方法 405)+ `clippy -p server` 净。
**未闭(B2,需真书)**:手动 POST 驱动真 reader/标注落盘/recall 回显——留有真书时验(纯函数 route 已确定性覆盖全部分支)。query 端点(`/book/query`)= S10c。

## 2026-06-24 · S10c book.query POST 端点(接 runtime::query + adapter 注入)

> 切片1 第三刀;`[ADR-0014/0016/0028]` LLM 命令 REST 投影。承 S10b 的 server crate。

**触达**:
- `crates/server/Cargo.toml` — 加 `runtime` 依赖(route_query 需 `runtime::query` + `ModelAdapter`/`NativeAdapter`)
- `crates/server/src/lib.rs:AppState` — 扩 `adapter: Box<dyn ModelAdapter + Send>`(`+ Send` 供 `Arc<Mutex<_>>` 跨 worker 线程;LLM 后端经它触模型)`[ADR-0028]`
- `crates/server/src/lib.rs:route` — 单列 `POST /book/query` 分支于 GET-only `route_book` 之前(`book.*` 命名空间但 LLM 命令、秒级非确定性叶子,方法分派对它例外)`[ADR-0014]`;GET→`query_method_not_allowed` 405(文案单列免误导)
- `crates/server/src/lib.rs:route_query` — body `{q, anchor_lid?}` → 直调 `runtime::query`(确定性档位检索 + LLM 合一轮 + 确定性交叉验停 = 结构红线 `[ADR-0004/0016]`)→ 返 `QueryResponse`;缺 q→400 `INVALID_RANGE`;**anchor 缺省取 `reader.state().viewport.anchor_lid`**(读到哪问到哪);`scope` 入参暂不接(内层切片0 固定 local→chapter auto,无 scope 旋钮,留切片1+);provider 错经 query 映射 `PROVIDER_ERROR`(category=provider→502)透传不降级 `[ADR-0015]`
- `crates/server/src/lib.rs:UnconfiguredAdapter` — `.env` 缺失兜底 adapter(complete/chat 均返 `AdapterError`);book/reader/memory 浏览不被 LLM 配置阻塞,仅 query 触模型时诚实报错 `[ADR-0015]`
- `crates/server/src/main.rs:main` — `NativeAdapter::from_env()` 装配 adapter,失败则 eprintln 警告 + 兜底 `UnconfiguredAdapter` 入 `AppState`

**入口**:`cargo run -p server -- <book_dir>`(读 `.env`);前端经 `POST /book/query`(body `{q, anchor_lid?}`)消费(S10d 问答框)。
**测试**:`cargo test --workspace`(**78 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 20 + server 17**;server +5:StubAdapter 确定性返 QueryResponse(scope local/全真 LID citation)+ 显式 anchor + 缺 q 400 + GET 405 + provider 错 502)+ `clippy -p server` 净。**StubAdapter 实现 `ModelAdapter`** 把 query 路由层脱真 LLM 做成确定性可测(守 A2,超出原 checkpoint 的"仅 B2")。
**未闭(B2,需真书 + `.env`)**:真跑 `POST /book/query` 返结构红线 100% 的 `QueryResponse`(走 glm-5.1,真 LLM 不入自动测,守 A2/B2,镜像 S5b/S8)由用户拍板跑。`scope` 入参待 `runtime::query` 长出 scope 旋钮(切片1+)。

## 2026-06-24 · S10d Vue/Vite 前端 app(连续正文 + 四动作 + 标注 overlay + 读位感 + 问答框)

> 切片1 第四刀;`[ADR-0028]` 前端栈落地(解 ADR-0021 PENDING)。最小连通画面 = S10a+S10b+S10d。

**触达**(新 `packages/web` Vue3+Vite,pnpm workspace 成员):
- `packages/web/package.json` — `@understand-book/web`(deps:`vue`;dev:`vite`/`@vitejs/plugin-vue`/`vue-tsc`/`typescript`);scripts `dev`/`build`(vue-tsc + vite)/`typecheck`
- `packages/web/tsconfig.json` — extends 根 tsconfig + `lib:[ES2022,DOM,DOM.Iterable]` + `types:[vite/client]`;`vite.config.ts` — `@vitejs/plugin-vue` + dev proxy `/api`→`127.0.0.1:8787`(同源无 CORS,承 ADR-0022;`UNDERSTAND_BOOK_ADDR` 可覆写)
- `packages/web/index.html` / `src/main.ts` / `src/env.d.ts`(`.vue` shim + vite/client)/ `src/style.css`(连续正文 CSS:段落自然流、无框/无 LID 标号、anchor 淡左缘、hl 高亮底色、note 行内徽标)
- `packages/web/src/api.ts:api` — 类型化命令面 REST 客户端 `[ADR-0028]`:`manifest/text/query/goto/scroll/highlight/note/state/recall` 九端点;import 生成 DTO `Manifest`/`QueryResponse`/`ToolError`;手定 `Viewport/ViewportEffect/ReaderState/MemoryRecord`(memory 类型未走 ts-rs);`ApiError` 携 §4.4 信封(category/error_code 供 UI 分流);`http<T>` 统一 GET/POST + 错误透传
- `packages/web/src/App.vue` — `<script setup>` 单组件:① 连续正文列(`GET /book/text` 逐 `visible_lids` 取真原文,段落承 `data-lid` 隐形锚、点击选中)② 四动作(上/下翻→`reader.scroll`、跳转 LID→`reader.goto`、🖍 高亮→`reader.highlight`、📝 笔记→`reader.note`,真经 HTTP)③ 标注 overlay(`memory.recall` 回显 highlight 底色 + note 徽标,**单源=记忆层**)④ 读位感条(章节=anchor 顶层段原文首行 + 进度%=anchor 在叶序下标/叶总数)⑤ 问答框(`book.query`,citations 可点跳原文、incomplete/scope/model_supplement 显式)`[ADR-0027/0028/0007]`

**入口**:`pnpm -C packages/web dev`(Vite dev server + proxy → tiny_http);需先 `cargo run -p server -- <真书目录>`。打包静态服留 S10e。
**测试**(确定性闸,守 A2 可验部分):`pnpm install`(workspace 加 web 成员,38 包)✓ + `pnpm -C packages/web typecheck`(vue-tsc 0 error)✓ + `pnpm -C packages/web build`(vue-tsc + vite,12 模块 → `dist/`)✓。`cargo test --workspace` 78 不受影响(纯前端刀)。
**实测落点回填**:① 连续正文取数粒度 = **逐 visible_lid 取**(窗口 ≤2*radius+1 叶,localhost 单用户可接受;整窗 `book.text` range 批取属优化,留实测)→ ADR-0028;② 标注 overlay 锚定 = 段落 `data-lid` + 客户端按 lid 过滤 recall(选区富文本 web-highlighter 留实测)→ ADR-0028。
**未闭(B2,浏览器人工验,需真书 + 跑 server [+ `.env` 做问答])**:浏览器加载书显连续正文 / 四动作真驱动后端 / 标注回显 / 章节+进度% / 问答返 citations 可跳——留有真书时人工验(类型 + 构建已确定性闸覆盖)。`memory.recall` 暂 `{}` 全取客户端过滤(book_id 精确作用域待前端拿到 book_id,留后);章节标题取 LID 首段(顶层非必为「章」,精确容器上溯留后)。

## 2026-06-24 · S10f /agent/chat 外层 E agent + reader 双向注入 + effects/trace 装配

> 切片1 第六刀;`[ADR-0030]` agent 阅读器形态后端落地。前端对话主入口 = 外层 E agent(非内层 book.query)。

**触达**:
- `crates/reader/src/lib.rs:Reader::{highlight,note}` — 加 `layer:&str` 入参(人走命令面默认 `long_term`、agent 提议态传 `session`)`[ADR-0030 决策4]`。人机对称无特供:agent 标注仍走 reader 闭环(content=原文片段逻辑复用),只是落 session 层;`§0.5` 对齐选 A(改 reader 公共签名)否决「orchestrator 绕过 reader 直写 memory」(破对称 + 重复取片段逻辑)
- `crates/runtime/src/orchestrator.rs:OuterOutcome` — 加 `effects:Vec<AgentEffect>` + `trace:Vec<TraceStep>`(runtime 内部结构,非冻结命令面,加 `#[derive(TS)]` 导出前端)`[ADR-0030 决策3/5]`
- `...:AgentEffect`(tagged enum `kind`)— `Goto{before_anchor,after_anchor}`(undo=goto(before))/ `Highlight{mem_id,lid}` / `Note{mem_id,lid,text}`(undo=memory.delete(mem_id));**视口变更按回合合并成单条 Goto**(事务性 undo,`with_goto` 比对回合首尾 anchor),highlight/note 每次一条
- `...:TraceStep{tool,args,result_digest}` — 查询踪迹(book.query 的 citations 链落 `result_digest`,`digest()` 截 200 字)
- `...:run` — **签名改注入 `reader:&mut Reader` + `messages:&mut Vec<Message>`**(删内部 `Reader::new`,与 server `AppState` 共享同一视口 + 跨回合 messages)`[ADR-0030 决策2]`;`dispatch` 改返 `(String, Option<AgentEffect>)`;终答入 messages(下一回合可见上轮回答);`new_session()` 产仅 system 的初始 messages(供 `/agent/new` 重置)
- `crates/runtime/src/main.rs` — `chat` 子命令适配新签名(自建 reader + new_session)
- `crates/server/src/lib.rs:AppState` — 加 `messages:Vec<Message>`;`route` 加 `POST /agent/chat`(body `{message}`→ 注入同一 book/store/reader/messages/adapter 调 `run`→ 返 `OuterOutcome`)+ `POST /agent/new`(清 messages 回 new_session);非 POST→405 `agent_method_not_allowed`、缺 message→400
- `crates/server/src/main.rs:main` — `AppState` 构造加 `messages:new_session()`
- `packages/web/src/generated/{OuterOutcome,AgentEffect,TraceStep}.ts` — ts-rs 新生成 3 个 agent DTO(S10g 前端消费)

**入口**:`POST /agent/chat`(前端对话区 S10g)/ `cargo run -p runtime -- <dir> chat <q>`(CLI 真跑)。
**测试**:`cargo test --workspace`(**87 = base-schema 11 + memory 6 + read-tools 14 + reader 10 + runtime 25 + server 21**;runtime +2:视口变更合并单条 Goto + trace / messages 跨回合保留+重置;server +4:/agent/chat 驱动共享 reader 返 effects + 标注落 session 层、/agent/new 清 messages、GET 405、缺 message 400)+ `clippy -p reader -p runtime -p server` 净。**ChatStubAdapter** 脚本化外层 chat 把 /agent/chat 脱真 LLM 做成确定性可测(守 A2)。
**实测落点回填**(ADR-0030)：`AgentEffect`/`TraceStep` 字段定型(见上);视口 undo 粒度 = **按回合合并单条 Goto**(事务性);agent 标注 layer 注入点 = reader.* 加 layer 参数。
**未闭(B2,需真书 + `.env`)**:真跑 `POST /agent/chat` 一次「问→agent goto→高亮」后 `/reader/state` 见视口被 agent 改 + effects 可 undo(走 glm-5.1,真 LLM 不入自动测,守 A2/B2)由用户拍板跑。messages 累积上限/截断、idle 软提示留实测(ADR-0030 何时回头)。S10g 前端 agent UI(分屏+提议卡+踪迹)+ S10e 打包待做。

## 2026-06-24 · S10g agent 形态 UI(分屏对话 + 可撤销提议 + 踪迹)+ memory.delete 补全

> 切片1 第七刀;`[ADR-0030 决策1/5]` agent 阅读器形态前端落地。含 S10g-pre 后端补 `memory.delete`(完成 V3 §4.3 冻结契约第三命令,切片0 S6a 只实现了 save/recall)。

**触达**:
- `crates/memory/src/lib.rs:MemoryStore::delete` — **新增**:`retain` 删指定 mem_id + 落盘;找不到 → `MEMORY_NOT_FOUND`(not_found,禁静默降级 `[ADR-0015]`)。完成 V3 §4.3 `memory.delete(mem_id)→{ok}`(用户显式删,区别议题7 后台 usage 遗忘 `[ADR-0018]`)
- `crates/server/src/lib.rs:route_mut` — 加 `/memory/delete` 分支(body `{mem_id}`→ `store.delete`→ `{ok:true}`;缺 mem_id→400、找不到→404)
- `packages/web/src/api.ts:api` — 加 `save`(memory.save 客户端,「保留」升 long_term 用)/ `delete`(memory.delete)/ `agentChat`(POST /agent/chat→OuterOutcome)/ `agentNew`(POST /agent/new);import 生成 DTO `OuterOutcome`/`AgentEffect`/`TraceStep`
- `packages/web/src/App.vue` — 右侧 Q&A 框**改为 agent 对话区**(主入口 `[ADR-0030 决策1]`,直问 book.query 退为 agent 内部工具):`sendAgent`(接 `/agent/chat`,回来 `syncViewport` 同步被 agent 改的视口 + 刷新标注);**提议卡**渲 `effects[]`——`Goto`「↩ 返回 before_anchor」(`undoEffect`→reader.goto)/ `Highlight`·`Note`「保留(`keepEffect`→同内容 long_term re-save = 同 mem_id upsert 升层)/撤销(`undoEffect`→memory.delete)」;**踪迹**折叠渲 `trace[]`(tool+args+result_digest);对话末「凝练成笔记(`distill`→memory.save note,content=answer)」;「新对话」(`newChat`→agentNew 清前端 transcript);提议处置态 `handled` 防重复操作。AgentEffect 联合类型在 TS helper(`effLabel`/`isGoto`/`gotoBack`)里 narrow,避开模板收窄。`<style scoped>` 加 agent 区样式

**入口**:浏览器对话区 → `/agent/chat`;agent goto/highlight/note 即时驱动共享 reader,提议卡 undo 走反向命令(reader.goto / memory.delete)。
**测试**:`cargo test --workspace`(**89 = base-schema 11 + memory 7 + read-tools 14 + reader 10 + runtime 25 + server 22**;memory +1 delete 往返+MEMORY_NOT_FOUND、server +1 /memory/delete 端点 200+404)+ `clippy -p memory -p server` 净 + `pnpm -C packages/web typecheck`(vue-tsc 0)✓ + `build`(14 模块→dist)✓。
**实测落点回填**(ADR-0030)：提议确认 = **逐条**(保留/撤销 per-effect,非整回合事务);「保留」升级 = 同内容 long_term re-save 复用 content-addressed upsert(mem_id 不含 layer);Highlight 保留需 recall(layer=session) 取原文片段(effect 不载 content)。
**未闭(B2,浏览器人工验,需真书 + 跑 server + `.env`)**:对话分屏 / agent goto 后阅读区同步且可一键返回 / 高亮可保留·撤销 / 踪迹可展开 / 凝练成笔记——留有真书时人工验(类型+构建已确定性闸覆盖)。S10e 打包(tiny_http 服 dist 同源)= 切片1 收尾末刀。

## 2026-06-24 · 切片1 修缮 A:start.bat 修复 + agent 分屏布局回归 + 对话/笔记 MD+LaTeX(commit 13d6c4a)

> 切片1 阅读器修缮(非新子切片);三处独立修复 + 一个渲染增强。

**触达**:
- `start.bat`(新)— 双击启动器(dev:server :8787 + vite :5173)。修 base.json-missing 分支 for-loop 的 `^(...^)` 转义括号嵌在 if 块内**破坏块解析**、致双击「一闪就关」(哪怕 base.json 存在)→ 换 `dir /b /ad`(无括号嵌套陷阱)。
- `packages/web/src/style.css` / `App.vue` — 修 S10g 回归:右栏 class 从 `.qa` 改 `.agent` 时丢了 `width`,默认 flex 让 agent 回答后撑满全屏、按钮错位 → `.agent { flex: 0 0 24rem }` 固定侧栏 + `.transcript` 内容换行。
- `packages/web/src/md.ts`(新)`:renderMarkdown` = markdown-it(`html:false` 防 XSS)+ `@traptitech/markdown-it-katex`(`$..$` / `$$..$$`);`main.ts` import `katex/dist/katex.min.css`。
- `packages/web/src/App.vue` — agent 答案 + 笔记内容改 `v-html=renderMarkdown(...)`;`.md` 排版规则放**全局** `style.css`(scoped 够不到 v-html 注入内容,顺修上一刀潜在失效)。
- `packages/web/package.json` — 加 `markdown-it`/`katex`/`@traptitech/markdown-it-katex` + `@types/markdown-it`。

**入口**:`start.bat [book_dir]` 双击;浏览器对话区 / 笔记卡渲染。
**测试**:`pnpm typecheck`(vue-tsc 0)+ `build` 绿;md 渲染 10/10(粗体/列表/代码/`$E=mc^2$`/`$$\int$$`/XSS 转义/null);start.bat dry-run exitcode 0(修前 255 `( was unexpected`)。

## 2026-06-24 · 切片1 修缮 B:人自由高亮(段内字符区间)+ 笔记编辑器(H1/H2/N1)`[ADR-0031]`

> 自由高亮(段内任意文字、可删可改)+ 笔记专用编辑界面。跨后端(H1)+ 前端(H2/N1)。

**触达**:
- `docs/adr/0031-*`(新)— 自由高亮 = 段内 **UTF-16 字符区间**(相对 LID 文本);memory Record 加可选 `range`;兑现冻结 `reader.highlight(lid, range?)`(V3 §4.2)。
- `crates/memory/src/lib.rs` — `TextRange{start,end}`;`Record`/`SaveInput` 加 `range: Option<TextRange>`;`content_mem_id` **仅 Some 时**纳入 range(老 note 哈希不变=向后兼容;同段同子串不同位置=两条不同 mem_id)。
- `crates/reader/src/lib.rs:Reader::highlight` — 签名加 `range: Option<(u32,u32)>`:`Some` → `encode_utf16` 切子串作 content + 存 range,越界 → `INVALID_RANGE` 不降级;`None` → 整段(向后兼容 / agent)。
- `crates/runtime/src/orchestrator.rs` — agent `reader.highlight` 传 `None`(整段,人机对称无特供)+ memory.save `SaveInput` 补 `range:None`。
- `crates/server/src/lib.rs:route_mut` — `/reader/highlight` 解析 body `range:{start,end}` 传 reader;`/memory/save` 补 `range:None`。
- `packages/web/src/api.ts` — `TextRange` + `MemoryRecord.range` + `highlight(lid, range?)`。
- `packages/web/src/App.vue` — 选区捕获 `onProseMouseUp`(`Range.toString().length` 算段内 UTF-16 偏移)→ 浮动「🖍 高亮选区」`confirmHighlight`;`renderSeg`(段内高亮包 `<mark>`,合并重叠区间 + 转义防 XSS);高亮卡 `deleteHighlight`/`modifyHighlight`(改=移除后重选);**笔记模态编辑器**(`noteEditor` 状态 + `notePreview` 实时 MD/LaTeX 预览 + `saveNote`/`openEditNote`)替换 `window.prompt`。
- `packages/web/src/style.css` — `.hl-mark`/`.hl-card`(全局,v-html 用);App.vue scoped 加 `.hl-popover`/`.note-modal`/`.nd-*`。

**入口**:正文框选 → 浮动按钮 → `POST /reader/highlight {lid, range}`;高亮卡删除/改范围;📝 笔记 → 模态编辑器。
**测试**:`cargo test --workspace` **92**(memory 8 / reader 11 / server 23,各 +1)+ clippy 净;`typecheck`+`build` 绿;**真实 CJK 端到端**:`slice(2,6)`=`"由人民邮"` 前后端逐字一致 + 同段多条 + 删除 + 越界→400 全过(临时 memory 目录隔离,不碰真实库)。
**实测回填**(ADR-0031):UTF-16 三处同口径(DOM 选区 / JS `slice` / Rust `encode_utf16`)端到端一致;agent 高亮维持整段。
**未闭(B2,浏览器人工验)**:DOM 选区捕获 + 浮窗/模态视觉 + 高亮重绘——留浏览器肉眼确认(后端区间 + 渲染逻辑已确定性覆盖)。

## 2026-06-25 · S10e 打包形态:tiny_http 同端口服务 SPA dist + API `/api` 前缀归一

> 切片1「前端阅读器」打包收尾;兑现 ADR-0028 决策7:单进程单端口同源服务,无 CORS。

**触达**:
- `crates/server/src/main.rs:main` — 加 `web_dist_dir()`(`UNDERSTAND_BOOK_WEB_DIST` 覆盖,默认 `packages/web/dist`)并在 worker 中先判静态文件,静态 I/O 不持 `AppState` 锁。
- `crates/server/src/main.rs:static_response` — 非 API 的 `GET` 请求服务 `dist/<path>`;具体文件不存在则 fallback 到 `index.html`;路径拒绝 `..`、`.`、反斜杠和盘符冒号。
- `crates/server/src/main.rs:normalize_api_url` — 打包 SPA 的 `/api/*` 请求剥 `/api` 后再交给冻结命令面路由(`/book/*`、`/reader/*`、`/memory/*`、`/agent/*`),避免改 `server::route` 纯函数契约。
- `crates/server/src/main.rs:mime_for` — 覆盖 Vite 产物常见 MIME(html/js/css/json/svg/png/jpg/webp/ico/woff/woff2)。
- `skills/read/SKILL.md`(新) — `/understand-book:read` 启动说明:先 `pnpm -C packages\web build`,再 `cargo run -p server -- <book-dir>`,同端口打开 SPA+API。

**入口**:`pnpm -C packages\web build` → `cargo run -p server -- <book-dir>`;浏览器打开 server 打印的 `http://127.0.0.1:8787`。
**测试**:`cargo test -p server` 23(lib)+4(main) 全绿;`cargo build -p server` 绿;`pnpm -C packages\web build` 绿;真实同源 smoke(临时端口 18787,真书基座):`GET /` → HTML + app mount,`GET /api/book/manifest` → JSON + tree,`GET /not-a-real-spa-route` → HTML fallback。
**未闭**:浏览器人工完整验(S10c/f/g 的真 LLM + UI 操作)仍按前文 B2 清单执行;S10h/S10i 隐藏 LID 和自然定位入口未做。
## 2026-06-25 · start.bat 切到 S10e 打包单进程启动

> 切片1 启动入口修缮;把旧 dev 双进程(server + Vite)入口改为 S10e 产品形态入口。

**触达**:
- `start.bat` — 启动模式改为 packaged S10e:默认 `UNDERSTAND_BOOK_ADDR=127.0.0.1:8787`,默认 `UNDERSTAND_BOOK_WEB_DIST=%CD%\packages\web\dist`;校验 book `base.json`;默认 dist 缺失时自动 `pnpm -C packages\web build`;启动单个 `cargo run -p server -- <book_dir>` server 窗口并打开同端口 URL。
- `start.bat` — 移除 Vite dev server 常驻启动;`pnpm` 仅在 dist 缺失需要构建时才要求存在。

**入口**:双击 `start.bat` 或 `start.bat <book_dir>`;关闭 server 窗口停止服务。
**测试**:`cmd /c start.bat __missing_book_for_syntax_check__` 正常进入缺失 book 错误分支(exit 1),验证 batch 括号块解析与路径检查;未启动 server/浏览器。
## 2026-06-25 · asset 契约修订:Formula 升格为第四类核心 asset + FormulaSemantics

> 文档/契约切片;未改 schema/adapter 实现代码。把公式从长尾类型升格为核心 asset,并定义 agent 交互必须消费的公式语义剖面。

**触达**:
- `docs/adr/0029-*` — 修订 ADR-0029:`NodeKind` 目标从 Code/Table/Image 扩为 Code/Table/Image/Formula;新增 FormulaSemantics 决策(参数 parameters、组合含义 composition、上下文关系 context_links,均带 LID 证据);Formula 不再是长尾。
- `docs/切片方案-asset一等对象.md` — asset 刀总声明与 SA0-SA6 重写:SA0 统计 formula_count;SA1 加 Formula/FormulaSemantics schema;SA2/SA3 识别 md/epub 公式;SA4 禁止公式句切;SA5 专门抽公式语义剖面;SA6 真书验公式语义可检索。
- `CONTEXT.md` — `asset 叶子` 扩四类,新增 `公式语义剖面 (FormulaSemantics)` 术语。
- `docs/切片方案-切片1前端阅读器.md` — S10j 前端消费目标扩到 Formula,要求展示公式原文与公式语义剖面。
- `docs/adr/0032-*` — 粒度体检纳入 formula asset 候选数量,并规定 Formula 叶子固定不做句级展开。
- `docs/参考对照-文档世界状态-优化登记.md` / `需求文档-V3.md` — 同步四类 asset 与 agent 公式上下文要求。

**入口**:后续实现从 `docs/切片方案-asset一等对象.md` 的 SA0 开始;不得跳过粒度体检直接改 schema。
**测试**:`rg` 扫描 asset/Formula 相关旧表述,确认 Formula 已从长尾改为核心类型;本切片未改可执行代码,未跑 cargo/pnpm。

## 2026-06-25 · SA0 段/句粒度体检报告(GranularityProfile)

> asset 一等对象实现第一刀;只做正式构建前体检,不改 schema/adapter asset kind/正式 LID 构建。

**触达**:
- `packages/core/src/granularity.ts:buildGranularityProfile` — 新增确定性 `GranularityProfile`:段落数、估算句数、每段句数 avg/p50/p90/max、长段计数、`paragraph/hybrid/sentence` 三档 projected_lid_count 与 expansion_ratio、推荐档位。
- `packages/core/src/granularity.ts:countMarkdownAssetCandidates/countEpubAssetCandidates` — Markdown 源文本和 EPUB XHTML 的 code/table/image/formula 候选计数;不改变现有 `SourceBlock` 或 `NodeKind`。
- `skills/build/granularity-profile.ts` — SA0 CLI:`pnpm exec tsx skills/build/granularity-profile.ts <epub|md>` 输出人读摘要 + JSON。
- `packages/core/test/granularity.test.ts` — 覆盖句数估算、Markdown/XHTML/EPUB asset 候选计数、LID 膨胀估算和推荐逻辑。
- `skills/build/SKILL.md` — build 编排骨架加入第 0 步 SA0,明确用户确认粒度后才进入正式构建。

**入口**:`pnpm exec tsx skills/build/granularity-profile.ts <epub|md>`;输出推荐但不自动构建。
**测试**:`pnpm -C packages/core test`(38 例全绿)+ `pnpm -C packages/core typecheck` + `pnpm exec tsx skills/build/granularity-profile.ts packages/core/test/fixtures/sample.md`。

## 2026-06-25 · SA1 schema 扩:NodeKind asset 四值 + ManifestNode.kind + FormulaSemantics

> asset 一等对象第二刀;只做 schema/DTO/TS 导出,不碰 ingestion、segment 或 adapter 行为。

**触达**:
- `crates/base-schema/src/lib.rs:NodeKind` — 增 `Code/Table/Image/Formula` 四个 serde snake_case 枚举值,保持只增不改。
- `crates/base-schema/src/lib.rs:FormulaSemantics` — 新增公式语义剖面 schema:parameters/composition/context_links,均带 LID 证据字段;本刀先独立导出,未挂入 `ReadOnlyBase` 必填字段。
- `crates/read-tools/src/lib.rs:ManifestNode` — `book.manifest` 投影新增 `kind: NodeKind`,从 `LidNode.kind` 透传。
- `packages/core/src/generated/Formula*.ts` / `NodeKind.ts` — ts-rs 生成 FormulaSemantics 相关 TS 类型和扩展后的 NodeKind。
- `packages/web/src/generated/ManifestNode.ts` — ts-rs 生成 manifest 节点 `kind` 字段,前端类型通过。
- `packages/core/src/zod.ts` — 手写 zod 镜像同步 NodeKind 四值和 FormulaSemantics schema。

**入口**:`book.manifest()` 返回的 `tree[].kind`;FormulaSemantics schema 供 SA5 抽取/存储接入。
**测试**:`cargo test -p base-schema -p read-tools`;`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;`pnpm -C packages/web typecheck`;`cargo test --workspace`;`pnpm -C packages/web build`。

## 2026-06-25 · SA2 md-adapter 识别 code/table/image/formula asset leaf

> asset 一等对象第三刀;只做 Markdown ingestion 的 assetKind 标记,不改 EPUB、segment 透传或 read-tools 行为。

**触达**:
- `packages/core/src/segment.ts:SourceBlock` — 新增 `assetKind?: "code" | "table" | "image" | "formula"`;SA4 前 `segment()` 不消费该字段,仍产 paragraph。
- `packages/core/src/md-adapter.ts:markdownToBlocks` — 识别 fenced code、Markdown table、独立 image 行、块级公式和独立行内公式为带 `assetKind` 的 leaf;asset 文本保留源标记序列化。
- `packages/core/test/md-adapter.test.ts` — 覆盖五类 Markdown asset leaf 标记、代码块内空行不腰斩、表/图/公式原文保真,并确认当前 segment 行为和分区不变式不变。

**入口**:`markdownToBlocks(src)` 返回 `SourceBlock[]` 中的 `assetKind`;SA4 后才透传为 `LidNode.kind`。
**测试**:`pnpm -C packages/core test`(40 例全绿)+ `pnpm -C packages/core typecheck`;`pnpm exec tsx skills/build/split-lid.ts ...` 未跑通,当前环境报 `tsx` 不在 PATH。
## 2026-06-25 · SA3 epub-adapter 识别 pre/table/img/math asset leaf

> asset 一等对象第四刀;只做 EPUB ingestion 的 `assetKind` 标记与忠实源标记序列化,不改 `segment()` kind 透传、不做 SA4/SA5。
**触达**:
- `packages/core/src/epub-adapter.ts:RawBlock` — 新增 `assetKind?: AssetKind`,并在 `epubToSource` 透传到 `SourceBlock.assetKind`。
- `packages/core/src/epub-adapter.ts:xhtmlToBlocks` — `<pre>` 标为 `code` 并保留换行缩进;`<table>` 序列化为确定性表文本;`<img>` 合成为 `![alt](src)`;`<math>` 保留 MathML 源标记并标为 `formula`。
- `packages/core/test/epub-adapter.test.ts` — 覆盖 pre/table/img/math 四类 asset、当前 segment 仍产 paragraph、分区不变式 100%。
**入口**:`xhtmlToBlocks(html)` / `epubToSource(zip)` 返回带 `assetKind` 的 EPUB `SourceBlock[]`;SA4 后才透传为 `LidNode.kind`。
**测试**:`pnpm -C packages/core test`(41 例全绿)+ `pnpm -C packages/core typecheck`。
## 2026-06-25 · SA4 segment 透传 SourceBlock.assetKind 到 LidNode.kind

> asset 一等对象第五刀;只做 `segment()` 的 kind 透传,不改 Markdown/EPUB 识别规则、不做 FormulaSemantics。
**触达**:
- `packages/core/src/segment.ts:SourceBlock.assetKind` — 注释更新为 segment 会透传到 `LidNode.kind`。
- `packages/core/src/segment.ts:segment` — leaf 节点 kind 从固定 `paragraph` 改为 `b.assetKind ?? "paragraph"`,heading 标题叶仍为 `paragraph`。
- `packages/core/test/md-adapter.test.ts` — Markdown code/table/image/formula asset 经 segment 后产对应 `NodeKind`,分区不变式仍 100%。
- `packages/core/test/epub-adapter.test.ts` — EPUB pre/table/img/math asset 经 segment 后产对应 `NodeKind`,分区不变式仍 100%。
**入口**:`segment(markdownToBlocks(src))` / `segment(epubToSource(zip).blocks)` 现在生成 `code/table/image/formula` 叶子节点。
**测试**:`pnpm -C packages/core test`(41 例全绿)+ `pnpm -C packages/core typecheck`。
## 2026-06-25 · SA5 FormulaSemantics 候选证据闸 + agent prompt formatter

> asset 一等对象第六刀;只做 FormulaSemantics 的 TS 侧候选清洗/证据闸和 agent 上下文格式化,不把语义剖面挂进 `ReadOnlyBase` 必填字段,不做真书重建。
**触达**:
- `packages/core/src/formula-semantics.ts:gateFormulaSemanticsCandidate` — 新增确定性证据闸:校验 `formula_lid` 必须是 formula 节点,`source_lid` 必须指回公式,`evidence_lids` 必须存在且落在公式上下文 LID 集合内;悬空/越界/缺证据项进入 pending。
- `packages/core/src/formula-semantics.ts:formatFormulaSemanticsForPrompt` — 新增 agent 侧公式上下文格式化,把 composition、parameters、context_links 和证据 LID 展开为稳定文本。
- `packages/core/test/formula-semantics.test.ts` — 覆盖合法候选固化、悬空 LID 丢弃、上下文外证据丢弃、缺证据项 pending、composition 未锚到公式时不产只读语义剖面。
**入口**:LLM 抽取候选先调 `gateFormulaSemanticsCandidate(candidate, lidNodes, { contextLids })`;只有 `result.semantics` 可进入只读语义剖面,pending 项只能作为待确认/model_supplement。
**测试**:`pnpm -C packages/core test`(45 例全绿)+ `pnpm -C packages/core typecheck`。

## 2026-06-25 · ADR-0033 Core/Profile/Reader 解耦 + profile 深路径切片方案

> Grill 落档;只写架构决策和阶段切片方案,不改可执行代码。

**触达**:
- `docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md` — 冻结 Core Schema / Book Profile / Reader Profile 三层边界;当前内置 profile=`technical_learning`;暂不迁移 GraphNode envelope;Pass2/synthesize/consolidation/reader.* / adapter/provider/迁移的归属边界。
- `docs/切片方案-profile深路径.md` — P0-P6 阶段方案:ADR 落档、`technical_learning.pass2_longrange_v1`、`book.synthesize` 深路径、reader.* 全集与 agent policy、memory consolidation 四层、ReActAdapter/provider registry、增量构建与 profile artifact / memory 迁移。

**入口**:后续深路径实现从 `docs/切片方案-profile深路径.md` 的 P1 开始;P0 仅为本次文档落档。
**测试**:`rg` 扫描 ADR-0033 / profile 深路径关键边界;本切片未改可执行代码,未跑 cargo/pnpm。

## 2026-06-25 · P1 technical_learning.pass2_longrange_v1 + context/query far

> Profile 深路径第一刀;把 Pass2 长程边候选接入确定性 gate/audit sidecar,并让读时 context/query 消费 long_range 边。

**触达**:
- `packages/core/src/pass2.ts:gateTechnicalLearningPass2LongRange` — 新增 `technical_learning.pass2_longrange_v1` 候选闸:消费带 `ProfileArtifactHeader` 的 `TechnicalLearningPass2Output`;校验端点 graph node id、`scope="long_range"`、edge type 白名单、evidence_lids 真实存在;通过后降成 `GraphEdge(scope=long_range)` 并产带 header 的 audit sidecar。
- `packages/core/test/pass2.test.ts` — 覆盖合法 output 降级、audit header 保留、悬空端点/悬空证据/空证据/非法 scope/非法 type 确定性丢弃。
- `agents/pass2-longrange-linker.md` — 从占位改为真实 subagent 契约:输入/输出带 `ProfileArtifactHeader`,只产 long_range candidate,不产节点,必须输出 `scope="long_range"`,不重建悬空 LID。
- `crates/read-tools/src/lib.rs:Book::context` — 新增 `granularity=near|mid|far`;far 累积 near + concept occurrences(mid) + `scope=long_range` 边,保留 `context_near` wrapper。
- `crates/runtime/src/lib.rs:Scope/retrieve/query` — query ladder 扩为 `local→chapter→cross_chapter→global`;cross_chapter 消费 context far,global 加全书 anchored leaves。
- `crates/read-tools/src/main.rs` / `crates/server/src/lib.rs` / `crates/runtime/src/orchestrator.rs` — CLI、REST 和 agent tool spec/dispatch 接入 `granularity`。
- `packages/web/src/generated/Via.ts` — ts-rs 生成类型新增 `concept` via variant。

**入口**:Pass2 候选先调 `gateTechnicalLearningPass2LongRange(...)`;读时 `book.context?lid=...&granularity=far` 或 agent `book.context({granularity:"far"})` 可召回 long_range 指针;`book.query` 自动外扩到 cross_chapter/global。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;`cargo test -p read-tools -p runtime`;`cargo test --workspace`;`pnpm -C packages/web typecheck`。

## 2026-06-25 · profile 深路径契约补充:discourse / reader_profile / synthesize policy 形状

> 补齐 ADR-0033 落档时遗漏的具体 artifact 字段格式;仍只改文档,不改代码。

**触达**:
- `docs/切片方案-profile深路径.md` — 新增 `technical_learning v0 artifact contracts`: `ProfileArtifactHeader`、`TechnicalLearningDiscourseIndex`、`TechnicalLearningPass2Input/Output`、`ReaderProfile`、`SynthesizePolicy`、`TechnicalLearningAgentPolicy` 的 TS 形状与红线。

**入口**:P1+ 实现这些 artifact 时以该小节为输入契约草案;实现前仍需按切片重新 A1 声明。
**测试**:`rg` 扫描关键字段形状;本切片未改可执行代码,未跑 cargo/pnpm。

## 2026-06-25 · profile 深路径契约消费矩阵补充

> 补明每个 P 刀消费哪些 1.5.x 契约,并补出 `book.context` discourse projection 切片。

**触达**:
- `docs/切片方案-profile深路径.md` — 新增 `1.5.7 Contract consumption matrix`,明确 P1/P2/P2a/P3/P4/P5/P6 分别消费 `TechnicalLearningDiscourseIndex`、`SynthesizePolicy`、`ReaderProfile` 等契约的方式;新增 P2a `book.context` discourse projection。

**入口**:后续实现 P1/P2/P2a 时按矩阵确认输入契约,避免 profile artifact 写了但无人消费。
**测试**:`rg` 扫描 P2a 和 Contract consumption matrix;本切片未改可执行代码,未跑 cargo/pnpm。

## 2026-06-25 · P2 book.synthesize 接手收紧

**触达**:
- `crates/runtime/src/lib.rs:SynthesizeResponse` - 新增 `book.synthesize` 响应 DTO,导出到前端生成类型。
- `crates/runtime/src/lib.rs:synthesize` - 实现显式 LID 集综合,不外扩检索,过滤 citations 到输入/归并证据范围,支持确定性分批归并。
- `crates/runtime/src/lib.rs:tests::synthesize_*` - 覆盖悬空 citation 过滤、空/未知 LID、分批归并路径。
- `crates/runtime/src/orchestrator.rs:tool_specs/dispatch` - 外层 agent 暴露并分发 `book.synthesize` 工具。
- `crates/server/src/lib.rs:route_synthesize` - HTTP `POST /book/synthesize` 接入 runtime 并补路由测试。
- `packages/web/src/generated/SynthesizeResponse.ts` - ts-rs 生成前端响应类型。

**入口**:`POST /book/synthesize {lids, task?}` 或 agent tool `book.synthesize({lids, task?})`。
**测试**:`cargo test -p runtime`; `cargo test -p server`; `pnpm -C packages/web typecheck`。

## 2026-06-25 · P2 book.synthesize 消费 FormulaSemantics sidecar

**触达**:
- `crates/read-tools/src/lib.rs:Book::load` - 可选读取同目录 `formula_semantics.json`,缺失时为空,不改 `ReadOnlyBase`。
- `crates/read-tools/src/lib.rs:Book::formula_semantics` - 按公式 LID 查询 FormulaSemantics sidecar。
- `crates/runtime/src/lib.rs:formula_semantics_hint` - 将真实 FormulaSemantics 格式化进 `book.synthesize` prompt;缺 sidecar 时才保留占位提示。
- `crates/runtime/src/lib.rs:tests::synthesize_includes_formula_semantics_sidecar_in_prompt` - 验证公式组合含义和参数解释进入 prompt,且 citation 仍锁在输入 LID。
- `crates/read-tools/src/lib.rs:tests::load_reads_optional_formula_semantics_sidecar` - 验证 `formula_semantics.json` 可被加载。

**入口**:`formula_semantics.json` sidecar + `book.synthesize({lids:[formula_lid], task?})`。
**测试**:`cargo test -p read-tools`; `cargo test -p runtime`。

## 2026-06-25 · P2 book.synthesize 消费 discourse_index sidecar

**触达**:
- `crates/read-tools/src/lib.rs:TechnicalLearningDiscourseIndex` - 新增 technical_learning discourse sidecar 的 Rust 读时载体,不进入 `ReadOnlyBase`。
- `crates/read-tools/src/lib.rs:Book::load` - 可选读取同目录 `discourse_index.json`,缺失时为空。
- `crates/read-tools/src/lib.rs:Book::discourse_item` - 按 LID 查询 discourse item。
- `crates/runtime/src/lib.rs:discourse_hints` - 仅把输入 LID 集内部的 summary/function/relation 注入 `book.synthesize` prompt,过滤输入外 target/evidence。
- `crates/runtime/src/lib.rs:tests::synthesize_includes_only_input_scoped_discourse_hints` - 验证输入内 relation 进入 prompt,输入外 relation 被过滤。
- `crates/read-tools/src/lib.rs:tests::load_reads_optional_discourse_index_sidecar` - 验证 `discourse_index.json` 可被加载。

**入口**:`discourse_index.json` sidecar + `book.synthesize({lids, task?})`。
**测试**:`cargo test -p read-tools`; `cargo test -p runtime`。

## 2026-06-25 · P2 book.synthesize 编译收紧 + HTTP sidecar smoke

**触达**:
- `crates/runtime/src/lib.rs:retrieve` - 移除误入 query 检索路径的 `SynthesizeMode::from_task(task)` 引用,恢复 runtime 编译。
- `crates/runtime/src/lib.rs:synthesize` - 分批局部综合路径调用 `build_synthesize_prompt` 时传入已解析 `mode`,与单批/归并路径保持同一 SynthesizePolicy。
- `crates/server/src/lib.rs:tests::book_synthesize_loads_formula_and_discourse_sidecars` - 新增真实 `Book::load` HTTP smoke:临时 book dir 同时带 `formula_semantics.json` 与 `discourse_index.json`,经 `POST /book/synthesize` 验证两类 sidecar hint 进入 prompt,且响应仍锁定输入 `source_lids`。

**入口**:`POST /book/synthesize {lids, task?}`;本 smoke 覆盖从 book 目录 sidecar 加载到 HTTP route 再到 runtime prompt 的端到端路径。
**测试**:`cargo test -p read-tools`;`cargo test -p runtime`;`cargo test -p server`;`cargo test --workspace`;`pnpm -C packages/web typecheck`。

## 2026-06-26 · P2a book.context discourse projection

**触达**:
- `crates/read-tools/src/lib.rs:Via::Discourse` - 新增 `book.context` 的 discourse via 指针类型,携带 source/target/type/family/direction/confidence/evidence_lids,不携带原文。
- `crates/read-tools/src/lib.rs:Book::discourse_context_items` - 将 `TechnicalLearningDiscourseIndex.relations[]` 投影进 context;同父 LID 进 near,跨父 LID 进 far,并过滤悬空 target/evidence。
- `crates/read-tools/src/lib.rs:tests::context_projects_discourse_relations_to_near_and_far` - 覆盖 near/far 分层、输入外/悬空 relation 不投影。
- `packages/web/src/generated/Via.ts` - ts-rs 生成 `discourse` via 联合类型。

**入口**:`book.context(lid, granularity=near|far)`;HTTP 和 agent 既有 `book.context` 路径自动获得 discourse via。
**测试**:`cargo test -p read-tools`;`cargo test --workspace`;`pnpm -C packages/web typecheck`。

## 2026-06-26 · profile 预构建缺口切片落档

**触达**:
- `docs/切片方案-profile深路径.md:1.5.7` - 在契约消费矩阵补入 PB0-PB4,覆盖 profile metadata/header、FormulaSemantics sidecar、discourse_index sidecar、Pass2 audit sidecar、build smoke。
- `docs/切片方案-profile深路径.md:1.6` - 新增预构建缺口盘点,明确 P2/P2a 只完成读时消费,正式构建产物仍待 PB 刀补齐。
- `docs/切片方案-profile深路径.md:PB0-PB4` - 新增五个独立预构建切片,每刀声明做/不做/判据/触达。
- `docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md:影响` - 追加执行补记,说明 PB0-PB4 是 ADR-0033 sidecar 规则的构建落地,不改变边界决策。

**入口**:后续正式做预构建补齐时从 PB0 开始,不要把 discourse/Formula/Pass2 audit 写盘混入 P3/P4/P5。
**测试**:`rg "PB0|PB1|PB2|PB3|PB4|预构建缺口|执行补记" docs/切片方案-profile深路径.md docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md docs/代码链路.md`。

## 2026-06-26 · Book MCP 只读边界落档

**触达**:
- `docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md:决策12` - 冻结 Book MCP 只暴露只读 `book.*` 工具面,不继承 reader 当前视口,不暴露 reader/memory 写命令。
- `docs/切片方案-profile深路径.md:1.5.8` - 新增 Book MCP readonly boundary 与 `BookMcpTools` 接口草案。
- `docs/切片方案-profile深路径.md:P7` - 新增 Book MCP 只读工具面切片,要求 `book_query` 显式 `anchor_lid`。

**入口**:后续实现 MCP server 从 P7 开始;不要把 MCP 只读工具面混入 P3/P4/P5 reader/memory/provider 切片。
**测试**:`rg "Book MCP|BookMcpTools|anchor_lid|决策12|P7" docs/adr/0033-core-schema-book-profile-reader-profile解耦-technical-learning作为当前profile.md docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-06-26 · PB0 profile metadata header writer

**触达**:
- `packages/core/src/profile-artifact.ts:buildProfileArtifactHeader` - 新增 technical_learning profile header 构造与必填字段校验。
- `packages/core/src/profile-artifact.ts:buildProfileMetadata` - 新增 `profile_metadata.json` 最小 `{header}` 形状。
- `packages/core/src/pass2.ts:ProfileArtifactHeader` - 改为复用 profile artifact 统一 header 类型并保持原导出兼容。
- `skills/build/pass1-batch.ts` - 在写 `base.json` / `source.txt` 同目录时同步写 `profile_metadata.json`。
- `packages/core/test/profile-artifact.test.ts` - 覆盖 header 默认值、metadata 形状与空字段失败。

**入口**:`pass1-batch.ts` 预构建写盘流程;后续 PB1-PB3 sidecar 复用 `buildProfileArtifactHeader` 的同一 header 来源。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;`.\node_modules\.bin\tsx.CMD skills\build\pass1-batch.ts packages\core\test\fixtures\sample.md skills\build\fixtures\pass1-sample-4windows.json "0"` 生成 `profile_metadata.json`。
## 2026-06-26 路 PB1 FormulaSemantics sidecar materialization

**触达**:
- `packages/core/src/formula-semantics.ts:buildFormulaSemanticsSidecar` - 将 gated FormulaSemantics 候选固化为带 `ProfileArtifactHeader` 的 `{header, items}` sidecar。
- `packages/core/src/zod.ts:FormulaSemanticsSidecarZ` - 增加预构建写盘前的 sidecar 形状自检。
- `skills/build/pass1-batch.ts` - 增加可选 `formula-candidates.json` 输入,复用 PB0 header 写出 `formula_semantics.json`。
- `crates/read-tools/src/lib.rs:parse_formula_semantics_sidecar` - `Book::load` 兼容 PB1 `{header, items}` 与旧数组格式。

**入口**:`pass1-batch.ts <book> <outputs.json> <idxs> [formula-candidates.json]` 写出 `formula_semantics.json`;读时 `Book::load` 自动加载。
**测试**:`packages/core/test/formula-semantics.test.ts` 覆盖 sidecar materializer;`crates/read-tools/src/lib.rs::tests::load_reads_headered_formula_semantics_sidecar` 覆盖新格式,旧格式测试保留;CLI smoke 写出 items=1/pending=0。
## 2026-06-26 路 PB2 TechnicalLearningDiscourseIndex sidecar materialization

**触达**:
- `packages/core/src/discourse-index.ts:buildTechnicalLearningDiscourseIndex` - 新增 discourse item/relation 确定性 gate,只校验 LID/enum/confidence/evidence,不判断标签语义质量。
- `packages/core/src/zod.ts:TechnicalLearningDiscourseIndexZ` - 增加 `discourse_index.json` 写盘前形状自检。
- `skills/build/pass1-batch.ts` - 增加可选 `discourse-candidates.json` 输入,复用 PB0 header 写出 `discourse_index.json`。
- `packages/core/test/discourse-index.test.ts` - 覆盖合法 sidecar、非法 relation 丢弃、非法 item 丢弃。

**入口**:`pass1-batch.ts <book> <outputs.json> <idxs> [formula-candidates.json] [discourse-candidates.json]` 写出 `discourse_index.json`。
**测试**:`pnpm -C packages/core test`;`pnpm -C packages/core typecheck`;CLI smoke 写出 `discourse_index.json` items=1/dropped=1。

## 2026-06-26 · PB2b discourse extractor 两阶段 prompt 方案落档

**触达**:
- `docs/切片方案-profile深路径.md:PB2b` - 新增 discourse extractor two-stage prompt 切片:Step A 逐 LID 分类,Step B 基于分类连局部 relation。
- `docs/切片方案-profile深路径.md:1.5.7` - 契约消费矩阵补入 PB2b,明确 prompt 只产候选,gate 决定是否落 `discourse_index.json`。

**入口**:后续实现从 `agents/discourse-index-extractor.md` 开始;不要把 PB2b 混进 PB3 Pass2 build orchestration。
**测试**:`rg "PB2b|two-stage|少连边|evidence_lids.*source LID.*target LID" docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-06-27 · PB3 Pass2 prompt Grill 决策落档

**触达**:
- `docs/PB3-pass2-prompt-grill.md` - 新增 PB3 Pass2 prompt/build orchestration 决策记录,冻结 candidate-driven Pass2、accepted/pending/rejected、edge contracts、split evidence、support_level、soft compatibility、source-window packet、v1 seed signals、cross-window long_range gate。
- `docs/切片方案-profile深路径.md:PB3` - PB3 小节改为强引用 grill 文件,明确实现前必读,并把判据改成 `long_range_candidates.json` + candidate classification + `pass2_audit.json`。

**入口**:后续实现 PB3 前先读 `docs/PB3-pass2-prompt-grill.md`;不要回到开放式 LLM 抽长程边方案。
**测试**:`rg "PB3-pass2-prompt-grill|long_range_candidates|accepted_edges|pending_edges|rejected_candidates|support_level|source_evidence_lids|target_evidence_lids" docs/PB3-pass2-prompt-grill.md docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-06-28 · PB2b discourse extractor 两阶段 prompt + gate 收紧

> 范围含代码:除两阶段 prompt + fixture 外,把 PB2b 原则里的硬线由确定性 gate 兜底(B2),不靠 LLM 自觉。不扩 sidecar schema、不抽 long_range、不混 PB3。

**触达**:
- `packages/core/src/discourse-index.ts:relationError` — 加 `sourceLid` 参数 + 三条收紧:`low_confidence`(< `MIN_RELATION_CONFIDENCE`)、`evidence_missing_source`/`evidence_missing_target`(evidence 必须同时含 source=item.lid 与 target_lid)。
- `packages/core/src/discourse-index.ts:validItemShape` — 加 `summary_too_long`:`local_summary` 超 `MAX_LOCAL_SUMMARY_LEN` 整 item 丢弃(不可信标注整条拒)。
- `packages/core/src/discourse-index.ts` — 新增导出常数 `MIN_RELATION_CONFIDENCE=0.5`、`MAX_LOCAL_SUMMARY_LEN=200`(占位默认,实测回填);`DroppedDiscourseCandidateReason` 加 4 个 reason。
- `agents/discourse-index-extractor.md` — 新增两阶段 prompt:Step A 逐 LID 分类(只产 mode/local_function/rhetorical_move/local_summary,闭集 enum)、Step B 基于分类连局部 relation(少连边、evidence 含两端);只产 items,header 构建期注入。
- `skills/build/fixtures/discourse-sample.json` — 最小 fixture(sample.md LID),覆盖「定义+解释」(1.3→1.2 explains)、「例子」(1.4.2→1.2 exemplifies)、「相邻但不连」(1.5.2 弱 continues 被低置信丢弃)。
- `packages/core/test/discourse-index.test.ts` — 新增 `PB2b gate 收紧` 4 测(低置信丢弃 / evidence 缺 source 或 target / summary 超长整 item 丢 / 合法保留)。

**入口**:预构建 `pass1-batch.ts <book> <outputs> <idx> [formula] [discourse-candidates.json]` 第 5 位参数;subagent 实现从 `agents/discourse-index-extractor.md` 开始。gate 不变沿用 `buildTechnicalLearningDiscourseIndex`。
**测试**:`pnpm -C packages/core test`(58 全绿,discourse-index 7 测含 4 新)+ `pnpm -C packages/core typecheck`(tsc 0)。CLI smoke:`pass1-batch.ts sample.md … "0" "" discourse-sample.json` → `discourse index items=4 dropped=1`,写盘内容人工核对(1.3/1.4.2 relation 留、1.5.2 弱边丢)。schema(`TechnicalLearningDiscourseIndexZ`)保持不变。

## 2026-06-28 · PB3-1 Pass2 候选契约 + edge type 扩 11 类 + cross-window helper

> PB3 第一子刀(A4):仅落地 build-only 契约与跨窗口判定,**不**实现 candidate builder seed 信号 / gate / work packet / prompt / 编排。依据 `docs/PB3-pass2-prompt-grill.md` §2/§4/§12。

**触达**:
- `packages/core/src/pass2.ts:TechnicalLearningLongRangeEdgeType` — grill §4 扩为 11 类(加 `supports/rebuts/summarizes`);新增导出 `TECHNICAL_LEARNING_LONG_RANGE_EDGE_TYPES`(冻结列表,P1 gate 的 `LONG_RANGE_EDGE_TYPES` Set 由它构建);**未**加 `related_to/same_problem/reuses_formula`。
- `packages/core/src/pass2-build.ts` — 新文件(PB3 build-only,不被 `Book::load` 读)。`LongRangeCandidate`(grill §2 最小候选形状)+ `LongRangeCandidateIndex`;`buildLidToWindowIndex(windows)` 叶 LID→窗口序号;`isCrossWindow(sourceLids,targetLids,map)` 实现 grill §12 跨窗口硬定义(source/target 各有 LID 落不同窗口才为真;未解析 LID 不贡献信号)。
- `packages/core/test/pass2-build.test.ts` — 6 测:edge type 列表 11 类含新 3 类、不含禁用 3 类;LID→窗口映射;跨窗口 true/同窗 false/未解析 false。

**入口**:PB3-2 candidate builder 将消费 `buildLidToWindowIndex`/`isCrossWindow`;`LongRangeCandidate` 是 Pass2 work packet(PB3-4)与 prompt 输入。
**测试**:`pnpm -C packages/core test`(64 全绿,+6 pass2-build;pass2 P1 gate 3 测仍绿)+ `typecheck`(tsc 0)。

## 2026-06-28 · PB3-2a Pass2 确定性候选生成(shared-node bridge,grill §11 信号 4+1+2)

> PB3 第二子刀(A4)。决策:候选 v1 走 shared-node 路线,**不加**语义桥;意思相关但无共同概念的边接受预构建漏、读时 scope 外扩兜底(grill §11 已把语义桥列 deferred)。signal 3(formula 桥)拆 PB3-2b。

**触达**:
- `packages/core/src/pass2-build.ts:buildLongRangeCandidates` — 确定性候选生成。**signal 4**(同一 graph node occurrences 跨 ≥2 窗口)作 join 主干:取该 node 最早窗口为 source 侧,与其每个较晚窗口里共现的其他 node 配成有向候选;两端 LID 必过 `isCrossWindow`。**signal 1**(local_function definition/explanation → application/example/procedure_step ⇒ hint exemplifies/applies)、**signal 2**(rhetorical_move prerequisite/main_point → concept_elaboration/worked_example ⇒ hint builds_on/prerequisite)只定 `relation_hints` 不决定成对。按有向节点对去重合并(source/target LID、reason、hint 并集,seed_score 取大),输出按 candidate_id 排序。两端都复现时产双向候选(v1 高召回,精度交 LLM+gate)。
- `packages/core/src/pass2-build.ts:LongRangeCandidateInput` — `{graphNodes, lidToWindowIndex, discourseIndex?}`;辅助 `occLidsOf`(claim 取 source_lid,实体/概念取 occurrences)、`hintsFor`、`uniqSort`/`uniqHints`。
- `packages/core/test/pass2-build.test.ts` — +5 测:桥接+signal1 hint、无 discourse 时空 hint/score 0.4、signal2 rhetorical_move hint、无复现节点不产候选、同有向对跨多窗口合并(双向候选)。

**入口**:PB3-4 work packet / Pass2 prompt 消费 `buildLongRangeCandidates(...)` 的输出;seed_score=min(1,0.4+0.3×命中信号数)(占位,实测回填)。
**测试**:`pnpm -C packages/core test`(69 全绿,+5 pass2-build)+ `typecheck`(tsc 0)。
**不做(留后)**:signal 3 formula 桥(PB3-2b);PB3 gate/packet/prompt/编排;语义桥/二跳/标题匹配/same_problem(grill 禁)。

## 2026-06-28 · PB3-2b Pass2 候选 signal 3(formula 定义/使用桥)

> PB3 候选生成补齐 grill §11 第四信号。重构 `buildLongRangeCandidates` 为 `emit` 闭包统一合并,shared-node 与 formula 两路共用同一去重/排序。

**触达**:
- `packages/core/src/pass2-build.ts:LongRangeCandidateInput` — 加可选 `formulaSemantics?: FormulaSemantics[]`。
- `packages/core/src/pass2-build.ts:buildLongRangeCandidates` — 提取 `emit(srcId,tgtId,srcLids,tgtLids,hints,reasons,signals)` 闭包(自指/非跨窗口拒,跨信号合并取 score 大);新增 `lidToNodes`(LID→节点 id)解析 formula 桥端点;**signal 3 循环**:对每个 `FormulaSemantics`,`formula_lid` 与其 `context_links.target_lid` 跨窗口时,连两端 LID 上的 graph node,hint `applies/builds_on`,reason `signal3_formula_bridge:<formula_lid>`。
- `packages/core/test/pass2-build.test.ts` — +2 测:formula 跨窗口桥接产候选;同窗口 formula link 不产候选。

**入口**:`buildLongRangeCandidates({graphNodes, lidToWindowIndex, discourseIndex?, formulaSemantics?})` 现覆盖 grill §11 四信号全。
**测试**:`pnpm -C packages/core test`(71 全绿,+2 pass2-build)+ `typecheck`(tsc 0)。

## 2026-06-28 · PB3-3 PB3 gate(split evidence / support_level / 跨窗口硬校验)

> PB3 第三子刀。吃 Pass2 LLM 的 accepted/pending/rejected,确定性硬校验 accepted_edges(grill §9),过的降成 `GraphEdge(scope=long_range)`,split evidence/support_level/rationale 留 audit;pending+rejected+gate 丢弃只进 audit,绝不进 base.json。

**触达**:
- `packages/core/src/pass2-build.ts:gatePass2BuildOutput` — PB3 gate。硬校验顺序(grill §9):edge type ∈ 11 类、scope=long_range、source/target node 存在、source/target evidence 各非空、`evidence_lids` 覆盖 source∪target evidence、evidence 全真实、`support_level≠weak_inference`、`weight≥MIN_LONG_RANGE_WEIGHT`、source/target evidence 跨窗口(复用 `isCrossWindow`)。过 → `GraphEdge`(仅 Core 字段)+ audit accepted;否则 `gate_dropped` 带 `Pass2GateDropReason`。
- `packages/core/src/pass2-build.ts` 新类型:`SupportLevel`、`TechnicalLearningAcceptedEdge`(grill §7 split evidence)、`RejectedReason`/`RejectedCandidate`(grill §3)、`Pass2LlmOutput`、`Pass2GateDropReason`、`Pass2AuditEdge`、`Pass2BuildAuditSidecar`(header+accepted+pending+rejected+gate_dropped)、`Pass2BuildResult`;常数 `MIN_LONG_RANGE_WEIGHT=0.5`(占位)。
- `packages/core/test/pass2-build.test.ts` — +2 测:合法 accepted 降级 + pending/rejected 仅进 audit;11 种硬校验违例各自 `gate_dropped` 正确 reason。

**入口**:PB3-5 编排把 `buildLongRangeCandidates` → Pass2 subagent → `gatePass2BuildOutput` → 写 `base.json` long_range 边 + `pass2_audit.json`。注:P1 旧 gate `pass2.ts:gateTechnicalLearningPass2LongRange` 仍在(简版,不含 split evidence/support/跨窗口),PB3-5 用新 gate,旧 gate 后续评估是否退役。
**测试**:`pnpm -C packages/core test`(73 全绿,+2 pass2-build)+ `typecheck`(tsc 0)。

## 2026-06-28 · PB3-4 Pass2 work packet + edge type contracts + prompt 重写

> PB3 第四子刀。把「LLM 看到什么、必须怎么做」定下来:work packet 类型(grill §10)、每 edge type 的判定 rubric(grill §5/§6)、重写 subagent prompt 为 candidate-driven + 默认拒绝 + accepted/pending/rejected(grill §3/§13)。packet 实际装配(带正文)留 PB3-5。

**触达**:
- `packages/core/src/pass2-build.ts:EDGE_TYPE_CONTRACTS` — 11 类 edge 的 `{when, when_not, evidence, roles, direction}` rubric(grill §5);direction 按 §6:仅 `analogous_to` undirected,`contrasts` 默认 directed,余 directed。`EdgeTypeContract` 类型。
- `packages/core/src/pass2-build.ts:Pass2WorkPacket` + `CandidateNodeSnapshot` — grill §10 per-source-window 分类单元(source_window 正文/leaf_lids/title_path + source_nodes/discourse/formula + candidate_targets + edge_type_contracts)。
- `agents/pass2-longrange-linker.md` — 从「P1 自由抽边」重写为「PB3 逐候选分类」:输入 `Pass2WorkPacket`,默认拒绝流程(grill §13)、关键区分(prerequisite vs builds_on 等)、support_level 规则(grill §8)、输出三类 JSON;红线含证据分两侧/跨窗口/scope=long_range。
- `packages/core/test/pass2-build.test.ts` — +2 测:contracts 覆盖全 11 类且自洽;仅 analogous_to undirected。

**入口**:PB3-5 装配 `Pass2WorkPacket`(注入源窗口正文/title_path)喂 subagent;subagent 输出经 `gatePass2BuildOutput`。
**测试**:`pnpm -C packages/core test`(75 全绿,+2 pass2-build)+ `typecheck`(tsc 0)。

## 2026-06-28 · PB3-5 Pass2 预构建编排 + 写盘(PB3 收口)

> PB3 末刀。`pass1-batch` 接线:确定性候选 → (Pass2 subagent 输出 fixture)→ PB3 gate → 合并 long_range 边进 base + 写 `pass2_audit.json` + build-only `long_range_candidates.json`。仅 TS 改动,Rust/read-tools 未动(base.graph_edges 已支持 scope=long_range)。

**触达**:
- `skills/build/pass1-batch.ts` — 新增第 6 位参数 `[pass2-output.json]`;`profileHeader` 提前到 base 固化前;`buildLidToWindowIndex(windows)` → `buildLongRangeCandidates({graphNodes:nodes, lidToWindowIndex, discourseIndex?, formulaSemantics?})` 写 `long_range_candidates.json`(build-only);有 pass2 输出时 `gatePass2BuildOutput(...)` → `Pass2BuildAuditSidecarZ.parse` 自检 → long_range 边合并进 `base.graph_edges`(`[...edges, ...longRangeEdges]`)统一 `ReadOnlyBaseZ.parse` + 写 `pass2_audit.json`。
- `packages/core/src/zod.ts` — 新增 `TechnicalLearningLongRangeEdgeTypeZ`(11)、`SupportLevelZ`、`Pass2AuditEdgeZ`、`RejectedCandidateZ`、`Pass2GateDropZ`、`Pass2BuildAuditSidecarZ`(产出前自检)。
- `packages/core/test/pass2-build.test.ts` — +1 集成测:合成 2 窗口基座 `buildLongRangeCandidates`→手写 Pass2 输出→`gatePass2BuildOutput` 产 long_range `GraphEdge`,audit 过 `Pass2BuildAuditSidecarZ`。
- `skills/build/fixtures/pass2-sample.json` — Pass2 输出 fixture(accepted/pending/rejected 各一)。

**入口**:`pass1-batch.ts <book> <outputs> <idx> [formula] [discourse] [pass2-output.json]`。
**测试**:`pnpm -C packages/core test`(76 全绿,+1 pass2-build)+ `typecheck`(tsc 0)。CLI smoke(sample.md,1 窗口 0 节点):`long_range candidates=0` + `pass2_audit.json` accepted=0/pending=1/rejected=1/gate_dropped=1(c1 missing_source、c2 weak→pending、c3 rejected),zod 校验通过、写盘内容人工核对。成功写回长程边路径由集成测试确定性覆盖(sample.md 单窗口无法产跨窗口候选)。
**PB3 收口**:PB3-1..PB3-5 全完成。确定性骨架 `buildLongRangeCandidates`(grill §11 四信号)→ Pass2 subagent 分类(`agents/pass2-longrange-linker.md`)→ `gatePass2BuildOutput`(grill §9)→ base long_range 边 + `pass2_audit.json` 全打通。**未做(留 PB4)**:read-time smoke(`book.context far` 读写回后的 long_range 边);P1 旧 gate `gateTechnicalLearningPass2LongRange` 退役评估。

## 2026-06-28 · ADR-0034/0035 落档:route 导航原语 + 人主动带读/访客向导两投影 `[docs]`

> Grill 落档(读《参考_硅基天启:灭世之技术推演》insight)。演讲核心方法论 = mechanism/policy 分离,恰是 ADR-0033 我们的架构;两个 insight 各对一个故事(主动带读=教学状态机 / 引导外部 agent=世界模型+小解释)。只写架构决策与切片更新,不改可执行代码。八条决策已落 ADR。

**触达**:
- `docs/adr/0034-route导航原语-...md` — route 作为新 Core 导航原语 + 两投影:统一(一机制两投影)/确定性边界(route 零 LLM,LLM 住书里用 route 不止解释)/route_from 前沿内核 + route_to BFS/Core 结构排序 + policy 教学排序分层(架在 book.context 上)/前沿按 5 类导航语义分组(back/forward/concretize/cross/continue)/人投影默认逐停靠点带读 + 自动巡航 opt-in。
- `docs/adr/0035-book-mcp访客会话-...md` — 修订 P7 无状态 → 连接式访客会话;记忆三分(①世界模型可借 / ②读者私人不外借 / ③访客会话 ephemeral);TCP 式握手挥手 + 临时游标 cursor{at_lid,last_frontier};book_guide(返路线,book_query 姊妹);住户/访客 = 临时住户 lite。
- `docs/切片方案-profile深路径.md` — 新增 §1.5.9 navigation contracts;P3 加「agent 主动带读」+ 触达 0034;P4 标 reader_profile=② 绝不外借 + 触达 0034/0035;**P7 重写**为连接式访客向导面;**新增 P8** route Core 原语(P3/P7 共享底座);§3 完成判据加 route 行。
- `CONTEXT.md` — 新术语:route / route_from·route_to / 前沿(frontier)+导航类别 / 住户·访客 / 三类记忆 / 访客会话 / book_guide。

**入口**:后续实现从 **P8(route Core)** 起,再 P3(人带读)/ P7(访客向导)。
**测试**:`rg "route_from|route_to|导航类别|住户|访客|book_guide|访客会话|ADR-0034|ADR-0035" docs/ CONTEXT.md`;本切片未改可执行代码,未跑 cargo/pnpm。
**OPEN(未 grill / 待定)**:① **反馈信号**——agent 靠什么"反馈"调整带读(无眼动,候选:用户消息 / viewport 手动滚动 / memory / quiz),下一个 grill;② route 命令面落点 + 命名;③ route_to / book_guide / 访客会话 归 P7/P8 的具体边界。

## 2026-06-28 · ADR-0036 落档:带读反馈信号模型(解决 OPEN①) `[docs]`

> §0.5 Grill 四问收敛(承 OPEN①)。把"agent 靠什么反馈调整带读"定下来:显式 NL 提问为唯一主信号 / 反馈意图二维 / 裸信号结构兜底消歧 / 人访客两投影 / viewport 模式分裂。只写架构决策,零可执行代码改动。

**触达**:
- `docs/adr/0036-反馈信号模型-...md`(新)— 五决策:①显式 NL 提问唯一主信号(viewport 弱旁路/memory 慢先验/quiz 留后);②反馈意图二维(导航轴→route 5 类 / 讲法轴→policy 讲法层)+ 开放 NL 不立闭集词表,agent 据语义定 `{轴+类别+target}`,target 经 book.concept 解析(route 之外);③裸"没懂"= `route_from(at).back ∩ 未读前置` 结构收窄 + 可撤销提议 + 二次信号升级,不靠 LLM 神判;④人/访客同骨架两插槽(历史来源 ②/③、讲法整形 reader_profile/无)+ 终裁者不同;⑤viewport 偏离角色随模式分裂(默认 turn-based=静默 re-sync rebase `at`,巡航 opt-in=中断信号停+问)。命门=route 零 LLM,LLM 只产 `{轴+类别+target}` 不产路/LID。
- `docs/切片方案-profile深路径.md` — §1.5.9 红线加反馈信号行;P3 做/判据加 NL 提问→`{轴+类别}` + 裸信号结构兜底 + viewport 静默 re-sync,触达 +0036;P7 做/判据加访客反馈两插槽(③历史/讲法塌缩/访客自身终裁),触达 +0036。
- `CONTEXT.md` — 新术语:反馈信号 / 导航轴·讲法轴。

**入口**:实现仍从 **P8(route Core)** 起,P3 带读 loop 消费反馈走 ADR-0036;P7 访客面同骨架换插槽。
**测试**:`rg "反馈信号|导航轴|讲法轴|ADR-0036|结构兜底|静默 re-sync" docs/ CONTEXT.md`;本切片未改可执行代码,未跑 cargo/pnpm。
**OPEN 余下**:② route 命令面落点 + 命名;③ route_to / book_guide / 访客会话 归 P7/P8 的具体边界。(① 已由本 ADR 关闭)

## 2026-06-28 · OPEN② 收口:route 命令面落点 + 命名(更新 ADR-0034 影响段 + P8 判据) `[docs]`

> §0.5 Grill 两问收敛(承 OPEN②)。route 落点定为 book.* 一等叶子命令 + 两命令命名 + 参数/暴露形状。不新开 ADR(命名落点未到独立架构权衡份量,吝啬提议),更新 ADR-0034 自标的 OPEN + P8 判据收口。零代码。

**触达**:
- `docs/adr/0034-...md` 影响段 — 关掉"命令面落点 OPEN":归 `book.*` 一等叶子命令(确定性/零 LLM/只读/毫秒级,符 ADR-0014 叶子 + ADR-0007 人机对称);命名 `book.route_from(at,k?)`(返全 5 类、无 category 过滤参)+ `book.route_to(from,target,k?)`(BFS,target 先 book.concept 解析);三段落点(Core `read_tools::Book::route_*` 架在 context 上 / `orchestrator::tool_specs()`+dispatch 两命令均暴露 / REST 自动 GET);错误信封 ADR-0015。
- `docs/切片方案-profile深路径.md` P8 — 做/不做/判据加命令面落点(tool_specs 暴露、参数最小返全 5 类、不给 category 过滤参、错误信封);触达 `[ADR-0007/0013/0014/0028/0034]`。

**入口**:实现仍从 P8 起;`book.route_from` 内部吃 `Book::context(at,"far")` 的边按 `edge_type→NavCategory` 重组。
**测试**:`rg "book.route_from|book.route_to|一等叶子命令|category 过滤参" docs/`;本切片未改可执行代码。
**OPEN 余下**:③ route_to / book_guide / 访客会话 归 P7/P8 的具体边界。(①② 已关闭)

## 2026-06-28 · OPEN③ 收口:P7/P8 crate 边界 + 访客暴露双层(补 ADR-0035 决策7) `[docs]`

> §0.5 Grill 两问收敛(承 OPEN③)。Q1 层归属:route mechanism 共享兑现两投影、book_guide=runtime lite 命令、VisitorSession=server,不字面共享 run()。Q2 暴露边界:Tier 1 无状态只读 / Tier 2 book_guide 带会话,红线靠物理无路由。补 ADR-0035 决策7 + P7 判据。零代码。

**触达**:
- `docs/adr/0035-...md` — 决策2 限定作用域(连接式仅 Tier 2 book_guide);**新增决策7**(暴露双层 + crate 边界 + 红线焊法):Tier 1 无连接无状态只读(book_manifest/text/context/concept/query/synthesize,不建会话)/ Tier 2 带会话 book_guide(握手/挥手/GC 只压此层);crate 落点 route_*=read-tools Core 共享、book_guide=runtime lite LLM 命令(query 姊妹、不复用住户 run())、VisitorSession=server AppState;裸 route 不给访客(v1);红线靠访客 MCP dispatch 物理无 reader/memory 分支(非运行时权限判)。影响段:访客 N 会话=Tier 2 only,GC 只压 Tier 2。
- `docs/切片方案-profile深路径.md` P7 — 做加 Tier 双层 + crate 落点;不做加 裸 route 不给访客/物理无路由/Tier 1 不建会话/book_guide 不塞 server;判据加 Tier 1 不建会话 + dispatch 物理无 reader/memory 分支可测。

**入口**:P7 实现按 Tier 双层;route_* 从 P8 起;book_guide 在 runtime 复用 query/synthesize mini-loop + route_*。
**测试**:`rg "Tier 1|Tier 2|物理无路由|决策7|lite LLM 命令" docs/`;本切片未改可执行代码。
**OPEN 全部关闭**:①(ADR-0036 反馈信号)/ ②(ADR-0034 影响段 route 命令面)/ ③(ADR-0035 决策7 P7/P8 边界)。route 两投影带读设计线 grill 收敛,下一步进代码(P8 route Core 起)。

## 2026-06-28 · P8-1 route_from Core 前沿原语(NavCategory 映射 + 5 类分组 + 结构排序) `[Rust]`

> P8 第一子刀(A4 拆 P8-1 route_from / P8-2 route_to / P8-3 命令面)。承 ADR-0034 决策4/5。**仅 read-tools,未碰 runtime**(命令面暴露留 P8-3)。

**触达**:
- `crates/read-tools/src/lib.rs:NavCategory`(新 enum,5 类 back/forward/concretize/cross/continue,ts-rs 导出 packages/web)+ `RankedStep`(lid/edge_type/why/evidence_lids/score)+ `Frontier`(5 类 Vec,`continue_` serde/ts rename `continue`)`[ADR-0034 决策5]`。
- `crates/read-tools/src/lib.rs:nav_category_of`(自由函数)— `edge_type→NavCategory` 固定确定性映射表;back=prerequisite/depends_on、forward=builds_on/refines/elaborates/explains/prepares/causes/results_in、concretize=exemplifies/applies/answers、continue=next_sibling/continues、**未知 local 边→cross 兜底**(保覆盖全边类型不丢边)。
- `crates/read-tools/src/lib.rs:Book::route_from(at,k?)` — 调 `context(at,"far",MAX)` 拿全投影项 → `route_step` 逐项转 (NavCategory, RankedStep) → 分组 → 组内 `score=weight/(1+树距)` 降序(tiebreak lid→edge_type)→ 各类 top-K。零 LLM、纯结构。invalid at 透传 `context` 的 LID_NOT_FOUND(not_found);叶子无边返空 5 类非 error。
- `Book::route_step`(私)— Via→步映射:**Tree 仅取 next_sibling→continue**(parent/prev_sibling/child 不进前沿 v1);Edge 用 e.edge_type+weight;Concept→co_occurrence/cross(占位权重 `CONCEPT_COOCCURRENCE_WEIGHT=0.5`);Discourse 用 relation_type+confidence+真 evidence_lids。
- `Book::lid_tree_distance`(私)— 物化路径公共前缀外层数和(占位口径)。
- 常数 `DEFAULT_ROUTE_K=DEFAULT_NEAR_K`、`CONCEPT_COOCCURRENCE_WEIGHT`(占位,实测回填 ADR-0034 何时回头)。
- `packages/web/src/generated/{NavCategory,RankedStep,Frontier}.ts` — ts-rs 自动产出。

**入口**:`read_tools::Book::route_from`(库 API;命令面 `book.route_from` 暴露留 P8-3)。`route_to`(P8-2)、`nearest_valid_lid` 错误增强(需扩 ToolError 共享面,单独评估)、教学 reorder(P3 policy)**本刀不做**。
**测试**:`cargo test -p read-tools`(**29 绿**,+6:nav 映射全桶+未知兜底 / 远边+概念共现+sibling 分组 / 叶子空 5 类非 error / invalid at not_found / discourse 关系映射 / k 截断保高分;+3 ts-rs export NavCategory/RankedStep/Frontier)。
**两处自拍决定(待 P3 实测校)**:未知 local 边→cross 兜底;Tree 仅 next_sibling 进前沿。
**未闭(P8 余下)**:P8-2 `route_to` BFS 派生;P8-3 `orchestrator::tool_specs()`+dispatch 暴露两命令 + REST GET + nearest_valid_lid 错误增强评估。

## 2026-06-28 · P8-2 route_to BFS 派生(同批边路径式导航) `[Rust]`

> P8 第二子刀。承 ADR-0034 决策3(route_to = route_from 同批边上 BFS 的确定性组合)。仅 read-tools,命令面留 P8-3。

**触达**:
- `crates/read-tools/src/lib.rs:Book::route_to(from,target,k?)` — 在 route_from 同批边上跑确定性 BFS,返 `from→target` 导航步路径 `Vec<RankedStep>`(全真 LID/真边)。`target` 须已解析 LID(NL→入口在 route 之外,`book.concept`)。`k`=跳数预算。from/target 非真→LID_NOT_FOUND(not_found);from==target 或 预算内不可达→空路径非 error。BFS prev 回溯 + visited 防环,邻居按固定类序展开保确定性。
- `Book::route_neighbors`(私)— route_from 5 类前沿按 (back,forward,concretize,cross,continue) 序展平、按 lid 去重(保首现);BFS 内用 `route_from(lid, MAX)` 全量前沿避免 top-K 截断藏路。
- 常数 `DEFAULT_ROUTE_HOPS=8`(占位,实测回填 ADR-0034 决策3 预算全程)。

**入口**:`read_tools::Book::route_to`(库 API;命令面 `book.route_to` 暴露留 P8-3)。
**测试**:`cargo test -p read-tools`(**35 绿**,+6:直达 1 跳 / 多跳链 1.1→1.2→1.3 / from==target 空 / 不可达(章节 LID)空 / invalid 端点 not_found / 跳数预算 k=1 够不到空)。新增 `book_chain()` fixture(长程链无直边)。
**未闭(P8 余下)**:P8-3 `orchestrator::tool_specs()`+dispatch 暴露 `book.route_from`/`book.route_to` + REST GET + nearest_valid_lid 错误增强评估。

## 2026-06-28 · P8-3 route 命令面暴露(tool_specs + dispatch + REST GET)`[Rust]` —— P8 收口

> P8 末刀。把 `book.route_from`/`book.route_to` 暴露到三处命令面。承 ADR-0034 影响段(book.* 一等叶子命令)+ ADR-0007 人机对称 + ADR-0028 只读叶子 GET。

**触达**:
- `crates/runtime/src/orchestrator.rs:tool_specs()` — 加 `book.route_from(at,k?)` / `book.route_to(from,target,k?)` 两 ToolSpec(LLM 可见,bounded 非 manifest token 炸弹)。
- `crates/runtime/src/orchestrator.rs:dispatch()` — 加两分支:`book.route_from`(缺 at→INVALID_RANGE)、`book.route_to`(缺 from/target→INVALID_RANGE,返 `{from,target,path}` 信封);只读不产 effect;ToolError 原样回喂不降级(ADR-0015)。
- `crates/server/src/lib.rs:route_book()` — REST GET 加 `route_from`/`route_to` 两 leaf(mirror book.context:缺参→400 validation、非法 k→400、invalid LID→404 透传 ToolError)。
- `packages/web/src/generated/{NavCategory,RankedStep,Frontier}.ts` — 已于 P8-1 产出;`Frontier.continue` 键经 serde/ts rename 正确(非 continue_),守 §1.5.9 `Record<NavCategory,_>` 契约。

**入口**:外层 LLM 经 tool_specs 见两命令、dispatch 执行;HTTP `GET /book/route_from?at=&k=` / `GET /book/route_to?from=&target=&k=`。
**测试**:`cargo test -p runtime -p server -p read-tools`(**全绿**:read-tools 35 / runtime 34 +3(tool_specs 暴露两名 / route_from 返 5 类+invalid not_found / route_to 包 path+缺参 validation)/ server 27 +1(route_from/route_to GET 200+5键、400 缺参/非法k、404 invalid))。预存 `failed to parse serde attribute` ×4 警告非本刀引入(read-tools 单建为 0)。
**交叉验证发现**:ADR-0034 影响段写「REST 自动 GET」,实为 `route_book` 手工逐命令 wiring(非自动)——已按现实补手写分支;ADR 措辞待修(见下『待办』)。
**P8 收口**:P8-1 route_from + P8-2 route_to + P8-3 命令面 三子刀全完成。route Core 导航原语(确定性、零 LLM、人机对称)落地,P3 人带读 / P7 访客 book_guide 的共享底座就位。
**待办(非阻塞)**:① ADR-0034 影响段「REST 自动 GET」措辞修为「手工 wiring」;② nearest_valid_lid 错误增强(需扩共享 ToolError,单独评估);③ route 权重/距离/前沿规模实测回填(ADR-0034 何时回头)。

## 2026-06-28 · P3-1 带读骨架(prompt 驱动主动带读 + 管道回归测试)`[Rust]`

> P3 首刀(A4 拆 P3-1 骨架 / P3-2 反馈消歧兜底 / P3-3 policy / P3-4 前端)。承 ADR-0034 决策6 + ADR-0036。架构方向(§0 用户拍板):**复用 run()+prompt 驱动带读,Rust 仅确定性兜底**(本刀不含兜底)。

**触达**:
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加「主动带读」引导段:用户请求『带我读/一步步讲/接着讲』时逐停靠点——reader.state 取当前 anchor → book.route_from(anchor) 看 5 类前沿 → 按用户意图挑**一个**下一停靠点(LID 只取自 route_from,不可编造)→ reader.gotoLid 真翻(可撤销 Goto)→ book.synthesize([上一停靠点,新停靠点]) citation-gated 解释 → 讲完即停等人,一回合只进一个停靠点 `[ADR-0034 决策6/ADR-0036]`。
- `crates/runtime/src/orchestrator.rs:tests::guided_read_one_stop_pipeline`(新)— scripted FakeAdapter 演一个带读回合,断言管道串通。

**入口**:前端对话区『带我读这章』→ `/agent/chat` → `run()`;带读全靠现有外层 loop + 新 prompt 引导,**无新编排函数**(架构方向兑现处)。
**测试**:`cargo test -p runtime`(**35 绿**,+1:带读回合工具序列 reader.state→book.route_from→reader.gotoLid→book.synthesize + 单条 Goto(1.1→1.2)合并可撤销 + citations⊆输入 + 终答非空 + !incomplete)。
**B2 边界**:确定性测只覆盖「带读管道串得通」(回归保护);**prompt 带读智能(挑哪个停靠点 / 讲多细 / 何时回看)无法 cargo test,须靠真 LLM 后端手动验**。
**未闭(P3 余下)**:P3-2 NL→{轴+类别}消歧 + 裸「没懂」结构兜底(`route_from.back ∩ 未读前置`)+ viewport 静默 re-sync;P3-3 `TechnicalLearningAgentPolicy` 教学 reorder/过滤;P3-4 Vue 带读 UI。

## 2026-06-28 · P3-3 技术学习教学整形 policy + book.guided_route_from `[Rust]`

> P3 第三子刀。承 ADR-0037(§0.5 grill 落档)+ ADR-0034 决策4/7。**新增住户教学整形 route 投影**:= route_from(Core) + technical_learning 教学整形(runtime profile policy,与 `book.synthesize` Core+policy 同构,零 LLM 确定性)。§0.5 拍板:Y1 新增姊妹工具 + C 中性默认序占位。

**触达**:
- `crates/runtime/src/lib.rs:GuidedGroup`(新 `{category:NavCategory, steps:Vec<RankedStep>}`,ts-rs 导出)+ `TEACHING_ORDER`(中性默认教学序常量 `continue>back>concretize>forward>cross`,占位待 P4 reader_profile 回填)+ `technical_learning_reorder(Frontier)→Vec<GuidedGroup>`(按教学序重排+剔空组,确定性)+ `pub fn guided_route_from(book,at,k?)`(route_from + 整形)`[ADR-0037 决策1/2/4]`。
- `crates/runtime/src/orchestrator.rs:tool_specs()` — 加 `book.guided_route_from(at,k?)` ToolSpec(LLM 可见)。
- `crates/runtime/src/orchestrator.rs:dispatch()` — 加 `book.guided_route_from` 分支(缺 at→INVALID_RANGE;返 `{at, groups}` 信封;只读不产 effect;ToolError 原样回喂)。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 带读引导②改优先 `book.guided_route_from`(教学整形版;裸 route_from 降为底层/访客)。
- `crates/server/src/lib.rs:route_book()` — REST GET 加 `guided_route_from` leaf(mirror route_from:缺 at→400、非法 k→400、invalid at→404,返 `{at, groups}`)。
- `packages/web/src/generated/GuidedGroup.ts` — ts-rs 自动产出(`{category, steps}` 契约)。

**入口**:带读 LLM 经 tool_specs 见 `book.guided_route_from`、dispatch 执行;HTTP `GET /book/guided_route_from?at=&k=`。住户带读优先用本工具拿整形过的 route。
**测试**:`cargo test -p read-tools -p runtime -p server`(**全绿**:read-tools 35 / runtime 39 +4(reorder 中性序+剔空 / 空前沿无组 / tool_specs 暴露 guided / dispatch guided 返 {at,groups}+剔空+缺 at validation)/ server 27 含 guided GET 200+{at,groups}/400 缺参·非法k/404 invalid)。
**B2 边界**:教学整形 reorder/剔空**确定性可单测**(判据"policy 教学排序作用在分组上"达成);**带读用 guided_route_from 挑停靠点的智能靠真 LLM 手动验**。
**未闭(P3 余下)**:P3-2(NL→{轴+类别} + viewport re-sync,均 prompt;裸兜底已推迟 P4);P3-3 余项(reader_profile 个性化整形 = P4 接入;"何时建议 goto/highlight/note/公式"深度 prompt policy 未扩,P3-1 有基础动作引导);P3-4 Vue 带读 UI。

## 2026-06-28 · P4-1 确定性已读账本(reader 翻页 → memory 记账)`[Rust]`

> P4 首刀(ADR-0038 重定位:Claude 式透明账本)。承 ADR-0038 决策1「① 确定性账本(无 LLM,直存)= 真读过的 LID 历史」。§0 拍板数据模型:**复用 Record `type="read"`,内容寻址天然去重**;reader `goto/scroll` 落点 anchor 真叶记账。**解锁 P3-2 裸兜底真历史源**(`未读前置 = back ∩ (全集\已读集)`,消费方在 P3-2,本刀不写)。

**触达**:
- `crates/memory/src/lib.rs:MemoryStore::mark_read(book_id,lid,now)` — 新:已读记账,复用 `save(type="read", layer="long_term", content="")`,内容寻址 upsert(重读=同 mem_id、count++、generated_at 刷新);非 note/highlight ⇒ 无 citation。
- `crates/memory/src/lib.rs:MemoryStore::read_lids(book_id)→Vec<String>` — 新:已读集/reading journey,recall(type=read) 按 generated_at 触达序(tie-break mem_id)确定性排序、去重。
- `crates/reader/src/lib.rs:Reader::goto_lid` — 签名加 `store:&mut MemoryStore, now`;落点成功后 `mark_read(真叶 leaf_lids[i])`(goto 容器→记子树首叶,非传入容器 lid)。
- `crates/reader/src/lib.rs:Reader::scroll` — 签名加 `book, store, now`,**返回类型 `()`→`Result<ViewportEffect,ToolError>`**(记账=持久写,失败诚实传播不静默);落点 anchor 记账。
- `crates/server/src/lib.rs:route_mut()` — `/reader/goto`·`/reader/scroll` 分支传 `&mut state.store, now`;scroll 改 match Result。
- `crates/runtime/src/orchestrator.rs:dispatch()` — `reader.gotoLid`·`reader.scroll` 分支传 `store, now`;scroll 改 match Result。

**入口**:人/agent 经命令面翻页 → `reader.goto_lid`/`reader.scroll` → 委托 `memory.mark_read` 落持久账本(与 note/highlight 委托 memory.save 同构,标注/账本单源=记忆层)。HTTP `POST /reader/goto|/reader/scroll`;agent `reader.gotoLid|reader.scroll`。
**测试**:`cargo test -p memory -p reader -p server -p runtime`(**全绿**:memory 10 +2(read_ledger 标记/去重/触达序/跨书隔离/无citation·落盘持久)/ reader 27 +1(goto+scroll+容器记真叶+未翻到不入集)/ server 4 / runtime 39)。
**B2 边界**:已读集「读过 vs 没读过」+ 记账落盘**确定性可单测**(判据达成);记账触发点=anchor 落点(不近似、不记 visible 全部、不记容器 lid)。
**未闭(P4 余下)**:P4-2 reader_profile 确定性派生(已读集 + note/highlight/qa 的 LID,解锁 P3-3 已读降权)/ P4-3 用户主动 LLM 记忆(显式记·反复提及,读时 memory.save)/ P4-4 四层透明文件产物。**回收点**:P3-2 裸兜底现已有真历史源(read_lids),可捡起。

## 2026-06-28 · P4-2 reader_profile 确定性派生 + 已读降权接入(解锁 P3-3)`[Rust]`

> P4 第二刀,A1 拆两子刀连做:P4-2a 派生(memory)+ P4-2b reorder 接入(runtime)。承 ADR-0038 决策3(四层产物=确定性派生)+ ADR-0037(P3-3 reader_profile 留 P4)。§0 拍板已读降权语义 = **组内稳定降权排后**(未读在前·已读沉底,保留回看入口,不剔除)。

**触达**:
- `crates/memory/src/lib.rs:ReaderProfile`(新结构 `{book_id, read_lids, focus_lids, puzzle_lids}`,② 读者私有,无认知水平推断)+ `MemoryStore::derive_reader_profile(book_id)`(确定性聚合:已读集 + 关注点 note/highlight LID + 疑惑点 qa LID)+ `anchor_lids_of_type`(私有,去重·LID 序)`[ADR-0038 决策3]`。**qa 类型未落地 ⇒ puzzle_lids 诚实暂空**。
- `crates/runtime/src/lib.rs:technical_learning_reorder(f, read_set)` — 加 `read_set` 参数,组内 `sort_by_key(read_set.contains)` 稳定降权(未读 false<已读 true);`read_set` 空 ⇒ 退化纯 TEACHING_ORDER(向后兼容)`[ADR-0037/0038]`。
- `crates/runtime/src/lib.rs:guided_route_from(book, at, k, profile)` — 加 `&ReaderProfile` 参数,从 `profile.read_lids` 建 read_set 传 reorder(focus/puzzle 留后续)。import `memory::ReaderProfile` + `HashSet`。
- `crates/runtime/src/orchestrator.rs:dispatch()` — `book.guided_route_from` 分支 `store.derive_reader_profile(book_id)` 传入。
- `crates/server/src/lib.rs:route_book()` — 签名加 `store: &MemoryStore`(仅 guided 用);调用处 `route_book(&state.book, &state.store, ...)`;guided 分支派生 profile 传入。

**入口**:住户带读 LLM 经 `book.guided_route_from` / HTTP `GET /book/guided_route_from?at=` 拿**已读降权**后的教学整形前沿;profile 从 P4-1 持久账本派生(read_lids)。
**测试**:`cargo test --workspace`(**全绿**:memory 11 +1(derive 派生·去重·跨书隔离·qa 诚实空)/ runtime 40 +1(组内未读升首·已读沉底保原序·不剔除)/ server 27 / read-tools 35;两现有 reorder 测试随签名加 `&HashSet::new()`)。
**B2 边界**:派生 + 组内稳定降权**确定性可单测**(判据达成);账本→profile→降权两段拼接由类型系统保证,未加冗余闭环集成测试。
**未闭(P4 余下)**:P4-3 用户主动 LLM 记忆 / P4-4 四层透明文件。**qa 落地**(E loop 问答 save type=qa)未做 ⇒ 疑惑点维度待填。synthesize 的 `reader_profile=not_attached` prompt 表达层未接(留后续)。**可捡**:P3-2 裸兜底(read_lids 真历史源)/ P3-3 已读个性化现已通(guided 降权)。

## 2026-06-29 · P4-3 用户主动 LLM 记忆(agent judgment 触发 + context type + citation 闸)`[Rust]`

> P4 第三刀,一条线(ADR-0039 修正 ADR-0038:**砍确定性计数器**,改 agent 读时 judgment 主动记;memory=构建用户上下文 + 三护栏)。§0.5 两 PENDING 实现期拍板:**type 名=`context`**、**citation 闸=无效丢弃·零有效仍可存**。底层 memory crate 未动(`SaveInput.citations` / `Record.{citations,generated_at}` P4 前已就位)。

**触达**:
- `crates/runtime/src/orchestrator.rs:tool_specs()` `memory.save` — `type` enum 加 `"context"`;加可选 `citations:[string]` 参数;description 述明 context = 主动构建的用户上下文(背景/偏好/关注/卡点,认知诚实)`[ADR-0039]`。
- `crates/runtime/src/orchestrator.rs:dispatch()` `memory.save` 分支 — **citation 确定性闸**:仅当显式传 `citations` 时进闸,逐 lid 校验 `∈ book.base.lid_nodes`(承 reader.gotoLid 同款),无效**确定性丢弃·不阻断·零有效仍存**,有效转 `MemCitation{lid,book_id,note:None}` 传 `SaveInput.citations`(原硬编码 None);不传 → None(承 memory crate note/highlight 自动派生)。`context` 走既有 `ty!="position"⇒long_term` 分支,直接落 long_term`[ADR-0039 决策3]`。import 加 `MemCitation`。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加 judgment 主动记引导:显式「记住/记下」∨ judgment 值得构建进读者长期理解 → `memory.save(type='context',…,citations?)`;守三护栏(认知诚实带证据措辞 / citations 锚真 LID / 只记跨会话值得复用·记错可删)。

**入口**:住户带读/问答 LLM 在 E loop(`run()`)读时,据 SYSTEM_PROMPT judgment 主动调 `memory.save type=context`;HTTP `POST /memory/save`(承命令面 REST 投影)同面可达。
**测试**:`cargo test -p runtime`(**41 全绿**,+1 `dispatch_memory_save_context_gates_citations`:有效 1.1 保留 / 无效 9.9 丢弃 / 全无效零有效仍存 / 不传 citations 空仍存 / context 落 long_term / 不产 effect / recall 取回 3 条)+ `cargo build --workspace` 通过。
**B2 边界**:citation 闸 + long_term 落位 + 零有效仍存**确定性可单测**(判据达成);**agent judgment「该不该记」的智能留真 LLM 手动验**(本质无客观判据,守 B2 不靠 LLM 自评做闸)。
**未闭(P4 余下)**:P4-4 四层透明文件(context 连成成长时间线呈现)。**qa 落地**仍未做。context 记多/碎的 recall 裁剪策略(ADR-0039「何时回头」)留实测,**不复活计数器**。

## 2026-06-29 · P4-4 四层产物物化(只读派生 .md + 单向覆写)`[Rust]`

> P4 末刀(ADR-0040,§0.5 grill 拍两分支:**载体=物化 .md 文件**、**读写语义=纯派生只读快照·save/delete 后整页单向覆写**)。真相源唯一 memory.json;物化但单向,折中 ADR-0038(物化)× ADR-0012/0020(不双向)。**v1 两层零 LLM**(session 详档/raw 随 ADR-0038 砍 Phase1 失生产者,不做)。纯 memory crate 内,不动命令面/runtime/server。

**触达**:
- `crates/memory/src/lib.rs:MemoryStore::all_book_ids`(私有,distinct·排序)+ `context_timeline(book_id)`(私有,context 记忆按 generated_at 时间线序,tie mem_id)`[ADR-0039/0040]`。
- `crates/memory/src/lib.rs:MemoryStore::render_reader_profile_md` — 渲染 `reader-profile.md`(所有书分段:已读集 + 关注点 note/highlight + 卡点 qa[暂空诚实] + agent 记的 context 成长时间线带 cite),纯确定性无 LLM`[ADR-0040]`。
- `crates/memory/src/lib.rs:MemoryStore::render_handbook_md` — 渲染 `reading-handbook.md`(per-book 计数 + cross-book 读过哪些书/各自 context 条数;概念对齐留 ADR-0006),纯确定性`[ADR-0040]`。
- `crates/memory/src/lib.rs:MemoryStore::write_profile_files` — 把两 .md 物化到 memory.json 同目录(ASCII 文件名避 Windows 中文文件名工具坑);**best-effort**,失败不阻断账本。
- `crates/memory/src/lib.rs:MemoryStore::{save,delete}` — persist 后 `let _ = self.write_profile_files()`(账本变 → .md 单向覆写;mark_read 走 save 同触发)。

**入口**:用户/agent 直接看/grep `<memory 目录>/reader-profile.md` 与 `reading-handbook.md`(memory.json 同目录,默认 `<home>/.understand-book/memory/`);随 save/delete 自动刷新。
**测试**:`cargo test -p memory`(**12 全绿**,+1 `profile_files_materialize_and_overwrite_on_delete`:已读/关注/context 时间线+cite/qa 暂空/cross-book 落盘 + delete 后单向覆写旧 context 消失·已读仍在)+ `cargo test --workspace` 全绿(runtime 41/server 27/read-tools 35…,save/delete 新副作用 best-effort 不扰现有测试)。
**B2 边界**:渲染 + 落盘 + 单向覆写**确定性可单测**(判据达成);LLM 表达层摘要 v1 不做(守 B2,无 LLM 自评)。
**未闭(留后续)**:LLM 表达层摘要(讲成人话,不产事实);session 详档(需非后台确定性会话摘要源);cross-book 概念对齐(ADR-0006);reader-profile 体积膨胀裁剪(不复活计数器)。**qa 落地**仍未做 ⇒ 卡点维度恒空。**P4 大切片至此 P4-1~P4-4 全部完成**。

## 2026-06-29 · P3-2 裸「没懂」结构兜底(book.unvisited_back 确定性命令)`[Rust]`

> P3-2 末块(ADR-0036 决策3)。NL→{轴+类别} + viewport re-sync 早随 P3-1/P3-3 SYSTEM_PROMPT 落;**裸「没懂」兜底**当时依赖 P4 真历史源推迟,现 P4-1 `read_lids` 就位后捡起。§0.5 拍载体=**新独立确定性命令**(否决扩 guided 标 read 让 agent 心算交集,违 ADR-0036 命门)。无新 ADR(机制 ADR-0036 已钉)。

**触达**:
- `crates/runtime/src/lib.rs:unvisited_back(book, at, profile)` — 确定性原语:`route_from(at).back` 过滤 `read_lids` → 未读前置 RankedStep 列表(组内保 weight×距离 序,首项=最该回看)。未读过滤在 runtime policy 层消费 ② reader_profile,route Core 零 LLM 不破(guided_route_from 姊妹)`[ADR-0036 决策3]`。
- `crates/runtime/src/orchestrator.rs:tool_specs()` — 加 `book.unvisited_back` tool_spec(at,述明裸「没懂」用法)。
- `crates/runtime/src/orchestrator.rs:dispatch()` — `book.unvisited_back` 分支:派生 reader_profile → unvisited_back → `{at, unvisited_back}` 信封;缺 at→validation、invalid at→not_found(承 route_from),只读不产 effect。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加裸「没懂」(无 locus)编排引导:①调 unvisited_back ②空→synthesize 原地重讲(讲法轴)③非空→重讲+可撤销提议「先回看 首项」④二次「没懂」→ reader.gotoLid 升级带去回看;未读前置只能取自命令返回,不自判读没读。
- `crates/server/src/lib.rs:route_book()` — 加 `book.unvisited_back` GET 投影(派生 profile,`{at, unvisited_back}`);import 加 `unvisited_back`。

**入口**:住户带读 LLM 收到裸「没懂」→ `book.unvisited_back` / HTTP `GET /book/unvisited_back?at=` 拿确定性未读前置,据空/非空走讲法轴或回看提议。
**测试**:`cargo test --workspace` 全绿(runtime 41→43:+`unvisited_back_filters_read_prereqs_deterministically`(depends_on back fixture:全未读→[2.1,2.2]/读 2.1→[2.2]/全读→空/invalid→not_found)+ dispatch 信封接线;server 27 含新 GET 断言)。
**B2 边界**:`back ∩ 未读` 交集 + 空/非空判定**确定性可单测**(判据达成,守 ADR-0036 命门不靠 agent 心算);agent「是不是裸没懂」识别 + 编排(重讲/提议/升级)靠真 LLM 手动验。
**未闭(ADR-0036 何时回头)**:二次「没懂」升级前要不要先插一次反问;viewport re-sync「偏离」判定(完全跟随 vs 仅大跳)留实测;quiz 主动探测留后。

## 2026-06-29 · P4-5 qa-1 生产(qa 存 + puzzle_heat 派生)`[Rust]`

> P4-5 qa 落地首刀(ADR-0041,回收 P4-2 `puzzle_lids` 恒空缺口)。§0.5 grill 收敛:qa = LID 价值/提问热度信号(读者私人 ②),三消费方全锚 `anchor.lid`。本刀只生产(存 + 派生),消费(back 升权/recall/问题渲染)留 qa-2。

**触达**:
- `crates/memory/src/lib.rs:ReaderProfile.puzzle_lids: Vec<String>` → **`puzzle_heat: BTreeMap<String,u32>`**(lid→qa 记录条数;保留次数=价值热度信号)`[ADR-0041 决策3]`。
- `crates/memory/src/lib.rs:MemoryStore::qa_heat`(新私有)— 按 `anchor.lid` 聚合 qa 记录条数(内容寻址:不同问题=不同 record 计 1;重复同问题 upsert 不增),纯确定性无 LLM。
- `crates/memory/src/lib.rs:derive_reader_profile` — `puzzle_lids: anchor_lids_of_type(&["qa"])` → `puzzle_heat: qa_heat(book_id)`。
- `crates/memory/src/lib.rs:render_reader_profile_md` 卡点段 — 渲染 `- {lid} (×{count})`(真实问题文本渲染留 qa-2);`render_handbook_md` 卡点计数改 `puzzle_heat.len()`。
- `crates/runtime/src/orchestrator.rs:tool_specs()` `memory.save` — type enum 加 `"qa"`;description 述明 qa = book.query 答完后存(anchor=问题 LID、content=用户原问题)。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加 qa 记录引导:用 book.query 答完书内提问后 `memory.save(type='qa', anchor_lid=<query anchor>, content=<用户原问题>)`;事实记录不判断卡否;操作/闲聊/元问题不记。
- dispatch `memory.save` qa **无需改**:走既有 `ty!="position"⇒long_term`、非 note/highlight 不自动派生 citation,qa 直接流通。

**入口**:住户 E loop(`run()`)读时,据 SYSTEM_PROMPT 在 book.query 答题后调 `memory.save type=qa`;HTTP `POST /memory/save` 同面可达。puzzle_heat 经 `derive_reader_profile` 派生(消费方 qa-2 接)。
**测试**:`cargo test -p memory -p runtime` 全绿(memory 13 +1 `derive_reader_profile_qa_heat_aggregates`:不同问题聚合计数/重复 upsert 不增/跨书隔离/read·note 同 lid 不计入;runtime 44 +1 `dispatch_memory_save_qa_lands_longterm_and_feeds_heat`:qa 落 long_term+anchor 设+不产 effect+heat 聚合);`cargo build --workspace` + server 27/4 绿(struct 字段变更无外溢)。
**B2 边界**:qa 存储 + heat 派生**确定性可单测**(判据达成);**agent「是不是实质问题/该不该存 qa」的 judgment 靠真 LLM 手动验**(本质无客观判据,守 B2)。
**未闭(qa-2)**:`technical_learning_reorder` back 组按 heat 升权(压已读)+ `reader-profile.md` 卡点段渲染真实问题文本 + LID-local recall(`memory.recall(lid,type=qa)`)prompt 引导。

## 2026-06-29 · P4-5 qa-2 消费(back 组卡点升权 + 问题渲染 + LID-local recall)`[Rust]` —— qa 落地收口

> P4-5 qa 落地末刀(ADR-0041),消费 qa-1 的 `puzzle_heat`。三消费方全锚 `anchor.lid`:确定性 back 升权 / 透明渲染 / LID-local recall。

**触达**:
- `crates/runtime/src/lib.rs:technical_learning_reorder` — 加 `puzzle_heat: &BTreeMap<String,u32>` 参数;**仅 back 组** qa 卡点升权(压已读):组内 `sort_by_key((tier, Reverse(heat)))`,Tier A 问过(heat>0,heat 降序)/ B 未读+没问过 / C 读过+没问过沉底,卡点=读过+问过落 Tier A 冒顶;tiebreak 保 route_from score 序(稳定排序)。其余 4 组保持仅已读降权 `[ADR-0041 决策5/6/7]`。
- `crates/runtime/src/lib.rs:guided_route_from` — 传 `&profile.puzzle_heat` 给 reorder(消费 ② 读者私人 puzzle_heat;focus 留后续)。`book.guided_route_from` 命令面签名不变 ⇒ orchestrator dispatch / server route_book 零改动。
- `crates/memory/src/lib.rs:MemoryStore::qa_questions`(新私有)— 某书某 LID 的 qa 提问文本(generated_at 序),供透明展示读"问了什么"(区别 `qa_heat` 只数条数)。
- `crates/memory/src/lib.rs:render_reader_profile_md` 卡点段 — 升级为 `- {lid} (×{count})` + 嵌套真实问题文本(承 ADR-0040 透明 .md)。
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 加 LID-local recall 引导:带读/答某 LID 时先 `memory.recall(lid, type=qa)` 看读者历史提问,贴合其关心点(卡过的多讲)。

**入口**:住户带读 LLM 经 `book.guided_route_from` 拿 **back 组卡点升权**后的整形前沿(卡过的前置冒顶);经 `memory.recall(lid,type=qa)` 拿 LID 历史提问;用户 grep `reader-profile.md` 看卡点段真实问题。
**测试**:`cargo test --workspace` 全绿(runtime 45 +1 `..._back_promotes_qa_heat_over_read`:Tier A heat 降序/卡点压已读冒顶/未读居中/读过没问沉底/forward 不受 heat 影响;memory 14 +1 `reader_profile_md_renders_qa_questions`:lid×count + 嵌套问题文本;3 现有 reorder 测试随签名加 `&BTreeMap::new()`,断言不变=向后兼容)。
**B2 边界**:back 升权排序 + 渲染**确定性可单测**(判据达成);**agent「该不该 recall qa」「贴合卡点怎么讲」的 judgment 靠真 LLM 手动验**(守 B2)。
**P4-5 收口**:qa-1 生产 + qa-2 消费 两子刀全完成。qa = LID 价值/提问热度信号落地,回收 P4-2 `puzzle_lids` 恒空缺口;三消费方(确定性升权/LID-local recall/透明渲染)全锚 anchor.lid。
**未闭(ADR-0041 何时回头)**:count 噪声(3vs2)实测;re-ask 的 usage.count 要不要加权进 heat;concretize/cross 升权(back 不够时);跨读者"书内在价值"(多读者场景);reader-profile.md 体积裁剪(承 ADR-0040,不复活计数器)。

## 2026-06-29 · PB5-1 bookId 通用化(去硬编码 + 文件名 slug 派生)`[ADR-0042]`

**触达**:
- `packages/core/src/book-id.ts:deriveBookId` — 新增纯函数(零文件 IO/LLM):basename 去最后扩展名 → `slugify`(小写、空格/下划线/点→连字符、剥非 `[a-z0-9-]`、折叠 trim);`--book-id` override slug 化后采用;**非 ASCII 主导(nonAscii > asciiAlnum)或 ASCII 字母数字=0 或 slug 为空 → 抛错要 `--book-id`**(命门:不静默建 CJK/空目录坏工具链,承 [[windows-cjk-path-tooling]] + PB0 fail-fast)。
- `skills/build/pass1-batch.ts:48` — 去硬编码 `bookId="game-programming-patterns"` → `deriveBookId(book)`;`game-programming-patterns.md` 派生回同值,产物目录 `.understand-book/<bookId>/` 不变。

**入口**:`tsx skills/build/pass1-batch.ts <book> ...`(bookId 现从书路径派生);`deriveBookId` 三处共用(PB5-2 `build-status` / PB5-3 `emit-input` 后续接入)。
**测试**:`packages/core/test/book-id.test.ts`(6 例)— slug/path 剥离·ASCII-safe 规范化·override·非 ASCII 主导报错·空报错·ASCII 主导含少量非 ASCII 取 ASCII。`npx vitest run` 全量 82 测试绿(既有 81 不破)。
**偏离记录**:ADR-0042/切片方案草图写 `skills/build/`,实际落 `packages/core/src/`——纯确定性函数属 core 且测试基建(`pnpm -r test`/vitest)只覆盖 `packages/*`,放 `skills/build/` 无 runner 覆盖违 A2 测试契约。
**未闭(PB5-2/3)**:`--book-id` override 的 CLI 入口尚未接进 `pass1-batch`(本刀只 dehardcode,override 形参就位待 PB5-3 CLI 改造消费);`build-status`/`emit-input` 待建。

## 2026-06-29 · PB5-2 跨会话续建视图 build-status(存在性+content-hash 校验,无状态位)`[ADR-0042]`

**触达**:
- `packages/core/src/build-resume.ts:pass1ContentHash` — `sha256(buildPass1Input(window).text)` hex;命门=算自与抽取**同一份**正文,新鲜度锚。
- `packages/core/src/build-resume.ts:computeBuildStatus` — 纯函数(零文件 IO):逐窗重算 expected hash,对比磁盘侧 `existing: Map<id,{content_hash}>` → `{done, pending}`;在且一致=done,缺失/失配=pending(**位置 id 键、二值、无状态位/watermark/lock**)。
- `skills/build/build-status.ts` — 薄 CLI:载书(epub/md)→ `deriveBookId(book, --book-id?)` → 逐窗读 `.understand-book/<bookId>/.build/pass1/<id>.json` 取 content_hash → `computeBuildStatus` → 打印 done/pending ids。新会话冷启动第一步。

**入口**:`tsx skills/build/build-status.ts <book> [--book-id <id>]`(SKILL.md 续建 loop 第 1 步,PB5-3 写进契约)。
**测试**:`packages/core/test/build-resume.test.ts`(6 例)— 全缺→全 pending / 全抽一致→全 done / 删窗 json→该窗 pending / content_hash 失配→pending 不静默复用 / hash 确定性且对正文敏感。`npx vitest run` 全量 88 测试绿(既有 82 不破)。CLI 端到端烟测:2 窗书全 pending → 写窗 0 正确 hash → done=1/pending=1。
**B2 边界**:done/pending 判定**确定性可单测**(判据达成),零 LLM。
**未闭(PB5-3)**:`emit-input`(现算单窗输入喂 subagent,不落盘)+ `pass1-batch` 改造消费 `.build/pass1/*.json` + 逐窗原子写 + pending 拒绝收口(`--allow-partial`)+ SKILL.md 冷启动续建 loop 落地。

## 2026-06-29 · PB5-3 续建 loop 落地(emit-input + pass1-write 原子写 + pass1-batch 消费 + SKILL.md 契约)`[ADR-0042]`

**触达**:
- `packages/core/src/build-resume.ts:buildPass1Artifact` — 纯函数(PB5-3a):content_hash 从窗口正文 TS 重算 + 挂 subagent {nodes,edges} → `Pass1Artifact`。命门:hash 永远 TS 算,agent 不手算。
- `skills/build/load-book.ts:loadBookWindows/windowById` — 共享载书→重算 LID 树/窗口(确定性派生),续建各 CLI 复用。
- `skills/build/emit-input.ts` — (PB5-3b)现算窗口 id 的 `[LID]` 前缀正文打印 stdout 喂 subagent,不落盘;诊断走 stderr。
- `skills/build/pass1-write.ts` — (PB5-3b)`buildPass1Artifact` 重算 hash + 读 subagent {nodes,edges} + **临时文件+rename 原子写** `.build/pass1/<id>.json`。逐窗原子写=续建命根。
- `skills/build/pass1-batch.ts` — (PB5-3c)去 outputs.json/idxList 位置参数 → 消费 `.build/pass1/*.json` + `computeBuildStatus` 续建判定 + **pending 默认拒绝收口**(`--allow-partial` 兜底)+ flag 化 sidecar 路径;锚定率分母=done 窗口叶子。
- `skills/build/SKILL.md` — (PB5-3d)「跨会话续建」段命令名对齐实现(build-status/emit-input/pass1-write/pass1-batch)+ 命门 + 铁律;顶部状态注更新(PB5 已实现,余=真书真 LLM 端到端)。

**入口**:冷启动续建 loop(SKILL.md):`build-status` → 逐窗 `emit-input`+subagent 抽取+`pass1-write` → 全 done `pass1-batch` 收口。新 Claude 会话零上下文纯靠 `.build/` 接手。
**测试**:`packages/core/test/build-resume.test.ts`(7 例,+buildPass1Artifact);`npx vitest run` 全量 89 测试绿。CLI 端到端烟测:emit→write→status 翻 done;pass1-batch 三场景(pending 拒绝收口 exit1 / `--allow-partial` 部分收口 / 全 done 收口产 base.json);hash 跨管线(write↔status↔batch)确定性一致;非法 id 报错。
**B2 边界**:续建判定/原子写/拒绝收口**确定性可单测+烟测**(判据达成),零 LLM;subagent 抽取在环不变。
**未闭(ADR-0042 何时回头)**:内容寻址续建(书改了复用未变窗口)/ 跨版本增量([ADR-0019])/ Pass2·discourse 纳入同款续建 / 真书数十窗实测(build-status 重算开销、单会话能抽几窗、id 漂移 orphan)。

## 2026-06-29 · PB4 read-tools CLI context granularity bug 修复 `[ADR-0013/0014]`

> 真书读时烟测暴露:`context` CLI usage 宣传 `[near|mid|far] [k]`,但实现把 args[4] 当 k 解析、永远调 `context_near`,granularity 形同虚设。库侧 `Book::context` 早已实现且单测覆盖,纯属 CLI 接线漏接。

**触达**:
- `crates/read-tools/src/main.rs` `context` 分支 — args[4]=granularity(`Option<&str>`)、args[5]=k;`book.context_near(lid,k)` → `book.context(lid, granularity, k)`。granularity 缺省时库侧默认 near(向后兼容 `context <lid>`)。

**入口**:`read-tools <book_dir> context <lid> [near|mid|far] [k]`(headless 确定性叶子工具,读时驱动)。
**测试**:`cargo test -p read-tools` 35 绿(库侧 `context_far_accumulates...`/`context_rejects_unknown_granularity` 已覆盖语义)。CLI 解析内联于 `main()` 不单测;靠确定性烟测验(B2):`context 1.18.15.7 far all` → 9 条 `scope:long_range`/`layer:far`;判别性确认 `near` 同 LID → 0 条 long_range,证 granularity 真生效。
## 2026-06-30 · PB6 profile-sidecar 独立抽取趟方案落档

**触达**:
- `docs/切片方案-profile深路径.md:PB6` - 新增独立 profile-sidecar 抽取趟:复用 Pass1 window/input/hash,但单独 status/write/batch;一趟 subagent 同时产 discourse_items 与 formula_semantics 候选。

**入口**:后续实现从 `agents/profile-sidecar-extractor.md` 与 `skills/build/profile-sidecar-{input,status,write,batch}.ts` 开始;不要改 `pass1-batch` 职责边界。
**测试**:`rg "PB6|profile-sidecar|formula_lids|profile-sidecar-batch" docs/切片方案-profile深路径.md docs/代码链路.md`。
## 2026-06-30 · PB6 profile-sidecar 独立抽取趟骨架落地

**触达**:
- `packages/core/src/profile-sidecar-build.ts` - 新增独立 profile-sidecar artifact helper:复用 Pass1 window/input/hash,派生 `formula_lids`,计算 sidecar done/pending。
- `packages/core/test/profile-sidecar-build.test.ts` - 覆盖 `formula_lids` 确定性派生、artifact hash 口径、done/pending/hash 失配。
- `skills/build/profile-sidecar-input.ts` - 输出 `visible_lids`/`formula_lids` + `[LID]` 正文,喂合并 sidecar subagent。
- `skills/build/profile-sidecar-status.ts` - 扫 `.build/profile-sidecar/<id>.json` 做存在性 + content_hash 续建视图。
- `skills/build/profile-sidecar-write.ts` - 读取规范化 subagent JSON,容忍 UTF-8 BOM,原子写 `.build/profile-sidecar/<id>.json`。
- `skills/build/profile-sidecar-batch.ts` - 独立收口 sidecar 趟,默认拒绝 pending,只写 `discourse_index.json` / `formula_semantics.json`。
- `agents/profile-sidecar-extractor.md` - 新增合并抽取 prompt:Step A/B discourse,Step C formula,且公式 LID 只信确定性注入的 `formula_lids`。
- `skills/build/SKILL.md` - 新增 PB6 独立 sidecar 续建 loop,明确不改 Pass1 收口边界。

**入口**:`profile-sidecar-status` -> `profile-sidecar-input` + `profile-sidecar-extractor` + `profile-sidecar-write` -> `profile-sidecar-batch`。
**测试**:`pnpm -C packages/core test` 92 绿;`pnpm -C packages/core typecheck`;CLI smoke:input/status/write/batch on `packages/core/test/fixtures/sample.md`。
## 2026-06-30 · Pass2 消费 PB6 sidecar 前置关系补档

**触达**:
- `docs/PB3-pass2-prompt-grill.md` - 明确 Pass2 必须在 PB6 `profile-sidecar-batch` 收口后运行,从正式 `discourse_index.json` / `formula_semantics.json` 投影 `source_discourse` 与 `source_formula_semantics`。
- `docs/切片方案-profile深路径.md:PB6/P1` - 补充 PB6 是 Pass2 的 sidecar 前置输入,Pass2 不再从 Pass1 或临时 candidate JSON 读取 discourse/formula。

**入口**:后续实现 Pass2 编排时,先检查 PB6 sidecar 文件是否存在/新鲜,再装配 `Pass2WorkPacket`。
**测试**:`rg "PB6 profile-sidecar-batch|source_discourse|source_formula_semantics|Pass2 must run after" docs/PB3-pass2-prompt-grill.md docs/切片方案-profile深路径.md docs/代码链路.md`。

## 2026-06-30 · PB6 true-book profile-sidecar 逐窗口抽取收口

**触达**:
- `.understand-book/quantification-essence/.build/profile-sidecar/*.json` - 真书 22 个窗口逐窗原子落盘,`profile-sidecar-status` 判定 done=22/pending=0。
- `.understand-book/quantification-essence/discourse_index.json` - `profile-sidecar-batch` 写出 1139 条 discourse items,确定性 gate dropped=25。
- `.understand-book/quantification-essence/formula_semantics.json` - `profile-sidecar-batch` 写出 22 条 formula semantics,pending=5。

**入口**:`profile-sidecar-status` -> 逐窗 `profile-sidecar-input` + `profile-sidecar-extractor` + `profile-sidecar-write` -> `profile-sidecar-batch --book-id quantification-essence`。
**测试**:`profile-sidecar-status` 22/22 done;`pnpm -C packages/core test` 92 绿;`pnpm -C packages/core typecheck`;`cargo test -p read-tools` 35 绿。

## 2026-06-30 · Pass2 编排补齐(正式 PB6 sidecar -> work packet -> gate writeback)

**触达**:
- `packages/core/src/pass2-orchestrate.ts` - 新增 Pass2 编排纯函数:正式 sidecar 参与候选生成,构建 source-window `Pass2WorkPacket`,packet hash,artifact/status。
- `packages/core/test/pass2-orchestrate.test.ts` - 覆盖 PB6 discourse/formula 投影进 packet、零候选窗口 skipped、hash stale 判定。
- `skills/build/pass2-common.ts` - 载入真书 build 上下文:`base.json` + `discourse_index.json` + `formula_semantics.json` + 重算窗口。
- `skills/build/pass2-status.ts` / `pass2-input.ts` / `pass2-write.ts` / `pass2-batch.ts` - 新增 Pass2 续建 loop;batch 写 `long_range_candidates.json`,替换 `base.json` long_range 边并写 `pass2_audit.json`。
- `skills/build/SKILL.md` - 追加 Pass2 长程边编排操作手册。

**入口**:`pass2-status` -> 逐 pending 窗口 `pass2-input` + `pass2-longrange-linker` + `pass2-write` -> `pass2-batch`。
**测试**:`pnpm -C packages/core test` 94 绿;`pnpm -C packages/core typecheck`;真书 smoke `pass2-status` 得 candidates=153 / pending=7 / skipped=15;`pass2-input 0` 含 candidates=37,source_nodes=39,discourse=74,formula=1;`pass2-batch` 在 pending 未清时拒绝收口。

## 2026-06-30 · Pass2 true-book candidate classification + writeback 收口

**触达**:
- `.understand-book/quantification-essence/.build/pass2/*.json` - 7 个有候选窗口完成 Pass2 分类落盘,`pass2-status` 判定 done=7/pending=0/skipped=15。
- `.understand-book/quantification-essence/long_range_candidates.json` - `pass2-batch` 写出 153 个确定性候选。
- `.understand-book/quantification-essence/base.json` - `pass2-batch` 替换 long_range 边,写入 13 条 gated long_range edges。
- `.understand-book/quantification-essence/pass2_audit.json` - 记录 accepted=13,pending=0,rejected=140,gate_dropped=0。

**入口**:`pass2-input` -> 保守分类 `pass2-longrange-linker` 输出 -> `pass2-write` -> `pass2-batch --book-id quantification-essence`。
**测试**:`pass2-status` done=7/pending=0/skipped=15;`pass2-batch` long_range_edges=13;`cargo test -p read-tools` 35 绿;`pnpm -C packages/core test` 94 绿;`pnpm -C packages/core typecheck`;`read-tools context 1.18.15.7 far all` 可见 `scope=long_range` 边。

## 2026-06-30 · S10j asset 原文渲染 + formula 语义剖面入口 `[Vue/TS + Rust]`

**触达**:
- `crates/server/src/lib.rs:route_book` — 新增 `GET /book/formula_semantics?lid=` 只读投影,返回 `Book::formula_semantics(lid)`,缺失返回 `FORMULA_SEMANTICS_NOT_FOUND`。
- `packages/web/src/api.ts` — 新增 `FormulaSemantics` 客户端类型与 `api.formulaSemantics(lid)`。
- `packages/web/src/App.vue` — `loadWindow` 合并 `ManifestNode.kind`,按 `code/table/image/formula` 分派渲染;公式叶子拉取并展示参数、组合含义、上下文关系;asset 保留 `data-lid` 可选中/高亮/回跳。
- `packages/web/src/style.css` — 新增 asset/code/table/image/formula 阅读流样式,长原文横向/换行约束不撑破布局。

**入口**:前端启动后 `book.manifest` 提供 kind,阅读流逐 `visible_lids` 调 `book.text`;公式 LID 额外调 `/book/formula_semantics?lid=` 展示剖面。
**测试**:`cargo test -p server` 28+4 绿;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-06-30 · S11 Mintlify docs 工作台化前端优化方案落档

**触达**:
- `docs/切片方案-切片1前端阅读器.md:§7` — 新增 S11 优化方向:Mintlify docs 工作台化、3 栏信息架构、视觉基线、LID 隐形/debug 折叠、左右 rail 数据源和 S11a-S11e 小切片顺序。

**入口**:后续从 S11a 开始,先做 layout shell + visual baseline,再做左 rail、右 context tabs、selection actions、responsive polish。
**测试**:`rg "S11|Mintlify docs 工作台化|layout shell \+ visual baseline" docs/切片方案-切片1前端阅读器.md docs/代码链路.md`。

## 2026-06-30 · S11b left rail navigation + debug foldout `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:buildOutline` — 从 `book.manifest()` 派生 chapter/section outline,后台用 `book.text(lid)` 建标题缓存。
- `packages/web/src/App.vue:doGoto` — 左 rail outline 点击复用既有 `reader.goto(lid)` 路径刷新 viewport。
- `packages/web/src/components/LeftRail.vue` — 渲染可搜索 outline、当前位置 active 高亮,普通模式不显示 raw LID/goto。
- `packages/web/src/style.css` — 增加 outline search/list/item 样式,保留 debug foldout 的 raw coordinate 工具。

**入口**:前端初始化 `api.manifest()` 后生成 outline;用户点击左侧 outline item -> `reader.goto(lid)`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。

## 2026-06-30 · S11c right context tabs `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:selectedFormula` — 从当前选中 reader segment 投影公式剖面,传给右 rail Formula tab。
- `packages/web/src/App.vue:latestTrace` — 从最近一次 agent outcome 投影 trace,传给右 rail Trace tab。
- `packages/web/src/App.vue:contextRecords` — 从当前 selected LID 与 viewport visible LIDs 派生附近 notes/highlights。
- `packages/web/src/components/RightRail.vue` — 启用 Agent/Trace/Formula/Notes tabs;Agent tab 保留既有 chat/proposal flow,其余 tabs 渲染对应上下文。

**入口**:右侧 rail tab 切换;Agent tab 继续走 `/agent/chat`,Trace/Formula/Notes 消费 App 已有状态。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;HTTP smoke `/`, `/api/book/manifest`, `/api/reader/state`。

## 2026-06-30 · S11d selection/actions `[Vue/TS]`

**触达**:
- `packages/web/src/components/TopBar.vue` — 移除旧的全局 Highlight/Note 操作,topbar 只保留翻页、新对话、debug。
- `packages/web/src/components/ReaderPane.vue` — 为 selected paragraph/asset 增加 inline block actions: Highlight block / Note。
- `packages/web/src/App.vue:highlightBlock` — block action 复用既有 `reader.highlight(lid)` 整段高亮路径。
- `packages/web/src/App.vue:noteSelection` — 段内选区 floating toolbar 增加 Note,用选中文本预填 anchored note。
- `packages/web/src/style.css` / `packages/web/src/App.vue` scoped style — 增加 block action pills 与双按钮 selection toolbar 样式。

**入口**:点击 reader 段落/asset 选中后出现 inline actions;鼠标选中单个 LID 内文本后出现 floating toolbar。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;HTTP smoke `/`, `/api/reader/state`。

## 2026-06-30 · S11e responsive polish `[Vue/TS]`

**触达**:
- `packages/web/src/style.css` — 增加 1023px 断点:取消三栏挤压,改为 left+reader 两栏、right rail 下置。
- `packages/web/src/style.css` — 增加 767px 断点:单列堆叠、topbar 换行、outline 限高、触控目标放大。
- `packages/web/src/style.css` — 移动端 reader prose/asset/code/notes 调整宽度、间距与换行,避免横向溢出。

**入口**:浏览器视口宽度 <=1023px / <=767px 时触发响应式布局。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`;HTTP smoke `/`, `/api/reader/state`。
## 2026-06-30 · S11f Markdown 标题井号显示修复 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:renderSeg` — chapter/section 显示层剥离 Markdown 标题前缀,保留原文与 LID/range 锚点不变。
- `packages/web/src/App.vue:firstTitleLine` — outline/title cache 使用清洗后的标题文本,避免 `#`/`##` 泄漏到大纲。
- `packages/web/src/components/ReaderPane.vue` / `packages/web/src/style.css` — 为 chapter/section 段落加标题样式类。

**入口**:阅读流渲染与左侧 outline 标题缓存。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-06-30 · S11g 当前会话切换书入口 `[Rust + Vue/TS]`

**触达**:
- `crates/server/src/lib.rs:route_open_book` — 新增 `POST /book/open {dir}` 重新加载 Book、重建 Reader、重置 agent messages。
- `packages/web/src/api.ts:api.openBook` — 前端命令面客户端接入切书端点。
- `packages/web/src/components/TopBar.vue` / `packages/web/src/App.vue:openBook` — 顶栏 Open book 按钮输入本地 book dir,成功后清空当前阅读/对话状态并重载 manifest/state。

**入口**:TopBar `Open book` -> 输入 `.understand-book/<book_id>` 或 book 目录路径。
**测试**:`cargo test -p server`;`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11h 可折叠大纲与可调三栏宽度 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue` — 新增左侧 outline 开关、左右 rail 宽度状态与桌面端拖拽调整逻辑。
- `packages/web/src/components/TopBar.vue` — 新增 `Outline` 顶栏开关,用于隐藏/恢复左侧大纲。
- `packages/web/src/style.css` — workspace grid 改为 CSS 变量驱动栏宽,新增左右拖拽手柄,保留 tablet/mobile 响应式覆盖。

**入口**:TopBar `Outline` 按钮切换左 rail;桌面端拖拽左右分隔条调整左/中/右工作区宽度。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11i Agent 回答选区保存为笔记 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue` — 删除整段 agent answer 存笔记路径,新增 `questionAnchorLid` 冻结提问时阅读位置,`saveAgentSelection` 把用户选中的回答片段保存到该 LID。
- `packages/web/src/components/RightRail.vue` — 给 agent answer 增加选区捕获与浮动 `Note` 按钮,由用户自行选择要保存的句子或片段。

**入口**:右侧 Agent 回答文本中选中任意片段后点击浮动 `Note`,写入该问题发出时的 reader anchor 的 long_term note。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11j 正文选区 Ask AI 引用提问 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue` — 正文选区浮动工具条新增 `Ask AI`,保存 `{lid, quote}` 为 ask draft;发送 agent 消息时拼接引用原文与用户问题,用选区 LID 冻结 `questionAnchorLid`,并把 `questionQuote` 固化到该 chat turn。
- `packages/web/src/components/RightRail.vue` — Agent tab 收到 ask draft 时自动激活,输入区显示 `Quoted source` 引用块与独立问题输入框,支持清除引用;发送后引用块移动到用户问题上方作为对话记录。

**入口**:正文中选中文本 -> 浮动工具条 `Ask AI` -> 右侧 Agent composer 显示引用原文与问题输入 -> Send。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11k 正文行内 LaTeX 渲染 `[Vue/TS]`

**触达**:
- `packages/web/src/md.ts` — 新增 `renderInlineMarkdown`,复用现有 markdown-it + KaTeX 配置但不包外层 `<p>`。
- `packages/web/src/App.vue:renderSeg` — reader 段落/标题显示层改走 inline Markdown/LaTeX 渲染,段内高亮仍按原 UTF-16 range 分片后包 `<mark>`。

**入口**:ReaderPane 正文 `v-html=renderSeg(seg)`;普通段落中的 `$...$` 公式按 KaTeX 行内公式显示。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11l Agent 工具价值 prompt 补强 `[Rust]`

**触达**:
- `crates/runtime/src/orchestrator.rs:SYSTEM_PROMPT` — 新增工具价值判断规则:引用原文优先直接解释,限制 `book.concept` fan-out,按任务类型选择 `book.text/context/query/synthesize/route`。

**入口**:`/agent/chat` 外层 E agent loop;前端 Ask AI 引用提问拼入 `引用原文 [LID: ...]` 后由 prompt 约束工具选择。
**测试**:`cargo test -p runtime`。
## 2026-07-01 · S11m 跨 LID 自由选区 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:selectionRanges` — 选区不再限制单个 `data-lid`,扫描阅读区内所有相交 LID 并计算各自 UTF-16 range。
- `packages/web/src/App.vue:confirmHighlight/noteSelection/askSelection` — 高亮按 LID 拆成多次 `reader.highlight(lid, range)`,Note/Ask AI 用完整选区文本锚到起点 LID。

**入口**:ReaderPane 正文任意跨段选中文本 -> floating toolbar `Highlight` / `Note` / `Ask AI`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11n Note Markdown/折叠/来源路径 `[Vue/TS]`

**触达**:
- `packages/web/src/components/ReaderPane.vue` — note 卡片改为可折叠 `details`,长 note 默认折叠,summary 显示来源 quote 或锚点 LID。
- `packages/web/src/components/RightRail.vue` — Notes tab 同步显示 note 来源路径并折叠长 note。
- `packages/web/src/App.vue:saveAgentSelection` — 从 Ask AI 回答选区保存 note 时,把原引用文本写成 Markdown blockquote 作为来源。
- `packages/web/src/style.css` — 补齐 note/Markdown 的 blockquote、table 与折叠 summary 样式。

**入口**:ReaderPane note 卡片、RightRail Notes tab、Agent answer selection `Note`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11o Agent 答案选区公式保真 `[Vue/TS]`

**触达**:
- `packages/web/src/components/RightRail.vue:markdownTextFromRange` — 保存 Agent 回答选区为 note 前,把选区内 KaTeX DOM 的 `annotation[encoding="application/x-tex"]` 还原为 `$...$`/`$$...$$`,避免生成 `Ft\mathcal{F}_tFt` 这类混合文本。

**入口**:RightRail Agent answer selection `Note`。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11p Note 来源定位按钮与折叠预览 `[Vue/TS]`

**触达**:
- `packages/web/src/components/ReaderPane.vue` — note summary 来源改为按钮,点击通过 `goto` 事件定位来源 LID;长 note 折叠时显示三行 preview。
- `packages/web/src/components/RightRail.vue` — Notes tab 来源按钮同样触发 `goto`,长 note 折叠时保留 preview;继续保留 KaTeX 选区还原。
- `packages/web/src/App.vue` — 接入 ReaderPane/RightRail 的 `goto` 事件到既有 `doGoto`。
- `packages/web/src/style.css` — 增加 note source button 与 folded preview 样式。

**入口**:ReaderPane note source button / RightRail Notes tab source button。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。
## 2026-07-01 · S11q Note 来源跳转临时高亮 `[Vue/TS]`

**触达**:
- `packages/web/src/App.vue:focusSource/doGoto/renderSeg` — 来源跳转携带 quote，跳到 LID 后用 `sourceFocus` 临时高亮 quote 或整段。
- `packages/web/src/components/ReaderPane.vue` — reader 内 note source button 发出 `{lid, quote}`。
- `packages/web/src/components/RightRail.vue` — Notes tab source button 发出 `{lid, quote}`。
- `packages/web/src/style.css:.source-focus-mark` — 来源临时高亮使用 mint 轻量样式。

**入口**:ReaderPane note source button / RightRail Notes tab source button。
**测试**:`pnpm -C packages/web typecheck`;`pnpm -C packages/web build`。



## 2026-07-01 S12 continuous reader model decision docs

**Touched**:

- `docs/adr/0043-reader连续滚动视口-后端区间窗口-前端虚拟流-note-overlay.md` - records the accepted continuous scrolling reader architecture.
- `docs/切片方案-S12连续滚动阅读模型.md` - decomposes implementation into S12a-S12e verifiable slices.

**Entry point**: future implementation starts at S12a reader viewport interval semantics.
**Test**: `rg "ADR-0043|S12a reader viewport interval semantics|连续滚动阅读模型" docs/adr docs/切片方案-S12连续滚动阅读模型.md docs/code-trail-S12-continuous-reader.md`.

## 2026-07-01 S12a reader viewport interval semantics `[Rust]`

**Touched**:

- `CONTEXT.md:interval viewport` - adds the S12 glossary terms for interval viewport, virtual reader buffer, and note overlay.
- `crates/reader/src/lib.rs:Viewport` - adds `top_lid`, `bottom_lid`, and `width` while retaining derived `anchor_lid`.
- `crates/reader/src/lib.rs:Reader` - replaces center-window state with `top_idx + width`; `scroll(delta)` moves the top interval, `goto_lid` lands the target leaf at top, and read ledger marks every visible LID.

**Entry point**: `Reader::new` now receives viewport width; HTTP `/reader/state`, `/reader/goto`, and `/reader/scroll` return the expanded viewport through existing server passthrough.
**Test**: `cargo test -p reader`; `cargo test -p server`.

## 2026-07-01 S12b viewport DTO and progress wiring `[Vue/TS]`

**Touched**:

- `packages/web/src/api.ts:Viewport` - mirrors the expanded reader viewport DTO with `top_lid`, `bottom_lid`, and `width`.
- `packages/web/src/App.vue:progressPct` - computes reading progress from `top_lid` instead of derived `anchor_lid`.
- `packages/web/src/App.vue:loadWindow` - selects and loads chapter context from `top_lid`; no-selection agent questions use `top_lid` as their default anchor.

**Entry point**: frontend initialization and reader command responses flow through `loadWindow(st.viewport)` / `loadWindow(effect.viewport)`.
**Test**: `pnpm -C packages/web typecheck`; `pnpm -C packages/web build`.

## 2026-07-01 S12c virtual reader buffer `[Vue/TS]`

**Touched**:

- `packages/web/src/components/ReaderPane.vue:onScroll` - emits `scroll-edge` when the native reader pane approaches the top or bottom of its scroll range.
- `packages/web/src/components/ReaderPane.vue:scrollRestoreId` - recenters the native scroll position after App swaps in the next interval buffer.
- `packages/web/src/App.vue:onScrollEdge` - locks concurrent edge loads, calls `reader.scroll(+/- width/2)`, reloads the interval viewport, and triggers scroll restoration.

**Entry point**: native scroll in `.reader-pane` drives `scroll-edge` -> `App.onScrollEdge` -> `/reader/scroll`.
**Test**: `pnpm -C packages/web typecheck`; `pnpm -C packages/web build`.
## 2026-07-01 S12d note overlay split `[Vue/TS]`

**Touched**:

- `packages/web/src/App.vue:visibleNotes` - derives notes for the current `visible_lids` interval only.
- `packages/web/src/components/ReaderPane.vue:note-overlay` - moves note cards out of the per-segment loop into an independent visible-notes overlay/list.
- `packages/web/src/style.css:note-overlay` - styles the overlay as a bounded independent layer so notes do not alter segment heights.

**Entry point**: ReaderPane receives `visibleNotes` from App and renders notes independently from the prose `seg` loop; highlights remain inline/segment-local.
**Test**: `pnpm -C packages/web typecheck`; `pnpm -C packages/web build`.
## 2026-07-01 S12e remove page buttons and keyboard fallback `[Vue/TS]`

**Touched**:

- `packages/web/src/components/TopBar.vue` - removes the old explicit up/down page buttons and `scroll` event from the top bar.
- `packages/web/src/App.vue` - removes the old topbar `doScroll` wiring; continuous scroll now enters through ReaderPane native scroll or keyboard.
- `packages/web/src/components/ReaderPane.vue:onKeydown` - adds ArrowUp/ArrowDown/PageUp/PageDown scrolling when the reader pane has focus, without stealing editable-field keys.
- `packages/web/src/style.css:reader-pane` - adds focus styling and removes unused `.icon-pill` button sizing.

**Entry point**: focus the reader pane and use Arrow/Page keys, or scroll natively; TopBar no longer exposes page controls.
**Test**: `pnpm -C packages/web typecheck`; `pnpm -C packages/web build`.
## 2026-07-02 S13a note 卡回正文 seg 循环 [Vue/TS]

**触达**:
- packages/web/src/components/ReaderPane.vue — note 卡从 <aside class="note-overlay"> 移回 seg 循环,hl-card 之后按 
otesOf(seg.lid) 渲染;新增 
otesByLid computed + 
otesOf 函数;删除 <aside> overlay 块。
- packages/web/src/style.css — 删除 .note-overlay 和 .note-overlay .note-card 死样式。
- docs/adr/0043-reader连续滚动视口-后端区间窗口-前端虚拟流-note-overlay.md — 决策7 修订:note 卡从独立 overlay 回退为段内 seg 循环渲染。

**入口**:ReaderPane seg 循环每段 hl-card 之后渲染该 LID 的 notes;note 内容仍来自 App isibleNotes prop。
**测试**:pnpm -C packages/web typecheck;pnpm -C packages/web build。
## 2026-07-02 S13b note source 按钮只高亮不导航 [Vue/TS]

**触达**:
- packages/web/src/components/ReaderPane.vue — note source 按钮事件从 ocus-source 改为 ocus-source-local,不再触发 goto 导航。
- packages/web/src/App.vue — 新增 ocusLocalSource,只设 sourceFocus 高亮对应段,不调 pi.goto、不移动视口;RightRail 的 ocus-source 仍走 ocusSource/doGoto(那边 note 可能不在视口,需导航)。

**入口**:ReaderPane 内 note source button -> ocus-source-local -> App ocusLocalSource -> 设 sourceFocus。
**测试**:pnpm -C packages/web typecheck;pnpm -C packages/web build。
## 2026-07-02 S13b-fix note source 高亮全段不生效 [Vue/TS]

**触达**:
- packages/web/src/App.vue:focusLocalSource — quote 为 null(noting quote)时不再设 null 而是创 { lid, quote: null },使 sourceFocusRange 返回 [0, text.length] 高亮整段;有 quote 时精确匹配。

**入口**:ReaderPane note source button。
**测试**:pnpm -C packages/web typecheck;pnpm -C packages/web build。
## 2026-07-02 S13c hl-card 恢复可见 + sourceFocus 可清除 [Vue/TS]

**触达**:
- packages/web/src/style.css — .hl-actions 从合并规则(与 .note-card[open] .note-preview 共用 display:none)拆出,恢复 display:flex;.hl-ex 去掉 white-space:nowrap 改为 2 行 clamp,高亮文本正常换行显示。
- packages/web/src/App.vue — 新增 onSelectSeg,点击段时清 sourceFocus;onProseMouseUp collapsed 分支清 sourceFocus;@select 绑定从内联改为 onSelectSeg。

**入口**:ReaderPane 段点击 / 空白处 mouseup / 滚动 → sourceFocus 清除。
**测试**:pnpm -C packages/web typecheck;pnpm -C packages/web build。
## 2026-07-02 S13d 阅读位置/上次打开的书持久化(session.json 往返) [Rust]

**触达**:
- crates/server/src/main.rs:main — 启动时调 load_session(&session_path);argv 与 session book_dir 不同且可加载则用 session 目录;restore_top_lid 恢复 top;构造 AppState 后调 save_session(&guard,Some(dir)) 写初始 session.json(否则后续 save_session(None) 读不到旧 book_dir 永不写)。
- crates/server/src/main.rs:use — 加载 load_session,save_session;session_path = memory_dir/session.json。
- crates/server/src/lib.rs:AppState — 加 session_path:Option<PathBuf> 字段。
- crates/server/src/lib.rs:save_session(state,dir) — 用 serde+io 写 session.json{book_dir,top_lid};dir=Some 覆盖,None 沿用旧 book_dir(读现有文件)。
- crates/server/src/lib.rs:load_session — 静默读 session.json,缺/坏返 None。
- crates/server/src/lib.rs:route_open_book / route_mut(goto,scroll) — 成功后 save_session(state,Some(dir)或None)。
- crates/server/src/lib.rs:SessionState{pub book_dir, pub top_lid}。
- crates/reader/src/lib.rs:restore_top_lid(&mut self,_book,lid) — 按 leaf_lids 定位设 top_idx,不存在静默忽略(无写入)。

**入口**:server <book_dir> 启动 -> load_session 选目录 -> restore_top_lid -> save_session(Some) 写初始;后续 goto/scroll 持续更新;top_lid 写 ~/.understand-book/memory/session.json。
**测试**:cargo test -p server --lib(29 绿)。
**附带修复**:lib.rs 第20/32行 `n` PowerShell 转义损坏、.and_then 类型不匹配、main.rs Arc::new(Mutex::new(AppState { 构造头误删、load_session/save_session 可见性补全。
