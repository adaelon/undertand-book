# 切片方案 · 切片1 第一刀:前端阅读器 + localhost 服务连通

> **定位**:切片0 收尾(契约冻结基线 v1,V3 §6.1)后第一刀。把「阅读器前端页面 + 与后端连上」展开成 A1 切片声明 + A4 子切片。
> **配套**:架构决策见 `docs/adr/0028`(前端切片架构)+ `docs/adr/0030`(E agent 阅读器形态)+ `docs/adr/0029`(asset 一等对象,供 S10j 消费);被消费的冻结契约见 `需求文档-V3.md §4`;术语见 `CONTEXT.md`(命令面 REST 投影 / 连续正文渲染·LID 隐形 / 读位感 / agent 可撤销提议 / 读时会话边界 / asset 叶子)。
> **状态**:**已规划落档,未开工**(用户指定不在本对话做)。下次开工从 S10a 起。

---

## 0. §0.5 锁定决策摘要(全文见 ADR-0028 + ADR-0030)

1. **前端栈** = Vue/Vite SPA + localhost 服务(解 ADR-0021 PENDING);Vue app 落 `packages/web`。
2. **服务** = 新 `crates/server`,**tiny_http 同步**、线程每连接,`Mutex` 包单 `Book`+`MemoryStore`+`Reader` 会话(切片0 单用户单书);DAG 顶端依赖 read-tools/memory/reader/runtime/base-schema。同步:直调同步 `runtime::query`,不上 tokio(承 ADR-0024)。
3. **HTTP** = 命令面 1:1 REST 投影(`book.*`→GET、`reader.*`/`memory.*`→POST,端点名=命令名,错误透传 §4.4 信封)。
4. **不引 EPUB 阅读器框架**;薄 Vue 层把视口 `visible_lids` 渲染为**连续正文**,LID 是隐形接缝、被用时显形。
5. **无一等页码**;位置参照 = 视口当前 / 章节名(容器 LID)/ 概念内容;orchestrator 读模式每轮喂 viewport;UI 显**章节定位 + 进度%**。
6. **类型契约** = ts-rs 从 Rust 权威导出 API DTO 到 `packages/web`(承 ADR-0021,冻结契约的 TS 面自动生成防漂)。
7. **dev/打包** = Vite dev proxy(HMR)/ 打包 tiny_http 同端口服 `dist/` + API 单进程同源无 CORS(承 ADR-0022)。
8. **agent 形态**(ADR-0030,后补)= 前端对话主入口接**外层 E agent**(`/agent/chat`→`orchestrator::run`,reader 双向共享);agent 动作=**可撤销提议**(前端交互层,`OuterOutcome` 加 `effects[]`/`trace[]`,提议单元=一次对话回合);agent 标注落 `session` 层、用户确认升 `long_term`;五动作(goto 可返回 / highlight·note 询问保留 / 回答分屏+凝练成笔记或丢弃 / 查询踪迹可见);会话边界 = **用户手动「新对话」**(简化,不自动 idle 判定),新会话精炼上下文(`memory.recall` 兜底,完整轨迹摘要留 consolidation 刀)。

---

## 1. A1 切片总声明

- **做**:新建 localhost 服务(`crates/server`,tiny_http 同步)把冻结命令面投影成 REST + Vue/Vite SPA(`packages/web`)渲染 LID 窗口连续正文 + 闭环四动作(gotoLid/scroll/highlight/note)+ memory 标注回显,经 localhost 连通;读位感(章节+进度%);**前端对话主入口接外层 E agent(`/agent/chat`),agent 动作=可撤销提议 + 查询踪迹可见 + 回答分屏(详 ADR-0030)**。
- **不做**(明确排除):
  - ❌ 把 E agent loop 改成走 HTTP(agent 仍库内调用,HTTP 是同一命令面给「人」的另一投影)
  - ❌ 引 EPUB 阅读器框架(epub.js/foliate/readium)
  - ❌ 面板系统 / `reader.*` 全集(只闭环四动作 + state)
  - ❌ 多用户 / 多书并发 / 多会话(单会话 Mutex)
  - ❌ 一等页码 / axum·tokio
  - ❌ 印刷版 page-list 标签(留切片1+ 后续)
  - ❌ 完整 consolidation 轨迹摘要 / reader-profile 常驻上下文(留 consolidation 刀;本刀 agent 会话上下文用 `memory.recall` 兜底)
  - ❌ messages 跨会话持久化 / idle 自动判会话边界(本刀用户手动「新对话」清 messages)
  - ❌ agent 提议态做进后端 staging / memory 加 provenance 字段(可撤销落前端交互层,ADR-0030)
- **完成判据**:① 浏览器加载一本书、显示连续正文(LID 不可见);② 点四动作经 HTTP 真正驱动后端(返 effect);③ 标注从 memory 回显;④ 章节定位 + 进度% 显示;⑤ **外层 E agent 经 `/agent/chat` 可对话,reader 双向共享(agent goto 改用户视口)、动作可撤销(undo)、踪迹可见、回答分屏**;⑥(收尾)打包单进程同源启动。

---

## 2. A4 子切片(每步独立可验,依赖文件状态非会话内存)

> 语言归属:S10a–c·S10f = **Rust**(server/runtime);S10d·S10g = **Vue/TS**(packages/web);S10e 跨段(构建+打包)。

### S10a · server crate 骨架 + book.* 只读 GET + ts-rs API DTO 导出 `[Rust]`
- **做**:新建 `crates/server`(tiny_http、线程每连接、`Mutex<AppState{book}>`)。`book.*` 四叶子工具 → GET:`/book/manifest`、`/book/text?lid=&end=`、`/book/context?lid=&k=`、`/book/concept?name=`。错误原样透传 §4.4 分类信封(`{error_code,category,recovery?}`)。给 `QueryResponse/Citation` + 各 book 响应 + `ToolError` 信封补 `#[derive(TS)]` 导出到 `packages/web` 生成目录。
- **不做**:不接 reader/memory/query(S10b/c);不服静态资源(S10e);不接 agent(S10f)。
- **判据**:`cargo test -p server`(handler 单测用 `sample_base`)全绿 + clippy 净;手动 curl 各 GET 返正确 JSON + 错误返信封;ts-rs 生成 TS 类型文件。
- **触达**:`[ADR-0028/0021/0014/0015]`
- **实测落点**:tiny_http 路由/查询串解析样式;ts-rs 跨 crate 导出哪些类型清单。

### S10b · reader.*/memory.* POST 端点 + Reader/MemoryStore 会话状态 `[Rust]`
- **做**:`AppState` 扩 `Reader`+`MemoryStore`。POST:`/reader/goto`、`/reader/scroll`、`/reader/highlight`、`/reader/note`、`/reader/state`、`/memory/save`、`/memory/recall`(JSON body)。reader.* 返 effect;highlight/note 委托 memory.save、render 读 recall(标注单源 memory)。
- **不做**:不接 query(S10c);不做 reader 全集 / 面板。
- **判据**:`cargo test -p server` 全绿;手动 POST 驱动 reader 返 effect、标注落 memory、recall 回显;非法 LID 返 `LID_NOT_FOUND` 不降级。
- **触达**:`[ADR-0028/0015/0027/0006]`
- **实测落点**:人读视口半径(`DEFAULT_RADIUS=3` 偏小,人读窗口/无限滚动)。

### S10c · book.query POST 端点(接 runtime::query) `[Rust]`
- **做**:POST `/book/query`(body `{q, anchor_lid, scope?}`)→ 同步直调 `runtime::query`(NativeAdapter from_env)→ 返 `QueryResponse`。读模式起点:可由 `reader.state` 当前 anchor 作默认 anchor。
- **不做**:不做 synthesize;不把 query 经外层 E loop(裸调内层即完整;外层 E 在 S10f)。
- **判据**:真跑返 `QueryResponse`、citations 结构红线 100%(走 `.env`/glm-5.1,**B2 人工验**,不入自动测)。
- **触达**:`[ADR-0028/0016/0004]`
- **实测落点**:HTTP 层 query 延迟;偶发 provider 错是否在 HTTP 层重试(承 S9 内层裸调暂无重试)。

### S10d · Vue/Vite app(packages/web)+ 连续正文渲染 + 四动作 + dev proxy `[Vue/TS]`
- **做**:`packages/web`(Vue3+Vite,pnpm workspace)。import ts-rs 生成类型。组件:连续正文列(`GET /book/text` 取 `visible_lids` 渲染成无分隔连续文章)+ 四动作交互(滚动→/reader/scroll、跳转→/reader/goto、选区高亮→/reader/highlight、记笔记→/reader/note)+ memory 标注 overlay(/memory/recall)+ 读位感条(章节 + 进度%)+ 问答框(/book/query)。Vite dev proxy `/api`→tiny_http。
- **不做**:不做打包静态服(S10e);不做 agent 对话 UI(S10g);标注选区 UX 可先 CSS span,web-highlighter 类工具留实测。
- **判据**:`pnpm -C packages/web dev` 起,浏览器加载一本书显示连续正文;四动作经 HTTP 真驱动后端、标注回显、章节+进度% 显示(**B2 浏览器人工验**)。
- **触达**:`[ADR-0028/0027/0007]`
- **实测落点**:连续正文取数粒度(一次取整窗 vs 滚动渐进);标注 overlay 锚定到 DOM 的方式。

### S10f · /agent/chat 端点:外层 E agent + reader 注入 + effects/trace `[Rust]`
- **做**:改 `runtime::orchestrator::run` 签名——**删内部 `Reader::new`、改 `reader: &mut Reader` 注入**(与 `store` 对称);`OuterOutcome` 加 `effects: Vec<AgentEffect>`(本回合副作用:goto 前后 anchor、highlight/note 的 id+lid)+ `trace: Vec<TraceStep>`(tool_calls 序列摘要)。server 加 POST `/agent/chat`(body `{message}`)→ 持 `Mutex<AppState>` 把同一 `book/store/reader` 注入 `run` → 返 `{answer, incomplete, effects, trace, viewport}`。agent 标注走 `session` 层(提议态)。会话:`AppState` 持 `messages`,用户「新对话」清空(POST `/agent/new`)。
- **不做**:不做 idle 自动判会话边界;不做 consolidation 轨迹摘要(用 memory.recall 兜底);不持久化 messages 跨进程。
- **判据**:`cargo test -p server`/`-p runtime`(注入 reader 后多跳 + effects/trace 装配,FakeAdapter)全绿;真跑 `/agent/chat` 一次「问→agent goto→高亮」后 `/reader/state` 见视口已被 agent 改、effects 列出可 undo 项(**B2 人工验**)。
- **触达**:`[ADR-0030/0028/0026/0016/0015/0007]`
- **实测落点**:`AgentEffect`/`TraceStep` 字段定型;跨调用 messages 累积上限/截断;agent 标注 session 层的 layer 注入点。

### S10g · Vue agent 形态 UI:分屏 + 可撤销提议 + 踪迹 `[Vue/TS]`
- **做**:对话区(右半,分屏)接 `/agent/chat`;渲染 `effects[]` 为「提议」卡——goto「↩ 返回提问前」、highlight/note「保留(升 long_term)/撤销(memory.delete)」;渲染 `trace[]` 为可折叠查询踪迹(book.query 检索范围 + citations 链);对话末「凝练成笔记(memory.save note)/丢弃」;「新对话」按钮(/agent/new)。agent goto 驱动左侧阅读区视口同步(读 viewport)。
- **不做**:不做 reader-profile 常驻上下文展示(consolidation 刀);提议确认逐条 vs 整回合事务取简单先做、实测细化。
- **判据**:`pnpm -C packages/web dev` 浏览器:agent 回答分屏显示、agent goto 后阅读区同步且可一键返回、agent 高亮可保留/撤销、踪迹可展开(**B2 浏览器人工验**)。
- **触达**:`[ADR-0030/0028/0007]`
- **实测落点**:agent 翻页的视觉明示方式;提议卡的交互密度。

### S10e · 打包形态:tiny_http 服 dist + 一句话启动 skill `[跨段]`
- **做**:`vite build`→`dist/`;tiny_http 同端口既服 `dist/` 静态、又服 REST API + `/agent/chat`(单进程同源无 CORS)。`skills/read/`(`/understand-book:read`)拉起服务(承 ADR-0022)。
- **不做**:不做 marketplace 发布;不做多书选择 UI。
- **判据**:一句话启动 → 浏览器打开同端口 SPA + API 同源跑通;无 CORS。
- **触达**:`[ADR-0028/0022]`
- **实测落点**:tiny_http 服静态文件的 MIME/缓存样式;启动 skill 与 Rust 二进制的衔接。

---

## 3. 完成判据复述(本刀「完成」的充要)

```
浏览器加载书 + 连续正文(LID 隐形)
  ∧ 四动作经 HTTP 真驱动后端(返 effect)
  ∧ 标注单源 memory 回显
  ∧ 读位感(章节+进度%)
  ∧ 外层 E agent 经 /agent/chat 可对话(reader 双向共享 + 动作可撤销 + 踪迹可见 + 回答分屏)
  ∧ book.query 问答可用(结构红线 100%,B2 真跑验)
  ∧ 打包单进程同源一句话启动
  ∧ (asset 刀完成后)阅读器按 ManifestNode.kind 渲染 code/table/image/formula 原文,并能展示公式语义剖面
```
最小连通(页面↔后端)= S10a+S10b+S10d;S10c 问答 + S10f/S10g agent + S10e 打包 = 完整画面收尾。

---

## 4. 实测数字回填清单(本刀跑完回填)

| 数字 | 回填 |
| --- | --- |
| 人读视口半径 / 是否无限滚动渐进加载 | ADR-0027 |
| ts-rs 跨 crate 导出类型清单 | ADR-0028/0021 |
| HTTP 层 query 延迟 / 是否加重试 | ADR-0016/0028 |
| 连续正文取数粒度 / 标注 overlay 锚定方式 | ADR-0028 |
| AgentEffect/TraceStep 字段定型 / messages 累积上限 | ADR-0030 |
| agent 翻页视觉明示 / 提议确认逐条 vs 事务 | ADR-0030 |
| code/table/image/formula 前端渲染样式与公式语义剖面可读性 | ADR-0029/0028 |

---

## 5. UX 修缮:隐藏 LID 后的用户定位入口(两刀)

> **触发**:现有前端暴露「跳转 LID」输入框,与 ADR-0028「连续正文渲染 / LID 隐形」冲突。LID 是内部稳定坐标,不是普通用户可理解的导航语言。
> **目标**:保留 `reader.goto(lid)` 作为内部命令,把用户可见入口改成目录、搜索、标注/引用回跳等自然定位方式。
> **不做**:不改后端 LID 地基;不新增 page number;不把 LID 当 UI 主概念;不引全文索引服务,除非下一刀实测证明前端派生不足。

### S10h · 目录导航替换可见 LID 输入 `[Vue/TS]`
- **做**:移除默认可见的「跳转 LID」输入框,改成目录/章节导航入口。前端从 `book.manifest()` 读取树,只展示章节/小节标题与进度位置;点击目录项时内部携带对应 `lid` 调 `reader.goto(lid)`。当前 anchor 在目录中高亮。原 LID 输入降级为 debug/高级折叠入口,默认不显示。
- **不做**:不做自然语言搜索(S10i);不改 `reader.goto` 后端签名;不展示裸 LID 作为用户主导航文字。
- **判据**:浏览器中普通模式看不到 LID 跳转框;用户可通过目录点击跳到章节/小节;跳转仍经 `reader.goto(lid)` 真驱动后端;当前阅读位置能在目录中被标出。
- **触达**:`[ADR-0028/0007/0014]`
- **实测落点**:manifest 中哪些 `NodeKind`/层级适合展示到目录;章节标题取 `book.text(lid)` 首行还是 manifest 派生字段。

### S10i · 书内自然定位:搜索/标注/引用回跳 `[Vue/TS + 可选 Rust]`
- **做**:新增用户自然语言定位框,输入原文片段或概念词,返回「标题/片段预览/类型」结果列表;点击结果内部调用 `reader.goto(lid)`。最小实现优先前端派生:对已加载 manifest 的章节标题、当前窗口文本、memory note/highlight 做过滤;若需要跨全书正文搜索,再补一个确定性只读端点或复用 `book.concept(name)`/`book.query` 的结果作为候选。标注卡、笔记卡、agent citation/trace 中的 LID 一律显示为片段预览,点击回跳。
- **不做**:不把 `book.query` 当唯一搜索引擎;不让 AI 判定搜索正确性;不做模糊最近邻自动跳转(无结果就明确提示无结果,守禁宽松降级)。
- **判据**:用户输入「撤销」「对象池」这类词能看到可点击候选;点击候选跳到对应正文;标注/笔记/引用卡都能回跳原文且不暴露裸 LID;无结果时不自动跳最近位置。
- **触达**:`[ADR-0028/0014/0015/0030]`
- **实测落点**:前端派生搜索是否足够;如不足,是否新增 `book.search(q)` 这类确定性叶子工具,需另起 ADR 因为会扩命令面。
---

## 6. Asset 原文渲染接入(前端保证)

> **触发**:asset 刀负责让 code/table/image/formula 成为带 `kind` 的 LID 叶子,并保证 `book.text(asset_lid)` 返回源标记原文;Formula 还提供参数/组合含义/上下文关系的语义剖面。切片1 前端必须消费这条读时能力,不能只把 asset 当普通段落文本糊进连续正文。
> **边界**:asset 刀提供数据契约(`ManifestNode.kind` + `book.text` 原文 + FormulaSemantics);本节只管阅读器 UI 渲染与交互。

### S10j · 按 ManifestNode.kind 渲染 code/table/image/formula 原文 `[Vue/TS]`
- **做**:前端读取 `book.manifest()` 后保留每个 LID 的 `kind`;`loadWindow` 拉 `visible_lids` 原文时携带 kind。渲染层按 kind 分派:Paragraph/Chapter/Section 继续连续正文;Code 渲染为保留换行缩进的代码块;Table 渲染为可横向滚动的表格/等宽表文本(先忠实显示,结构化表格留后);Image 渲染为图片占位/预览,展示 `alt` 与 `src`,并保留源标记;Formula 渲染为公式块/行内公式原文,并提供参数、组合含义、上下文关系的语义剖面入口。asset 仍可被选中、跳转、作为 citation/trace 回跳目标。
- **不做**:不在前端生成或猜测图片描述;不把 image src 下载/内联进只读基座;不做 table 行列语义解析;不在前端猜公式参数含义(只展示 asset 刀提供的 FormulaSemantics);不把 code/table/image/formula 改写成普通 paragraph。
- **判据**:真书 manifest 中存在 `Code/Table/Image/Formula` 时,阅读器能在连续流中按类型显示对应原文;`book.text(asset_lid)` 的内容在 UI 中可检查;Formula 能展示参数/组合含义/上下文关系;agent citation/trace 或目录/搜索跳到 asset LID 时视口定位正确;普通段落渲染不回退。
- **触达**:`[ADR-0029/0028/0014]`
- **实测落点**:Code 是否需要语言标签;Table 先等宽文本还是 Markdown 表格解析;Image src 路径相对书目录还是绝对文件 URL;Formula 渲染用 KaTeX/MathML 还是保守原文;公式语义剖面在侧栏/tooltip/trace 中的展示密度;asset 在连续正文中的视觉密度。

---

## 7. S11 · Mintlify docs 工作台化阅读器优化

> **触发**:当前前端阅读器仍有明显调试页气质:裸 LID/工具条显眼、双栏信息架构拥挤、视觉语言不统一。参考 `DESIGN-mintlify.md` 后,本轮 grill 冻结方向为 **Mintlify docs 工作台化**:采用文档产品的 3 栏信息架构、白底 hairline、紧凑 topbar、docs prose 和上下文 rail,但不做营销 hero。
> **边界**:S11 是前端阅读器 UI/信息架构优化;不重写 reader/agent 后端行为,不新增后端搜索接口,不把 Mintlify 的营销 hero/大渐变搬进阅读器。

### §7.1 信息架构

**决策**:采用 3 栏 docs 工作台。

**否决**:
- 双栏换皮:不能解决目录、搜索、trace、formula profile、agent 面板拥挤。
- Agent-first 单右栏:会削弱文档导航和上下文浏览。

**命门**:阅读正文仍是第一主视图,agent 是右侧上下文 tab 之一。
**何时回头**:真书浏览中左右 rail 抢占阅读宽度时,先做 rail 折叠而不是回退双栏。

```text
Topbar 56px
  left: product/book identity
  center: current chapter / breadcrumb
  right: progress %, new chat, debug toggle

Main grid
  left rail 240px:
    search-pill
    outline/chapter nav
    current position

  center reader:
    720-760px docs prose
    asset blocks as docs components
    subtle anchor/selection states

  right rail 320-384px:
    context tabs
    Agent | Trace | Formula | Notes
```

### §7.2 视觉基线

**决策**:第一刀同步救视觉。

**否决**:
- 只搭结构不改观感:首屏仍像调试工具,不满足本轮目标。
- 第一刀全量重画所有细节:范围过大,容易打坏 reader/agent 行为。

**命门**:S11a 必须让首屏达到可接受的 Mintlify docs 水平,但不精修每个细节组件。

```text
Design baseline from DESIGN-mintlify.md:
  canvas: #ffffff
  ink: #0a0a0a
  surface: #f7f7f7
  hairline: #e5e5e5
  mint accent: #00d4a4 only for active/accent states
  prose: 16px / 1.55
  rail text: 13-14px
  buttons: rounded-full pills
  rails: flat, hairline borders
  code/source: mono stack
  no hero, no gradient background, no decorative blobs
```

### §7.3 交互原则

**决策**:默认 LID 隐形,debug 折叠保留。

**否决**:
- 裸 LID 常驻:冲突 ADR-0028「LID 隐形」和 S10h 方向。
- 完全移除 debug:验收 asset/citation/trace 时不方便。

**命门**:普通用户入口用目录、搜索、片段预览和上下文卡;内部坐标只在 debug 中显形。

```text
Default:
  no raw LID
  no goto-lid input
  no raw trace args
  selection toolbar -> Highlight / Note
  selected block -> subtle inline actions

Debug foldout:
  current lid
  goto lid
  raw text/source
  raw trace args
```

### §7.4 左右 rail 数据源

**决策**:左 rail 第一版纯前端派生;右 rail 是上下文 tabs。

**否决**:
- 立即新增 book.search(q):扩命令面,应等前端派生实测不足后另起切片。
- 右 rail 永远只放 agent:Trace/Formula/Notes 会继续挤进聊天流。

**何时回头**:真书实测当前工作集搜索不够时,再评估确定性 `book.search(q)`。

```text
Left rail sources:
  outline = book.manifest tree
  title = book.text(container_lid) first line cache
  search = manifest title cache + visible window text + memory note/highlight

Right rail tabs:
  Agent: existing /agent/chat flow
  Trace: latest query/agent trace
  Formula: selected formula semantics
  Notes: selected/nearby notes and highlights
```

### §7.5 S11 小切片顺序

```text
S11a layout shell + visual baseline [Vue/TS]
  做:
    - 56px compact topbar
    - 3-column docs grid
    - left rail / center prose / right rail tab shell
    - Mintlify baseline tokens: colors, type, hairlines, pill buttons, mono code
    - 移除旧工具条的调试页气质
    - 轻量组件拆分: TopBar / LeftRail / ReaderPane / RightRail
    - 当前 scroll/goto/highlight/note/agent 行为继续可用
  不做:
    - 完整目录搜索行为
    - Formula/Trace/Notes 真 tab 重组
    - selection floating toolbar
    - responsive drawer 完整交互
  判据:
    - `pnpm -C packages/web typecheck` + `pnpm -C packages/web build` 通过
    - 浏览器 smoke: scroll/goto/highlight/note/agent still usable
    - 首屏不再像 debug page

S11b left rail navigation + debug foldout [Vue/TS]
  做:
    - manifest outline
    - title cache via book.text(container_lid)
    - click -> reader.goto(lid)
    - active current position
    - hide raw LID/goto by default
    - debug foldout retains current lid/goto/raw text
  不做:
    - 后端全文搜索
    - 模糊最近邻自动跳转
  判据:
    - 普通模式看不到裸 LID 输入
    - 目录点击真驱动 reader.goto
    - 当前位置在 outline 中高亮

S11c right context tabs [Vue/TS]
  做:
    - Agent tab keeps chat
    - Trace tab shows latest query/agent trace
    - Formula tab shows selected formula profile
    - Notes tab shows selected/nearby notes and highlights
  不做:
    - 重写 agent 后端协议
    - FormulaSemantics 新抽取逻辑
  判据:
    - 右 rail tabs 切换不丢 agent 会话
    - 选中 formula asset 时 Formula tab 展示剖面
    - trace 不再只塞在聊天气泡内部

S11d selection/actions [Vue/TS]
  做:
    - floating selection toolbar
    - selected block inline actions
    - remove old toolbar action row
  不做:
    - 跨 LID 复杂选区
  判据:
    - 选中文本后可高亮/记笔记
    - 段落/asset 选中态轻量不扰阅读

S11e responsive polish [Vue/TS]
  做:
    - left rail drawer on tablet/mobile
    - right rail bottom sheet/tabs
    - mobile touch target cleanup
  不做:
    - 新移动端专用功能
  判据:
    - <1024px 不出现三栏挤压
    - mobile 主要阅读/agent 操作可达
```

### §7.6 S11 总判据

```text
首屏视觉达到 Mintlify docs 工作台水平
  ∧ 中央 reader prose 保持长读可读性
  ∧ 左 rail 提供自然定位入口且默认不暴露 LID
  ∧ 右 rail 承载 Agent/Trace/Formula/Notes 上下文 tabs
  ∧ 现有 reader/agent/asset 功能不退化
  ∧ 每刀都有 typecheck/build 或浏览器 smoke 验收
```
